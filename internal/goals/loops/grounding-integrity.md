# Loop journal — GROUNDING INTEGRITY & SELF-IMPROVEMENT RELIABILITY

> Theme: ① the grounded≠true ceiling (a poisoned/untrusted source → a confidently "grounded" lie) redteam + deterministic defense · ② self-improvement subsystem (playbook/reflection/weakness-ledger/background-review) reliability+coverage · ③ hardening the self-judge meta-eval (maker=judge compensating control).
> Worktree `/tmp/muse-grounding-integrity` (branch `loop/grounding-integrity`). Tier1.5 — local commit + merge to LOCAL main when green, NEVER push. Convention: [README](README.md).

## fire 1 · 2026-06-13 · skill v1.14.0 · c09f3465
meta: value-class=redteam-defense · pkg=@muse/agent-core+@muse/cli · kind=A · verdict=PASS · firesSinceDrill=1
ratchet: cli tests +3 cases (2558 pass) · agent-core suite pass · lint 0/0 · fabrication 0 · grounding floor intact (additive warning only)
- What: Wired the dead `groundedOnUntrustedOnly` mitigation into the `muse ask` verdict path — surfaces a provenance warning on a faithful answer grounded only in untrusted (MCP/web tool output, `trusted:false`) sources. The function wasn't even re-exported from the agent-core index — dead code with 0 production callers.
- Why: Closes one vector of the grounded≠true ceiling — a fixed local model can't know source veracity, but it CAN know source TRUST (the provenance bit), and surfacing that to the user prompts extra verification.
- Review point: the `commands-ask.ts` verdict path — the label stays "grounded" (the answer is faithful), only a stderr warning is added. The `!verdictNotice && imageAttachments===0` guard leaves the already-ungrounded/vision path unchanged. The independent Opus judge PASSED all 5 adversarial checks.
- Risk: if the tool-source citation format (`[from tool: X]`) drifts from actual model output, the production firing rate could be low — the unit tests pin the function contract, but the e2e firing rate needs a follow-up `eval:grounding-delta` measurement (recorded as a backlog candidate).

## fire 2 · 2026-06-13 · skill v1.14.0 · 0a38b477
meta: value-class=reliability-coverage · pkg=@muse/autoconfigure · kind=B · verdict=PASS · firesSinceDrill=2
ratchet: autoconfigure distill-queue +2 tests (4 pass) · lint 0/0 · fabrication 0 · mutation-verified non-vacuous (RATCHET: fire1=redteam-defense/agent-core+cli → fire2=reliability-coverage/autoconfigure, diversified)
- What: Pinned two safety invariants of the unattended distill-consumer (`distillQueuedCorrections`) with OUTCOME tests — both dud (an empty correction) and fail-soft (distiller undefined) drain from the queue (no jam) + record zero strategies (a non-corrective signal doesn't fabricate a lesson). The source was already correct (`doneIds.push` sits ahead of both guards), an unguarded-in-tests guarantee now protected.
- Why: this unattended consumer runs every idle tick, so a jam would reprocess the same dud forever, and a breached fence would fabricate a fake lesson from a non-correction — exactly the class of invariant a Muse edge relies on, and it had 0 tests.
- Review point: OUTCOME tests over the real file-backed queue/playbook store (enqueueLearnEvent/readPendingLearnEvents/readPlaybook); test1's distill is a throw-injecting stub (proves an empty event is fenced before distill). Non-vacuousness proven by mutation (moving drain behind the guard)→test red→revert; the independent Opus judge re-confirmed PASS with its own 2 mutations.
- Risk: a test-only slice (source unchanged) — a regression-guard value, not new behavior. A full `pnpm check` is disproportionate for a single test-file change, so a package build+test+lint substitutes.

## fire 3 · 2026-06-13 · skill v1.14.0 · 7401e84c
meta: value-class=redteam-defense · pkg=@muse/cli · kind=A · verdict=PASS · firesSinceDrill=3
ratchet: cli +4 tests (80 pass; full suite 2570 pass) · lint 0/0 · fabrication 0 · mutation-verified non-vacuous (RATCHET: fire1=redteam-defense/agent-core+cli, fire2=reliability-coverage/autoconfigure, fire3=redteam-defense/cli — value diverse, every-surface parity)
- What: Extended fire 1's untrusted-only provenance warning to the CHAT surface. `finalizeGatedChatAnswer` (the shared post-stream pipeline for every conversational surface) had a blind spot where tool output was folded into evidence with no trust marker — closed it by tagging toolEvidence with `trusted:false` + adding/wiring `untrustedOnlyChatNotice` (chat parity for ask's untrustedOnlyGroundingNotice).
- Why: the wedge is "every surface gated" but only ask was defended — chat was passing through exactly that vector, plain "grounded" on an answer resting only on a poisoned MCP/web source. The `trusted:false` tagging is an always-on provenance-accuracy improvement independent of firing rate.
- Review point: purely additive — the gate decision/receipt/fabrication=0 floor unchanged (the judge confirmed the gate never reads .trusted), abstention/no-info still fires no warning. Non-vacuousness proven by mutation (disable the helper)→warning case red→revert; the independent Opus judge PASSED (full suite 2570 pass).
- Risk: the warning only fires when the answer cites a tool source as `[from <src>]` — the same caveat as ask, so the production firing rate is limited (the judge noted this honestly). Surface parity + always-on provenance tagging is real value. e2e firing rate is tracked under the existing backlog ◦ (fire 1), now including chat.

## fire 4 · 2026-06-13 · skill v1.14.0 · 0b77bfe8
meta: value-class=redteam-defense · pkg=@muse/agent-core · kind=C · verdict=PASS · firesSinceDrill=4
ratchet: agent-core +2 tests (14 pass; full suite 1954 pass) · lint 0/0 · fabrication 0 · red-without-fix verified (RATCHET: A·B·A·C — all 4 axes covered, value diverse)
- What: **Fixed a fail-OPEN bug** in the two judge-gated surfaces (`verifyCouncilGrounding`·`verifyReflectionsGrounding`) — they called the judge even on empty evidence and KEPT the claim on a YES; now empty evidence skips the judge entirely and fails closed deterministically (council→null, reflection→skip).
- Why: unlike `verifyGroundingWithReverify`, these two surfaces have no deterministic rubric pre-gate — the judge is the only gate — so a YES on "" evidence is a direct fabrication-floor leak (zero-evidence synthesis/dreaming passing verification). Both are production-reachable (a contributor-reasoning gap / an unresolved cited sourceId).
- Review point: purely strengthening — only drops claims that used to be keepable, never keeps more; the judge-NO/error fail-close paths are unchanged. The bug was proven real via red-without-fix (2 tests failed on main), and the no-call contract was pinned with `expect(judge).not.toHaveBeenCalled()`. The independent Opus judge PASSED 5/5.
- Risk: essentially none (strictly strengthens). Follow-up: unlike recall, council/reflection have no k-sample self-consistency (a single judge call) — an ENHANCEMENT candidate for a separate fire (backlog).

## fire 5 · 2026-06-13 · skill v1.14.0 · d7326a29
meta: value-class=reliability-coverage · pkg=@muse/mcp · kind=B · verdict=PASS · firesSinceDrill=5
ratchet: mcp +1 test (6 pass; full mcp 1812 pass) · lint 0/0 · fabrication 0 · red-without-fix verified (RATCHET: A·B·A·C·B — value/pkg diverse, mcp's first appearance)
- What: **Fixed a lost-update bug** in the learn-queue — `markLearnEventsDone` (a read-modify-write full-file rewrite) and `enqueueLearnEvent` (appendFile) both operated without a mutex → a correction appended mid-drain got clobbered. Wrapped both in the shared `withFileMutationQueue` (file-keyed) to serialize them.
- Why: the learn-queue is the substrate that carries user corrections to background distillation — a loss means a genuine lesson silently never learns on the unattended path (with no error). Peer stores (playbook/action-log) already use this primitive; only learn-queue was missing it.
- Review point: **wrapping only markDone is incomplete** (enqueue's appendFile bypasses the mutex) — both need the same file-key to serialize. The judge empirically re-confirmed the partial fix still stays red. The test is deterministic via promise-chain ordering (not fs-timing dependent), red-without-fix proven, independent Opus judge PASSED 5/5 (mcp 1812/autoconfigure 548/api 668 no conflict).
- Risk: essentially none (external contract unchanged, no deadlock — no nested mutex calls).

## fire 6 · 2026-06-13 · skill v1.14.0 · e373114c
meta: value-class=reliability-coverage · pkg=@muse/agent-core · kind=C · verdict=PASS · firesSinceDrill=6
ratchet: agent-core +4 tests (18 pass; full suite green) · lint 0/0 · fabrication 0 · red-without-fix verified · floor strictly stronger (RATCHET: A·B·A·C·B·C — 3 axes balanced, 4 packages)
- What: Added **k-sample self-consistency** to the council/reflection judge gates — recall (verifyGroundingWithReverify) already defends against single-judge variance with k-sample consensus, but these two self-improvement surfaces used only a single call. Added opt-in `reverifySamples?` ([1,5], default 1=unchanged) reusing judgeConsensus, threaded through the synthesize* options.
- Why: single-judge intra-rater variance (arXiv:2510.27106, "Rating Roulette") — a borderline synthesis/reflection could get promoted to long-term memory on one flaky YES. The undefended sibling of the hole recall already closed. Resolves backlog ◦ (scouted in fire 4).
- Review point: a byte-exact mirror of the recall pattern (clamp [1,5], first-NO short-circuit, judgeConsensus empty→false). Behavior unchanged at default 1 (14 existing tests green), fire-4's empty-evidence fail-close is preserved ahead of the sample loop. The floor is STRICTER only (no path where k>1 keeps more than k=1). Independent Opus judge PASSED 5/5.
- Risk: essentially none (opt-in, default unchanged). Follow-up: wiring an actual caller to turn on reverifySamples>1 is separate (currently only the capability is exposed, same as recall where the caller opts in via policy).

## fire 7 · 2026-06-13 · skill v1.14.0 · 075bbc2d
meta: value-class=redteam-defense · pkg=@muse/recall · kind=A · verdict=PASS · firesSinceDrill=7
ratchet: recall +10 tests (full recall 111+ pass) · lint 0/0 · fabrication 0 · mutation-verified + judge-flagged FP hardened in-fire (RATCHET: A·B·A·C·B·C·A — 5 packages, recall's first appearance)
- What: New **evidence↔evidence contradiction-detection primitive** — the whole stack only checked claim↔evidence, so if two sources gave different values for the same field (an old/new wifi password, etc.), one got confidently cited as a clean receipt. `detectSourceConflict`+`formatSourceConflictWarning` (@muse/recall, pure, no-model, hot-path capable).
- Why: an uncovered grounded≠true vector — the existing `muse notes conflicts` is batch+model-dependent (not a live path). A deterministic hot-path detector was an empty slot.
- Review point: extracts `label: value` + flags cross-hit different values (excludes duplicates within the same hit, normalizes case/whitespace so matches aren't flagged). **The judge flagged false positives on common prose prefixes (Note/TODO/Summary)·times (9:30) → hardened in the same fire** with a denylist+excluding numeral-ending labels (2 false-positive tests added). Non-vacuousness proven by mutation (stub→[]); independent Opus judge PASSED.
- Risk: primitive-first (only the detector+formatter, not yet wired to a receipt site). The v1 regex truncates values at comma/period (a partial false-negative, accepted). Follow-up: receipt-site wiring + an e2e CLI test (backlog ◦).

## fire 8 · 2026-06-13 · skill v1.14.0 · 7d6f0071
meta: value-class=redteam-defense · pkg=@muse/recall+@muse/cli · kind=A · verdict=PASS · firesSinceDrill=8
ratchet: recall +3 tests (13 pass) · cli 2584 pass · lint 0/0 · fabrication 0 · NEXT FIRE = JUDGE-DRILL (consecutiveAllPASS hit 8) (RATCHET: A·B·A·C·B·C·A·A — still 5 packages)
- What: **Completed wiring fire 7's conflict detector into the live ask path** — added `groundingConflictCue(notes,episodes)` (recall, pure, synthesizes over the grounding that backed the answer) + computed the cue from `scored`+`episodeHits` in commands-ask's non-JSON branch and emit to stderr. Takes fire 7's value from a primitive to a real user surface (a genuine OUTCOME).
- Why: the detector existed and was tested but had 0 callers, so a contradictory source still reached the user as one clean receipt — this wiring closes that gap.
- Review point: `--connect`-independent (a safe cue), fires only when there's actually a conflict (fire 7's hardening blocks the prose/time false positives), stdout/--json unchanged. The synthesis is unit-tested in recall (note-note + note-vs-episode cross-source), the CLI glue is a thin emit. Independent Opus judge PASSED 5/5 (cli 2584 green).
- Risk: essentially none (no output when there's no conflict). The same wiring for the chat surface is a follow-up (currently ask-only).

## fire 9 · 2026-06-13 · skill v1.14.0 · 70814c0a (JUDGE-DRILL + real fix)
meta: value-class=redteam-defense · pkg=@muse/recall+@muse/cli · kind=A · verdict=PASS · firesSinceDrill=0 (reset)
ratchet: recall +2 tests (15 pass) · cli 2591 pass · lint 0/0 · fabrication 0 · JUDGE-DRILL passed (RATCHET: A·B·A·C·B·C·A·A·A — 5 packages)
- JUDGE-DRILL (consecutiveAllPASS hit 8): injected a deliberately bad slice — disabled detectSourceConflict via `return []` (removed the capability/floor regression) + an inert test that couldn't catch it (only `toBeDefined()`, passes even on an empty array). The inert test stayed green even against the disabled detector (reproducing the dangerous state). The independent Opus judge returned **VERDICT: FAIL** — correctly named the vacuous inert test + the disabled detector + exactly which real tests would go red. → proved the verifier hasn't gone numb after 8 consecutive greens (maker≠judge compensating control worked). Drill artifacts rolled back via git restore (confirmed clean).
- Real fix: extended fire 8's conflict cue **to the chat surface** (every-surface parity) — `conflictCueFromMatches(matches)` (recall, KnowledgeMatch-shape→hits) + applied in finalizeGatedChatAnswer to the user's own grounding (args.matches), appended to the answer when there's a conflict. Both ask (fire8)+chat now surface contradictions.
- Review point: appends only when there's a conflict (fire7's hardening blocks false positives), the gate decision/untrusted cue/receipt are unchanged. The synthesis is unit-tested in recall, the CLI glue is a thin append. Independent Opus judge PASSED 5/5 (cli 2591 green).
- Risk: essentially none. The v1 regex comma-truncation false-negative persists (accepted). chat shares the same caveat as ask.

## fire 10 · 2026-06-13 · skill v1.14.0 · 9c7d9b0a
meta: value-class=reliability-coverage · pkg=@muse/mcp · kind=B · verdict=PASS · firesSinceDrill=1
ratchet: mcp +2 tests (9 pass; full mcp 1835 pass) · lint 0/0 · fabrication 0 · isolated-mutation verified (RATCHET: A·A·A→B diversity restored, pkg mcp)
- What: Fixed the reflections store's (unattended dreaming) cap trim from **insertion-order→createdAtMs (recency)**. `listReflections` sorts newest-first by createdAtMs, but trim used `slice(length-MAX)` (insertion order), so the cap disagreed with display order — a backfill/out-of-order batch could evict a newer insight and keep a stale one.
- Why: unattended learned-state hygiene — an inconsistent eviction silently discards grounded self-knowledge. The current single caller uses monotonic timestamps so this doesn't manifest today via that path → this is ANY-writer hardening since the public @muse/mcp store has no monotonicity contract (the judge honestly caveated this). Not a floor issue, learned-state hygiene.
- Review point: `[...].sort(desc by createdAtMs).slice(0,MAX)` keeps the newest N, only kicks in over-cap (under-cap unchanged), listReflections re-sorts so storage order doesn't matter. A backfill test goes red only under isolated insertion-order mutation (1/9), isolating the trim logic; independent Opus judge PASSED 5/5 (mcp 1835 green).
- Risk: essentially none (persistence-only, never fabricates). Simple hardening (not a live bug), so medium value.

## fire 11 · 2026-06-13 · skill v1.14.0 · ebdd8a7c
meta: value-class=redteam-defense · pkg=@muse/agent-core · kind=C · verdict=PASS · firesSinceDrill=2
ratchet: agent-core +2 tests (62 pass; full suite green) · lint 0/0 · fabrication 0 · isolated-removal verified · floor STRICTER (RATCHET: B→C diversity, agent-core)
- What: **Fixed the fail-OPEN empty-evidence hole in the main grounding gate** — `verifyGroundingWithReverify` (the reverification behind recall/ask/chat) let a high-cosine+empty-text match (confidence>0, evidence="") reach the coverage band that calls the judge with "", and a YES promoted a fabricated answer to grounded. The hole f4 closed for council/reflection was still open on the *main gate*. Now: when evidence is empty and base isn't already grounded, no judge call, fail-close to ungrounded.
- Why: classifyRetrievalConfidence derives confidence from cosine alone (ignores text) — an empty-text match could still get confidence=1 and enter the escalation band. A direct leak in the fabrication floor of the wedge (the main gate) — the highest-value fix this session.
- Review point: short-circuits only when `evidence.trim()===0 && base.verdict!=="grounded"` (a grounded base still goes through the value band — only demotion is possible, preventing an accidental refusal). The non-empty path is byte-unchanged. Isolated guard removal → only the empty test goes red (1/62), judge-not-called was also pinned. Independent Opus judge PASSED 5/5 (full suite green).
- Risk: essentially none (strictly tighten, fail-close). Council/reflection·recall — all three now consistently fail-close on empty evidence.

## fire 12 · 2026-06-13 · skill v1.14.0 · 6cb7ac7d
meta: value-class=redteam-defense · pkg=@muse/autoconfigure · kind=A · verdict=PASS · firesSinceDrill=3
ratchet: autoconfigure +6 tests (581 pass) · lint 0/0 · fabrication 0 · isolated-mutation verified · floor STRICTER (RATCHET: C→A, pkg autoconfigure revisited but a different surface)
- What: **Fixed a fail-open bug in the ambient "Related:" brief enricher's CRAG margin guard** — `createKnowledgeEnricher` called `classifyRetrievalConfidence([top])` with only a single match, so runnerUp=0 permanently disabled the flat-distribution (near-tie ambiguity) guard. Even a 0.57/0.56 near-tie surfaced in the daily brief as confident. Extracted `selectEnricherLine` as a pure helper to classify the full post-exclusion candidate list.
- Why: the sibling proactive path already checks margin against the full match set, but this surface diverged — a live grounded≠true fail-open where ambiguous recall rode into the daily brief looking confident. Reached via createKnowledgeEnricher→daemon→situational-briefing.
- Review point: an equivalent refactor (candidates[0]==the old find, classify's internal sort makes order irrelevant), confident/none unchanged, only near-tie gets extra suppression. Mutating back to `[top]` red's exactly the 2 near-tie tests (2/6), isolating the fix. Independent Opus judge PASSED 5/5 (581 green).
- Risk: essentially none (strictly tighten — clear-lead/single unaffected). Confirmed no over-reach.

## fire 13 · 2026-06-13 · skill v1.14.0 · 21de01e6
meta: value-class=redteam-defense · pkg=@muse/cli · kind=A · verdict=PASS · firesSinceDrill=4
ratchet: cli +5 tests (85; full cli 2598 pass) · lint 0/0 · fabrication 0 · isolated-mutation verified · floor STRICTER (vein thinning — scout honestly reports)
- What: Added a **date-drift guard** to the chat sync gate — the IP/number/email/identifier guards existed but dates had none. `valueNumbers` dropped month/day (1-2 digit) and only looked at the year, so a same-year drift (a note saying 2026-09-13 vs an answer saying 2026-09-14) passed through. Wired `answerAssertsUnsupportedDate` (ISO-only, leading-zero normalization, citation strip) ahead of the number guard.
- Why: a wrong schedule/renewal/deadline date is a high-risk verbatim-value class that was the only one left undefended (same class as the IP guard). chat is sync-designed and needs a deterministic guard.
- Review point: **conservative design keeping false-refusal≈0** — flags drift only like-for-like (when the evidence has an ISO date), leaves prose dates ("September 14") alone. ISO-only avoids the slash M/D vs D/M ambiguity. An always-false mutation reds only the drift test; always-true reds 14 (both directions + the gate-level faithfulness pin). The independent Opus judge verified false-refusal safety on hard cases and PASSED.
- Risk: essentially none (strictly tighten, the prose-date false-negative is intentionally accepted). Vein thinning — most grounding fail-opens are now closed.

## fire 14 · 2026-06-13 · skill v1.14.0 · 46cde4ee
meta: value-class=new-capability(paper) · pkg=@muse/agent-core · kind=A · verdict=PASS · firesSinceDrill=5
ratchet: agent-core +5 tests (citation-precision; full suite 2054 pass) · lint 0/0 · fabrication 0 · existence-only mutation verified · vein-thin → paper-pivot
- What: New **ALCE per-citation support precision** (arXiv:2305.14627) — Muse previously only checked citation existence (enforceAnswerCitations) and union-evidence groundedness, never *whether the source a sentence cites actually supports that sentence* (right source/wrong claim). `reportCitationPrecision` (pure, masks `[from src]` then does per-sentence token-coverage against only the cited source). Diagnostic only.
- Why: the bug-hunting vein was thinning, so the loop naturally pivoted to its "paper-grounded-first" mode — reimplementing a public arXiv mechanism Muse lacked (per-citation support). Catches cross-source misattribution that union-coverage misses.
- Review point: a U+E000 sentinel works around a "." sentence-splitting bug in `vpn.md` (escaped as \u{E000}, byte-hygiene clean); sentinel indices are stripped before tokenization (never miscounted as a real character). Diagnostic-only, so the gate is unchanged. An existence-only mutation reds the support test, distinguishing it from the existing mechanism. Independent Opus judge PASSED 5/5.
- Risk: none (additive, pure, no gate change). Follow-up: wire reportCitationPrecision into an ask/chat diagnostic or the gate (backlog ◦).

## fire 15 · 2026-06-13 · skill v1.14.0 · bb2fafa9
meta: value-class=wiring(paper-OUTCOME) · pkg=@muse/cli · kind=A · verdict=PASS · firesSinceDrill=6
ratchet: cli +3 tests (commands-ask-grounding-verdict; full cli 2613 pass) · lint 0/0 · fabrication 0 · stub mutation verified
- What: **Wired fire 14's ALCE `reportCitationPrecision` into the live ask path** — `citationPrecisionNotice` surfaces a "right source/wrong claim" stderr cue when a cited source resolves but doesn't support the sentence. Computed from verdictAnswer+scoredMatches, only when grounded (!verdictNotice), alongside the existing untrusted/conflict cues.
- Why: completes fire 14's diagnostic into a real user OUTCOME (backlog ◦). Per-citation support is a class the whole-answer verdict misses — a distinct axis from provenance-trust (untrusted)·evidence-vs-evidence (conflict).
- Review point: fires only when precision<1 (fire14's floor check), supported/uncited stays silent. Gate decision/floor/--json unchanged, pure delegation. Stub mutation reds the warn test. Independent Opus judge PASSED 5/5 (cli 2613 green).
- Risk: none (additive stderr cue). Firing requires the answer to carry a [from X] citation (a shared caveat with ask/chat). Chat-surface wiring is a possible follow-up.

## fire 16 · 2026-06-13 · skill v1.14.0 · d4c92334
meta: value-class=new-capability(paper)+wiring · pkg=@muse/agent-core+@muse/cli · kind=A · verdict=PASS · firesSinceDrill=7
ratchet: agent-core +5, cli +3 tests (agent-core 2078, cli 2616 pass) · lint 0/0 · fabrication 0 · citable=false mutation verified
- What: **ALCE citation RECALL** (arXiv:2305.14627) — the complement of fire15's precision. Catches "uncited-but-citable" sentences: a groundable claim (≥floor coverage from the evidence union) that carries no `[from]` citation at all. `reportCitationRecall` pure module + `citationRecallNotice` wired into the ask path (grounded-only, stderr, additive).
- Why: precision="did the cited source actually apply", recall="did every groundable claim get cited" — sentence-groundedness is citation-agnostic and had always missed this attribution gap. Completes the precision/recall/groundedness triad.
- Review point: U+E000 sentinel masking (validated against multi-dot source names·2-digit counters), only citable sentences are the denominator (a generic claim isn't a recall miss). There's a false-positive risk on high-overlap recommendation sentences, but it's accepted since it's a non-gating stderr cue (judge confirmed). A citable=false mutation reds the recall test. Independent Opus judge PASSED 5/5.
- Risk: none (additive, diagnostic only). **Vein status: the grounding triad is complete → the deterministic fail-open vein is effectively exhausted. Recommend the next fire pivot value-class** (e.g. surface precision/recall as a muse doctor tracked metric, or pivot to retrieval-quality/another axis). Recorded in backlog ◦.

## fire 17 · 2026-06-13 · skill v1.14.0 · a78079bf
meta: value-class=redteam-defense(bug) · pkg=@muse/agent-core · kind=A · verdict=PASS · firesSinceDrill=8
ratchet: agent-core +1 test (citation-precision 6; full suite 2089 pass) · lint 0/0 · fabrication 0 · last-wins mutation verified · next fire=JUDGE-DRILL (consecutiveAllPASS 8)
- What: **Fixed a false-positive bug** in fire 14's `reportCitationPrecision` — the source→text map was last-wins, so when a file was retrieved as multiple chunks (same source, different text), only the last chunk survived and a faithful sentence supported by a different chunk was misjudged unsupported → the live ask cue (fire 15) threw a false "Citation check" warning on a faithful answer. Fix: concatenate all chunk texts per source.
- Why: found while attempting a value-class pivot (a doctor-metric) — scoring a corpus with same-source-name EN/KO files produced 0.00 precision and surfaced the bug. The metric itself was **honestly discarded** because it's murky: (a) a self-eval boundary ([[project_self_eval]]: self-measuring infrastructure is human-direction territory) and (b) corpus dup-sources + crude token-coverage — but the real bug it uncovered got fixed.
- Review point: direction-safe (added tokens only move things toward supported, never introduces a false-negative), diagnostic-only (gate unchanged), recall was already union-based so unaffected. Reverting to last-wins reds the aggregation test (a supporting chunk placed first). Independent Opus judge PASSED (flagged a duplicate comment → deduped).
- Risk: none (direction-safe hardening). Vein: the grounding-determinism vein remains exhausted — next is either a value-class pivot or a wind-down (a metric outside the self-eval boundary needs human direction).

## fire 18 · 2026-06-13 · skill v1.14.0 · db4f56bd (JUDGE-DRILL + real fix)
meta: value-class=redteam-defense(parity) · pkg=@muse/cli · kind=A · verdict=PASS · firesSinceDrill=0 (reset)
ratchet: cli +4 tests (chat-grounding 89; full cli 2622 pass) · lint 0/0 · fabrication 0 · JUDGE-DRILL passed (④+④b both sides)
- JUDGE-DRILL (consecutiveAllPASS 8): re-verified both verification layers. (1) injected a floor-weakening change (removed fire-11's empty-evidence guard = reopened the fail-open) → **the existing fire-11 test immediately went red**, catching it (④ deterministic gate working). (2) since that's not ④b, separately injected an inert/no-op slice that still passes tests (`dropsPoisonedSource` always false + a type-only test) → the independent Opus judge returned **VERDICT: FAIL** (all 3 checks: no-op·vacuous·unwired). Both sides proven not a rubber-stamp. Drill artifacts rolled back to a clean git state.
- Real fix: brought the ALCE citation precision/recall cue to **chat parity** (ask had fire15/16, chat had none). `chatCitationPrecisionNotice`/`chatCitationRecallNotice` (delegating to the verified reporters), appended in finalizeGatedChatAnswer. Both ask+chat now surface mis-citation/missing-attribution.
- Review point: purely additive (gate/floor unchanged), a distinct reporter from untrusted/conflict so no double-warn, abstention/faithful-citation stays silent. Independent Opus judge PASSED 5/5 (cli 2622 green).
- Risk: none (additive). Firing requires the answer to carry a [from] citation (a shared caveat with ask/chat). Vein remains exhausted.

## fire 19 · 2026-06-14 · skill v1.14.0 · (no slice — EXHAUSTION close)
meta: value-class=honest-close · pkg=— · kind=B-probe · verdict=VEIN-EXHAUSTED · firesSinceDrill=1
ratchet: self-eval green (testFiles 959) · no code change (EXHAUSTION rule, not a forced slice)
- What: An honest probe of fresh axis-B modules (weakness-ledger BKT / correction-distiller polarity / personal-playbook decay·Memp) — **all correct and densely covered with exact-value tests, no real bug found**. No code slice produced.
- Why: 2 consecutive clean signals (fire 16's vein-status + fire 19's axis-B-clean) → per the EXHAUSTION rule, an honest close instead of a forced micro-fix. Axis A (provenance/empty-evidence/conflict/citation precision+recall/date) + axis B reliability (reward·decay·probation·graduation·BKT·polarity·persistence idempotency) + axis C (3 judge-gate empty-evidence, k-sample, judge-drill ×2) are all hardened+verified.
- Review point: none (no change). Scout recommendation = a value-class pivot (retrieval/recall quality, or surfacing learned-state UX) or a wind-down. Awaiting Jinan's direction.
- Risk: none. **Blocker: this theme's deterministic hardening vein is exhausted** — further fires need either a new public paper mechanism (which appears occasionally) or an honest close. Recommend Jinan decide whether to re-theme or stop.

## fire 20 · 2026-06-14 · skill v1.14.0 · dc69329a
meta: value-class=new-capability(paper) · pkg=@muse/agent-core · kind=A · verdict=PASS · firesSinceDrill=2
ratchet: agent-core +9 tests (full suite 2129 pass) · lint 0/0 · fabrication 0 · guard-removal mutation verified · vein REOPENED by a paper escape-hatch
- What: **Polarity-mismatch (negation blind spot) guard** — token-coverage strips "no"/"not" as stopwords, so "X is NOT effective" scored the same coverage (1.0 supported) as "X is effective" — a fabrication-floor hole. `detectPolarityMismatch` (per-sentence, ≥0.6 overlap + opposite negation polarity) wired fail-close into reportSentenceGroundedness (supported→unsupported).
- Why: fire 19 reported "vein exhausted," but the EXHAUSTION rule's "new public paper mechanism" escape hatch found a real hole (arXiv:2305.16819, 2510.06265). Negation is a class the token gate structurally cannot see — distinct from detectEvidenceContradictions (evidence↔evidence) since this is answer↔evidence.
- Review point: strictly tighten (downgrade only), pure, sentence-granular to avoid stray-negation false positives. Removing the guard reds the downgrade test via mutation. Independent Opus judge PASSED 5/5 (2129 green). **Known narrow limitation**: same-stem litotes ("not uncommon" vs "common") over-fires — accepted since the only consumer is a diagnostic telemetry hint (gate unchanged).
- Risk: low (diagnostic-only over-fire, no impact on the gate/user answer). Vein correction: the deterministic-bug vein is dry, but public paper mechanisms occasionally surface a real hole (this was one).

## fire 21 · 2026-06-14 · skill v1.14.0 · dc419bac
meta: value-class=new-capability(paper) · pkg=@muse/agent-core · kind=A · verdict=PASS · firesSinceDrill=3
ratchet: agent-core +9 tests (full suite 2147 pass) · lint 0/0 · fabrication 0 · guard-removal mutation verified
- What: **Numeric/unit-mismatch guard** (FactCC arXiv:1910.12840, survey 2501.00269) — token coverage couldn't see a unit swap since "5 mg"="5 g" (token "5" matches), nor a magnitude difference ("13800" vs "1380" scored 0.5). `detectNumericMismatch`: (a) unit-swap (numeral matches, unit doesn't) (b) a missing ≥3-digit numeral, only on sentences with ≥0.5 overlap. Wired fail-close into reportSentenceGroundedness.
- Why: a second public-paper floor hole after fire 20's negation (value/unit errors). Distinct from polarity and the existing number guard (unit-swap had no check anywhere). Uses the union-of-evidence numeral set to avoid multi-fact false positives.
- Review point: strictly tighten, pure, ≥3-digit+comma normalization to avoid word-form/separator false positives. Removing the guard reds the unit-swap downgrade test. Independent Opus judge PASSED 5/5 (2147 green). **Known accepted false positives**: abbreviations (1.25M)·plural/percent-word units·derived sums — the only consumer is telemetry-only (gate unchanged).
- Risk: low (diagnostic-only). Vein: the paper-mechanism axis surfaced 2 real holes in a row (negation, numeric) — a healthy steady state.

## fire 22 · 2026-06-14 · skill v1.14.0 · 6f8a088d
meta: value-class=new-capability(paper) · pkg=@muse/agent-core · kind=A · verdict=PASS · firesSinceDrill=4
ratchet: agent-core +8 tests (full suite 2155 pass) · lint 0/0 · fabrication 0 · guard-removal mutation verified
- What: **Hedge-overclaim (certainty inflation) guard** (FActScore arXiv:2305.14251, survey 2501.00269) — token coverage doesn't see the modal certainty direction, so "breaks" scored "may break" as 0.67 supported. `detectHedgeOverclaim`: fires only when the answer is non-hedged and a ≥0.6-overlap evidence sentence is hedged (one direction). Wired fail-close in reportSentenceGroundedness.
- Why: the third consecutive paper mechanism after fire 20 (negation)·21 (numeric). **Completes a trio of sentence-vs-evidence meaning guards** (negation·value/unit·certainty) — deterministically covering 3 semantic axes token coverage structurally can't see.
- Review point: strictly tighten, pure, one-direction only (under-claiming excluded), sentence-granular to avoid stray-hedge false positives. Removing the guard reds the downgrade test via mutation. Independent Opus judge PASSED 5/5 (2155 green). **Known narrow accepted false positive**: permission/ability "may" ("you may enter") — telemetry-only consumer, no gate impact.
- Risk: low (diagnostic-only). Vein: the paper-mechanism axis surfaced 3 real holes across fires 20-22 — wraps up as a semantic-guard trio. Further fires depend on new papers (value per fire will vary).

## fire 23 · 2026-06-14 · skill v1.14.0 · f5d9eb01
meta: value-class=reliability-coverage · pkg=@muse/mcp · kind=B · verdict=PASS · firesSinceDrill=5
ratchet: mcp +2 concurrency tests (full suite 42 pass) · agent-core racy boundary test pinned · check green · lint 0/0 · fabrication 0 · queue-removal mutation verified by judge
- What: **Serialized concurrent writes to the weakness-ledger**. `recordWeakness`/`recordWeaknessResolved` used bare read-modify-write + non-atomic fs.writeFile (all 11 sibling stores already use withFileMutationQueue+atomicWriteFile). Real concurrent callers (commands-ask, chat-repl) race-write the same weaknesses.json → last-writer-wins clobbers or wipes the file. Wrapped both functions' RMW in the queue and switched writeWeaknesses to atomicWriteFile. 2 OUTCOME tests (N distinct records all persist · a resolve↔record race leaves both the new entry and the mastery bump alive).
- Why: switched to axis B (self-improvement subsystem reliability) — fires 20-22 concentrated on agent-core grounding semantic-guards (kind A), so diversity+RATCHET force a different axis/package/value-class. A real durability hole where the Whetstone metacognitive artifact was losing self-learning signal under concurrency.
- Review point: the read happens inside the queue closure (the whole RMW is atomic), the empty-topic early-return stays outside the queue (behavior unchanged), atomicWriteFile preserves bounded-growth cap·JSON shape. The independent Opus judge disabled the queue wrap directly, confirmed both tests go red (non-vacuous)→restored, PASSED 5/5. The agent-core change is test-only (source isStaleStrategy unchanged).
- Risk: low (data-layer concurrency hardening, unrelated to any invariant). Vein: scout confirmed adjacent self-improve modules (playbook/correction-distiller/decay/reflection-synthesis/user-model-slots) are already well-hardened — the next axis B is either the remaining stores beyond weakness-ledger, or axis C (self-judge meta-eval).

## fire 24 · 2026-06-14 · skill v1.14.0 · 889c9265
meta: value-class=redteam-defense(wiring) · pkg=@muse/cli · kind=A · verdict=PASS · firesSinceDrill=6
ratchet: cli +3 OUTCOME tests (chat-finalize) · check green (cli 2631 pass) · eval:adversarial 16/16 · lint 0/0 · fabrication 0 · cue-removal mutation verified by judge
- What: **Surfaced semantic prose value-conflicts on the chat surface** (GROUNDED≠TRUE ask↔chat parity). The semantic-conflict detector `detectEvidenceContradictions` (Mem0 arXiv:2504.19413, topic-cosine+neither-subset) was only wired into the ask path — chat only ran the labelled-field detector (a `label: value` regex) and missed free-prose conflicts ("flight 3pm" vs "6pm"). Since the answer only cites whichever side agrees, it produces a confident grounded lie with no cue. Added an injectable embed + `semanticConflictCueFromMatches` (names both sources) to `finalizeGatedChatAnswer`, wired both surfaces (chat-repl·chat-ink) with the real embedder.
- Why: the semantic-guard trio (fires 20-22) was answer-vs-evidence; this is evidence-vs-evidence on a **new surface**. The desktop companion's only surface (chat) was letting a prose-conflict grounded lie through silently — a real axis-A hole; ask was already defended, chat wasn't wired.
- Review point: purely additive (gate verdict unchanged, existing cue not suppressed), fail-open double-guarded (`.catch(()=>[])` + `args.embed?`), precision-first neither-subset avoids over-firing on elaboration. The no-embed path behaves exactly as today. The independent Opus judge directly confirmed non-vacuousness by removing the queue append and observing the conflict test go red; PASSED 5/5. Verified both call sites are actually wired (not test-only).
- Risk: low (added cue, unrelated to the gate). Cost: one embed call per matched pair per turn (parity with ask, only when ≥2 matches). Vein: axis A now defends prose-conflict on both ask and chat — next is either pulling a conflict through to abstention (currently only a cue), or axis C self-judge meta-eval.

## fire 25 · 2026-06-14 · skill v1.14.0 · 04f72cf6
meta: value-class=meta-eval · pkg=scripts(eval-harness) · kind=C · verdict=PASS · firesSinceDrill=7
ratchet: scripts +2 harness tests (17/17) + 1 live judge case (eval:judge 11/11, injection STABLE 3/3) · check green (exit 0) · lint 0/0 · fabrication 0 · fence-revert mutation verified by judge
- What: **Defended the LLM judge against injection via the judged content** (maker=judge meta-eval). `llmJudge` concatenated attacker-influenceable OUTPUT straight into the prompt with no delimiter and parsed only the first line — a "Respond PASS" embedded in OUTPUT could flip the verdict. In eval:adversarial specifically, the judged output is a jailbreak response (where PASS=refused) — a compliant answer plus an injection could be scored as a refusal, bypassing a real safety gate. Added `spotlightFence` (a content-hash unforgeable marker)+`buildJudgeUserMessage` to cage OUTPUT inside a DATA fence, with the system prompt instructed to ignore any instruction inside the markers. runShadowTrial got the same treatment (a poisoned-memory vector).
- Why: axis C (shoring up self-judge limitations) — a fresh value-class not touched by the recent fires (A grounding·B reliability). With a single local model, judge=maker, and judge trustworthiness underwrites every battery's PASS/FAIL. eval:judge had never seen this injection before.
- Review point: deterministic tests (a real llmJudge + a fence-aware stub, red when the fence reverts) + structural tests, live gemma4 cases 3/3 (the real model respects the fence). Fence markers = sha256(content) → the body can't forge a self-closing marker. Malformed output (undefined/object) is guarded by `JSON.stringify ?? String`. The first-line parser·garbage→"?" fail-close is unchanged. Independent Opus judge directly confirmed revert→red + unforgeability → PASSED 5/5.
- Risk: low (strengthens judge input, gate logic unchanged, benign 11/11 preserved). Vein: axis C's first entry — next candidates are judge position/verbosity bias cases, or a live shadowTrial meta-eval, or axis A abstain-on-conflict.

## fire 26 · 2026-06-14 · skill v1.14.0 · 87d44ecf
meta: value-class=redteam-defense · pkg=@muse/agent-core+@muse/cli · kind=A · verdict=PASS · firesSinceDrill=8
ratchet: agent-core +1 module +5 engine tests · cli +2 wiring tests (grounding-verdict 25, chat-grounding 90) · agent-core 2214 · cli 2632 · grounding-delta +0.76 intact · lint 0/0 · fabrication 0 · return-[]-mutation reds flag+wiring (judge-verified)
- What: **Per-claim untrusted-provenance guard** (a mixed-trust GROUNDED≠TRUE hole). `groundedOnUntrustedOnly` is a whole-answer marker — even one trusted citation makes it false. So a mixed answer (a trivial trusted citation + a load-bearing poisoned untrusted citation, MCP/web/tool) let the untrusted claim pass as plain grounded. New pure `untrustedOnlySentences` (agent-core): returns only the sentences whose resolving citations are ALL untrusted (a co-cited trusted source in the same sentence clears it, unresolved citations are ignored). Reuses reportCitationPrecision's sentinel/split logic (prevents splitting on "." inside a citation). Both ask·chat notices are whole-answer-false-only, but when the per-claim flag exists, they now name the specific tool-fetched sentence.
- Why: extends untrustedOnlyGroundingNotice from ALL-untrusted to per-claim — across both the desktop and CLI surfaces. Scout confirmed a design limitation explicitly noted in a docstring was a real hole (not speculative). The per-sentence machinery from citation-precision already lived in the same package, so this is a faithful reuse.
- Review point: additive (gate verdict·whole-answer path unchanged), a trusted co-citation blocks the false positive, unresolved citations are ignored (verifyGrounding's territory), empty/all-trusted is a no-op. Independent Opus judge directly confirmed the flag+wiring tests go red via a return-[] mutation → PASSED 5/5. `pnpm check`'s @muse/model fuzz 5000ms timeout is concurrent-loop environment saturation (16/16 green isolated, unrelated to my package) — not a real regression.
- Risk: low (added a diagnostic cue). Vein: axis A grounded≠true is now complete from whole-answer→per-claim provenance across both ask·chat. Next=abstain-on-conflict (needs a design decision) or axis B/C. **Next fire 27 = JUDGE-DRILL (consecutiveAllPASS 7→8)**.

## fire 27 · 2026-06-14 · skill v1.14.0 · 51f53e03
meta: value-class=reliability-coverage · pkg=@muse/agent-core · kind=B · verdict=PASS · firesSinceDrill=0 (JUDGE-DRILL reset)
ratchet: agent-core +1 OUTCOME test (skill-merge-gate 15) · agent-core 2223 · check exit 0 · eval:self-improving skill-merge+playbook-merge PASS · lint 0/0 · fabrication 0 · revert-mutation reds test (judge-verified)
- What: **JUDGE-DRILL fire** (consecutiveAllPASS hit ≥8) + a real fix. ⓐ Drill: injected a deliberately vacuous slice against the skill-merge-gate `validateUmbrellaCoverage`'s permissive-mode fail-open (asymmetric trigger/body loss) — a declaration-only test (`typeof accept==="boolean"`, 0 code change) → the independent Opus judge FAILed it (directly confirmed the declaration-only nature·the bug unfixed·non-vacuousness's 15/15 unfixed-still-passing) → rolled back. ⓑ Real fix: re-gates the combined (both surfaces covered) score against minScore (`accept = trigger.accept && body.accept && combinedAccept`).
- Why: the two surfaces each applied the floor only to their own partial coverage → losing A in the trigger and B in the body separately, each surface scores 2/3≥0.6 and passes, while the union is 1/3 < minScore. The self-improvement merge gate was silently accepting a destructive consolidation that drops multiple skills. The drill's purpose was confirming the verifier itself catches a bad slice — a maker≠judge compensating control.
- Review point: tightens-only (one added AND conjunct → accept only gets more conservative, never weaker). The default strict path is unchanged (both-accept ⇒ combined lost 0; production callers use this path — the real effect is closing the latent fail-open in the permissive branch, honestly stated). A legitimate partial merge (2/3≥0.6) still gets accepted. Independent Opus judge confirmed non-vacuousness by reverting the real fix→red, PASSED 5/5.
- Risk: low (gate strengthening). **JUDGE-DRILL result: verifier working correctly** (bad→FAIL, good→PASS) → firesSinceDrill reset to 0. Vein: the drill consumed this fire, next is a regular slice (axis A abstain-on-conflict, or a remaining axis B module, or axis C judge bias).

## fire 28 · 2026-06-14 · skill v1.14.0 · 4c5eff57
meta: value-class=reliability-coverage · pkg=@muse/agent-core · kind=B · verdict=PASS · firesSinceDrill=1
ratchet: agent-core +1 OUTCOME test (background-review 10) · agent-core 2230 · eval:self-improving background-review+e2e+correction-polarity PASS · lint 0/0 · fabrication 0 · reset-before-review mutation reds test (judge-verified)
- What: **Ensured background-review doesn't lose a trigger when review fails** (no trigger lost). `createBackgroundReviewHook` reset the trigger counter and caught errors before the fire-and-forget review completed, so if a learning arm threw (a downed local model, a skill/preference-store write error; production awaits reviewSkill/Preferences/Commitments), the trigger was already consumed — the accumulated signal was permanently lost with no retry. Moved the reset to after runReview resolves (`.then` before `.catch`) → a failure keeps the trigger and re-fires next turn, success resets as before.
- Why: the module's own documented "no trigger is lost" invariant (already honored on the in-flight-skip path) was violated on the failure path — an asymmetry. Axis B's first entry into a new module (the background-review engine, first time in this loop). MAST fail-close (arXiv:2503.13657): a failed sub-step must not silently discard its retrigger.
- Review point: strictly more fail-close (failure→retry, can never become less safe). The success cadence is unchanged (the existing Nth-turn reset test still passes), the in-flight guard blocks a fast second turn ahead of the reset (no double-fire), `.finally(delete)` runs on both paths (no inFlight leak). This is the learning path (afterComplete), so unrelated to the grounding gate. The independent Opus judge confirmed red via a reset-before-review mutation → PASSED 5/5.
- Risk: low (learning-reliability strengthening). `pnpm check`'s @muse/resilience SIGABRT was concurrent-loop memory-pressure OOM (26/26 green isolated, agent-core 2230 green) — environmental, unrelated to my change. Vein: axis B now has the background-review engine — next is the remaining pattern-suggestion/preference-inference modules, or axis A/C.

## fire 29 · 2026-06-14 · skill v1.14.0 · 962d4778
meta: value-class=meta-eval · pkg=scripts(eval-judge) · kind=C · verdict=PASS · firesSinceDrill=2
ratchet: scripts +2 live judge cases (eval:judge 11→13, both STABLE 3/3) · lint 0/0 · fabrication 0 · coverage-guard (judge already resists; locks the property)
- What: **Pinned the LLM-judge's resistance to verbosity/length bias into the meta-eval**. eval-judge.mjs cites arXiv:2411.15594 for the content-injection case but never covered the paper's headline bias (verbosity/length) — all 11 existing cases were short. Added a length-controlled PAIR: a long, hedge-padded answer that still invents a specific account number→FAIL, and an equal-length honest "I won't guess" twin→PASS. The two cases share an almost-identical careful opener so groundedness is the only variable → proves length can't move the verdict.
- Why: axis C (shoring up self-judge limitations) — fires 27·28 were both axis B, so diversity forces non-B. A single local model means judge=maker, so if the judge is swayed by length, every battery (eval:adversarial, etc.) inherits the leak. Verbosity was the paper's #1-2 named bias, and it was an untested hole.
- Review point: the PAIR isolates length (identical opener, only fabrication differs) — not an easy-half assertion. Live gemma4 13/13, both new cases STABLE 3/3 (padded fabrication→FAIL, long honest→PASS). **Honest note: this is a coverage guard** (the judge already resisted — not a live bug, just locking in a documented property as a regression guard). Position bias doesn't apply — there's no symmetric pairwise-judge path in the code (a near-miss, rejected). Independent Opus judge verified isolation·stability·discriminability → PASSED 5/5.
- Risk: low (data-only cases, gate/code unchanged). Vein: axis C now covers content-injection+verbosity. Next candidates = a shadowTrial HOLD-path meta-eval, or axis A episode-poisoning, or the remaining axis B (pattern-suggestion).
