# Loop journal — `local-speed`

Theme: local-model speed/efficiency routing (KV-quant env · cascade · spec-decode) — preserving accuracy/grounding floor.
Worktree `/tmp/muse-local-speed` · branch `loop/local-speed` · Tier2 push + 3-fire main merge.
Convention: [README](README.md).

## fire 1 · 2026-06-21 · local-speed · 5b6078cd
meta: value-class=new-capability · pkg=scripts(bench harness) · kind=measurement-infra · verdict=PASS · firesSinceDrill=1
ratchet: testFiles 1075 (+1 scripts node:test) · fabrication 0 · no eval delta (measurement substrate, no runtime touch)
- What: New `bench:local` harness — repeatable latency/throughput measurement + regression guard for local Ollama models. Pure core `scripts/lib/bench-metrics.mjs` (nsToMs·tokensPerSecond·sampleFromOllamaTimings·percentile·mean·summarizeSamples·detectRegression) + 10 node:test cases (`scripts/bench-metrics.test.mjs`, auto-folded into the `self-eval:test` glob) + live runner `scripts/bench-local.mjs` (raw Ollama `/api/generate` stream:false, server-authoritative timing, LOCAL-OLLAMA-ONLY skip). `package.json` "bench:local", baseline json gitignored.
- Why: Per spec, "if there's no repeatable bench to *measure* speed yet, build bench:local as the first slice." The existing `dogfood-local-llm.mjs` is one-shot·manual·has no stats/regression-guard/testable core → subsequent speed slices (cascade·spec-decode·KV-quant) need a deterministic substrate to use as a regression guard. `detectRegression` is that gate.
- Review point: timing units (ns→ms÷1e6, tok/s=count/(ms/1000)), TTFT=load+prompt_eval, detectRegression drop/rise direction + base>0 guard (first-run no-baseline → doesn't emit a false regression), percentile interpolation edges (n=1/empty/p0/p100), baseline is machine-specific → gitignored.
- Risk: low — 0 lines of runtime/package source changed (scripts/ is eslint-ignored, belongs to no package) → grounding/tool-selection bytes unchanged → accuracy regression 0 by construction. The live runner skips (exit 0) when Ollama is down, per smoke:live convention.
  Verified: bench-metrics.test 10/10 + 2 MUTATION-FIRST cases (tok/s formula · regression-direction flip → RED, revert → green) · pnpm check rc=0 (api 864 + cli 2857) · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (reproduced its own mutation, 0 defects).
  Found (recorded in backlog): `scripts/reflection-guard.test.mjs` is a pre-existing FAIL on origin/main — the false-done re-run marker `'const actNow'` in `apps/cli/src/chat-repl.ts` has drifted. Unrelated to this slice (no retry/reflection surface added) → out of scope, backlog ◦.

## fire 2 · 2026-06-21 · local-speed · dc6b7472
meta: value-class=new-capability · pkg=@muse/model+@muse/autoconfigure · kind=adapter-wiring · verdict=PASS · firesSinceDrill=2
ratchet: pkg/kind DIFFERS from fire 1 (scripts/measurement-infra → model+autoconfigure/wiring) · fabrication 0 · default wire byte-identical
- What: Opt-in wiring for Ollama `num_batch` (prompt-processing batch size, Ollama default 512) — a larger batch = higher prompt-eval throughput (the dominant latency for a long prompt). `MUSE_OLLAMA_NUM_BATCH` env → autoconfigure → `OllamaProvider({numBatch})` → native `/api/chat` `options.num_batch`. The adapter is the single authority on validation (>0·finite·trunc, otherwise undefined→omit). Unset = wire is byte-identical to today.
- Why: The next unwired adapter speed lever after KV-quant (doctor already detects/recommends it) and prefix-reuse (already shipped). Directly attacks backlog line 2326 "10K-token prompt eval ≈ 40s." Measurable with fire 1's bench:local (when Ollama is up on the box).
- Review point: opt-in default-off (accuracy regression 0 by construction), num_batch is a throughput knob, not a sampling parameter (generated tokens unchanged), junk/0/neg/NaN env → omit (never wires a broken option), generate+stream paths identical (single buildNativeChatBody), no non-Ollama leakage.
- Risk: low — the default path is unchanged. Unmeasured = only the *magnitude* of the speed win is unknown (the slice claims "a wired opt-in lever," not "a measured number"; measured tuning is a backlog follow-up).
  Verified: model 366 pass + autoconfigure 622 pass · 2 MUTATION-FIRST cases (A: num_batch never emitted → 2 RED; B: always-512 → 5 RED including the existing wire snapshot; revert→green) · pnpm check rc=0 (api 864 + cli 2857) · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (reproduced its own mutation, traced every edge value's omit, 0 defects).
  Sibling audit: num_thread/num_gpu (hardware-offload knobs) enumerated then deferred to backlog ◦ (hardware-specific · Ollama's auto-detection is usually accurate · needs per-box benching).

## fire 3 · 2026-06-21 · local-speed · 3a038607
meta: value-class=new-capability · pkg=@muse/multi-agent · kind=cascade-routing(paper) · verdict=PASS · firesSinceDrill=3
ratchet: pkg/kind DIFFERS (scripts/infra → model/adapter → multi-agent/cascade) · fabrication 0 · default path byte-identical
- What: FrugalGPT (arXiv:2305.05176) cascade escalation-decision primitive C1. `shouldEscalateToHeavy(confidence, threshold=-1.0)` + `DEFAULT_CASCADE_ESCALATE_LOGPROB` (@muse/multi-agent tiering.ts) + an optional `priorConfidence?: ReadonlyMap<id, number|undefined>`/`escalateThreshold` on `planTieredRun` — escalates a task classified fast, present in priorConfidence, and with a low fast-pass mean-logprob, to heavy (plan assignment = a real terminal-state change). confidence = mean token logprob (≤0, higher = more confident); undefined/NaN/-Inf → escalate (the safe direction, mirroring classifyTier's default-to-heavy).
- Why: The existing classifyTier only *pre*-decides tier from text (lexical) — a weak fast answer is accepted as-is. The missing lever was a real cascade (fast model first → escalate to heavy if weak). summarizeTokenConfidence (@muse/agent-core) already computes confidence (commands-ask.ts:2870) but doesn't use it for the escalation decision → that's the gap. Runtime two-pass (C2) + measurement (C3) are decomposed into the backlog (DECOMPOSE-ON-DEFER, not scoped down to the easy part).
- Review point: default (no priorConfidence) = plan byte-identical (`.has(id)===true` guard), a heavy task is never de-escalated (`tier==="fast"` guard), classifyTier unchanged (only const→let), no package coupling (plain number, no agent-core type import), strict `<` (exactly -1.0 does not escalate).
- Risk: low — behavior inert by default. C1 is a wired behavior change in the real planner (planTieredRun, called from apps/api multi-agent-routes.ts:460), not a dead primitive (the ④ judge explicitly ruled it not declaration-only). C2 is the runtime loop that fills priorConfidence with real logprobs.
  Verified: multi-agent 215 pass · 2 MUTATION-FIRST cases (A: `<`→`>` → 5 RED; B: `.has` guard → `!==undefined` → 1 RED on the ABSENT-untouched case; revert→green) · pnpm check rc=0 (api 880 + cli 2857; the first run's messaging-webhooks 1-FAIL was a parallel env-leak flake = isolated 880/880 + my change is unrelated to api/messaging, rerun rc=0) · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (reproduced its own mutation, ruled the declaration-only crux HONEST, 0 defects).

## fire 4 · 2026-06-21 · local-speed · b60fbebb
meta: value-class=micro-fix · pkg=apps/cli · kind=correctness-hardening(doctor) · verdict=PASS · firesSinceDrill=4
ratchet: pkg/kind DIFFERS (scripts/infra → model/adapter → multi-agent/cascade → cli/correctness) · fabrication 0 · runtime unchanged (doctor advisory string only)
- What: Hardened `muse doctor`'s `ollamaPerfPostureCheck` — added a dedicated WARN making explicit that quantized KV cache (`OLLAMA_KV_CACHE_TYPE=q8_0/q4_0`) is **INERT** without `OLLAMA_FLASH_ATTENTION=1` (Ollama silently falls back to f16 KV, zero gain). Previously the (KV set · flash off) case fell into a generic "set flash" message, hiding the fact that the user's KV setting was doing *nothing*.
- Why: A real silent-misconfiguration gap (ollama/ollama#13337 reports "server silently falls back to f16 → unexpected OOM"). KV-quant is a named lever. Source-verified (verify-then-apply): the Opus scout confirmed via the Ollama FAQ + PR #6279 (smcleod.net) + #13337 — KV-quant needs both flash attention AND an FA-capable model arch. The warning honestly covers the arch nuance with "needs a flash-attention-capable model," a version-brittle allowlist is not hardcoded (deferred to backlog).
- Review point: the 4-case branch is correct (flash+kv=ok / neither=set both / flash only=set KV (FLASH not included) / kv only=new INERT), `.toLowerCase()` handles "Q4_0", the not-FA-capable-model case (flash+kv+non-FA-model→"ok" but actually inert) is deferred to backlog ◦ (needs a runtime arch query, no hardcoding).
- Risk: none — only one doctor advisory string changed, unrelated to model/runtime/grounding/tool paths → accuracy regression 0 trivially.
  Verified: cli 2861 pass (2 new OUTCOME cases) · MUTATION-FIRST (remove the INERT branch → 2 RED "expected … to contain INERT"; revert→green) · pnpm check rc=0 (api 887 + cli 2861) · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (re-verified the factual claim's accuracy via web search, reproduced its own mutation; flagged 1 duplicate-comment defect → fixed).
  Sibling audit: exposing Muse-process speed env (MUSE_OLLAMA_NUM_BATCH etc.) in doctor + an FA-capable-model-arch warning → 2 items to backlog ◦.

## fire 5 · 2026-06-21 · local-speed · fe634ab8
meta: value-class=new-capability · pkg=@muse/multi-agent · kind=cascade-execution · verdict=PASS · firesSinceDrill=5
ratchet: (multi-agent, cascade) 2/5 fires, distinct kind from fire 3 (decision→execution) · fabrication 0 · additive (0 in-repo callers)
- What: FrugalGPT cascade EXECUTION primitive `runCascade<T>({fast,heavy,run,confidenceOf,threshold})` → `{result,tier,escalated,fastConfidence}` (@muse/multi-agent cascade-run.ts). Runs the fast model first → accepts that answer if confident (one model run = the latency win), escalates to heavy ONCE if confidence is low/unmeasurable. Single bounded escalation (no loop, a MAST step-repetition/termination guard). Reuses fire 3's `shouldEscalateToHeavy`.
- Why: fire 3 was the cascade DECISION (whether to escalate) + planner wiring; this is the EXECUTION that turns that decision into real compute savings (a confident lookup never pays the heavy cost). Model-agnostic = the caller injects run/confidenceOf (this package's idiom: summarizeWorkerOutput/verifyFinalAnswer/detectConflicts are all injection-based). Injecting a real model-run+summarizeTokenConfidence is C2b (autoconfigure, backlog).
- Review point: confident→only 1 run (the efficiency claim is directly verified via `calls).toEqual([fast])`), low→[fast,heavy] order·heavy result, exactly 2 runs even when both are low (no re-escalation), strict `<` (exactly -1.0 keeps), additive (tiering.ts unchanged, only 2 export lines in index.ts).
- Risk: none — new, 0 in-repo callers → existing behavior unchanged → accuracy regression 0 by construction. Honest limitation: this ships the capability, not yet a *measured* latency win (that's C2b/C3; noted in backlog).
  Verified: multi-agent 221 pass (5 new OUTCOME cases, scored on run-count/order/result) · MUTATION-FIRST (flip the `!` gate → 5/5 RED; revert→green) · pnpm check rc=0 (api 887 + cli 2861) · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (explicitly ruled the declaration-only crux NOT declaration-only, reproduced its own mutation, grep-confirmed no loop, ratchet 2/5 OK, 0 defects).
  decompose: C2-core DONE; C2b (autoconfigure injecting a real run+confidenceOf into ask --tiered/orchestration) + C3 (live eval) remain in backlog.

## fire 6 · 2026-06-21 · local-speed · 4ef37893
meta: value-class=new-capability · pkg=apps/cli · kind=doctor-discoverability · verdict=PASS · firesSinceDrill=6
ratchet: cli 2/6 (fire 4 correctness-hardening vs fire 6 capability, distinct kind) · fabrication 0 · advisory-only, no runtime change
- What: New `museSpeedEnvCheck`+`readMuseSpeedEnv` (apps/cli) wired into `muse doctor` — reports Muse-PROCESS speed env (`MUSE_OLLAMA_NUM_BATCH` fire 2's lever · `MUSE_OLLAMA_NUM_CTX` · `MUSE_OLLAMA_KEEP_ALIVE`) posture on every doctor run + a concrete tuning hint when num_batch is unset. A separate surface from `ollamaPerfPostureCheck` (server launchctl env).
- Why: fire 2 shipped the num_batch lever but never told the user anywhere = only half-delivered. doctor is where Muse's speed posture is reported → make the lever discoverable. As a merge fire, chose a complete, no-dead-layer slice instead of stacking cascade C2b (live ask-path wiring, multi-fire · Ollama-gated) as another dead layer (avoiding the no-caller point fire 5's judge raised about runCascade).
- Review point: a real push into the doctor checks array (commands-doctor.ts assembly point, rendered every run), all 3 env names are the real names the runtime actually reads (autoconfigure num_ctx/num_batch · adapter keep_alive — judge re-confirmed), always "ok" (an unset optional knob is a safe default, not a misconfiguration, so a warn would be noise), whitespace via `.trim()` treated as unset (a broken `num_batch=` doesn't slip through), the hint doesn't overclaim (num_batch=prompt batch size, larger value=throughput↑ at VRAM cost).
- Risk: none — an advisory diagnostic only, unrelated to model/runtime/grounding/tool → accuracy regression 0. Always-ok, so it can't raise the worst-case verdict (existing verdict unchanged).
  Verified: cli 2865 pass (4 new OUTCOME cases) · MUTATION-FIRST (hint always-empty → 3 RED; revert→green) · pnpm check rc=0 (api 888 + cli 2865) · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (re-confirmed env names via grep, confirmed wired-and-live, ruled value-first, 0 defects, safe to merge).

## fire 7 · 2026-06-21 · local-speed · 2dacfb99
meta: value-class=new-capability · pkg=@muse/model+@muse/autoconfigure · kind=adapter-wiring · verdict=PASS · firesSinceDrill=7
ratchet: (model+autoconfigure, adapter-wiring) 2/7 (fire 2 num_batch throughput vs fire 7 output cap = distinct lever) · fabrication 0 · default wire byte-identical
- What: Opt-in generation-length default cap `MUSE_OLLAMA_NUM_PREDICT` — a num_predict ceiling applied only to requests without a maxOutputTokens. env → autoconfigure → `OllamaProvider({numPredict})` → native options. Priority: request.maxOutputTokens > this.numPredict (default) > omit. The adapter is the single authority on validation (>0·finite·trunc, otherwise omit). Unset = stays at today's unlimited (-1).
- Why: Ollama's default num_predict=-1 (unlimited), so a generate call without maxOutputTokens can let a looping local model blow up in latency/resources. ★The actual unlimited path = the **main agent runtime** (agent-runtime.ts:323,384 `this.defaults?.maxOutputTokens`, unset by autoconfigure → foreground muse ask/chat is unlimited) + orchestrate workers (when the caller doesn't set it). [Correction: the first motivation wrongly named the background loops (proactive-notice·memory-auto-extract·followup-firing·knowledge-recall) as unlimited → the ④ judge caught a GROUNDED≠TRUE slip; actual verification found those are all capped (512/200/200/24). The code is correct and the gap is real, but the example was wrong, so it's corrected here].
- Review point: opt-in default-off (unset → wire byte-identical, existing snapshot green), request maxOutputTokens always wins (an explicit N-token request ignores the global cap), junk/0/neg/3.5 env → omit (parseInteger's integer regex + >0 guard), no non-Ollama leakage (only the ollama case generates it), generate+stream paths identical (buildNativeChatBody).
- Risk: low — the default path is unchanged (accuracy regression 0 by construction). When the cap is SET a long answer may truncate, but (a) it's the same kind of opt-in trade-off as the already-shipped per-request maxOutputTokens, (b) the grounding gate operates on the generated text → truncation only hurts completeness, not faithfulness/fabrication-rate (explicitly confirmed by the ④ judge). Honest limitation: unmeasured (Ollama down) — the magnitude of the cap's latency win follows up via C3/bench.
  Verified: model 382 pass + autoconfigure 623 pass (4+1 new OUTCOME cases, scored on the wire body) · MUTATION-FIRST (flip the priority → "maxOutputTokens WINS" 1 RED; revert→green) · pnpm check rc=0 (model 382 + api 888 + cli 2873) · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (code correct·gap real·gate green; but caught a motivation misgrounding → code comment+journal correction completed).
  Sibling audit: exposing num_predict in fire-6's doctor museSpeedEnvCheck → deferred to backlog ◦ (staying focused). The adapter option siblings (num_ctx/num_batch/keep_alive) already have defaults — num_predict was the only no-default runaway knob.
lesson: don't conclude a generate() call site "doesn't set maxOutputTokens" from a grep alone — actually read each call's arguments to confirm (this time all the background loops turned out to be capped). The real unlimited path was the main path where runtime defaults are empty. The motivation itself is a grounding target too.

## fire 8 · 2026-06-21 · local-speed · 5971e7ae
meta: value-class=micro-fix · pkg=apps/cli · kind=doctor-discoverability · verdict=PASS · firesSinceDrill=8
ratchet: (cli, doctor-discoverability) 2/8 (fire 6 num_batch, fire 8 num_predict) · fabrication 0 · advisory-only, no runtime change
- What: Added `MUSE_OLLAMA_NUM_PREDICT` (fire 7's generation cap) to fire-6's `museSpeedEnvCheck`/`readMuseSpeedEnv` — doctor now reports all 4 Muse speed envs (num_batch/num_ctx/keep_alive/num_predict). A consistency fix.
- Why: fire 7 shipped the num_predict env but the fire-6 doctor check only reported 3 and silently omitted num_predict → the "speed env" line was incomplete/misleading. Makes the shipped lever discoverable (same rationale as fire 6's num_batch).
- Review point: env name is real (autoconfigure:197 read + adapter:385 num_predict mapping, ④ judge re-confirmed via grep), whitespace `.trim()` guard (numPredict="  "→not reported), always "ok" (optional knob), the num_batch hint still shows when only num_predict is set (num_batch is unset, so it's coherent).
- Risk: none — doctor advisory only, unrelated to model/runtime/grounding → accuracy regression 0.
  Verified: cli 2875 pass isolated (2 new OUTCOME) · MUTATION-FIRST (remove the num_predict push → 1 RED; revert→green) · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (env name real · env-flake honestly reported · value-first · ratchet OK · 0 defects). ⚠️full pnpm check OOMed (SIGABRT) from box memory pressure (concurrent loops) + an unrelated flake (@muse/model web-search-policy property-fuzz · @muse/cli document-reader PDF 5s timeout) — both isolated-green, my change is apps/cli string-only and unrelated → ruled an env flake (same class as fire 3·6).

## fire 9 · 2026-06-21 · local-speed · efc944ab
meta: value-class=test(regression-guard) · pkg=@muse/model · kind=judge-drill+grounding-guard · verdict=PASS · firesSinceDrill=0
ratchet: (model) 3,7,9 but kind=regression-guard distinct from 2,7 adapter-wiring · fabrication 0 · TEST-ONLY, no source change
- ★JUDGE-DRILL (triggered by consecutiveAllPASS=8, cannot be deferred): fake bad-slice → confirm ④ judge FAIL → rollback → real fix.
- Drill: the deliberately bad slice = an opt-in `MUSE_OLLAMA_LEAN_PROMPT` that drops the system message on a long turn (>8000 chars) for "speed." The deterministic gate was GREEN (opt-in · happy-path test only). The independent Opus ④ judge returned **exactly FAIL** — the system message carries the grounding/citation/honesty contract + the evidence itself (commands-ask.ts:2008-2089), so dropping it = fabrication; the >8000 trigger fires precisely on grounded turns; opt-in can't excuse it (the flag's whole purpose breaks the floor); the test was a tell (it named the dropped content "grounding rules" and scored the removal as success). → proves the ④ gate is not a rubber stamp (validates the gate the 8 prior fires relied on). Rolled back via git restore.
- Real fix (shipped): 2 grounding-preservation regression guards (@muse/model adapter-ollama.test.ts) — (1) the system message is ALWAYS wired through even on a large prompt (roles==["system","user"]), (2) the configured num_ctx is unchanged regardless of prompt length (never silently shrunk). Elevates the invariant the drill exposed from "caught by the judge" to "caught by the deterministic suite" (deepens the fabrication=0 floor's defense).
- Review point: scored on the wired body (not declaration-only), MUTATION-FIRST on both guards (the judge reproduced it itself: filter out system → test1 RED; num_ctx Math.min(,4096) → test2 + 6 existing-snapshot RED; revert→green), source is byte-clean (test-only → accuracy regression 0 trivially), the invariant is real (system=grounding contract · num_ctx shrinkage=silent truncation, documented in the adapter's comment).
- Risk: none — test-only, runtime unchanged.
  Verified: model 384 pass (2 new guards) · 2 MUTATION-FIRST cases (independent judge reproduced RED) · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (guard slice, reproduced its own mutation · confirmed the invariant is real · honestly reported the env-crash · 0 defects). ⚠️full pnpm check aborted from box OOM (SIGABRT, packages/runtime-state unrelated) but packages/model had 384 pass within that same run (same env-flake class as fire 8).
lesson: a judge-drill must design the fake slice to *pass the deterministic gate* while *violating only the invariant* to actually test the JUDGE (if a source change breaks an existing test, ③ catches it and the judge goes unverified). The grounding contract rides on the system message → a "prompt diet/lean" class of speed optimization is almost always a floor violation. Shipping the drill's byproduct (the invariant regression guard) as the real fix converts the drill into a permanent defense.

## fire 10 · 2026-06-21 · local-speed · 792a408a
meta: value-class=wiring · pkg=@muse/agent-core · kind=runtime-logprobs-plumbing · verdict=PASS · firesSinceDrill=1
ratchet: @muse/agent-core FRESH (fires 1-9 untouched) · fabrication 0 · default wire byte-identical
- What: Opt-in token logprobs wired into the agent runtime. `AgentRunInput.logprobs`/`topLogprobs` → forwarded as `ModelRequest.logprobs` at both request-build seams (loopRequest generate + streamLoopRequest stream), returned round-trip as `AgentRunResult.response.logprobs`. A `logprobsFromInput()` helper (unset→{} = byte-identical, keeps both seams in sync). AGENT runs can now be confidence-scored with `summarizeTokenConfidence`.
- Why: the decomposed FIRST step of cascade C2b (DECOMPOSE-ON-DEFER). The agent path didn't forward logprobs, so a confidence-gated cascade was impossible (the direct ask path commands-ask.ts:2870 already had logprobs). This closes that prerequisite gap. The consumer (wiring runCascade) is the C2b-wiring backlog item.
- Review point: both seams wired (a missing stream path would silently drop it — sibling-audited; the cache/prepareModelRequest seam isn't a model call → unnecessary), default (logprobs unset) = no logprobs field on ModelRequest, byte-identical (preparedRequest.request is only messages|metadata|model → the {} spread can't clobber it, judge confirmed), logprobs are observational-only (decoding unchanged), a non-capable provider ignores it (doesn't throw).
- Risk: low — opt-in default-off → accuracy regression 0 by construction. logprobs are observation-only (generation unchanged). Dead layer? Round-trip complete + consumable today via summarizeTokenConfidence + a decomposed prerequisite (precedent: fire 3/5's substrate) → the ④ judge explicitly ruled it ACCEPTABLE (not filler, closes a real prerequisite gap).
  Verified: agent-core 2571 pass (3 new round-trip OUTCOME cases — scored via captureProvider on request.logprobs + response.logprobs + meanLogprob≈-0.3) · MUTATION-FIRST (flip the helper gate → 3/3 RED including the default-off byte-identical guard; revert→green) · pnpm check rc=0 (agent-core 2571 + api 888 + cli 2878) · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (reproduced its own mutation · audited both seams for completeness · confirmed byte-identical is airtight · ruled the dead-layer crux ACCEPTABLE · 0 defects).
  decompose: C2b-plumbing DONE (this fire); C2b-wiring (actually connecting runCascade to ask --tiered/orchestration) + C3 (live eval) remain in backlog.

## fire 11 · 2026-06-21 · local-speed · 5f4df275
meta: value-class=new-capability · pkg=apps/api · kind=cascade-live-wiring · verdict=PASS · firesSinceDrill=2
ratchet: apps/api FRESH (fires 1-10 untouched) · fabrication 0 · default (env off) plan byte-identical
- What: LIVE-wired the FrugalGPT (arXiv:2305.05176) cascade into multi-agent orchestration. `createCascadeWorker` (apps/api multi-agent-routes.ts) bridges fire 5 (runCascade) + fire 10 (agent-run logprobs) + summarizeTokenConfidence: a FAST-classified worker runs the fast model with `logprobs:true` → confidence-scores it → escalates to heavy ONCE (bounded) if low. `MUSE_TIERED_CASCADE` opt-in wired into `buildTieredOrchestration` (default off → plan byte-identical).
- Why: actually consumes the prerequisites laid down by fire 3 (decision) · 5 (execution primitive) · 10 (logprobs plumbing) = cascade is finally LIVE. Resolves runCascade's dead-layer status. A confident lookup only pays the fast cost (latency win), a weak fast answer gets upgraded to heavy (a POSITIVE for accuracy — the exact opposite of fire 9's drill bad-slice).
- Review point: really wired (buildTieredOrchestration→createCascadeWorker, only when env is set + fast tier + !collapsed), opt-in default-off (plan byte-identical, an "OFF by default" test pins 1 fast run · no logprobs · no escalate), grounding contract preserved (spec.systemPrompt kept via prependSystem — not dropped like the drill's bad slice), bounded (runCascade caps at 2 calls), cascade skipped when collapsedToHeavy.
- Risk: low — opt-in default-off → accuracy regression 0; when on, upgrades weak answers (strengthens the floor). logprobs observational only.
  Verified: apps/api 892 pass (4 new OUTCOME cases — scored via confidenceRuntime on escalate[fast,heavy] · keep[fast] · logprobs:true · OFF-default) · 2 MUTATION-FIRST cases (confidenceOf→()=>0 → escalation RED; gate→true → OFF-default RED; revert→green) · pnpm check rc=0 with only a web-search-policy property-fuzz flake (isolated 384/384, same class as fires 6/8, unrelated to my change) + my packages all green · smoke:broad 51/0 · lint rc=0 · independent Opus ④ judge PASS (reproduced both mutations · confirmed cascade is LIVE · confirmed floor strengthening · ruled the NUL fix justified · 0 defects).
  Found + fixed (separate commit): packages/memory/src/recently-learned.ts had 3 raw NUL (\x00) separators breaking the byte-hygiene gate (introduced by learning-surfacing fire 11's 6df61b98 landing on origin/main → arrived via merge, blocking the shared pnpm check for every loop). Replaced with \x00 escapes (identical runtime NUL, only the source is escaped) → unblocked the shared gate. [separate (memory) fix commit]
  decompose: C2b-wiring (orchestration) DONE; wiring the ask --tiered single-query path + C3 (live eval) remain in backlog.

## fire 12 · 2026-06-21 · local-speed · 1e32082a
meta: value-class=new-capability · pkg=@muse/model+@muse/autoconfigure · kind=adapter-wiring · verdict=PASS · firesSinceDrill=3
ratchet: (model, adapter-wiring) 2/8 window (fire 7,12) — below threshold; completes the fire-2 sibling family · fabrication 0 · default wire byte-identical
- What: Opt-in wiring for the last 2 unwired Ollama adapter speed knobs — `MUSE_OLLAMA_NUM_THREAD` (CPU threads) + `MUSE_OLLAMA_NUM_GPU` (GPU layer offload). Mirrors the verified num_batch/num_predict pattern. ★KEY: `num_gpu=0` (CPU-only) is a VALID opt-in (unlike num_batch) → adapter `>= 0` validation + autoconfigure `parseNonNegativeInteger` (the num_gpu=0 test caught a real bug where `parseInteger` rejects 0). num_thread uses `> 0`.
- Why: the last 2 knobs enumerated by fire 2's sibling audit → completes the Ollama adapter speed-knob family (num_ctx/num_batch/num_predict/keep_alive/num_thread/num_gpu). As a merge fire, chose the complete, low-risk slice; the higher-value ask --tiered cascade is monolithic/multi-fire and legitimately deferred (backlog).
- Review point: num_gpu=0 reaches the wire (CPU-only, must not be dropped — a test caught the parseInteger 0-rejection bug → fixed via parseNonNegativeInteger), num_thread `>0` vs num_gpu `>=0` validation difference, opt-in default-off (both omit when unset = byte-identical), generate+stream both paths (single buildNativeChatBody), no non-Ollama leakage (only the ollama case), junk/edge cases (thread 0/neg/junk→omit; gpu 0→keep, neg/junk→omit, 33→keep).
- Risk: low — opt-in default-off → accuracy regression 0. A hardware/batching knob, generated tokens unchanged. num_gpu=0=CPU-only is an honest memory/speed trade-off (for OOM avoidance), noted as not a free speed win.
  Verified: model 387 + autoconfigure 624 pass (4 new OUTCOME cases — including the num_gpu=0 wire) · MUTATION-FIRST (num_gpu `>=0`→`>0` → num_gpu=0 RED; the ④ judge reproduced 3 mutations RED including removing the num_thread spread + the autoconfigure parseInteger misparse) · pnpm check rc=0 (model 387 + api 946 + cli 2887) · smoke:broad 52/0 · lint rc=0 · independent Opus ④ judge PASS (web-verified the meaning of num_gpu=0 · reproduced 3 mutations · ruled ratchet/value-first · 0 defects; merge-sound).

## fire 13 · 2026-06-21 · local-speed · 35b8fb07
meta: value-class=test(eval-harness) · pkg=scripts · kind=measurement-infra(C3 cascade proof) · verdict=PASS · firesSinceDrill=4
ratchet: (scripts, measurement-infra) 1/8 window (first since fire 1) · fabrication 0 · 0 lines of package source (scripts+1 line of package.json)
- What: cascade C3 proof `eval:cascade` — pure core `scripts/lib/cascade-eval.mjs` (meanMs · escalationRate · scoreCascadeEval: latencyWin + gateCorrect [escalate iff low-confidence]) + 8 node:test cases (auto-folded into the self-eval:test glob) + live runner `scripts/eval-cascade.mjs` (raw Ollama /api/chat logprobs, LOCAL-OLLAMA-ONLY skip, model-call timeout→clean skip). `package.json` `eval:cascade`.
- Why: the last leg of the cascade vein (fire 3 decision · 5 execution · 10 logprobs · 11 orchestration live) = the C3 proof. Measures whether the headline feature actually saves latency.
- ★LIVE measurement (Ollama up): fast=qwen3:8b/heavy=qwen3.6:35b-a3b → cascade is **23.9% faster** (12970ms vs 17045ms). Honest caveat (④ judge): escalation rate 0% (qwen3:8b was confident across the whole set, threshold -1.0) → the live run demonstrates only the *confident-query latency-win arm*; the escalate→heavy arm never fired. The runner's live gateCorrect is self-consistency (derived from the same predicate the scorer uses to re-confirm escalation), not an independent measurement — the adversarial proof of the gate is the UNIT test. → no overclaiming "PROVEN": stated honestly as latencyWin measured + gate logic unit-proven.
- Review point: pure-core OUTCOME scoring (verdict value), confidence proxy = mean content-token logprob (excluding <| markers), identical to summarizeTokenConfidence, scripts/ is eslint-ignored · unrelated to any package (runtime byte-identical), the runner's model-call uses AbortSignal.timeout→clean skip (judge defect 1 fix).
- Risk: none — 0 lines of package source → accuracy regression 0. eval:cascade is standalone (not in any aggregate gate, opt-in/manual).
  Verified: cascade-eval.test 8/8 · MUTATION-FIRST (gate `!==`→`===` → 4 RED; revert→green) · LIVE 23.9% latency win measured · confirmed the runner clean-skips (exit 0) on load · smoke:broad 52/0 · lint rc=0 · pnpm check had only a web-search-policy property-fuzz "Test timed out 5000ms" load-flake (isolated 1/3 green, box load 35 = Ollama 35B resident + concurrent loops, my change is scripts-only and unrelated) · independent Opus ④ judge PASS (reproduced its own mutation, honest about the 0%-escalation caveat · load-flake nuance · ruled value-first; flagged 2 defects [fetch timeout · gateCorrect self-consistency] → fetch timeout fixed + claim made honest, both completed).
  decompose: live proof of the escalate-arm (hard prompt/lower threshold) → backlog ◦. The ask --tiered single-query cascade also remains.

## fire 14 · 2026-06-21 · local-speed · 4a2c4587
meta: value-class=new-capability · pkg=apps/api · kind=startup-latency(warmup) · verdict=PASS · firesSinceDrill=5
ratchet: (apps/api) 2/8 window (fire 11 cascade-wiring vs 14 startup-warmup = distinct kind) · fabrication 0 · default-off startup byte-identical
- What: Opt-in model warmup `MUSE_WARMUP_MODEL` (apps/api `warmUpModelIfConfigured`) — at server startup, a small fire-and-forget generate (maxOutputTokens:1) preloads the model into Ollama so the first user request is warm. Wired as 1 line into the server.ts startup hook (next to the daemons).
- Why: keep_alive (the adapter default) keeps the model resident only *between* requests → the *first* request after server start bears a full cold load (tens of seconds for 12B). For an always-on companion (Muse's identity), first-request cold-start is the worst-felt latency. A different kind of latency lever, left over after the cascade vein was completed.
- Review point: opt-in default-off (unset → 0 generate calls = startup byte-identical), fail-soft (`Promise.resolve().then(()=>generate).catch()` — swallows both a sync throw and an async rejection, no unhandled rejection, never breaks server start — the ④ judge confirmed both the .catch and the wrapper are load-bearing via 3 mutations), returns void (doesn't delay listen), the warmup result is discarded (never touches any request's answer/grounding), non-cloud (reuses the existing provider, no new egress path).
- Risk: none — default-off → accuracy regression 0. Warmup only loads the model (generated tokens discarded), unrelated to grounding/tool.
  Verified: apps/api 959 pass isolated (5 new OUTCOME cases — fires-when-enabled/default-off/guard/fail-soft sync+async) · MUTATION-FIRST (enabled=true → default-off RED; the ④ judge additionally confirmed bare-void→sync-throw escape RED and .catch removal→unhandled-rejection leak) · build rc=0 · lint rc=0 · independent Opus ④ judge PASS (reproduced its own 3 mutations, confirmed fail-soft correctness · honest about box saturation · ruled value-first, 0 defects). ⚠️pnpm check + smoke:broad TIMED OUT from extreme box saturation (6 concurrent loops + Ollama 35B resident) (autoconfigure runtime-assembly-e2e 18× "Test timed out 5000ms", smoke >300s) — my change is apps/api-only and autoconfigure doesn't depend on apps/api (reverse direction) → cannot have broken it, ruled a box-saturation flake (apps/api isolated 959 + build + lint green).
  Sibling audit: exposing MUSE_WARMUP_MODEL in museSpeedEnvCheck (doctor) + measuring per-box cold-start delta → backlog ◦.
