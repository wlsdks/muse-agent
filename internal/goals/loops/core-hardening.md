# core-hardening — Muse core-edge hardening loop journal

> Theme: hardening Muse's core edge (deterministic grounding+citation gate + 4 surfaces: memory-integrity·self-development·orchestration·grounding-floor).
> Worktree `/tmp/muse-core-hardening` · branch `loop/core-hardening` (Tier2 — pushes to its own branch each fire, periodic rebase from origin/main). **Every 3 fires: ff-merge the branch into origin/main, then keep working on the branch (Jinan directive 2026-06-20).**
> Cron `d8c31fa3` (every 15m, session-only; was `cfe778e2` under skill v1.14.0, re-registered with loop-creator v2.0 at fire 6). Stop: `CronDelete d8c31fa3`. Convention: [README](README.md).

## fire 11 · 2026-06-20 · skill v2.0 · 14810a1f (honest EXHAUSTION, no code slice)
meta: value-class=refactor(work-list) · pkg=docs · kind=exhaustion-honest-exit · verdict=N/A · firesSinceDrill=1
ratchet: testFiles 1060→1060 · fabrication 0 · vein-status recorded
- What: targeted scouting across every surface (grounding-floor·local-tool·orchestration·anti-fab·memory-integrity·outbound·self-dev) — this session's **clean 1-fire core-edge vein has genuinely run dry**. Every remaining item is one of: marginal-additive (home/street labels)·speculative-no-caller (deep-nesting)·>1-fire-redesign (KO injection·comma list-vs-value)·cross-loop-territory (BKT/whetstone=agent-core-cognition)·design-choice-not-bug (doctor selector).
- Why no code slice: the v2 EXHAUSTION/marginal-value floor — a forced marginal slice (verification cost > marginal value) is worse-fit than an *honest exit + recorded vein-status*. Per the "not just what's easy to verify" principle, avoids low-value busywork like adding home/street.
- Review point: fires 1-10 stacked verified hardening across 6 (pkg,kind) surfaces + 2 verifier-catches (fires 6,10) — the theme's 1-fire surface is saturated. The next high-value move is a deliberate >1-fire decompose or a retheme (Jinan's call). 0 new lines of code, so ④b is N/A.
- Risk: 0 — no code changed.
lesson: once 1-fire-clean work dries up across every surface of a theme (after exhaustive scouting), rather than keep firing thin auto-slices, *explicitly record vein-status and escalate a deliberate-decompose vs retheme vs pause decision to Jinan* — recognizing the auto-loop's marginal-value floor is the honest move.

## fire 10 · 2026-06-20 · skill v2.0 · 6d6af23e (JUDGE-DRILL + real ship)
meta: value-class=new-capability · pkg=@muse/recall · kind=memory-integrity/source-conflict · verdict=PASS · firesSinceDrill=0(reset)
ratchet: testFiles 1058→1058 (+4 cases conflict.test) · fabrication 0 · @muse/recall 338 tests green · precheck:grounding 2/3 PASS · pnpm check exit 0 · lint clean
- DRILL (firesSinceDrill≥10 trigger): deliberately injected a NAIVE global comma-widen (`[^\n.,;]`→`[^\n.;]`, all labels) + cherry-picked tests (address-only) → 334 tests passed → ④b's adaptive judge **confirmed FAIL** (concrete evidence of spurious conflicts on 6/6 benign lists, including reordered-identical/grown-list variants, and recommended a label-gate design). Verifier confirmed working → rolled back.
- The real slice: made source-conflict comma-values **LABEL-GATED** — the regex allows commas, but only `ADDRESS_LABELS` (address/주소/위치/…) preserve the comma; everything else still truncates at the first comma (byte-identical to current behavior). Address conflicts (London/Paris, Korean addresses) are now caught, 0 new false positives on benign lists.
- Why: source-conflict is a user-facing grounding cue — a false positive erodes trust. Fire 9 analyzed the global-widen's FPs, fire 10's drill live-confirmed it via the judge, and the gated design resolves only the false-negative (the judge proved byte-for-byte equivalence).
- Review point: ④b proved byte-for-byte equivalence on the non-address path (0 new FPs); mutation both directions (address=original goes RED, list-guard=naive-widen goes RED). Residual: ADDRESS_LABELS is missing home/street (same false-negative direction, additive); single-line multi-field swallowing is cosmetic (conflict still surfaces).
- Risk: low — the non-address path is unchanged (equivalence proven), purely additive. ④b Opus adaptive judge PASS.
lesson: fix regex-extraction broadening's false positives *surgically with a LABEL-GATE instead of a global change* — open the comma only for the legitimate value class (addresses) and keep the rest byte-identical, closing only the false-negative without the fire-6-class mass false positive. Injecting a real candidate naively into the drill is two birds one stone (verifier confirmation + a real fix soon after).

## fire 9 · 2026-06-20 · skill v2.0 · 0a6db466 (analysis + 3-fire main merge, no code slice)
meta: value-class=refactor(work-list) · pkg=docs · kind=exhaustion-analysis+merge · verdict=N/A · firesSinceDrill=9
ratchet: testFiles 1057→1057 · fabrication 0 · branch fires 7-8 → origin/main ff-merge (3-fire obligation)
- What: this session's core-edge **easy-clean (non-regex·non-agent-core) vein has thinned** — targeted scouting turned up mostly already-built/non-issue findings (A2A label bound=already fire 63, feeds SSRF=internal feed, non-issue, formatDueLocal=clean+surface-mismatch risk, classifyMemory=done at fire 8, multi-hop recall=1b'/1c done, remainder structurally blocked). The remaining high-value item (source-conflict comma-values) isn't a 1-fire clean (below).
- Why no code slice: source-conflict comma-value broadening catches the false-negative (missing an address conflict like London↔Paris), but **introduces a new comma-list false positive** (shared first element) — the same failure mode as fire 6 (regex-extraction false positives). Needs a list-vs-value classifier + a large benign corpus — an >1-fire redesign → recorded as DECOMPOSE-ON-DEFER (no forced 1-fire push, per the EXHAUSTION marginal-value rule).
- Review point: fire 9 = discharging Jinan's 3-fire main-merge obligation (integrating fires 7-8's anti-fabrication+memory-integrity into origin/main) + an honest work-list refinement. 0 new lines of code, so ④b judge is N/A (the merge is already-PASSed per-fire commits).
- Risk: 0 — no code changed, the merge is ff-only (already-verified commits).
lesson: when a theme's easy-clean vein dries up within a session, instead of forcing a 1-fire slice, *honestly record the remaining high-value item as a DECOMPOSE* and handle the obligation (merge) — avoiding the failure mode you just hit (regex extraction) fits both the diversity and marginal-value rules.

## fire 8 · 2026-06-20 · skill v2.0 · 8e3616f4
meta: value-class=micro-fix · pkg=@muse/memory · kind=memory-integrity/spurious-write · verdict=PASS · firesSinceDrill=8
ratchet: testFiles 1057→1057 (+2 cases memory-operation.test) · fabrication 0 · @muse/memory 456 tests green · pnpm check exit 0 · lint clean
- What: `classifyMemoryOperation` returned "delete" for a retraction token even when `existing===undefined` (a key never stored) → auto-extract called `store.forget()` on a nonexistent key (the File/Kysely backends actually touch the persistence path). Fixed to `existing===undefined ? "noop" : "delete"` — NOOP has 0 side effects (Mem0 discipline).
- Why: diversity — non-agent-core (@muse/memory, a new (pkg,kind)), non-regex (a deliberate departure from fire 6's Korean-regex vein). Removing a spurious store-mutation = memory-integrity hardening.
- Review point: walked every branch (defined+retraction→still delete, undefined+retraction→noop, the rest unchanged); the existing "DELETE on retraction" (existing=Seoul) test still passes as-is; 0 legitimate deletions suppressed (a real key always has existing defined). Sibling-audit: it's a shared classifier, so both the fact and preference namespaces are auto-covered. Integration is OUTCOME-verified with a forget-spy asserting 0 calls.
- Risk: low — a 1-line guard, 0 regressions. ④b Opus adaptive judge PASS (re-confirmed RED via mutation).

## fire 7 · 2026-06-20 · skill v2.0 · 42b5455d
meta: value-class=new-capability · pkg=@muse/agent-core · kind=anti-fabrication/floor-total · verdict=PASS · firesSinceDrill=7
ratchet: testFiles 1057→1057 (+5 cases tool-argument-grounding.test) · fabrication 0 · agent-core 2491 tests green · precheck:grounding 2/3 PASS · pnpm check exit 0 · lint clean
- What: `groundToolArguments` (the fabrication=0 release-gate) only sanitized string + string[] and let **nested objects pass through raw** → a gap where a fabricated leaf like `meta.note` rode through the gate and PERSISTED. Added a nested-object branch: sanitizes each fabricated string leaf (preserves grounded+non-string leaves), the same partial-vs-empty `dropped` contract as the array branch. Makes the gate total across every value shape.
- Why: fabrication=0 is a release gate (CLAUDE.md). If the gate is string-only, an object-valued arg is a bypass route. A sibling-audit added an `!Array.isArray` guard (blocked, in the same fire, a bug where a mixed array would get corrupted into an index-keyed object in the object branch).
- Review point: diversity — (agent-core, grounding-floor) has appeared 3 times, but kind=anti-fabrication is a new (pkg,kind). Honest caveat: **0 tools currently use an object-valued grounded arg** (all are string/`tags` string[]) — this pre-empts a bypass before a tool ships that shape (defensive, not gold-plating — ④b confirmed via grep). Residual: 1-level only (array-of-objects/object-in-object isn't recursed — revisit if a real caller shows up).
- Risk: low — purely additive (the string/string[] path is unchanged, regression tests as-is), no-corruption/no-aliasing directly demonstrated (④b). ④b Opus adaptive judge PASS.
- note: fire 6 was rolled back after the v2 adaptive judge caught a 68% false-positive rate in the Korean pattern (an organic verifier-catch — stronger evidence than a synthetic drill). The allPASS streak reset at fire 6 → restarted from fire 7.

## fire 6 · 2026-06-20 · skill v2.0 · NO-SHIP (rolled back)
meta: value-class=new-capability · pkg=@muse/agent-core · kind=grounding-floor/multilingual-injection · verdict=FAIL(④b)→ROLLBACK · firesSinceDrill=6
ratchet: testFiles 1055→1055 (slice reverted) · fabrication 0 · no code shipped · branch==main
- What: a sibling-audit of fire 3 — attempted to add 2 Korean analogs of English patterns 2 (output-clamp) and 3 (role-hijack) to MEMORY_INJECTION_PATTERNS. Passed MUTATION-FIRST RED→GREEN, pnpm check/lint/precheck:grounding were all green, but the ④b adaptive Opus judge FAILed it.
- Why FAIL: **13/19 (68%) false positives** on benign Korean notes. `만`/`처럼` are among the most common Korean particles, so everyday phrasing like "친구처럼 말해줬다" (told me like a friend)·"결과만 출력" (output only the result)·"이제 너는 …했다" (now you have done…) got neutralized (destroyed) mid-note. My benign test only picked "친구처럼 편한" (comfortable like a friend, an adjective use) and missed the gap — the adaptive judge targeted everyday similes/descriptive verbs and caught it.
- Review point: the v2 adaptive adversarial judge + the MUTATION-FIRST gate **worked exactly as intended** — blocked a slice that passed every deterministic gate on a behavior-level false positive (a fixed checklist would have let it through). The rollback protects Korean-speaking users' own notes.
- Risk: 0 — no code landed (git restore), only journal/backlog lessons were committed.
lesson: canonical-form injection patterns must anchor on *rare tokens* (command-noun+override-verb) — anchoring on a common particle+everyday verb like `만`/`처럼` mass-false-positives on users' own notes. A new locale pattern lands only after verifying **STABLE 0 false positives against a large benign corpus**, not a small benign test.

## fire 5 · 2026-06-20 · skill v1.14.0 · e0916bd7
meta: value-class=new-capability · pkg=@muse/multi-agent · kind=orchestration/verifier-gated-resynthesis · verdict=PASS · firesSinceDrill=5
ratchet: testFiles 1054→1054 (+5 cases lead-worker.test + 1 ask-decompose retry case + reflection-guard registry) · fabrication 0 · @muse/multi-agent 123 tests green · eval:orchestration PASS · pnpm check exit 0 · lint clean
- What: `runLeadWorkerTask` used to only flag (H1) an incomplete synthesis → now a verifier-gated single re-synthesis attempt. Refactored into a `runSynthesis` helper, the retry prompt names the dropped sub-results (`reinforceSynthesisRequest`), and the retry is adopted **only if it's verified and the drop count strictly decreases**. Registered in the reflection-guard registry (verifier=verifySynthesisCoverage).
- Why: addresses MAST's done-by-self-report gap — instead of only warning about dropped sub-results, attempts one recovery. arXiv 2510.18254: a bare retry with no external verifier repeats the failure 85% of the time → a deterministic verifier backing is mandatory.
- Review point: the never-worsens invariant (only strict drop-count decreases are adopted, ④b verified every case) + an errored-verifier retry doesn't clear the flag (never claims false completeness, hardening the judge's caveat in-fire). The ask-decompose consumer test is honestly split into a 4-run (complete) and a 5-run (one retry) case (confirmed via git show that it's not a regression). Bounded to one attempt (no loop).
- Risk: low — the no-verifier/throw path is back-compat identical, synthesize is pure text (0 side effects). ④b Opus adversarial judge PASS (5 questions + verified honesty via git-history).

## fire 4 · 2026-06-20 · skill v1.14.0 · 54c24b66
meta: value-class=new-capability · pkg=@muse/model · kind=local-tool-calling/schema-sanitizer · verdict=PASS · firesSinceDrill=4
ratchet: testFiles 1054→1054 (+7 cases in adapter-ollama.test.ts) · fabrication 0 · @muse/model 325 tests green · eval:tools PASS (live local) · pnpm check exit 0 · lint clean
- What: the Ollama native /api/chat tool projection sent `inputSchema` through unsanitized — Gemini has `sanitizeGeminiSchema` but Ollama didn't, so a union `type` (`["string","null"]`)·nullable anyOf/oneOf silently broke llama.cpp's GBNF tool grammar. Added `sanitizeOllamaToolSchema` (union→non-null, nullable anyOf→single branch, drop the null branch, strip $schema/$id, depth-64+cycle recursion) and wired it into the projection.
- Why: tool-calling.md's core rule — the local model must select the right tool in one shot. A broken schema can drop the tool entirely, making selection impossible. Mirrors the Gemini sanitizer precedent (parity, not invention).
- Review point: the nullable→non-null collapse is lossless since `required` already carries optionality (confirmed `required` is unchanged); a triple-union narrows to the first non-null (GBNF needs a single type, lossy but valid). A clean schema is structurally identical (0 regressions). eval:tools green is weak evidence (Muse's tools are flat) — the mutation tests are the real evidence.
- Risk: low — purely additive (a clean schema passes through unchanged), degrades safely on cyclic/deep schemas (④b directly demonstrated). ④b Opus adversarial judge PASS (5 questions, adversarial schema probing).

## fire 3 · 2026-06-20 · skill v1.14.0 · 8c91315c
meta: value-class=new-capability · pkg=@muse/agent-core · kind=grounding-floor/multilingual-injection · verdict=PASS · firesSinceDrill=3
ratchet: testFiles 1054→1054 (+1 describe / 3 cases in injection.test.ts) · fabrication 0 · agent-core 2477 tests green · precheck:grounding 2/3 PASS pass^2 · pnpm check exit 0 · lint clean
- What: `MEMORY_INJECTION_PATTERNS` (the stored/tool-output neutralizer) only had 4 English-only patterns, so a poisoned note/tool-output carrying a Korean injection ("이전 지시를 무시하고…" — ignore the previous instructions and…) sailed through `neutralizeInjectionSpans` — a grounding-floor hole. Added one narrow Korean pattern (a Korean analog of the canonical ignore-instructions pattern, noun→ignore/forget word order).
- Why: Jinan is a Korean-speaking user who stores Korean notes — an identity-critical surface. Added only pattern 1's narrow analog rather than the policy's broad Korean set, to minimize false positives on the user's own notes (a broad set would corrupt benign Korean prose).
- Review point: benign Korean ("그 규칙은 무시해도 된다" — that rule can be ignored, etc.) only takes span-scoped collateral of the same class as the English baseline — the surrounding note is preserved (④b directly demonstrated 6 cases). The `u` flag is preserved through the `g`-flag reconstruction, no ReDoS (259ms@50k). Honest residual: the Korean analogs of act-as/output-only aren't covered (T1-a-ko-resid).
- Risk: low — purely additive (append 1 pattern, existing English/homoglyph/zero-width detection unchanged). ④b Opus adversarial judge PASS (5 questions, including a direct false-positive analysis).

## fire 2 · 2026-06-20 · skill v1.14.0 · 5efdf345
meta: value-class=wiring · pkg=@muse/mcp · kind=outbound-safety/audit-completeness · verdict=PASS · firesSinceDrill=2
ratchet: testFiles 1054→1054 (+1 describe / 4 cases in test/consented-action.test.ts) · fabrication 0 · @muse/mcp 1860 tests green · pnpm check exit 0 · lint clean
- What: a rule-4 gap where `performConsentedAction` (@muse/mcp) never wrote an action-log entry on any branch — added opt-in `actionLogFile?`/`now?`/`idFactory?` + a `log()` helper, appending a rationale-bearing `ActionLogEntry` on all 7 return branches (veto/no-consent/invalid-url/host-mismatch/timeout-or-transport-failed/redirect/performed).
- Why: outbound-safety rule 4 = "every outbound action, sent OR refused, appends a reviewable entry." Closes the audit gap before this gets wired into a standing-objective loop. Mirrors the web-action.ts precedent (the same await-log-before-return pattern).
- Review point: the credential (Bearer) never enters any logged field (authorization header only); the body goes through redactSecretsInText+a 500-char cap; no actionLogFile means no logging (back-compat). Residual: if a caller puts a credential directly in request.body, a generic token could miss redaction — that's a caller-data problem (same as web-action); Muse's own credential-reinjection threat is blocked.
- Risk: low — purely additive (control flow·gate outcome unchanged). Logging-failure propagation matches the web-action precedent. ④b Opus adversarial judge PASS (5 questions: confirmed enumeration completeness of all 7 branches).

## fire 1 · 2026-06-20 · skill v1.14.0 · f8ef07f8
meta: value-class=micro-fix · pkg=@muse/agent-core · kind=grounding-floor/injection-hardening · verdict=PASS · firesSinceDrill=1
ratchet: testFiles 1054→1054 (+3 cases in injection.test.ts) · fabrication 0 · precheck:grounding 2/3 PASS pass^2 (faithfulness-rate env-skip) · pnpm check exit 0 · lint clean
- What: `MEMORY_INJECTION_PATTERNS`'s fake-system pattern `/^\s*system\s*[:>]/iu` → `/imu` (added the `m` multiline flag). A `\nsystem:` role-hijack line injected mid-prose (a common evasion where an attacker prepends a benign lead-in) is now neutralized across every grounding/tool surface.
- Why: the original `^` only matched the very start of the string → a `system:` payload on a new line after benign text sailed through entirely, as long as it dodged patterns 1-3 (ignore/act-as/output-clamp). `m` is a superset that adds line-start matching without weakening existing detection, and only replaces the span so the surrounding prose is undamaged. `normalizeForInjectionDetection` preserves newlines, so it still works after normalization.
- Review point: a benign log-dump line that happens to start with `system:` only has that span replaced with `[removed: injected instruction]` (acceptable collateral on an untrusted surface). A mention of "system" without a colon/`>` is undamaged (confirmed with a benign test).
- Risk: none — the git diff is a 1-char flag + an additive 3-case test block. agent-core's 2474 tests + a whole-workspace `pnpm check` are green, 0 regressions in other consumers. ④b Opus adversarial judge PASS (all 4 questions with concrete evidence).
