# Loop journal — a-plus-roadmap

Theme: executing the Muse A+ roadmap. Queue = `docs/strategy/competitor-analysis-and-a-plus-roadmap.md` §10.3 (no self-scouting). cron `964ac861` (30m, session-only), Tier1 no-push, /tmp worktree. Stop: `CronDelete 964ac861`.

> Note: the A+ audit's 3 gaps (injection-provenance S1-S3 · gap② the user-model) are logged not in this
> journal but in [prompt-system.md](prompt-system.md) fires 20-22 — read both journals together when auditing A+ progress.

## fire 1 · 2026-07-11 · skill v2.x · f5a360393
meta: slice=D2-S1d · wave=W1 · pkg=scripts(eval-adversarial) · kind=eval-ratchet · verdict=PASS · firesSinceDrill=1
ratchet: roadmap remaining [ ] = 30/31 · self-eval pass · fabrication 0 · adversarialCases 16→19
- What: added 3 deterministic sandbox-escape cases to eval:adversarial (cwd-outside/~/.ssh write · network) — spawns the real muse-runner binary under seatbelt and scores the OS rejection in code (not a model refusal, agent-testing #5).
- Why: D2-S1b wired seatbelt, but the eval surface had no regression guard against a weakened confinement. Now the adversarialCases ratchet automatically catches a confinement regression.
- Review point: whether the network case is scored via the `accepted` listener flag (curl exit≠0 alone would let a guard-OFF pass fraudulently) — Opus's evaluator confirmed by independently reproducing the guard-ON/OFF mutation.
- Risk: the network case depends on a 300ms straggler delay (confirmed stable at pass^2). A machine without ~/.ssh targets $HOME root instead (still outside cwd/tmpdir, still rejected).

## fire 2 · 2026-07-11 · skill v2.x · 64557d3f9
meta: slice=D2-S2a · wave=W1 · pkg=@muse/tools · kind=security-classifier · verdict=PASS(re-judged) · firesSinceDrill=2
ratchet: roadmap remaining [ ] = 30/32 (D2-S2 split into a/b, +1) · self-eval pass · fabrication 0 · topology 22 new tests
- What: a pure shell-topology classifier classifyCommandTopology — quote-aware detection of constructs a shell `-c` script can hide from DS-2's string guard (substitution/process-substitution/heredoc/eval); a non-shell command is analyzable (near-miss). Split D2-S2 into a (classifier)/b (wiring).
- Why: DS-2 can't see a catastrophe hidden inside `$(...)`. This classifier becomes the decision point for downgrading un-analyzable commands to explicit approval in D2-S2b.
- Review point: Opus's first pass FAILed (a newline is a command separator but eval-detection missed it = a false negative; `$((` arithmetic misdetected as command-substitution = a false positive) → instead of rolling back, the named defects were repaired within the retry budget, then re-judged PASS. The maker=Sonnet/judge=Opus model difference blocking self-preference caught a real defect.
- Risk: a sudo/env wrapper (`sudo sh -c '$(x)'`) goes undetected because the program resolves through sudo → logged as VQ-15 (strip the wrapper when wiring D2-S2b). Zero user-visible effect until wired (honestly labeled "foundation/not yet connected" in the CHANGELOG).
lesson: shell command separators aren't just `; | & (` — they also include a **newline**; a command-position scanner must re-arm on `\n`. `$(` and `$((` (arithmetic) must be distinguished or near-misses false-positive.

## fire 3 · 2026-07-11 · skill v2.x · b3f581400
meta: slice=D2-S2b · wave=W1 · pkg=apps/cli(chat-ink-core) · kind=security-wiring · verdict=PASS · firesSinceDrill=3
ratchet: roadmap remaining [ ] = 29/32 · self-eval FOREIGN-fail (below) · fabrication 0 · chat gate 135 tests (+9)
- What: wired D2-S2a's pure classifier into the live approval gate chatToolApprovalGate. An un-analyzable run_command ① can't be silently allowed even if disguised with risk=read (the read fast-path gets `&& topology.analyzable`) ② the approval prompt shows an "unanalyzable shell construct" warning. Not an unconditional reject (a human can approve a legitimate heredoc).
- Why: so a human notices and approves the `$()`/heredoc/eval constructs DS-2's string guard can't see. Finding: trust.json isn't wired at runtime and run_command is already "execute" so there's no live auto-approve seam → full downgrade is VQ-16.
- Review point: Opus's evaluator PASSed (a bypass-proof invariant · arg-hostility · independent mutation-RED confirmation). A cosmetic over-cap-construct=undefined → "(undefined)" render was fixed with a `?? "un-analyzable"` fallback + test.
- Risk: a concurrent Telegram loop polluted the tree (api/messaging/web/MUSE_TELEGRAM_* env uncommitted) → self-eval's envInventory went FOREIGN-fail (my diff has 0 MUSE_ env vars), 2 @muse/api tests FOREIGN-failed. Staged only my own files explicitly, touched nothing external. self-eval exit-0 is expected to recover once the other loop commits.
lesson: when a concurrent loop pollutes the tree mid-fire, self-eval/whole-package gates go FOREIGN-fail — judge by isolating my own files (build+my tests+lint), commit explicit paths, and never run docs:env since it sweeps up foreign env vars too.

## fire 4 · 2026-07-11 · skill v2.x · 56f59f477
meta: slice=D1-S1 · wave=W1 · pkg=@muse/agent-core · kind=loop-guard · verdict=PASS · firesSinceDrill=4
ratchet: roadmap remaining [ ] = 28/32 · self-eval pass · fabrication 0 · agent-core 2841 tests green · pingpong 19 new · eval:computer-task PASS (local-forced 1/1, no false-abort)
- What: the ping-pong loop guard tool-loop-pingpong.ts — detects the model infinitely alternating between two tools (A,B,A,B) via a trailing-alternation run (window 20 · warn 6 · block 10), recursively strips volatile fields (runId/tsIso/id/ts/timestamp) from the result signature. On block → pingPongAbortedExecution (mirroring post-compaction), wired into both loops. Closes the blind spot in the existing stall detector (which only caught A,A,A).
- Why: openclaw's tool-loop-detection calls this out as a separate failure mode. A 12B falls into 2-tool back-and-forth easily. Added alternation detection to Muse, which previously only caught stalls (identical output).
- Review point: the biggest risk is a false positive (mistaking a legitimate multi-step sequence for a loop and aborting) → verified via units (genuine progress/stall/3-cycle=none) + an independent Opus false-positive battery + 2841 green model-loop tests. id-strip preserves args so distinct-arg cases don't get merged (confirmed by Opus). Bidirectional mutation-RED (the alternation condition · volatile stripping).
- Risk: eval:computer-task got hijacked to Gemini by an ambient GEMINI_API_KEY (VQ-17, an eval policy violation) → re-ran local-forced with MUSE_LOCAL_ONLY=true and PASSed (1/1, add-works · multiply-intact · no-collateral, a real multi-step task completed under the guard with no false-abort). The guard only aborts at 10-deep A↔B, so legitimate work never triggers it.
lesson: a "LOCAL OLLAMA ONLY" eval that doesn't actually force local gets hijacked by an ambient cloud key — the eval script must explicitly force MUSE_LOCAL_ONLY/MUSE_DEFAULT_MODEL (VQ-17).

## fire 5 · 2026-07-11 · skill v2.x · c338f66c5
meta: slice=D2-S6a · wave=W1 · pkg=@muse/tools+apps/cli · kind=security-ux · verdict=PASS · firesSinceDrill=5
ratchet: roadmap remaining [ ] = 27/33 · self-eval pass · fabrication 0 · risky-token 13 new tests
- What: risky-token highlighting in the approval prompt. Pure identifyRiskyTokens (destructive flags · sensitive paths · destructive verbs in command position, reusing DS-2's risk vocabulary, token-level) + emphasizeRiskyTokens (TRUSTED ANSI bold-red) wired into chatToolApprovalGate's detail (applied only after summarizeToolArgs' redact+strip).
- Why: DS-2 only code-rejects catastrophic commands and lets routine-risky ones through (e.g. rm -rf ./build) — make the risky part visible when a human approves (informed consent). Referencing openclaw's exec-approval span.
- Review point: the biggest UX risk is a false positive (over-highlighting → learned to be ignored) → a false-positive battery (ls · echo · rm inside quotes · ~/Documents · /tmp/x — all []) + an independent Opus check. Highlighting happens after redaction, so no secret exposure. ESC is an escaped \x1b (not a raw byte, byte-hygiene safe).
- Risk: -rf gets bounded over-flagged on non-rm commands too (e.g. grep -rf), but it's advisory (non-blocking) and doesn't fire on common safe commands, so Opus accepted it. (b) staging needs a no-external-effect contract, so it's separate.

## fire 6 · 2026-07-11 · skill v2.x · 27c9ef986
meta: slice=D2-S6b · wave=W1 · pkg=apps/cli · kind=security-staging · verdict=PASS · firesSinceDrill=6
ratchet: roadmap remaining [ ] = 26/34 · self-eval pass · fabrication 0 · actuator-tools 29 tests (+staging)
- What: stages the CLI fs-write gate's non-interactive rejection into the existing pending-approval-store (reusing @muse/messaging). buildCliPendingApprovalStager maps FsWriteDraft→PendingApproval and calls recordPendingApproval (resolvePendingApprovalsFile) — the exact file muse approvals reads. No-external-effect: a real fs-write-tool e2e confirms the file is never created + the entry round-trips.
- Why: previously a non-interactive CLI write was a silent deny → now it's a durable worklist item (auditability + unifying channel/local pending items). Honors the no-new-store rule (reuses the existing one).
- Review point: an independent Opus e2e (file never created + readback passes isPendingApproval) · a staging throw preserves deny · bidirectional mutation-RED. Secrets: the draft holds only a byte count (no content), the store is 0600. Zero edits to messaging/src (avoiding conflict with a concurrent Matrix loop).
- Risk: a CLI-write staging entry only has {path,action}, so approve→replay isn't possible (VQ-18, needs full-args like the channel path). A concurrent Matrix/Telegram loop heavily polluted the tree (api/web/autoconfigure/messaging) → committed only my 3 apps/cli files, isolated.
lesson: when reusing a store from a package another loop is actively churning, only import the barrel export and never touch that package's src — avoid conflict at the source (D2-S6b extended the CLI with zero edits to the messaging store).

## fire 7 · 2026-07-11 · skill v2.x · 3b6e67c59
meta: slice=D3-S7 · wave=W1 · pkg=@muse/stores+apps/cli · kind=safety-guard · verdict=PASS · firesSinceDrill=7 · ★W1 complete
ratchet: roadmap remaining [ ] = 25/34 · self-eval FOREIGN-fail (envInventory=MUSE_MATRIX_POLL_ENABLED, 0 of my own env vars) · fabrication 0 · stores 13 new tests
- What: the PID-reuse kill guard. Added osStartTime (OS start-time captured at spawn) to the background-process record, a pure pidIdentityMatches (preserves legacy/equality). stop/reconcile compare before kill/reconcile → on mismatch (reused/gone), refuse to kill + mark exited (fail-close, a new pid_reused result). Wired the CLI via ps -o lstart= -p (BSD+GNU, avoiding /proc).
- Why: if the original process dies and the PID gets reused, stopBackgroundProcess SIGTERMs an unrelated user process, and reconcile only checks isAlive so it treats it as stale-running. Referencing hermes' process_registry kernel start-time verification. Aligns with "risky execution = a deterministic guard."
- Review point: an independent Opus check (directly drove a reused-PID no-kill scenario · a truth table for the empty-string edge case · a throwing reader is unreachable because spawnSync doesn't throw, fail-safe) · bidirectional mutation-RED. Zero messaging edits (avoiding a concurrent Matrix-loop conflict) · zero env.
- Risk: a concurrent Matrix loop heavily polluted the tree (api/web/autoconfigure/messaging) + envInventory FOREIGN-fail (MUSE_MATRIX_POLL_ENABLED). Committed only my 5 files (4 stores + 1 cli), isolated. stop's reader-throw has no try/catch, but it never triggers under a real spawnSync in production.

## fire 8 · 2026-07-11 · skill v2.x · 8ed66e2b3
meta: slice=D1-S3 · wave=W2 · pkg=@muse/memory+agent-core+cli · kind=compaction-quality · verdict=PASS · firesSinceDrill=8 (≥8 → next fire is a judge-drill)
ratchet: roadmap remaining [ ] = 24/34 · self-eval pass · fabrication 0 · memory 18 new tests · ★W2 started
- What: staged summarization. summarizeDroppedContextInStages+chunkDroppedOnToolPairs (memory, pure). Chunks dropped content on tool-pair boundaries (never split right before role:"tool") → reuses summarizeDroppedContext per chunk (each FAIL-OPEN) → merges surviving chunks · caps at maxChars. Codified a verbatim-identifier instruction into SUMMARIZER_SYSTEM_PROMPT. Wired at agent-runtime:578+chat-ink-core:941.
- Why: the existing CMP-2 summarizes all dropped content in a single aux call → fidelity loss/full truncation on a large context. Referencing openclaw's summarizeInStages. Strengthens grounding via partial-failure preservation + identifier preservation.
- Review point: the existing summarizeDroppedContext stays byte-identical (confirmed additions-only) · partial failure preserves survivors vs. total failure falls back to the floor · single-chunk equivalence · bidirectional mutation-RED. Independent Opus verification.
- Risk: ★★a concurrent Matrix loop ran `git rebase` (b2e4bd55a) on the **shared main workspace** during evaluation → the auto-stash orphaned my uncommitted slice. Recovered without data loss (the working tree was intact, stash@{0} was a duplicate) but this came close to a serious accident. Committed only my 6 files, isolated.
lesson: ★when a concurrent loop runs `git rebase`/`pull --rebase` on the shared main worktree, its auto-stash can orphan another loop's uncommitted work — this loop will now commit slices as fast as possible to **minimize the exposure window between worker and evaluator**, and **moving this loop into an isolated /tmp worktree is the real fix** (needs Jinan's decision).

## fire 9 · 2026-07-11 · skill v2.x · d9c5732dc · ★JUDGE-DRILL + D1-S5a
meta: slice=D1-S5a · wave=W2 · pkg=@muse/agent-core · kind=budget-visibility · verdict=PASS · firesSinceDrill=0 (reset) · drill=PASS
ratchet: roadmap remaining [ ] = 23/35 · self-eval pass · fabrication 0 · agent-core 62 tests (new budget)
- ★JUDGE-DRILL (consecutive-all-PASS reached 8, can't be deferred any longer): injected a deliberately defective slice (redactCreditCards — a delimiter-leaking card-format bug + a declaration-only test + unverified mutation) → Opus's evaluator returned **FAIL** (directly probed delimiter/space/Amex leakage · flagged the declaration-only test · flagged the return text; ran mutation independently) → **proved the gate is trustworthy** (no rubber-stamping) → fully rolled back the drill (clean tree, zero foreign contact). maker≠judge self-verification passed.
- The real fix, D1-S5a: injects an "N/M tool calls used, giving my best final answer" one-shot notice before final synthesis when the tool budget is exhausted (proactive, a strict budget-only gate, excludes wallclock/stall). Pure budgetExhaustionNotice+Tracker (mirroring REVERIFY_NUDGE), wired into both loops.
- Why: previously, once toolCallCount≥maxToolCalls, activeTools=[] silently made tools disappear → the model could give a truncated answer without knowing the budget was exhausted. Referencing hermes' iteration_budget. No silent cutoffs.
- Review point: Opus confirmed the legitimacy of the design deviation (reactive→proactive: avoids a wasted round-trip when normal completion lands exactly at the cap) · the strict budget gate · guaranteed termination · that the existing agent-runtime tests were tightened (not loosened) · the honesty of the one-shot structurally-inert claim. Bidirectional mutation-RED.
lesson: ★the worker also used `git stash` on the shared main workspace (D1-S3's work got swept up → recovered without loss, already committed) — the worker briefing needs an explicit "never `git stash`, isolate via cp/git-apply" rule. Proactive injection beats reactive+continue (avoids a wasted round-trip on normal completion). Moving this loop to an isolated worktree is the real fix (awaiting Jinan's decision).

## fire 10 · 2026-07-11 · skill v2.x · fe1b505c1
meta: slice=D1-S5b1 · wave=W2 · pkg=@muse/agent-core · kind=invariant-lock · verdict=PASS · firesSinceDrill=1
ratchet: roadmap remaining [ ] = 22/36 · self-eval pass · fabrication 0 · agent-core tool-plan 6 tests (+1 lock)
- What: codified the PTC "programmatic=1" budget rule. run_tool_plan already counts as 1 call = 1 budget slot (regardless of internal step count) but implicitly so → added a WHY comment to the agent-runtime PTC intercept + a regression-lock test (a 3-step execution = effects[a,b,c] ∧ 1 slot consumed = toolsUsed["run_tool_plan"]). Accounting behavior unchanged.
- Why: if PTC's core property (N steps = 1 budget) silently broke during a refactor, PTC would be pointless. Referencing hermes' iteration_budget "PTC refund." Locking the invariant in code.
- Review point: Opus confirmed the change is comment-only (logic unchanged) · the behavior lock is bidirectional (not declaration-only — the drill just caught exactly that failure mode) · mutation-RED (counting steps individually → toolsUsed length 3 goes RED) · comment accuracy traced.
- Risk: none (locks existing behavior, unchanged). Zero user-visible change so CHANGELOG omitted (honestly). Next: D1-S5b2 = a sub-agent sub-budget (new plumbing).

## fire 11 · 2026-07-11 · skill v2.x · 2341ec4fb
meta: slice=D1-S5b2 · wave=W2 · pkg=@muse/multi-agent+apps/cli · kind=budget-isolation · verdict=PASS · firesSinceDrill=2
ratchet: roadmap remaining [ ] = 21/36 · self-eval pass · fabrication 0 · multi-agent+cli 29 tests (new+wired)
- What: a separate sub-agent sub-budget. Pure resolveSubAgentToolBudget (parent→max(3,floor(parent×0.5)), uncapped→5, a worker is always capped) + a shallow-override wired into the ask-decompose worker's execute (instead of inheriting the parent). synthesize/planner keep the parent budget. D1-S5 complete (b1+b2).
- Why: a worker fully inherits the parent's metadata.maxTools → an N-worker fan-out could reach N× the parent's budget. Referencing hermes' parent90/sub50. A worker's focused sub-task needs a smaller budget.
- Review point: Opus confirmed the pure rule guard is exhaustive (0/neg/NaN/Inf/frac→always a positive integer≥3) · behavioral wiring (worker=[5,5,5] · synthesis=10 · args.metadata unmutated) · safe parent extraction (uncapped→5) · bidirectional mutation-RED · sibling-audit honesty.
- Risk: orchestrator.ts (SupervisorAgent) and commands-board also inherit the parent's metadata (the same class) but are server-side with no maxTools convention → logged as a backlog follow-up. D3-S1 is next (sub-agent depth demotion), adjacent territory.

## fire 12 · 2026-07-11 · skill v2.x · f7d546f63
meta: slice=D3-S1a · wave=W2 · pkg=@muse/multi-agent+apps/cli · kind=recursion-bound · verdict=PASS · firesSinceDrill=3
ratchet: roadmap remaining [ ] = 21/37 · self-eval pass · fabrication 0 · multi-agent 34 tests (+depth) · MUSE_BOARD_MAX_DEPTH env
- What: board-task depth demotion. Added an optional depth to AgentTask (parent 0, sub depth+1), resolveBoardMaxDepth (MUSE_BOARD_MAX_DEPTH default 1 · floor 1), expandTaskIntoSubtasks no-ops (rejects re-decomposition) once depth≥maxDepth. Wired the CLI board expand path. The first decomposition is still allowed (only re-expand is blocked).
- Why: verify-first confirmed a sub-task could be re-expanded (grandchild infinite decomposition). Referencing openclaw's subagent-capabilities (depth≥max→leaf) and hermes' max_spawn_depth 1. A depth ceiling on the flat board.
- Review point: Opus confirmed the ceiling boundary (rejects at depth==maxDepth · allows below) · parent+1 · back-compat (missing depth = 0) · the parsing table (0/-1/abc/1.5→1 floored) · wiring · bidirectional mutation-RED. depth is omitted when 0, so no migration is needed for existing board JSON.
- Risk: none (an optional field, back-compat). A new env, MUSE_BOARD_MAX_DEPTH → docs:env updated (the worker handled it proactively, avoiding the envInventory gate). Next: D3-S1b = parent tool-deny inheritance (an executor gate).

## fire 13 · 2026-07-11 · skill v2.x · e4633c5a3
meta: slice=D3-S1b · wave=W2 · pkg=@muse/multi-agent+apps/cli · kind=defense-in-depth · verdict=PASS · firesSinceDrill=4
ratchet: roadmap remaining [ ] = 20/38 · self-eval pass · fabrication 0 · multi-agent 8 new tests
- What: parent tool-deny inheritance. Pure inheritParentToolDeny (intersects child⊆parent) + a structural clamp wired into the ask-decompose worker (worker allowedToolNames = the parent intersection, args.metadata unmutated, planner/synthesize unclamped). D3-S1 complete.
- Why: structurally enforce that a sub-agent can't use a tool the parent doesn't have (referencing openclaw's subagent-capabilities). The current worker already inherits metadata verbatim, but it's an unenforced convention → pre-place the clamp at the real enforcement point.
- Review point: Opus verified the pure helper's correctness (intersection · dedup · empty clamp · no mutation) · wiring · bidirectional mutation-RED + an **explicit significance judgment** (genuine defense-in-depth vs. a no-op lock).
lesson: ⚠tool-deny inheritance already holds in current production (a broader set is never passed), so the clamp is a production no-op — verified via a test-only seam. It's a defense-in-depth lock, not a live bug fix. Zero user-visible change, so honestly omitted from CHANGELOG. This kind of "lock an already-holding invariant" slice should honestly disclose the seam and pass via Opus's significance judgment.

## fire 14 · 2026-07-11 · skill v2.x · 820d5e425
meta: slice=D3-S2 · wave=W2 · pkg=@muse/agent-core+multi-agent · kind=liveness-seam · verdict=PASS · firesSinceDrill=5
ratchet: roadmap remaining [ ] = 19/38 · self-eval pass · fabrication 0 · agent-core+multi-agent 6 new tests
- What: a single-run heartbeat emission seam. Injected ModelLoopRunner.heartbeat (agent-core stays independent of multi-agent) + 3 model-loop emission points (streamModelTurn text-delta · tool-call · runToolBatch genuine-exec only) + emitHeartbeat try/catch. Reuses the existing detectStalled (zero new detectors). Fake-clock units confirm single-run stall detection + no false positive on a healthy stream.
- Why: the stale-detection registry exists but only one caller (the orchestrator) uses it → a single long-running run never emits a heartbeat, so an in-tool stall goes undetected. Referencing hermes' _heartbeat_loop. VQ-1 confirmed the wiring point (the model-loop streaming path).
- Review point: Opus confirmed emission gating (genuine-exec only, matching the progress tracker) · throw-safety · byte-identical when unwired · both tests are behavioral · bidirectional mutation-RED · the legitimacy of the deferral.
lesson: ⚠live-feeding the registry requires an architectural decision (autoconfigure→multi-agent dependency, or reordering apps/api's construction) → this slice stops at seam+emission+fake-clock units, feeding+a stall-abort poller are deferred to backlog. Honestly disclosed, Opus judged it a legitimate seam+test. Zero user-visible change, so CHANGELOG omitted.

## fire 15 · 2026-07-11 · skill v2.x · 9c69928e9
meta: slice=D3-S4a · wave=W2 · pkg=apps/cli · kind=capacity-cap · verdict=PASS · firesSinceDrill=6
ratchet: roadmap remaining [ ] = 19/39 · self-eval pass · fabrication 0 · cli 27 tests (+job-concurrency) · MUSE_JOBS_MAX_CONCURRENT env
- What: a job concurrency cap. Pure resolveJobsMaxConcurrent (default 3 · ≥1)+jobConcurrencyRefusal (rejects at >=cap) + countRunningJobs (reuses the existing jobSummary) + wired via startBackgroundJobOrRefuse (at-cap → an explicit rejection + exitCode 1, start never called; inline unchanged).
- Why: muse job run could spawn unlimited background jobs (verify-first) → resource exhaustion. Referencing hermes' cap-3 async rejection.
- Review point: Opus confirmed the parsing table (0/-1/2.5/abc→3 floored to 1) · countRunningJobs' fidelity against a real jsonl fixture (running only) · wiring (an at-cap spy never called + exitCode 1 + stderr, starts below the cap) · bidirectional mutation-RED.
- Risk: none. A new env → docs:env updated (worker proactive). Next: D3-S4b = boardTaskPrompt headroom summary budget + spill (more complex).

## fire 16 · 2026-07-11 · skill v2.x · c5ec0240c
meta: slice=D3-S4b · wave=W2 · pkg=apps/cli · kind=synthesis-budget · verdict=PASS · firesSinceDrill=7
ratchet: roadmap remaining [ ] = 18/39 · self-eval pass · fabrication 0 · cli 41 tests (+board-synthesis-budget) · 2 env
- What: a board-synthesis headroom summary budget + file spill. Pure perChildSynthesisBudget (max(2000,floor(h×0.5/n))) + budgetAndSpillOutputs (truncates overflow + spills the FULL original to ~/.muse/board-spill/, the path stated in the segment) + wired into makeAgentExecutor (real fs · a note in the answer). boardTaskPrompt stays pure. D3-S4 complete.
- Why: boardTaskPrompt embeds child outputs verbatim → N large children blow up the context. Referencing hermes' headroom×0.5/n · floor 2000 · file spill.
- Review point: Opus verified the budget formula's edge cases (div0/NaN/Inf→2000) · the boundary (<=) · a round-trip (spill===original · exact path match · no data loss) · executor's real-fs behavior · boardTaskPrompt staying pure · bidirectional mutation-RED.
- Risk: none. 2 new envs → docs:env (worker proactive). Next: D1-S7a = browser snapshot AX-tree refs (a different W2 axis, browser).

## fire 17 · 2026-07-11 · skill v2.x · 61ca1e65c
meta: slice=D1-S7a · wave=W2 · pkg=@muse/browser · kind=browser-ref-guard · verdict=PASS · firesSinceDrill=8
ratchet: roadmap remaining [ ] = 17/39 · self-eval pass · fabrication 0 · browser +3 tests (ghost-ref) · env 0
- What: browser-ref stability, fail-close. Closed a hole where `resolveTarget` (browser-tools.ts)'s numeric-ref branch let a ref with `describeElement(ref)`=undefined (stale/ghost/hallucinated, absent from the current snapshot) through to click/type/hover/upload on a ghost element — now rejected ("call browser_read"). resolveTarget is the single resolution point for all 4 act-tools, so the fix applies to all of them at once. 3 behavior tests (valid proceeds · ghost click/type rejected, calls never recorded = zero partial side effects).
- Why: if a 12B passes an invalid/stale ref, it acts without a real element behind it — an ungrounded manipulation. The roadmap's D1-S7a "ref-stability unit." Refs were already numeric index + no CSS-selector model exposure, so requirement (a) was already met — stability is the real delta.
- Review point: an independent Opus evaluator reproduced the mutation-RED (removing the guard → lines 542/551 both RED) · behavior-based assertions (not shape-only) · a legitimate flow (target branching · multi-field · describeElement populated) · format unchanged (grep @e = 0) · a threat-model check → PASS.
- Risk: low. A numeric-index collision (a different element inheriting the old index) is outside the tool-boundary guard = VQ-19 (a real-browser check adjacent to D1-S7d). Format is unchanged so the eval:computer-task/browser-agent contract is unaffected. Next = D1-S7b (step-budget+timeout injection, actions_used N/M).
- note: fire 18 forces a JUDGE-DRILL (firesSinceDrill=8 · consecutive-all-PASS=8≥8) — inject a deliberately bad slice → confirm ④b FAILs it → roll back → the real fix, counter reset.

## fire 18 · 2026-07-11 · skill v2.x · 7036af77d
meta: slice=JUDGE-DRILL+D1-S7b1 · wave=W2 · pkg=@muse/agent-core · kind=judge-drill+action-budget-core · verdict=PASS · firesSinceDrill=0 (reset)
ratchet: roadmap remaining [ ] = 17/40 (D1-S7b→b1/b2 split +1, b1 checked -1, net 0 → 17) · self-eval pass · fabrication 0 · agent-core +5 tests (action-budget) · baseline ENV repair (foreign)
- What: ① a baseline envInventory FOREIGN-fail (the prompt-lab merge didn't update Total 522→526) → a docs:env resync commit (0b6360cfe). ② JUDGE-DRILL: injected a deliberately defective D1-S7b step-budget (an off-by-one `used>max` + a declaration-only test + unwired) → independent Opus ④b FAILed it on 4 concrete violations (off-by-one · boundary-mutation-GREEN · no behavioral verification · unwired + duplicates an existing module) — no rubber-stamping. ③ rolled back → the real fix, D1-S7b1: a pure action-budget decision core (guardBrowserAction, an exact `used>=max` boundary, near-cap, actions_used N/M). Split D1-S7b into b1 (core)/b2 (wiring).
- Why: the hard-counter (firesSinceDrill=8 · consecutive-all-PASS=8) forced a re-verification of gate reliability. After the drill confirmed maker≠judge is alive, shipped the correct version of that same defect as the real fix (correctly rebutting the off-by-one and hollow-test).
- Review point: the drill judge and the b1 judge are separate Opus instances (fresh context). The b1 judge independently reproduced 2 mutations (`>=`→`>` gives 2 RED, near-cap gives 1 RED) · a behavior sequence · decision consistency · purity → PASS. The drill judge caught the planted defect (off-by-one · declaration-only) plus extra findings (unwired · duplicating step-budget.ts).
- Risk: low. b1 is an unwired core so it's zero user-visible (CHANGELOG comes in b2). The existing step-budget.ts is a token budget, so an action-count budget is legitimately separate. Wiring (b2) is the gate for the real bounded-task effect. Next = D1-S7b2 (buildBrowserTools' shared instance + fail-close on each act-tool + surfacing output).
- lesson: on a JUDGE-DRILL "roll back → real fix," shipping the *correct version* of the drill's own defect turns the drill into the actual slice spec (token efficient). The drill judge autonomously found unwired code and duplication beyond the planted defect → confirms the gate is adaptive reasoning, not replaying a checklist.

## fire 19 · 2026-07-11 · skill v2.x · cf9be0dcb
meta: slice=D1-S7b2 · wave=W2 · pkg=@muse/agent-core+@muse/browser+apps/cli · kind=action-budget-wiring · verdict=PASS · firesSinceDrill=1
ratchet: roadmap remaining [ ] = 16/40 · self-eval pass · fabrication 0 · agent-core+4 · browser+7 · cli-config+7 tests · env MUSE_BROWSER_MAX_ACTIONS
- What: wired b1's action-budget core for real. createBrowserActionTracker (an agent-core mutable seam reusing b1's pure primitive) is created once as a shared per-task instance by buildBrowserTools → wired via the minimal structural seam BrowserActionGuard into click/type/fill (3 tools). On exhaustion, fails closed at the top of execute (controller never reached) + on success attaches actionsUsed N/M · a budgetWarning. resolveBrowserMaxActions default 30 · MUSE_BROWSER_MAX_ACTIONS. D1-S7b complete (b1+b2).
- Why: b1 was an unwired core with zero real effect (the drill flagged the unwiring as a defect). b2 is the actual gate for a bounded-task effect — a 12B browser task can no longer click/submit unboundedly.
- Review point: Opus's 8-axis review PASSed — fail-close happens before controller/resolveTarget, the tracker is per-task (once per run, not recreated per execute), arithmetic is exact (a cap of N allows exactly N · used() never exceeds the cap), byte-identical when absent (the existing 81 tests unchanged), 2 independently reproduced mutations (removing the guard → controller reached → RED; removing the exhaustion check → cap unmet → RED). @muse/browser stays independent of agent-core (structural typing).
- Risk: low. Exhaustion is consumed at the very top, before approval/verification = an attempts bound (fail-close direction, only refuses more). The default of 30 is harmless for normal tasks (<30), so eval:browser-agent regression is safe by construction. Unwired siblings (upload/key/open counting) = backlog. Next = D1-S7c (a pending-dialog snapshot field + auto-dismiss).

## fire 20 · 2026-07-11 · skill v2.x · 36fc9811a
meta: slice=D1-S7c · wave=W2 · pkg=@muse/browser · kind=dialog-failclose · verdict=PASS · firesSinceDrill=2
ratchet: roadmap remaining [ ] = 15/40 · self-eval pass · fabrication 0 · browser +12 tests (dialog-policy) · baseline ENV repair (foreign MUSE_CHANNEL_ACK)
- What: made browser JS-dialog disposition fail-close. registerDialogHandler previously auto-ACCEPTed every dialog (confirm OK · prompt submitted its text) — a fail-open path — now replaced with pure dialog-policy.ts: decideDialogDisposition (confirm/prompt/unknown→dismiss, alert/beforeunload→accept)+planDialogResponse (dismiss has no response)+settleDialog (a fake spy verifies real accept/dismiss calls). The snapshot surface is kept, controller.ts+puppeteer:425's stale doc updated alongside.
- Why: Muse's #1 rule "guards are fail-close" + the browser is the largest untrusted channel. Auto-accepting a page-originated confirm ("delete account?")/prompt is over-consent — clicking approval ≠ approving a dialog response. Aligns with the roadmap's literal "auto-dismiss."
- Review point: Opus PASSed — the fail-close direction (confirm/prompt/unknown dismiss), settleDialog verified via a fake spy for real dismiss/accept calls (not enum-only), no accept fall-through in the wiring, prompt is never submitted, 2 independently reproduced mutations (confirm→accept · a ternary flip), the existing 153 tests green, tightening-only invariant (the accept set shrinks). controller.ts:425's stale nit was also fixed.
- Risk: a trade-off — an approved "click Submit→page confirm" flow is left incomplete via dismiss (fail-safe, surfaced in the snapshot). beforeunload stays accept (an approved navigation proceeds). eval:browser-agent doesn't use dialogs, so no regression. Next = D1-S7d (`<page>`-wrapping page content + media defanging, a real e2e — VQ-10 needs a new e2e harness).

## fire 21 · 2026-07-12 · skill v2.x · 3793cbd6b
meta: slice=D1-S7d1 · wave=W2 · pkg=@muse/browser · kind=injection-guard · verdict=PASS · firesSinceDrill=3
ratchet: roadmap remaining [ ] = 15/41 (D1-S7d→d1/d2 split +1, d1 checked -1 → 15) · self-eval pass · fabrication 0 · browser +13 tests (page-content-guard)
- What: 🔒 a deterministic injection guard on browser page text (the largest untrusted channel). Pure, self-contained page-content-guard.ts: defangPageText (escapes `</page>`/`<page>` break-out to full-width · neutralizes `](` to block markdown image/link exfiltration · an instruction-override regex → `[defanged-directive]`, bounded = ReDoS-safe) + wrapPageContent (escape-then-wrap) + defangElementName. snapshotToJson wraps text · elementsJson defangs names → covers every snapshot-tool along the assembly path. Split D1-S7d into d1 (guard)/d2 (real e2e).
- Why: browser page text reaches the model with zero escaping → injection ("ignore all previous instructions") · media exfiltration · `</page>` break-out are all possible. Muse's #1 rule "guards are deterministic code, never a prompt." Referencing hermes' inline `<page>` and openclaw's page defanging.
- Review point: Opus's threat-model review PASSed — escaping happens before wrapping (correct order), `](` genuinely blocks exfiltration, the instruction regex is ReDoS-safe (170KB at 7.5ms) + honest positioning (defense-in-depth atop a structural wrap, no oversell), the assembly path was actually demonstrated (a fake controller with malicious text → the tool output is defanged, OUTCOME-scored), 2 independently reproduced mutations (6/4 RED). Self-contained (no @muse/recall dependency).
- Risk: low. Defanging is byte-identical on clean prose (idempotent). Uncovered vectors (reference links · bare URLs · adaptive rephrasing) are honestly out-of-scope (the model consumes JSON, doesn't auto-fetch). The instruction-override regex can false-positive (a security article) but stays advisory and readable. Next = D1-S7d2 (a real detached-Chrome e2e — serve malicious HTML over HTTP→open→confirm defanging, reusing the eval:browser-agent harness, executing VQ-10). D1-S7 completes entirely once D1-S7d2 lands.

## fire 22 · 2026-07-12 · skill v2.x · 5a688ac8e
meta: slice=D1-S7d2 · wave=W2 · pkg=scripts+package.json · kind=browser-injection-e2e · verdict=PASS · firesSinceDrill=4 · ★D1-S7 complete
ratchet: roadmap remaining [ ] = 29/55 (recounted the whole §10.3 accurately; the previous journal's tail was missing) · self-eval pass · fabrication 0 · eval:browser-injection new (9/9 live)
- What: a real headless-Chrome e2e for D1-S7d1's injection guard. scripts/eval-browser-injection.mjs (model-free, Chrome only): malicious HTML (ignore-above · `![](exfil)` · an HTML-escaped `&lt;/page&gt;` · an injection anchor) served over loopback HTTP → a real PuppeteerBrowserController.open → confirmed the browser_open/read tool output is `<page>`-wrapped + defanged, 9/9 assertions (OUTCOME). Executed and checked off VQ-10 "write a new e2e harness." D1-S7 is entirely complete (a·b1·b2·c·d1·d2).
- Why: "browser needs a real e2e (mocking the assembly path is a lie — a lesson learned)." A FakeController-only unit doesn't prove the real page→innerText→snapshot→tool-output path. Proved the assembly path with real Chrome.
- Review point: Opus PASSed (a real controller+real Chrome, a live PASS, not skipped, OUTCOME-scored, mutation-kill 7/9 flip reproduced, robust cleanup). Opus flagged 2 issues, fixed immediately: (1) the boundary assertion was tautological against the original fixture (the browser parses `<page>` as an empty element → innerText never reaches it) → HTML-escaped it as `&lt;/page&gt;` so it's reached literally, making it genuine (re-verified: disabling the guard flips the boundary count to 2, FAIL), (2) removed a stray header slice-marker.
- Risk: low. Test/eval-infra only (zero production code). Skips only if Chrome is absent (this machine has it installed → runs live). eval:browser-injection needs Chrome in CI to RUN (skip if absent ≠ pass, stated explicitly). Next = D4-S4 (📈 deterministic file_edit repair, eval:computer-task +10pp pass^3). ※count correction: the previous journal missed the §10.3 tail (D6-*/D3-S3,S6/D2-S7/D7-*/D-KO-S3/D-E1/encryption-backup, ~14 items) → the actual remaining count is 29 (checked 26).

## fire 23 · 2026-07-12 · skill v2.x · c81433523
meta: slice=D4-S4 · wave=W2 · pkg=@muse/fs · kind=edit-repair · verdict=PASS · firesSinceDrill=5
ratchet: roadmap remaining [ ] = 28/55 · self-eval pass · fabrication 0 · fs +? tests (53 in fs-write-tools) · eval:computer-task 3/3 pass^3
- What: 📈 strengthened deterministic file_edit repair (fs-write-tools.ts). Added indent-preserve+escape-drift extensions to the existing ladder. (1) when fuzzy matches by relaxing indentation (trim), rebase new_string onto the file's real indentation (reindentToFile) — fixes a bug where the 12B's wrong indentation contaminated the file. (2) turned unescapeQuotes (`\"`/`\'`/`\\`) into a retry loop under a fail-close guard (only on a unique match). The exact/trailing-ws paths stay no-ops = byte-identical.
- Why: remaining eval:multifile-fix/computer-task failures came from a 12B getting indentation/escaping slightly wrong → exact match fails → fuzzy matches but replaces with the model's own (wrong) indentation, contaminating the file. Referencing hermes' 9-strategy fuzzy match's indent strategy.
- Review point: Opus PASSed — re-indent accuracy (traced through a consistent-base case) · conservatism (strictly-better than a verbatim splice even at the edges) · exact/trailing-ws byte-identical (no-op guaranteed) · fail-close (gated on a unique match, never wrong-place) · 2 independently reproduced mutations (disabling re-indent → indent contamination goes RED; removing unescapeQuotes → `\"` goes RED) · OUTCOME-scored (verified the resulting line's indentation). eval:computer-task 3/3, no regression.
- Risk: low. +10pp pass^3 is a stochastic north-star that can't be confirmed per-fire (gated by regression-STABLE 3/3, the mechanism is directional). Combined drift (newline+quote together) isn't repaired in one shot but fails CLOSED (refuses, doesn't contaminate). Unaddressed siblings (whitespace-collapse · first/last anchoring) = backlog, case-insensitive is permanently excluded. Next = D4-S1 (extend muse mcp serve: more reads + a write draft-first proxy + exposing grounded recall).

## fire 24 · 2026-07-12 · skill v2.x · d9855ec62
meta: slice=D4-S1a · wave=W3 · pkg=apps/cli · kind=mcp-write-proxy · verdict=PASS · firesSinceDrill=6
ratchet: roadmap remaining [ ] = 29/57 (D4-S1→a/b/c split, +2) · self-eval pass · fabrication 0 · cli mcp-serve +? tests (16) · ★W3 started
- What: `muse mcp serve`'s write draft-first proxy. The propose_action MCP tool (the 4th in buildMcpServeTools): an external client proposes an action+draft(+arguments) → parked into the existing PendingApproval queue (reusing recordPendingApproval, the same file muse approvals reads) (source "mcp-serve"), returns "pending approval," zero execution path. Split D4-S1 into a (write proxy)/b (register as a grounded surface 📈)/c (expand reads).
- Why: hermes' mcp_serve (turning itself into an MCP server)'s write must be an outbound-safety export in Muse — an external request may never auto-execute, only park in the approval queue. Clicking approval ≠ auto-executing an external request. No new store (reuses the existing one).
- Review point: Opus's outbound-safety threat-model review PASSed — zero execution branch in execute (action/arguments are just parked data, never dispatched) · blank fail-close (throws before staging) · a rejected stage never returns staged:true · a real no-external-effect verification (a temp round-trip + confirmed notesDir unchanged) · 2 independently reproduced mutations (removing staging → round-trip goes RED, removing blank validation → RED) · provenance distinction (source "mcp-serve" vs. cli). Also fixed a stale "three tools" header doc.
- Risk: low. An external caller fully controls draft/arguments → a spoofing surface on the raw display in muse approvals (VQ-20), but parking = no-effect + approval = human confirmation + fail-close, so this doesn't violate the contract (the same surface as the existing CLI-write path, not a new regression). eval:tools is N/A (an outbound MCP tool, not part of Muse's local model's selection set). Next = D4-S1b (register grounded-recall as a grounded surface, verify-*.mjs battery → groundedSurfaces +1).

## fire 25 · 2026-07-12 · skill v2.x · 05a8c0ebe
meta: slice=D4-S1b · wave=W3 · pkg=(none, doc-only) · kind=already-holds-invariant · verdict=PASS · firesSinceDrill=7
ratchet: roadmap remaining [ ] = 28/57 · self-eval pass · fabrication 0 · groundedSurfaces 38 (mcp-serve-grounding already counted) · live battery 4/4
- What: discovered and confirmed that D4-S1b (registering grounded-recall as a grounded surface) is an **already-holding invariant**. verify-mcp-serve-grounding.mjs shipped alongside `muse mcp serve`'s very first commit cc1fdde81 (07-07), including battery + release-gate registration (eval-self-improving.mjs:62) → already counted in groundedSurfaces (38). Zero code changes; re-ran it live 4/4 PASS as proof, then marked it doc-only.
- Why: fire 24's decomposition was written without knowing the battery existed → discovered and honestly marked already-holding. Faking a duplicate battery to inflate the count is exactly what the groundedSurfaces ratchet is meant to block (honesty over fake progress).
- Review point: Opus's honest-accounting verification PASSed — registered (the countGroundedSurfaces regex matches = 38, the mcp-serve entry counts as true) · behaviorally verified (a real SDK Client · initialize→tools/call · every citation resolves against a real seed, refusal without fabrication, not just "the tool responded") · live 4/4 (not skipped) · no gap against D4-S1b's specific acceptance (stdio subprocess · expanded reads are split into D4-S1c). Confirmed that checking `[x]` without code is honest here.
- Risk: none (zero code). Lesson: before decomposing a new grounded-surface slice, first confirm whether an existing verify-*.mjs/battery already covers it, to avoid a redundant sub-slice. Next = D4-S1c (expanded reads: calendar · task read tools + a real stdio round-trip contract).
- lesson: before a new grounded-surface slice, `ls apps/*/scripts/verify-*.mjs` + grep eval-self-improving.mjs's BATTERIES to confirm whether the surface is already registered — if so, live-demonstrate it and mark it honestly, never fake a duplicate (the ratchet guards against count-inflation).

## fire 26 · 2026-07-12 · skill v2.x · 2244aca88
meta: slice=JUDGE-DRILL+D4-S1c1 · wave=W3 · pkg=apps/cli · kind=judge-drill+mcp-calendar-read · verdict=PASS · firesSinceDrill=0 (reset)
ratchet: roadmap remaining [ ] = 29/59 (D4-S1c→c1/c2/c3 split +2, c1 checked -1) · self-eval pass · fabrication 0 · cli mcp-serve +3 tests (calendar_read) · baseline ENV repair (foreign 8 env)
- What: ① a baseline envInventory FOREIGN-fail (the digest/interruption merge added 8 env vars without docs:env) → a resync commit (3a7e73737). ② JUDGE-DRILL: injected a deliberately defective D4-S1c (the calendar window filter ignores the `toIso` upper bound + a shape/sort-only test) → independent Opus ④b FAILed it with an empirical probe (a `["a","b","c"]` leak) — concrete. ③ rolled back → the real fix, D4-S1c1: the calendar_read MCP tool (from/to parsed independently → delegates to LocalCalendarProvider.listEvents, structurally guaranteeing both bounds pass through). Split D4-S1c into c1/c2/c3.
- Why: the hard-counter (firesSinceDrill=8, exactly 8 fires since the last drill on fire 18) forced a re-verification. After confirming maker≠judge is alive, shipped the correct version (the provider filters the window, the tool delivers both bounds exactly) as the real fix.
- Review point: the drill judge and the c1 judge are separate Opus instances. The drill judge ran an empirical probe to catch the planted defect (the ignored upper bound · a hollow test) — adaptive, not rubber-stamped. The c1 judge PASSed the structural both-bound pass-through · a fail-close spy (the source is never called) · OUTCOME-scoring · 2 independently reproduced mutations · read-only confirmation.
- Risk: low. calendar_read is read-only (no event creation/change) · doesn't execute anything from event content. The window filter is delegated to the provider, so the tool only passes both bounds through (structurally can't drop one). eval:tools N/A (an outbound MCP tool). Next = D4-S1c2 (task read) · c3 (a real stdio round-trip).
- lesson: a JUDGE-DRILL "real fix" is ideal when it's the *structurally impossible* version of the drill's defect — redesigning "ignore the upper bound" into "parse from/to independently and deliver both in one call" means that whole defect class can't recur (a structural guarantee, not a test dependency).

## fire 27 · 2026-07-12 · skill v2.x · 5dcfa0b49
meta: slice=D4-S1c2 · wave=W3 · pkg=apps/cli · kind=mcp-tasks-read · verdict=PASS · firesSinceDrill=1
ratchet: roadmap remaining [ ] = 28/59 · self-eval pass · fabrication 0 · cli mcp-serve +4 tests (tasks_read)
- What: `muse mcp serve`'s tasks_read tool (symmetric with c1's calendar_read). A status enum (open by default/done/all) → delegates to the existing LocalFileTasksProvider.list(status), returns structured tasks (createdAt/completedAt ISO). Status is passed through exactly (not hardcoded) · an invalid status → throws + the source is never called (fail-close). McpServeDependencies got an injectable listTasks. A 1-line barrel export of the Task type from domain-tools.
- Why: the task portion of D4-S1c (expanded reads). Referencing hermes' mcp_serve read expansion (recall/notes · calendar · tasks). An external agent can read the user's to-do list (read-only, no create/complete/change).
- Review point: Opus PASSed — status pass-through (a fake actually receives "done"/"all," not a hardcoded "open") · fail-close on invalid status (a spy confirms the source is never called) · OUTCOME-scoring (status accuracy · task round-trip via ISO) · 2 independently reproduced mutations (hardcoded→pass-through goes RED, removing the guard→fail-close goes RED) · a benign barrel export (exposing an existing Task type, no new symbol) · read-only.
- Risk: low. Read-only (no task changes). completedAt's ISO serialization isn't directly tested (covered by the same pattern as calendar's endsAt, a coverage nit). eval:tools N/A (an outbound MCP tool). Next = D4-S1c3 (a real stdio subprocess round-trip contract — spawn muse mcp serve→JSON-RPC, a real process, not InMemory). D4-S1 (mcp serve extension) is fully complete once D4-S1c3 lands.

## fire 28 · 2026-07-12 · skill v2.x · f97f43515
meta: slice=D4-S1c3 · wave=W3 · pkg=apps/cli(scripts) · kind=mcp-stdio-contract · verdict=PASS · firesSinceDrill=2 · ★D4-S1 complete
ratchet: roadmap remaining [ ] = 27/59 · self-eval pass · fabrication 0 · mcp:stdio-contract new (a real subprocess live PASS) · ENV.md MUSE_CALENDAR_FILE apps/cli reader
- What: the real MCP stdio subprocess round-trip contract. verify-mcp-stdio-contract.mjs (pnpm mcp:stdio-contract, model-free): spawns a real muse mcp serve via StdioClientTransport(process.execPath+dist)→initialize→tools/list (6 tools)→tasks_read round-trips 2 seeded tasks (count·title)+status open→1. Demonstrated a real stdio wire, not InMemory. Fixed the stale sibling MCP_SERVE_INSTRUCTIONS ("3 tools"→the accurate 6, propose_action=park-not-execute). D4-S1 (mcp serve extension) fully complete (a·b·c1·c2·c3).
- Why: the existing verify-mcp-serve-grounding uses InMemoryTransport (an in-process protocol), so it never demonstrated a real subprocess spawn + stdio framing. hermes' mcp_serve requires "itself as a real MCP server" — the real contract is a stdio round-trip. Aligns with "mocking the assembly path is a lie" (a real wire).
- Review point: Opus's live re-run PASSed — a real subprocess ("listening on stdio (6 tools)" stderr) · not InMemory · seeded data round-trips data-sensitively (mutation: seed 1 fewer task → count goes RED, exit 1, reproduced) · skip = spawn failure only / assertion failure = exit 1 (honest) · MCP_SERVE_INSTRUCTIONS accurate and honest (propose_action described as parking, qualified) · leak-free cleanup (client.close→kills the subprocess · removes temp files).
- Risk: low. A contract script, so CI needs a dist build (pnpm mcp:stdio-contract builds first). Added apps/cli reader for ENV.md's MUSE_CALENDAR_FILE (referenced by c1/c3) = docs:env included within this slice. Next = D4-S2a (macOS Photos search/export, expanding the mac_photos actuator).
- lesson: a real-wire contract test that round-trips seeded known data (count · title) structurally guarantees data-sensitivity = not tautological (avoiding a weak test that only confirms connectivity).

## fire 29 · 2026-07-12 · skill v2.x · 140c9ffef
meta: slice=D4-S2a · wave=W3 · pkg=@muse/macos · kind=mac-photo-search · verdict=PASS · firesSinceDrill=3
ratchet: roadmap remaining [ ] = 27/60 (D4-S2a checked · VQ-21 added) · self-eval pass · fabrication 0 · macos +3 tests (imagesOnly)
- What: macOS photo search. Honored "never create a new tool" by extending the existing mac_spotlight_search with an imagesOnly flag (zero new tools). The mdfind ARGV stays unchanged (never embeds query into a predicate = injection-safe); when imagesOnly, returned paths are post-filtered in code by image extension (the cap applies after filtering, total = the filtered count, imagesOnly:true echoed). The returned path is itself the export handle. Default behavior stays byte-identical.
- Why: the Photos portion of hermes/openclaw-level macOS coverage. The constraint conflicted with Photos being a natural fit for a new tool → provided "find photos" by extending the existing file-finder (avoiding a confusable pair). A deep managed-library Photos.app export needs an automation permission + a tool-home fork, so it's VQ-21.
- Review point: Opus's 8-axis review PASSed — injection-safe (unchanged ARGV, post-filter only) · isImagePath accuracy (real extensions · case-insensitive · no-extension false · no directory false-positives) · default byte-identical · mutation-RED reproduced (removing the filter → .txt/.pdf leak goes RED) · no confusable tool · correct total/cap ordering.
- Risk: low. eval:tools timed out at 6m40s (a heavy local set, incomplete) but the change is additive (a photo keyword+an optional flag, no rename/confusable tool), so the selection-regression risk is near-zero — judged via the deterministic gate, timeout≠fail stated honestly. A deep Photos.app library is VQ-21. Next = D4-S2b (mac_system_set enum expansion: quit app).
- lesson: when a constraint ("no new tools") conflicts with a naturally new capability, ship the constraint-compliant version (a safe extension via post-filtering · an optional flag) and surface the deep version as a VQ — don't quietly downgrade to the easy version, but don't violate the constraint either.

## fire 30 · 2026-07-12 · skill v2.x · 41d0ce2ae
meta: slice=D4-S2b · wave=W3 · pkg=@muse/macos · kind=mac-quit-app · verdict=PASS · firesSinceDrill=4
ratchet: roadmap remaining [ ] = 28/62 (D4-S2 b/c/d split into separate checkboxes +2, b checked -1) · self-eval pass · fabrication 0 · macos +4 tests (quit_app)
- What: macOS quit-app. Added a quit_app enum + an app param (optional, following the volume-value precedent) to mac_system_set. osascript `tell application "<escapeAppleScript(app)>" to quit` — embeds the app name via the shared escaper (injection-safe). A blank/whitespace app fails closed (osascript never called). Zero new tools (constraint honored).
- Why: the quit-app portion of D4-S2's macOS coverage. mac_app_open only opens (/usr/bin/open), doesn't fit quit, and mac_system_set is the home for execute-risk local actions. The app name enters an AppleScript string = an injection surface → reusing the existing escapeAppleScript is the right answer.
- Review point: Opus's threat-model review PASSed — the escaper's backslash-first order prevents bypassing the escaper via escaping the escaper (`\`+`"`→`\\`+`\"`=literal), newline→space, breakout is unconstructible · blank fail-close (a called flag) · 2 independently reproduced mutations (removing the escaper → breakout goes RED, removing the guard → osascript still called goes RED) · additionalProperties kept · no confusion with mac_app_open (quit vs. open keywords separated).
- Risk: low. quit is graceful (may prompt to save, not a force-kill) and local, so draft-first isn't needed. eval:tools timed out on the heavy local set (additive enum+keyword, so selection-regression is near-zero, and Opus confirmed no confusion). Next = D4-S2c (dark mode: dark_mode_on/off, a parameterless osascript enum).
- lesson: whenever embedding a user/model-provided string into osascript, always route it through the shared escapeAppleScript (backslash-first) — no ad-hoc escaping. Reusing an existing tested escaper is the right answer for an injection surface.

## fire 31 · 2026-07-12 · skill v2.x · 5a9e4598a
meta: slice=D4-S2c · wave=W3 · pkg=@muse/macos · kind=mac-dark-mode · verdict=PASS · firesSinceDrill=5
ratchet: roadmap remaining [ ] = 27/62 · self-eval pass · fabrication 0 · macos +3 tests (dark_mode)
- What: macOS dark mode. Added parameterless dark_mode_on/dark_mode_off enum values to mac_system_set. A fixed osascript (System Events appearance preferences set dark mode to true/false) — no user input, so zero injection surface, no escaping needed (unlike quit_app). on→true, off→false.
- Why: the dark-mode portion of D4-S2's macOS coverage. A parameterless toggle fits naturally into mac_system_set (mirroring the mute branch).
- Review point: Opus PASSed — the on/off→true/false mapping is correct (not inverted) · a fixed script has zero injection surface · the test pins the captured script's true/false (not shape-only) · a ternary-flip mutation kills the off-test (independently reproduced) · unrelated branches (volume/mute/wifi/focus/sleep/quit_app) unchanged · additionalProperties kept.
- Risk: low. A fixed script needs no injection/escape handling. eval:tools timed out on the heavy local set (an additive enum, selection-regression near-zero). Next = D4-S2d (Bluetooth/brightness — the focus-pattern Shortcuts path, no clean CLI + MUSE_*_SHORTCUT env override). The D4-S2 macOS batch completes once D4-S2d lands (a·b·c·d, e = Contacts write separately).

## fire 32 · 2026-07-12 · skill v2.x · c6ab1ef8c
meta: slice=D4-S2d · wave=W3 · pkg=@muse/macos+apps/cli · kind=mac-bluetooth · verdict=PASS · firesSinceDrill=6
ratchet: roadmap remaining [ ] = 27/63 (D4-S2d split off brightness +1, d checked -1) · self-eval pass · fabrication 0 · macos +5 · cli doctor +4 tests · env MUSE_BLUETOOTH_*_SHORTCUT
- What: macOS Bluetooth. Added bluetooth_on/off enum values to mac_system_set (an exact mirror of the focus pattern: a named Shortcut via `shortcuts run` argv — no clean Bluetooth CLI). MUSE_BLUETOOTH_ON/OFF_SHORTCUT env override + bluetoothShortcutSetupMessage (missing→"Set Bluetooth" guidance) + a doctor check. Brightness (value→Shortcut-input) split into D4-S2d2.
- Why: the Bluetooth portion of D4-S2's macOS batch. Like focus, macOS has no clean CLI, so a named user Shortcut is the policy-safe path. The orchestrator fully wired the worker's unwired bluetoothShortcutsCheck (dead code) into runLocalDoctor + 4 behavior tests.
- Review point: Opus PASSed — correct name resolution (bluetooth_off→the off shortcut, override precedence) · the setup message says Bluetooth, not focus · a shortcut's argv has no shell injection · a fully wired doctor check (a separate check next to focus, no impact on the focus test) + ok/warn/undefined/override tests · mutation-RED (disabling name resolution → override/off go RED) · additionalProperties kept · docs:env.
- Risk: low. eval:tools timed out on the heavy local set (an additive enum). Next = D4-S2e (Apple Contacts "write," a draft-first gate — writing toward a local store, not a person, but close enough to outbound-safety to handle carefully).
- lesson: when a sub-agent avoids "out-of-scope files" by only creating a function and leaving it unwired, that's dead code = the same defect class the drill catches → the orchestrator must wire it in + test it. Wiring a separate function as a separate check leaves existing pinned tests unaffected (a parallel addition, not an extension).

## fire 33 · 2026-07-12 · skill v2.x · c5e6dab25
meta: slice=BASELINE-REPAIR(foreign) · wave=W3 · pkg=@muse/shared+docs · kind=baseline-repair · verdict=PASS · firesSinceDrill=7
ratchet: roadmap remaining [ ] = 27/63 (unchanged, no slice) · self-eval recovered (lint+envInventory FOREIGN-fail→green) · fabrication 0
- What: ① a non-zero baseline (both lint:fail+envInventory:fail, both FOREIGN) → per the rule, repairing the regression is the entire fire (no new slice). (1) a concurrent secret-detection loop committed 3 unnecessary `\/` escapes inside a char class + an unused findSecrets test import in secret-patterns.ts (no-useless-escape/no-unused-vars) → `\/`→`/` (identical inside a char class) · removed the import, secret 72 tests green (behavior unchanged). (2) a Windows loop added MUSE_WINDOWS_ACTUATORS without docs:env → resynced.
- Why: the two regressions blocked repo-wide lint (0/0)+envInventory gates, which also blocked my own progress (a shared baseline). The tree was clean (0 foreign uncommitted), so repairing the committed errors was safe. Rule ① explicitly states "a non-zero baseline means the repair is the whole fire."
- Review point: confirmed the regex behavior is unchanged (`\/`≡`/` inside a char class) via 72 tests · docs:env is generated · committed only my 3 files, isolated. No Opus gate needed (zero feature change, a deterministic repair).
- Risk: low. Editing foreign src (secret-patterns.ts) but only a committed lint fix (no feature-logic touch) + a clean tree = low conflict risk. If a concurrent loop re-edits the same line, git will merge it. Next = D4-S2e (Apple Contacts draft-first) — resuming now that the baseline is green.
- lesson: when a repo-wide gate (lint/envInventory) stays FOREIGN-fail, my own slice gates get blocked too, so a one-time baseline-repair fire (a trivial fix that never touches feature logic + a generated-artifact sync only) is warranted — but still avoid actively-churning foreign src, fixing only committed-static errors.

## fire 34 · 2026-07-12 · skill v2.x · 5d642a982
meta: slice=JUDGE-DRILL+D4-S2e · wave=W3 · pkg=@muse/macos+apps/cli · kind=judge-drill+contacts-write-draftfirst · verdict=PASS · firesSinceDrill=0 (reset) · ★D4-S2 complete
ratchet: roadmap remaining [ ] = 26/63 · self-eval pass · fabrication 0 · macos +6 · cli-gate +3 tests · toolCases 371→374 (eval:tools golden)
- What: ① baseline green (fire 33's repair held). ② JUDGE-DRILL: injected a deliberately defective D4-S2e contacts-write (calls approvalGate but ignores decision.approved = fail-open write, and deny/no-effect is untested) → independent Opus ④b FAILed it correctly (pointed to lines 47-51 discarding the decision · happy-path-only). ③ rolled back → the real fix, D4-S2e: mac_contacts_write enforces draft-first (mirroring message-send's sendMessageWithApproval: a gate throw→deny · `if(!approved) return` before osascript · deny/throw→zero writes verified by a spy · an action log · escapeAppleScript). buildContactsApprovalGate is non-interactive fail-close + registered + armed-lockstep. Added 3 eval:tools golden cases.
- Why: the hard-counter (firesSinceDrill=8, exactly 8 since the last drill on fire 26) was reached. The drill re-verified the outbound-safety gate (deny→no-effect) is reliable, then shipped the correct version of that defect. The roadmap explicitly calls for a "draft-first gate."
- Review point: the drill judge and the real judge are separate Opus instances. The drill judge pointed to the planted defect (ignoring decision.approved) and probed it to FAIL. The real judge PASSed gate enforcement (deny/throw→zero osascript calls, spy-verified) · deny tests · 2 independently reproduced mutations · the CLI gate's non-interactive fail-close · escaping · armed-lockstep. Opus flagged a sibling-parity gap (no non-interactive test for the CLI gate) → the orchestrator closed it by mirroring messaging's 3 tests.
- Risk: low. A local store write (not a 3rd-party send), but draft-first applies per the roadmap. eval:tools golden was added, but the heavy local-set timeout applies (judged via the deterministic gate + Opus). Next = D7-S1 (single-source slash-command registry). Brightness D4-S2d2 remains.
- lesson: when a JUDGE-DRILL's real fix is an outbound-safety concern, "enforce the gate (deny→no-effect) + spy tests" gives structural assurance by mirroring the message-send seam. When Opus flags a sibling-parity gap (the CLI gate's test), the orchestrator can close it immediately by mirroring an existing sibling's (messaging's) tests.

## fire 35 · 2026-07-12 · skill v2.x · 1373cb8ca
meta: slice=D7-S1a · wave=W3 · pkg=apps/cli · kind=slash-registry · verdict=PASS · firesSinceDrill=1
ratchet: roadmap remaining [ ] = 26/64 (D7-S1→a/b split +1, a checked -1) · self-eval pass · fabrication 0 · cli +6 tests (slash-command-registry)
- What: the single-source slash-command registry. Extracted chat-ink's local SLASH_COMMANDS (27 `{cmd,desc}` entries) into slash-command-registry.ts's single-entry shape (name · desc · category · aliases? · platforms), chat derives from slashCommandsForPlatform("chat") (removing the hardcoded array = single-sourced). desc stays byte-identical · order preserved (the autocomplete menu unchanged). Platform gating: 15 session-related entries are chat-only, 12 list/show entries are chat+cli (a future CLI/channel seam). Split D7-S1 into a (registry+chat)/b (reflect into CLI help).
- Why: like hermes' COMMAND_REGISTRY, a single entry driving multiple surfaces removes drift. Previously overlapping chat/CLI command descriptions were each independently defined, allowing drift — now single-sourced.
- Review point: Opus PASSed — the old 27-entry array is fully removed (git diff -31, zero remaining hardcoding) · chat stays byte-identical (order preserved, the /menu unchanged) · dedup is real Set-uniqueness + name/alias collision scanning (not shallow) · 2 independently reproduced mutations (dup → RED, disabling the gate → RED) · correct platform gating (in-session /undo · /compact · /cost aren't marked cli) · chat-core 154/154. 
- Risk: low. A refactor, so zero user-visible change (CHANGELOG omitted). Real reflection into CLI help (commander) is D7-S1b. Fixed an Opus doc-nit ("28 commands" in the roadmap → 27). Next = D7-S1b (cross-check the commander CLI descriptions against the registry, proving no drift on overlapping commands).
- lesson: metadata shared between chat/CLI should live in a single registry + platform gating; a derived function (slashCommandsForPlatform) replacing a hardcoded array structurally rules out drift, and dedup is proven via Set-uniqueness.

## fire 36 · 2026-07-12 · skill v2.x · 73f60dca3
meta: slice=D7-S1b · wave=W3 · pkg=apps/cli · kind=slash-registry-cli-drift-lock · verdict=PASS · firesSinceDrill=2 · ★D7-S1 complete
ratchet: roadmap remaining [ ] = 25/64 (D7-S1b checked) · self-eval pass · fabrication 0 · cli +1 test file (slash-command-registry.cli-drift, testFiles 1386→1387)
- What: ① baseline green (self-eval ok, testFiles 1386). ② D7-S1b: locked out drift between the registry's `cli` tags and the real CLI command surface. Ground truth = COMMAND_STUBS (the generated manifest that command-manifest.drift.test pins against the commander tree — the authoritative source for `muse --help`/completion). Finding: 3 of the 12 cli-tagged entries — jobs · pref · reflect — don't actually have a `muse <name>` (the CLI has runs/job · remember · reflections). Fix: added CommandEntry.cliName? → reflect={cliName:"reflections"} kept, jobs·pref became chat-only. slashCommandsForPlatform("cli") projects cliName ?? name (chat/channel keep name unchanged). 4 drift-lock tests: (a) every cli-tagged entry actually exists in COMMAND_STUBS (b) the projected command actually exists (c) reflect→reflections, never reflect (d) chat's 27 unchanged.
- Why: like hermes' single registry, one entry should drive multiple surfaces, but "the cli tag is a real CLI command" must be proven against the real manifest so tagging doesn't silently drift from reality. D7-S1a added the tagging (loosely); D7-S1b caught 3 mistagged entries against the real surface, fixed them, and locked it.
- Review point: Opus PASSed — checked against real state (COMMAND_STUBS pins the commander tree, not self-referential) · 2 independently reproduced mutations (a bogus cliName → RED, removing reflect's cliName → RED) · confirmed the 3 fixes as facts (jobs/pref/reflect absent · reflections real) · chat unchanged (27, name projection) · scope limited to 2 files · comment policy (only a 1-line WHY for cliName). Non-blocking note: the cli projection has no consumer yet (only chat consumes it) → drift-lock + tag correction is the honestly delivered scope, live rendering is future work.
- Risk: low. Internal metadata accuracy (zero user-visible → CHANGELOG omitted, same as D7-S1a). Sibling audit: all 12 cli-tagged entries were enumerated and cross-checked in this fire (only jobs/pref/reflect were mistagged, the other 9 confirmed real). Next = D4-S2d2 (brightness value-passing Shortcut-input, the top-to-bottom first unchecked item — a previous defer).
- lesson: a claim that "a registry tag drives multiple surfaces" is only verified by cross-checking the tag against that surface's **real manifest (a generated artifact)** — a self-reference (the registry vs. a hand-copied list) is tautological. Cross-checking against the real surface reveals mistagging (chat-slash names ≠ CLI command names: reflect/reflections), and an optional cliName absorbs the rename while the rest correctly become chat-only.

## fire 37 · 2026-07-12 · skill v2.x · 2c70214a2
meta: slice=D4-S2d2 · wave=W3 · pkg=@muse/macos+apps/cli · kind=macos-actuator-value-passing · verdict=PASS · firesSinceDrill=3 · ★D4-S2 fully closed
ratchet: roadmap remaining [ ] = 24/64 (D4-S2d2 checked) · self-eval pass · fabrication 0 · macos +9 test file · cli-doctor +4 tests (testFiles 1387→1388) · ENV +1 (MUSE_BRIGHTNESS_SHORTCUT)
- What: ① baseline green (self-eval ok, testFiles 1387). ② D4-S2d2: added a brightness enum to mac_system_set — a value-passing mechanism, unlike focus/bluetooth's parameterless design. value (0–100) is clamped+rounded → delivered to a named Shortcut ("Muse Set Brightness") via stdin input (`shortcuts run --input-path - --output-path -` + `String(level)` as stdin). Reused the existing precedent that mac_shortcut_run already uses (`--input-path -` + ShortcutsRunner(args,input?), zero new seam). DEFAULT_BRIGHTNESS_SHORTCUT+MUSE_BRIGHTNESS_SHORTCUT env (wired into the actuator) + brightnessShortcutSetupMessage (missing→"Set Brightness" action + Shortcut Input guidance). doctor: brightnessShortcutCheck (a single shortcut, mirroring focus/bluetooth) + wired into runLocalDoctor. value stays under groundedArgs.
- Why: the last remaining item in the D4-S2 macOS batch (brightness). The roadmap explicitly specifies "value→Shortcut-input" value-passing (distinct from the 3 parameterless siblings). No macOS brightness CLI, so a named Shortcut fallback is policy-safe (the same grounds as bluetooth).
- Review point: Opus PASSed — real verification of stdin value delivery (a fake runner asserts it received argv+input="60," not declaration-only) · an independently reproduced mutation (String(level)→""→RED) · clamp/round (150→"100" · -5→"0" · 33.6→"34") · a threat-model check (value is numeric-coerced + argv-not-shell) · groundedArgs kept · siblings unaffected (the volume-value path is unshadowed, 27/27) · doctor parity (52/52) · envInventory (registered in ENV.md). Sibling audit: brightness is the only value-passing case (volume uses osascript), no other member of this class exists.
- Risk: low. A local system-setting write (not 3rd-party). The setup wizard still needs the user to manually create the Shortcut (the same UX friction as bluetooth, guided by setupMessage). eval:tools golden wasn't added (an enum addition, not a new tool — same precedent as dark_mode/bluetooth; judged sufficient via the deterministic gate + Opus). Next = D5-S1 (privacy-routing follow-ups: codify context-free tools staying local · KO possessive tokens · setup guidance, the existing 20 contracts unmodified).
- lesson: for actuator value-passing, don't invent a new seam — reuse the existing stdin-input precedent (mac_shortcut_run's `--input-path -` + ShortcutsRunner input?) for zero injection surface (numeric-coerce + argv). Where a sibling actuator (focus/bluetooth) has a doctor check, mirror it for parity (the class of gap Opus has flagged before).

## fire 38 · 2026-07-12 · skill v2.x · 1a8ec021d
meta: slice=D5-S1 · wave=W4 · pkg=@muse/policy+apps/cli · kind=privacy-routing-followups · verdict=PASS · firesSinceDrill=4
ratchet: roadmap remaining [ ] = 23/64 (D5-S1 checked) · self-eval pass · fabrication 0 · policy +12 units (privacy-routing.test new) · cli setup +3 units (post-commit testFiles 1388→1389) · 20 contracts unmodified
- What: ① baseline green (testFiles 1388). ② D5-S1, 3 parts, 2 parallel Sonnet workers (disjoint files). (b) PrivacyRequestInput.usesTools? is a deterministic signal (the tier right after hasPersonalContext) → a tool-using request stays local regardless of text; resolvePrivacyRoutedModel passes it through (a route-flip unit: usesTools:true→local, omitted→cloud). Policy-layer codification/defense-in-depth (a chat cloud turn is already structurally toolless via buildCloudTurnRequest — Opus verified the tools field is 0). (c) documented the personaPreamble nuance (persona = an authored fixed string, but a chosen relationship surfaces → keeps personal, doc-only). (d) added KO colloquial possessives 내꺼/제꺼 (꺼 = aspirated, standard 내 거 was already caught by STANDALONE_PRONOUN) + false-positive guards (제거 removal · 내용 · 안내 keep 거≠꺼 → context-free, negative units); added a `muse setup cloud` privacy-routing guidance step (cloudPrivacyRoutingGuidance, wired into real action stdout · captured behaviorally via parseAsync).
- Why: the first slice of the D5 wave (routing · KO). The roadmap says "codify + a unit each + the 20 contracts unmodified" — codify a deterministic guard into the policy layer without touching the frozen 20 chat contracts (the wiring points). A tool is a personal-data conduit, so the privacy principle bans cloud even when context-free.
- Review point: Opus PASSed — a blank git-diff on the 20 contracts (unmodified) · 20/20 green · the route-flip proved at the resolver level (not classification-only) · usesTools is optional, byte-identical when omitted · independent probing of 꺼/거 discrimination (제거→context-free) · setup guidance really captured via stdout (not a constant-substring check) · fail-close preserved (usesTools only returns personal, opens no new cloud path) · the defense-in-depth comment really verified (buildCloudTurnRequest has 0 tools). Independently reproduced bidirectional mutation-RED.
- Risk: low. (b)'s usesTools isn't wired in production yet (inert), but it's honestly scoped (the roadmap asks for "codification" + the 20 contracts unmodified, and chat is already structurally toolless, already enforcing the principle) — Opus judged it not an overclaim. Next = D5-S2 (a unified resolveAuxiliaryModel(task,env), aux also passes the privacy gate · local-only-gate units).
- lesson: a "codify + a unit each + existing contracts unmodified" slice can additively attach an optional deterministic signal to the policy layer (omit = byte-identical) + verify with new units, without breaking the frozen contracts. Whether something's "inert" should be judged honestly against the roadmap's scope (codification vs. live-wiring) — if the wiring point is frozen, a policy-unit delivery is legitimate.

## fire 39 · 2026-07-12 · skill v2.x · 41c8f0a04
meta: slice=D5-S2 · wave=W4 · pkg=@muse/autoconfigure · kind=auxiliary-model-resolver · verdict=PASS · firesSinceDrill=5
ratchet: roadmap remaining [ ] = 22/64 (D5-S2 checked) · self-eval pass · fabrication 0 · autoconfigure +11 units (resolve-auxiliary-model.test new, testFiles 1391→1392) · ENV +5 (MUSE_AUX_*_MODEL)
- What: ① baseline green (testFiles 1391). ② D5-S2: `resolveAuxiliaryModel(task,env)`, a unified resolver (autoconfigure-model-provider.ts, next to resolveVisionModel, pure additive). Tasks: compaction/vision/rewrite/judge/embedding-rescue. Precedence: `MUSE_AUX_<TASK>_MODEL` (the new generalized knob) > legacy per-task knobs (vision→MUSE_VISION_MODEL · embedding-rescue→MUSE_RECALL_EMBED_MODEL; compaction/rewrite/judge have no legacy knob) > sessionModel. A local-first fail-close gate: if the selected model is cloud (classifyProviderLocality) and either isPersonalContext or MUSE_LOCAL_ONLY, override to sessionModel (keptLocalForPrivacy:true); otherwise cloud is honored. index.ts export + docs:env.
- Why: generalizes hermes-style auxiliary.<task> fragmented per-task model knobs (MUSE_VISION_MODEL · MUSE_AUX_COMPACTION · MUSE_RECALL_EMBED_MODEL) into a single resolver while honoring backward compatibility (legacy knobs still respected). Aux tasks are also subject to the privacy principle — a personal-context aux task (e.g. summarizing a personal conversation for compaction) must never leak to a cloud aux model.
- Review point: Opus PASSed — OUTCOME-verified the local-first gate (a cloud task-env + personal → keptLocalForPrivacy:true · model===session · route:local, a negative case honors cloud) · real classifyProviderLocality verification (gemini→cloud/ollama→local, the test doesn't claim the opposite) · 2 independently reproduced mutations (removing the override → a personal-cloud case goes RED · removing the legacy fallback → MUSE_VISION_MODEL goes RED) · resolveVisionModel/resolveDefaultModel unchanged (45 existing tests green) · envInventory:pass (5 dynamic keys documented, no orphans).
- Risk: low. Not wired (call-site migration is a follow-up) — matches the roadmap's acceptance exactly ("resolver · backward-compat · local-only-gate units," the same honest-scope pattern as D5-S1, Opus confirmed no overclaim). Zero user-visible change (CHANGELOG omitted). Next = D5-S3 (wire canUseNativeTools' dead code into a real gate, toolCalling=false→a text protocol/an explicit error, VQ-2's wiring point).
- lesson: a "generalize scattered config knobs" slice can guarantee backward compatibility deterministically by putting the new umbrella env (MUSE_AUX_<TASK>_MODEL) at the top of precedence with legacy knobs as fallback. Embedding a fail-close privacy gate (personal→local) into the aux resolver means the knob can never bypass privacy. Dynamic env keys aren't caught by the env-inventory scanner, so document concrete names to pass the envInventory gate.

## fire 40 · 2026-07-12 · skill v2.x · aecf44510
meta: slice=D5-S3 · wave=W4 · pkg=@muse/agent-core · kind=capability-gate-wiring · verdict=PASS · firesSinceDrill=6
ratchet: roadmap remaining [ ] = 21/64 (D5-S3 checked) · self-eval pass · fabrication 0 · agent-core +6 behavioral tests (model-tool-capability-gate.test new, testFiles 1392→1393)
- What: ① baseline green (testFiles 1392). ② D5-S3 (VQ-2): wired @muse/model's `canUseNativeTools` (toolCalling∧structuredOutput, dead code) into AgentRuntime's request path (prepareInvocation, right after modelTools at line 536) via `assertModelCanUseTools(selected, tools.length)`. If tools are exposed but the model lacks the capability, throws an explicit `ModelToolCallingUnsupportedError` (errors.ts, re-exported from index.ts). Fail-OPEN by design: zero tools · an unknown modelId · a listModels() throw → no block (existing behavior preserved); a per-instance `toolCapabilityCache` (provider.id/model) avoids repeated listModels calls on the hot path. executeToolPlanGated (the text-plan path) is untouched. Removed an Opus-flagged VQ-2 prefix comment (kept the WHY body).
- Why: VQ-2's conclusion — dead code + the text tool protocol isn't implemented. gemma4 (toolCalling=true) is unaffected, but non-tool-calling models (codex/* actually, BYO non-tool-calling cloud) should get an explicit error instead of tools being silently ignored. The full parser is deferred to a separate L (only needed for BYO non-tool-calling).
- Review point: Opus PASSed — the dead code got a real caller (called, not reimplemented) · confirmed the explicit error via the run path (not helper-only, toolCalling=false∧structuredOutput=false both throw · a capable model doesn't throw) · 2 independently reproduced mutations (disabling the throw · toolCount=0) · verified all 4 fail-open branches (0 tools · unknown · a listModels-throw → returns, doesn't throw; a flaky listModels doesn't regress an existing successful turn) · confirmed the cache prevents repeated calls · executeToolPlanGated untouched · scoped to 4 files.
- Risk: low. Fail-open by design, so it only throws for a positively-confirmed non-tool-calling model (the default gemma4 is unaffected). The gate fires when tools are exposed (not required), which matches the roadmap's intent for "an explicit error" (a non-tool-calling model simply can't use tool functionality anyway). Next = D5-S4 (an explicit MUSE_MODEL_FALLBACKS fallback chain, each fallback privacy/local-only-gated · a 1-line note when it fires · byte-identical when unconfigured).
- lesson: a dead-code capability function should be wired at the natural seam in the request path (right after building tools) but stay fail-OPEN (only throw when positively confirmed), so unknown/failed cases never block an existing successful path. A network-ish capability lookup (listModels) should be per-instance cached + conditional (tools>0) to avoid hot-path cost. A roadmap-tracking prefix (VQ-N) is also a task reference under the comment policy, so it got removed.

## fire 41 · 2026-07-12 · skill v2.x · 9cfbab2f4
meta: slice=D5-S4 · wave=W4 · pkg=@muse/autoconfigure · kind=fallback-chain-resolver · verdict=PASS · firesSinceDrill=7
ratchet: roadmap remaining [ ] = 20/64 (D5-S4 checked) · self-eval pass · fabrication 0 · autoconfigure +10 units (resolve-model-fallback-chain.test new, testFiles 1393→1394) · ENV +1 (MUSE_MODEL_FALLBACKS)
- What: ① baseline green (testFiles 1393). ② D5-S4: `resolveModelFallbackChain(env, isPersonalContext?)`, an explicit fallback-chain resolver (autoconfigure-model-provider.ts, next to resolveAuxiliaryModel, pure additive). "No hidden retries" — `MUSE_MODEL_FALLBACKS` (comma-separated) triggers a sequential chain only when set; unset/blank → `{chain:[],dropped:[]}` (byte-identical). Each fallback gets a fail-close gate (mirroring the local-first logic): a cloud fallback is dropped (with a reason) under MUSE_LOCAL_ONLY or isPersonalContext; context-free + non-local-only keeps it in the chain. Order preserved · empty entries filtered. index.ts export · docs:env.
- Why: the last wiring slice of the D5 wave. Referencing openclaw's allowlist+fallback, but honoring Muse's principle (explicit configuration only, and fallbacks are also privacy-gated). A fallback must not become a hidden cloud-egress path — each fallback is re-gated.
- Review point: Opus PASSed — OUTCOME-verified the privacy gate (local-only/personal→a cloud entry drops, a negative control keeps cloud when context-free — not an always-drop) · real classifyProviderLocality verification (gemini→cloud/ollama→local) · order preservation + parsing robustness (whitespace/trailing commas) · 3 unset-case byte-identical tests · 2 independently reproduced mutations (removing the local-only drop → RED) · resolveAuxiliaryModel/resolveVisionModel unchanged (21 combined green) · zero runtime-assembly touch · envInventory:pass.
- Risk: low. Not wired (chain→a ModelFallbackStrategy assembly + a "used fallback X" answer marker = follow-up) — runtime-assembly wasn't touched because it's another loop's HANG blocker, which was the right call. The roadmap's acceptance is "chain-walk · gate units · byte-identical when unconfigured" (unit-based), so this matches exactly (the same honest-scope pattern as D5-S2, Opus confirmed no overclaim). Zero user-visible change (CHANGELOG omitted). Next = D1-S6 (consolidate the turn-scoped one-shot recovery state, a behavior-preserving refactor). ※fire 42 reaches firesSinceDrill=8 → forces a JUDGE-DRILL.
- lesson: a "explicit configuration only · gated · byte-identical when unconfigured" fallback resolver should default unset to an empty chain (no hidden retries) + gate each entry fail-close. If the wiring target (runtime-assembly) is a HANG risk from another loop, it's honest to ship just the resolver + units and explicitly note the wiring as a follow-up (legitimate when the roadmap's acceptance is unit-based).

## fire 42 · 2026-07-12 · skill v2.x · 407009ac6
meta: slice=D1-S6a+JUDGE-DRILL · wave=W4 · pkg=@muse/agent-core · kind=turn-recovery-primitive+judge-drill · verdict=PASS · firesSinceDrill=0 (reset)
ratchet: roadmap remaining [ ] = 20/65 (D1-S6→a/b split +1, a checked -1) · self-eval pass · fabrication 0 · agent-core +4 units (one-shot-recovery-state.test new, testFiles 1394→1395)
- What: ① baseline green (testFiles 1394; a sibling desktop loop's MuseWebWindow.swift is uncommitted = unrelated, untouched via an explicit add). ② JUDGE-DRILL (firesSinceDrill=8 reached): injected a deliberate defect into D1-S6a — OneShotRecoveryState.claim() unconditionally returns true (violating the once-guarantee, inverting the spec) + a hollow test (never checking the 2nd claim=false). Independent Opus ④b FAILed it correctly (pointed to both defects: the inverted claim, the hollow test). ③ rolled back (overwrote the uncommitted new file) → the real fix: claim returns false once already claimed (if claimed.has→false) · a real guaranteed-once test (2nd claim=false · a guarded body fires exactly once · distinct branches independent · hasClaimed is a pure query) · index export. Mutation-RED (removing the once-guard → 3 RED). The corrected version PASSed under Opus.
- Why: D1-S6 (referencing hermes' turn_retry_state.py, "double retry structurally impossible") needs a primitive. The roadmap says "(S)" but model-loop.ts is a 1112-line central file touched by multiple loops → DECOMPOSE-ON-DEFER: a = the state object + a guaranteed-once unit (satisfies "each recovery branch fires at most once"), b = wiring model-loop's scattered flags for real (deferred). The hard-counter (firesSinceDrill=8) forced this fire to be the drill.
- Review point: the drill judge and the confirm judge are separate Opus instances. The drill judge pointed to the planted defect (claim always true · a hollow 2nd-claim check missing) to FAIL. The confirm judge PASSed the corrected version (a once-guard exists · a real guaranteed-once test · mutation caught · additive). 3 mutation-RED tests independently reproduced.
- Risk: low. An unwired primitive (real model-loop wiring = D1-S6b) — the roadmap's acceptance is "the primitive + a guaranteed-once unit," and wiring is decomposed into a separate, careful step. Zero user-visible change (CHANGELOG omitted). The batch is proceeding (per Jinan's instruction: 5-10 slices → push+stop the cron). Next = D2-S3 (extended de-obfuscation, VQ-3 first).
- lesson: a JUDGE-DRILL works well on a "structural invariant" slice like guaranteed-once — injecting a defect (an inverted guard) + a hollow test (missing the 2nd-call invariant) gives Opus exactly two things to point to precisely. A large central file refactor (model-loop, 1112 lines) should be split into a primitive+units (safe) and real wiring (careful), so the central file isn't rushed mid-batch.

## fire 43 · 2026-07-12 · skill v2.x · bd1b3374f
meta: slice=D2-S3 · wave=W4 · pkg=@muse/tools · kind=security-deobfuscation · verdict=PASS · firesSinceDrill=1
ratchet: roadmap remaining [ ] = 19/65 (D2-S3 checked) · self-eval pass (after baseline-repair) · fabrication 0 · tools +9 tests (dangerous-command, 36/36)
- What: ① baseline green. ② D2-S3 (🔒 security): extended the DS-2 dangerous-command normalizer. Per VQ-3's confirmation, only the 2 truly-missing vectors — `normalizeCommandNfkc` (command.normalize("NFKC"), full-width ｒｍ→rm) + `stripAnsiEscapes` (ECMA-48 CSI, a ReDoS-safe char-class regex) — added to the front of the normalizeCommandForGuard pipeline. A no-op on clean ASCII → the existing DS-2 stays byte-identical (RULES/helper/the existing 27 tests unmodified). Blocks full-width-homograph and ANSI-injection bypass payloads. ③ baseline-repair: foreign apps/api WIP (another loop's MUSE_TELEGRAM_POLL_ENABLED etc.) turned envInventory red → docs:env regenerated (a generated file).
- Why: referencing hermes' approval.py. A raw-regex gate is bypassable via shell obfuscation, so detection must happen on top of the normalized form. VQ-3 confirmed `$IFS`/line-continuation/comment-strip/home-path already exist and only NFKC+ANSI are truly missing. Detection-only, so folding never changes execution (safe under the threat model).
- Review point: Opus PASSed — independent bypass probing (full-width sudo/slash · ANSI variants · a full-width tilde all blocked) · confirmed detection-only (zero external callers, the executor at runner.ts:257 etc. runs the original) · no-over-block (a full-width character inside a quote, benign ANSI echo stays safe, quote-awareness preserved) · ReDoS-safe (a single quantifier char class) · the existing 27 unmodified · independently reproduced bidirectional mutation-RED.
- Risk: low. A pure deterministic transform, additive. ⚠️ foreign apps/api WIP dirtied the working tree (not part of my commit, explicit add) — patched over that loop's envInventory regression with docs:env, to be regenerated once that loop commits. ⚠️ the worker used git stash during isolated verification (forbidden) but no damage occurred (stash list was empty, my changes/foreign changes intact) — future worker briefings must explicitly forbid git stash. The batch is proceeding (per Jinan's instruction). Next = D2-S4 (mask secrets in runner stdout→model, after confirming VQ-4).
- lesson: extending a security de-obfuscation guard with detection-only transforms (leaving the original unchanged) added to the front of the normalization pipeline guarantees a no-op on clean input, so the existing gate stays unmodified. A concurrent loop adding an env var on the shared main worktree can turn my own self-eval's envInventory foreign-red — patch with a docs:env regeneration (safe since it's a generated file). Worker briefings must explicitly forbid git stash (a tempting shortcut during isolated verification).

## fire 44 · 2026-07-12 · skill v2.x · 39d715ccb
meta: slice=D2-S4 · wave=W4 · pkg=@muse/tools · kind=security-secret-masking · verdict=PASS · firesSinceDrill=2
ratchet: roadmap remaining [ ] = 18/65 (D2-S4 checked) · self-eval pass (after baseline-repair) · fabrication 0 · tools +5 tests (119/119)
- What: ① baseline green. ② D2-S4 (🔒): wired redactSecretsInText into 2 subprocess-output→model sinks — runner.ts's run_command + a sibling audit found muse-tools-skills.ts's skill_run (both leaked raw stdout/stderr to the model, the same vulnerability class). Truncation integrity preserved: capTruncated is computed against the capped-but-unredacted length before redaction (redaction changes length and would otherwise corrupt the flag). ③ baseline-repair: another round of foreign apps/api env additions turned envInventory red → resynced docs:env.
- Why: per VQ-4's confirmation (runner.ts:88-100 returns raw, no redaction). Referencing openclaw's secret-mask. A command/skill dumping env vars or printing config could leak secrets into the model's context → mask right before returning.
- Review point: Opus PASSed — both sinks are actually wired at the return path (not just an import) · masking is behaviorally verified (sk-proj/AKIA/ghp raw values removed + [redacted-) · truncation is really verified (a mid-secret cut → truncated:true, benign → false, existing truncated states preserved) · no over-masking (benign output stays byte-identical via toBe) · measured performance on 256KB at 17.7ms < a 250ms budget (SECRET_PATTERNS is ReDoS-safe) · confirmed no third sibling exists (other exec paths use regex.exec) · independently reproduced bidirectional mutation-RED.
- Risk: low. Additive masking (capping/decoding unchanged). ⚠️ foreign apps/api WIP env keeps growing, requiring a docs:env resync almost every fire (noise). The worker didn't use git stash this time (per the updated briefing). The batch is proceeding. Next = D2-S5 (calendar-store encryption, reusing the reflections template, 3 round-trip cases).
- lesson: a subprocess-output→model sink often has siblings (run_command · skill_run) — wiring one spot calls for a sibling audit to catch the whole vulnerability class. When adding a transform (redaction) at a spot with a length-dependent flag (truncation), compute the flag against the pre-transform length to preserve integrity. Measure a secret-masker's large-output performance to prove ReDoS absence (satisfying the "performance benign" acceptance criterion).

## fire 45 · 2026-07-12 · skill v2.x · 9160f5cdc
meta: slice=D2-S5 · wave=W4 · pkg=@muse/calendar · kind=security-encryption-at-rest · verdict=PASS · firesSinceDrill=3
ratchet: roadmap remaining [ ] = 17/65 (D2-S5 checked) · self-eval pass · fabrication 0 · calendar +4 round-trip tests (176/176) · ENV +1 (MUSE_CALENDAR_ENCRYPT)
- What: ① baseline green. ② D2-S5 (🔒, the backlog's "LAST encryption item"): encrypted calendar.json at rest. Mirrored the memory-encryption AES-256-GCM envelope **in-package** into @muse/calendar (following the belief-provenance precedent — avoiding a heavy dependency on @muse/memory/@muse/stores, using only node:crypto/node:os). calendar-encryption.ts (an envelope · encrypt/decrypt/isEnvelope · reuses MUSE_MEMORY_KEY + a calendar per-host fallback · MUSE_CALENDAR_ENCRYPT opt-in). local-provider's readAll (auto-detects the envelope → decrypts, and a wrong-key throw propagates outside the quarantine catch = fail-closed, ciphertext never destroyed) + writeAll (format-preserving: the flag OR isCalendarFileCurrentlyEncrypted).
- Why: the last item on the encryption-at-rest queue (events/locations/notes are sensitive). reflections/belief-provenance already proved the per-store template. The calendar package doesn't depend on memory, so an in-package mirror was used instead of importing the seam directly (resolving the heavy-dependency deferral the backlog had noted, via mirroring).
- Review point: Opus PASSed — a random iv (12)/salt (16) per encryption (not a fixed iv) · no plaintext in the raw bytes (title/location absent) · a read-back decrypts identically · fail-closed (a wrong-key throw happens outside the quarantine catch, verified the ciphertext file stays byte-unchanged) · format-preserving · byte-identical by default (the existing 176 unmodified) · no heavy dependency (package.json unchanged) · a single write path (the credential-store is separate) · independently reproduced bidirectional mutation-RED.
- Risk: low. Opt-in (default plaintext byte-identical). A CLI command (muse calendar encrypt/decrypt) is outside the acceptance criteria ("3 round-trip cases + format-preserving") so it was omitted — reflections had one, but D2-S5's acceptance didn't require it. The batch is proceeding (the 4th slice). Next = D-KO-S1 (extract truncateUtf16Safe + wire the 3 unsafe sites).
- lesson: when the encryption seam lives in a heavy package (@muse/memory) that the target package must never depend on, mirror the envelope in-package via node:crypto (the belief-provenance precedent). The core of fail-closed at-rest encryption is keeping a wrong-key throw outside the corrupt-quarantine path (ciphertext is user data, never destroy it). Format-preserving means detecting the existing file's encryption state at write time and keeping it.

## fire 46 · 2026-07-12 · skill v2.x · cfbd763af
meta: slice=D-KO-S1 · wave=W4 · pkg=@muse/shared+recall+tools+autoconfigure+voice · kind=ko-utf16-safe-extraction · verdict=PASS · firesSinceDrill=4
ratchet: roadmap remaining [ ] = 16/65 (D-KO-S1 checked) · self-eval pass · fabrication 0 · shared +utf16-safe.test · voice +wiring test (testFiles 1399→1400)
- What: ① baseline green. ② D-KO-S1 (★ KO): extracted truncateErrorBody's (shared) lone-high-surrogate drop into truncateUtf16Safe(text,cap)+sliceUtf16Safe(text,start,end) (deduplicated), truncateErrorBody now delegates (byte-identical). Wired the 5 sites across 4 files confirmed by VQ-6: recall/history-search (206 head · 213 middle-substring, both boundaries) · tools/tool-definition-helpers (108) · autoconfigure/knowledge-corpus (365) · voice/tts-truncate (19 window · 28 cut). sliceUtf16Safe drops both a leading lone-low and a trailing lone-high.
- Why: referencing openclaw's utf16-slice. A raw .slice can cut an astral character (emoji · CJK extension) in half, leaving a lone surrogate → invalid UTF-8 → downstream JSON/transport corruption. The safe pattern already existed in truncateErrorBody, so extracting it + wiring the unsafe sites achieves both deduplication and coverage.
- Review point: Opus PASSed — both boundaries are correct (a head-cap mid-emoji drops the lone-high, a start mid-pair drops the lone-low, the result scans clean of lone surrogates) · Hangul (a single BMP unit) stays byte-identical · truncateErrorBody's delegation is byte-identical (the existing tests unmodified and green) · the 4 sites are really wired (213's middle case uses sliceUtf16Safe) · surrounding logic unchanged (…/... append, the tts cut) · bidirectional mutation-RED (removing the drop → both the emoji test and the truncateErrorBody delegation proof go RED).
- Risk: low. Byte-identical-when-safe (Hangul/ASCII unchanged). ★A KO-priority slice. This is the 5th and last slice of the batch (per Jinan's instruction of 5-10 slices) — pushing to origin/main + stopping cron 964ac861 next. Next (when the loop resumes) = D-E1 (actually enforce the aggregate eval gate).
- lesson: when a safe-truncation pattern is inlined in a single function (truncateErrorBody), extracting it into a shared helper → delegating (verified byte-identical) + wiring the unsafe sites achieves deduplication and coverage together. UTF-16 safety needs not just head-truncation (a trailing lone-high) but also substring (a leading lone-low) — sliceUtf16Safe covers both boundaries.

## fire 47 · 2026-07-12 · skill v2.x · a01296db9
meta: slice=D1-S6b · wave=W4 · pkg=none(docs) · kind=already-satisfied-determination · verdict=PASS(NO_TARGET) · firesSinceDrill=5
ratchet: roadmap remaining [ ] = 15/65 (D1-S6b checked) · self-eval pass (envInventory baseline-repair) · fabrication 0 · 0 code changes
- What: ① envInventory baseline red (foreign apps/api WIP env) → a docs:env baseline-repair. ② D1-S6b's determination: independent Opus adversarial verdict = NO_TARGET. D1-S6's premise ("Muse's individual scattered raw-flag recovery could double-fire") turned out to be false in the current code. All 5 recovery branches are already at-most-once (false-done via a single non-looping call to runResistingFalseDone · reverify via ReverifyNudgeTracker.nudged per-turn · post-compaction/ping-pong via a terminal return + a dedicated Guard class · attributed-repair via a single pass). Forcing a wiring would be a behavior-unchanged artificial refactor (violating Jinan's "no unrelated refactors"). Marked already-satisfied (0 code changes).
- Why: D1-S6a supplied a primitive, but the real wiring target (an unguarded recovery re-fire) doesn't actually exist. An artificial redundant wiring is the same anti-pattern as fabrication/inflation — an honest "already encapsulated" determination is the correct call (following D4-S1b's already-registered-verify-mcp precedent).
- Review point: an independent Opus adversarially traced all 5 branches' recovery ACTION (not just the detector) — every one is bounded by a single non-looping call, a terminal return, or a dedicated stateful tracker. Not a self-determination (maker≠judge: the Done verdict came from an independent evaluator). OneShotRecoveryState continues to exist as a primitive for future new recovery branches.
- Risk: none (0 code changes). Honestly closed a case where the roadmap's premise didn't match code reality. Next = D-E1 (📈 actually enforce the aggregate eval gate, referencing VQ-12's time budget).
- lesson: when a roadmap slice's premise (scattered flags) doesn't match code reality (already encapsulated by a tracker), don't "fill" the slice with an artificial wiring — have an independent evaluator adversarially judge whether a real target exists, and honestly close it as already-satisfied. Keep the primitive (D1-S6a) for future use. "No unrelated refactors" also covers behavior-unchanged wiring.

## fire 48 · 2026-07-12 · skill v2.x · 49204a4f3
meta: slice=D-E1a · wave=W5 · pkg=scripts(eval-harness) · kind=eval-tier0-contamination-filter · verdict=PASS · firesSinceDrill=6
ratchet: roadmap remaining [ ] = 15/65 (D-E1→a/b/c split, a checked) · self-eval pass · fabrication 0 · scripts +4 units (eval-harness.test)
- What: ① baseline green. ② D-E1a (Jinan's pick: "decompose starting with the safe part"): eval-harness.mjs's Tier-0 contamination filter. detectTier0Contamination(observed)+TIER0_CONTAMINATION_PATTERNS (4 precise-regex markers) → runEvalSuite excludes a case from the total when its observed output leaks an infrastructure failure (an excluded counter, avoiding mistaking it for a behavior failure). **Core threat model: no over-exclusion** — a genuine behavior failure without an infra marker still counts toward the total (blocks pass-rate inflation; total+=1 moved to after the contamination check). An uncontaminated suite stays byte-identical.
- Why: VQ-21 confirmed eval-harness had no pre-exclusion for infra contamination. §591: "the one real piece of work that restores verification discipline from A back to A+." An infrastructure failure (backend down · tool crash · timeout) counted as a behavior failure would undermine gate reliability. D-E1's shared pre-push hook change has a large blast radius (many active loops) — discussed with Jinan and decided to decompose starting with the safe deterministic part (the Tier-0 filter).
- Review point: Opus PASSed — the over-exclusion threat is SAFE (contamination is set only from the detector's result, never derived from the score result; confirmed a behavior-failing case C still counts toward the total) · precision (benign "failed launch"/"30s timeout"/"supports vision" don't get flagged) · byte-identical (only adds `excluded`) · bidirectional mutation-RED (disabling the detector · over-exclusion → the "no over-exclusion" assertion goes RED) · zero touch to the hook/CI/package src.
- Risk: low. Deterministic · additive · zero blast radius (the shared hook is unchanged). D-E1b (extending pre-push + proving the hook blocks) is shared push infrastructure, so it's deferred to a careful fire (confirm with Jinan). Next = D-E1b or D6-S1a. ※a foreign reflection-guard.test.mjs failure belongs to another loop's proactivity (not mine).
- lesson: the core threat of an eval contamination filter is over-exclusion (hiding a real failure → pass-rate inflation) — deciding contamination only in the detector, separate from the score result, and moving the total increment after the contamination check ensures a behavior failure is never excluded. Catching over-exclusion via mutation is the key guard for this class. A shared-infrastructure change (the pre-push hook) with a large blast radius should be decomposed starting with the deterministic part.

## fire 49 · 2026-07-12 · skill v2.x · c541292b4
meta: slice=D6-S1a · wave=W5 · pkg=@muse/agent-core · kind=deterministic-consolidation-score · verdict=PASS · firesSinceDrill=7
ratchet: roadmap remaining [ ] = 14/65 (D6-S1a checked) · self-eval pass · fabrication 0 · agent-core +12 property units (consolidation-score.test, testFiles 1404→1405)
- What: ① baseline green. ② D6-S1a (I picked the direction — per Jinan's "the direction is yours"): sleep-consolidation's deterministic promotion score. scoreConsolidationCandidate(signals,nowMs,opts) = frequency(log2(1+hits)) × recency(a half-life of 2^(-ageDays/14d)) × diversity(distinctQueries 1..2/neutral). Input {hits,createdMs,lastHitMs,distinctQueries?} = RecallHitStats's real shape (distinctQueries is optional-honestly, since the ledger doesn't track it). isConsolidationCandidate(score,threshold). Pure selection-only (zero imports · no write = the D6 no-autowrite invariant structurally guaranteed).
- Why: split D6-S1 (L) into a (score)/b (draft proposal)/c (daemon). Referencing openclaw's dreaming promotion score (6 factors · a 14-day half-life), but off-by-default · no-autowrite · no LLM (Muse's correction-forgetting principle). D-E1b (the shared pre-push hook) has a large blast radius so it's deferred → chose a clean, bounded deterministic slice instead.
- Review point: Opus PASSed — the no-write/pure-selection invariant (zero imports · a frozen-input unit confirms no change) · monotonicity (hits/recency/the 0.5 half-life/diversity behave correctly) · boundary guards (hits≤0 · non-finite · future-clamped) · grounded inputs (matches RecallHitStats · distinctQueries honestly optional) · independently reproduced bidirectional mutation-RED.
- Risk: low. A pure function, unwired (the write side is D6-S1b's draft-first, the daemon is D6-S1c). The no-autowrite D6 invariant is structurally guaranteed by making the function side-effect-free. Next = D6-S1b (turn the promotion into a proactive draft card + a no-autowrite contract mutation).
- lesson: a deterministic score's inputs should be grounded in signals that are actually stored (RecallHitStats hits/lastHitMs) — a signal the ledger doesn't track (distinct-query) should stay optional with a neutral fallback, spec-complete but honestly labeled. The no-write invariant is structurally guaranteed by keeping the function pure (zero imports) and proven with a frozen-input unit.

## fire 50 · 2026-07-12 · skill v2.x · 6e0aba72e
meta: slice=D6-S1b · wave=W5 · pkg=@muse/agent-core · kind=consolidation-draft-proposal · verdict=PASS · firesSinceDrill=8
ratchet: roadmap remaining [ ] = 13/65 (D6-S1b checked) · self-eval pass · fabrication 0 · agent-core +5 units (consolidation-proposal.test, testFiles 1405→1406)
- What: ① baseline green. ② D6-S1b: turns sleep-consolidation candidates into a draft proposal. runConsolidationProposalPass({candidates,nowMs,nowIso,threshold,publish,promote?}) — filters via D6-S1a's score (isConsolidationCandidate) → publishes a draft proposal notice only for above-threshold candidates (AgentInitiatedNotice kind="memory_consolidation_proposal", "want me to remember this longer?...needs your approval before it becomes durable," sourceId=memoryId). buildConsolidationProposalNotice is pure. **The no-autowrite contract**: deps.promote (the durable writer) exists in deps but is **never called** (the correction-forgetting invariant), only referenced in types/comments · 0 call sites.
- Why: the draft-first half of D6-S1 (sleep-consolidation). The correction-forgetting principle — Muse must never write to durable memory without user approval. The score (D6-S1a) only selects candidates; promotion happens only on user confirmation (D6-S1c's daemon). Applies the outbound-safety deny/no-effect pattern to memory promotion.
- Review point: Opus PASSed — the no-auto-write invariant (promote referenced only in types/comments, never called; the test asserts promote fires 0 times even with all-strong candidates = can't fraudulently pass via weak suppression) · 2 independently reproduced mutations (injecting auto-write → the promote spy goes RED; flipping the threshold → selection goes RED) · really wired to D6-S1a's score (not reimplemented) · the notice's shape (kind/sourceId/confirmation text) · Date-free (nowMs/nowIso as inputs).
- Risk: low. Unwired (daemon consumption = D6-S1c). Contracted the no-autowrite invariant by never calling deps.promote and guarded it with mutation (injecting a promote call → spy RED). D6-S1's score(a)+proposal(b) are complete, daemon alignment (c) remains. Next = D6-S1c (wire the daemon · align with loop-v2 Sleep) or D-E1b. ※a foreign encrypted-file.ts diff belongs to another loop.
- lesson: a draft-first/no-autowrite contract should keep the write capability (promote) in deps but codify "never call it," then guard it with a mutation (inject a promote call → spy RED) to make the contract real. A no-write test must use strong candidates (all above-threshold) to verify — otherwise a weak-suppress false-pass (promote fires 0 times regardless) goes undetected.

## fire 51 · 2026-07-12 · skill v2.x · 83beadab2
meta: slice=D6-S1a/b REVERT + D6-S1c already-satisfied · wave=W5 · pkg=@muse/agent-core · kind=honest-redundancy-revert · verdict=INTEGRITY-CORRECTION · firesSinceDrill=0 (reset, the JUDGE-DRILL's role was organically fulfilled)
ratchet: roadmap remaining [ ] = 12/65 (D6-S1c checked · a/b honestly corrected) · self-eval testFiles 1407→1405 (an intended -2, removing duplicate tests) · fabrication 0
- What: fire 51 was scheduled to be a JUDGE-DRILL, but while scoping the next slice D6-S1c, a **major discovery** surfaced: recall-promotion.ts already comprehensively implements and wires all of D6-S1 (sleep-consolidation) — scoreRecallHit · selectPromotableMemories · selectForgettable · shouldConsolidateMemory · planMemoryConsolidationTick · the MUSE_SLEEP_PROMOTE opt-in daemon. Meaning what I built in fires 49 (D6-S1a) and 50 (D6-S1b) was a **duplicate reimplementation**. An independent Opus audit ruled D6-S1a REDUNDANT and D6-S1b DISTINCT-BUT-INERT+DESIGN-TENSION. Jinan made the strategic call (keep the status quo, opt-in auto-promote). → deleted consolidation-score.ts/proposal.ts + their tests (4 files) · removed the index.ts exports · honestly corrected the roadmap for D6-S1a/b/c (a=already-existing · b=Jinan chose to keep auto-write · c=already-satisfied).
- Why: honesty is a core value. Instead of defending shipped duplicate work, reverted it honestly. Draft-first vs. opt-in-auto-write is a strategic fork over how far the correction-forgetting invariant should extend into the soft persona layer — a call for Jinan (per the roadmap's rule: strategic VQs go to Jinan). Organically fulfilled the intent of a JUDGE-DRILL (a gate catching bad/inappropriate work) — an independent Opus caught that "shipped" work was actually a duplicate. firesSinceDrill reset to 0.
- Review point: an independent Opus audit surveyed recall-promotion.ts + the daemon wiring in full → ruled REDUNDANT/DESIGN-TENSION, recommended a revert rather than a defense. Jinan decided to keep the status quo via AskUserQuestion. Clean build (0 dangling refs) · lint 0/0. self-eval's testFiles -2 is intentional (removing duplicates).
- lesson: ★always confirm an existing implementation via **codegraph/grep before building a new capability** — not looking at recall-promotion.ts (in the memory package) wasted 2 fires building a duplicate score in agent-core. Not verifying a roadmap slice's premise ("doesn't exist yet") against code produces duplicates. When shipped work turns out redundant/inappropriate, don't defend it — get an independent evaluator's verdict and revert honestly. A strategic fork (how far an invariant should extend) belongs to Jinan, not to be pushed through in code.
- Next = D-E1c (self-eval commit-hook + the deterministic CI portion) or D6-S3 (external-edit drift). D-E1b (the shared pre-push hook) stays carefully deferred.

## fire 52 · 2026-07-12 · skill v2.x · 4e46612aa
meta: slice=D6-S3 · wave=W5 · pkg=@muse/memory · kind=memory-external-edit-drift-guard · verdict=PASS · firesSinceDrill=1
ratchet: roadmap remaining [ ] = 11/65 (D6-S3 checked) · self-eval pass · fabrication 0 · memory +6 units (external-edit test, testFiles 1405→1406)
- What: ① baseline green. ② applied the lesson: confirmed via codegraph/grep before building that D6-S3 wasn't implemented (memory-user-store-file only has withFileLock, no external-edit detection; "round-trip" was just JSON serialization). ③ D6-S3 (integrity): FileUserMemoryStore now blocks an external edit (manual · patch tools · anything bypassing the lock) between a lock-free read and a write via compare-and-swap. read() returns raw on-disk bytes → write(data,encrypted,expected?) re-reads right before the atomic write, and if currentRaw !== expected.raw, it snapshots .bak.<ts> (a copy, the original isn't deleted) + throws MemoryExternalEditError to block the clobber. Raw comparison covers plaintext and encrypted alike. Wired into all 4 write paths: patch/deleteByUserId/encryptAtRest/decryptAtRest. Removed an Opus nit (a test name referencing VQ-7).
- Why: VQ-7 confirmed the lock protects only Muse's own writers — external edits (the hermes memory_tool case) still got clobbered. A user's confided memory must never be silently overwritten (correction-forgetting · data safety). Compare-and-swap gives optimistic concurrency.
- Review point: Opus PASSed — an external edit is never clobbered/destroyed (throw-before-write + a copy, not a move — confirmed the external content survives on disk) · byte-identical when not engaged (the existing 47 unmodified) · encrypted drift detected (raw comparison) · no false positives on a normal write (currentRaw===expected) · independently reproduced bidirectional mutation-RED (removing the drift check → clobber goes RED). ★codegraph pre-check prevented a repeat of fire 51's duplication mistake.
- Risk: low. Opt-in per-call (no change when expected is omitted). An external edit is backed up, not deleted (honoring "never destroy Muse data"). Next = D-E1c (self-eval commit-hook + CI) or D6-S4 (provenance tags). D-E1b (the shared pre-push hook) stays carefully deferred.
- lesson: an integrity compare-and-swap's core is capturing raw bytes at read → re-reading and comparing right before write; a raw (pre-parse) comparison means the behavior is agnostic to plaintext vs. encrypted. On detecting an external change, back up via .bak (not delete) + throw, so user data is never destroyed. ★lesson applied: this time confirmed no existing implementation via codegraph before proceeding (preventing a repeat of fire 51's duplication).

## fire 53 · 2026-07-12 · skill v2.x · 877020fb2
meta: slice=D6-S4 · wave=W5 · pkg=apps/cli(test-only) · kind=autonomous-no-delete-contract · verdict=PASS · firesSinceDrill=2
ratchet: roadmap remaining [ ] = 10/65 (D6-S4 checked) · self-eval pass · fabrication 0 · cli +2 contract units (memory-consolidate-tick-user-fact-protection, testFiles 1406→1407)
- What: ① baseline green. ② applied the lesson: verify-first before building — an independent Opus audit confirmed D6-S4's invariant ("autonomous curation can't delete a user fact") is **already structurally established** (a provenance source auto|user tag exists · autonomous fade is non-destructive, a rank-down sidecar · the only autonomous forget is scoped to the recalled-* synthetic namespace · real deletion is user-triggered only). The gap = no end-to-end pin. ③ instead of building a new guard (dead code = fire 51's duplication trap), built a **mutation contract test**: seeded a real user fact into a real FileUserMemoryStore + drove fade+promote with strong recall-hits (non-vacuous), then ran runMemoryConsolidationTick and verified the user fact **survives unchanged**. ④ discovered a separate recalled-/recalled_ normalization bug (backlog).
- Why: the roadmap's acceptance is "an autonomous-deletion-forbidden contract (mutation)." If autonomous curation ever deleted a user-directed fact, correction-forgetting/trust would collapse. Pinning an already-established invariant is the correct output here (the guard already exists, so building a new one would be duplication).
- Review point: Opus PASSed — non-vacuous (the tick really does promote+fade while the user fact survives; a no-op tick would be meaningless) · mutation flips (removing the recalled- scope from the guard → the user fact gets deleted → RED, while the non-vacuous assertion still passes = proving real activity) · the correct invariant (the user fact survives, user-triggered forget is unaffected) · zero production change (honoring verify-first).
- Risk: low. Test-only. ★applied fire 51's lesson 2 fires in a row (D6-S3 and D6-S4 both used verify-first to confirm existing implementations/invariants before proceeding — zero repeated duplication). The discovered recalled_ bug is a separate slice, logged to backlog. Next = D-E1c (self-eval commit-hook; the CI part is N/A since Jinan doesn't run CI) or D6-S5. D-E1b (the shared hook) stays carefully deferred.
- lesson: a roadmap "contract (mutation)" acceptance means locking an **already-established invariant** with a **non-vacuous pin test** — drive a real store through a real path so the autonomous tick genuinely does its work (promote+fade), assert user data survives, then confirm via mutation (removing the guard → RED). If the guard already exists, don't build a new one — just test it (avoiding duplication). If a nearby bug (recalled_ mismatch) surfaces during test-writing and it's out of scope, don't fix it — log it to backlog.

## fire 54 · 2026-07-12 · skill v2.x · 753b47004
meta: slice=D3-S3 · wave=W5 · pkg=apps/cli · kind=idle-drain-contract+gap-fix · verdict=PASS · firesSinceDrill=3
ratchet: roadmap remaining [ ] = 9/65 (D3-S3 checked) · self-eval pass (envInventory baseline-repair) · fabrication 0 · cli +5 pure+1 integration units (proactive-consume, testFiles 1407→1411)
- What: ① baseline green. ② verify-first (reading the code carefully) found a narrow D3-S3 gap: chat-ink's tick only checks idle at the start (359), never right before inserting via setTurns (383) after an async fetch (376 is only unmount-related) → a busy flip during the fetch could still insert mid-generation (idleRef is synchronously updated every render). ③ fix+pin: extracted pure selectDrainedProactiveTurns({idleAtConsume,grouped,jobs,nudges}) (returns [] when busy) → the tick re-checks idleRef.current after the awaits. **No lost notifications**: moved seen-marking to after consumption (only when drained>0) → busy-deferred completions stay unseen → resurface on the next idle poll (avoiding "marked but never shown"). ④ baseline-repair: foreign apps/api env → docs:env.
- Why: referencing hermes' async_delegation (a completion queue drained only in an idle window). The "no insertion mid-generation" contract didn't exist, and verify-first found the additional gap of not re-checking at insertion time. A completion notification landing mid-answer breaks UX, and losing a deferred one is a lost event.
- Review point: Opus PASSed — nothing inserted while busy (idleRef re-checked after the awaits, setTurns only fires when drained>0) · deferred events not lost (seen-marking after consumption, the integration test verifies both "not shown while busy" AND "resurfaces on the next poll" = non-vacuous) · common cases preserved (the existing 59 unmodified) · mutation flips both ways (both the pure and integration tests go RED) · the pure helper is genuinely pure.
- Risk: low. The common idle-path is unchanged, only the busy-fetch edge case is deferred (fixed). Extracting UI logic into a pure helper unit-tests the contract without a React render. Next = D3-S6 (eval:orchestration ratchet 📈) or D2-S7 (expand eval:adversarial 📈). D-E1b/c (the shared hook) stays carefully deferred.
- lesson: a contract living inside a UI (useEffect) can be unit-tested by extracting the decision logic into a pure helper (without a React render) + one full-component integration test to verify the wiring. A state flip (busy) between async steps should be closed via "re-check at the consumption point," but dedup-marking (seen) must move to after consumption or deferred events get lost (marked-but-never-shown is worse than the original bug). Verify-first upgraded the roadmap's "pin" into "fix+pin."

## fire 55 · 2026-07-12 · skill v2.x · fea5d5bd9
meta: slice=D2-S7 · wave=W5 · pkg=scripts(eval-adversarial) · kind=safety-battery-expansion · verdict=PASS · firesSinceDrill=4
ratchet: roadmap remaining [ ] = 7/65 (D2-S7 checked) · self-eval pass · fabrication 0 · deterministic-guard battery 10→19 cases (topology +5 · obfuscation +4) · eval:adversarial 9/9 live pass
- What: ① baseline green. ② the next unchecked item top-to-bottom in the roadmap = D2-S7 (D-E1b/c stays deferred as shared push/commit-hook infrastructure, D6-S2 is skipped as attended). ③ added 2 deterministic-guard categories to eval:adversarial: `TOPOLOGY_BYPASS` (a sudo-wrapper · command substitution · a `;`-separator + 2 controls) · `OBFUSCATION` (`$IFS` word-splitting · an NFKC full-width homoglyph + 2 controls). Each case scores via pure `classifyDangerousCommand(command).dangerous === expectBlocked` (solveDangerousCommandCase/scoreDangerousCommandCase), Ollama-independent · no-skip. ★the worker left the guard in a **broken, unrestored state** after using collapseIfs neutralization for a mutation-RED demo and exiting without restoring it → discovered the corruption, restored it precisely with Edit (byte-identical, no cp backup used), then re-ran a proper mutation-RED (neutralize → the rm${IFS} case goes RED → restore → green).
- Why: closing out the W5 wave — expanding the battery coverage for the self-principle (a deterministic guard confirmed to block something in code, never relying on model refusal). Topology/obfuscation bypasses are exactly the class that leaks past 8B model refusal (e.g. in Korean), so the deterministic guard is the real defense line, and this locks that defense line in via regression tests.
- Review point: Opus's independent evaluator PASSed — behavioral verification (the dangerous boolean is really scored, not tautological) · the controls disprove over-blocking (rm inside quotes/comments isn't blocked) · deterministic routing (not model-refusal) · no-skip (always runs) · the guard source unmodified (byte-identical) · zero comment-policy violations. Independently reconfirmed mutation-RED (neutralizing the distribution → RED, restored).
- Risk: low. Test-only (the guard code itself unchanged, only coverage expanded). self-eval's adversarialCases proxy only counts `prompt:`-keyed cases, so the new `command:`-keyed cases don't increment it (the same design as the existing SANDBOX/SECRET cases — not a regression). Next = D3-S6 (eval:orchestration ratchet 📈) or D7-S3 (smart-tail + a real browser). D-E1b/c (the shared push/commit hook) stays deferred.
- lesson: ★a sub-agent worker can abnormally exit **without restoring** a production guard it modified for a mutation-RED demo — if a worker's report looks abnormal ("standing by"), always run `git diff <production file>` to check for lingering mutations before restoring (especially for security guards). Adding `command:`-keyed deterministic cases to a battery shouldn't move self-eval's `adversarialCases` (a `prompt:`-key counter) — a non-moving proxy is normal (a design consistency), not a mistaken regression.

## fire 56 · 2026-07-12 · skill v2.x · 89360c2d8
meta: slice=D3-S6 · wave=W5 · pkg=scripts(verify-orchestration) · kind=orchestration-battery-ratchet · verdict=PASS · firesSinceDrill=5
ratchet: roadmap remaining [ ] = 6/65 (D3-S6 checked) · self-eval pass · fabrication 0 · eval:orchestration folded in 2 MAST modes + capacity under pass^3 (the live model fan-in kept)
- What: ① baseline green. ② the next actionable roadmap item = D3-S6 (D-E1b/c the shared push/commit hook stays deferred, D6-S2 skipped as attended). ③ verify-first: read eval:orchestration (verify-orchestration.mjs) + the orchestrator source and confirmed live-verifiable seams (`workerTimeoutMs`→failed · `maxWorkers`→a slice cutoff · `result.results[]`). Folded 2 MAST modes (step-repetition = each worker fires once · unaware-of-termination = a hung worker's workerTimeoutMs explicitly terminates it and stays bounded) + D3-S4's capacity rejection (maxWorkers=2<3→runs 2 · the excess is absent) into pass^3 (MUSE_EVAL_REPEAT default 3, a single failure → exit 1) using deterministic rule-based workers. The existing live model fan-in case is kept; when Ollama is down the deterministic cases still run and gate. Zero product-code changes.
- Why: the roadmap says "ratchet eval:orchestration (2+ MAST modes · D3-S1/S2/S4 · pass^3)." Multi-agent systems fail through coordination (MAST) — step-repetition and unawareness of termination are its top modes. Deterministic cases make pass^3 stable (a single green run of a timer race isn't trustworthy). Scores the existing orchestrator's behavior (not a new guard).
- Review point: Opus's independent review PASSed — 3/3 non-vacuous mutation-RED (flipping each assertion's expectation → RED, restored via md5 byte comparison) · the assertions match real orchestrator behavior (the withDeadline rejection string · the slice cutoff · step status=completed|failed) · pass^3 really gates (a single iteration failure → exit 1) · skip semantics (the deterministic cases still gate even when Ollama is down) · a clean comment policy (removed a D3-S4 slice-marker).
- Risk: low. Test-only (the orchestrator src unchanged). Finding: step-level status is only completed|failed (timed-out only exists in the opt-in SubAgentRunRegistry, the assertion uses failed). Next = D7-S3 (smart-tail terminal + a real browser) or D-KO-S3 (i18n catalog, low priority). D-E1b/c (the shared hook) stays deferred.
- lesson: an MAST battery ratchet for multi-agent systems can drive the orchestrator's seams (workerTimeoutMs · maxWorkers) with deterministic rule-based workers, giving stable pass^3 verification with no LLM — scoring the real withDeadline/selectWorkers paths without mocks. Assertion strings (a deadline error, a status enum) must be confirmed from the source's real values before being pinned (never guessed). Never put a slice ID (D3-S4) in a comment (a policy violation, caught by a pre-commit scan).

## fire 57 · 2026-07-12 · skill v2.x · a336479e5
meta: slice=D7-S3 · wave=W5 · pkg=apps/web · kind=ux-smart-tail-scroll · verdict=PASS · firesSinceDrill=6
ratchet: roadmap remaining [ ] = 5/65 (D7-S3 checked) · self-eval pass · fabrication 0 · apps/web +1 unit file (chat-autoscroll, 7 tests) · real-browser measurement PASS
- What: ① baseline green. ② the next actionable roadmap item = D7-S3 (D-E1b/c the shared hook deferred, D6-S2/D7-S4 skipped as attended). ③ verify-first: Chat.tsx lines 103-105 unconditionally scroll to the bottom on every turns/activeTool change = a yank on every token while reading scrolled up. Extracted pure shouldStickToBottom (within 80 of the bottom) + a unit + wired into Chat.tsx (stickToBottomRef defaults true = jumps on mount · onScroll updates it from real geometry · the effect only runs while sticking). ④ a real-browser measurement (chrome-devtools, dist preview:4747, 40 tall messages injected): a real overflow · sticks true at the bottom · scrolled up 300px sticks false · bounded · zero blowout.
- Why: the roadmap's "smart-tail (web console) + a real-browser measurement," referencing hermes' terminal-output pattern. Getting yanked to the bottom on every token while reading scrolled up during streaming breaks UX. Tailing only near the bottom is the standard fix.
- Review point: Opus's independent review PASSed — the formula is correct (distance≤threshold, `<=` boundary and overscroll) · wiring is accurate (the mount jump preserved · an early return blocks the yank · onScroll updates the same ref) · unrelated behavior unchanged (+18/-1) · 2 non-vacuous mutation-RED variants · the real measurement proves layout-invariant + deterministic inputs (numbers). Limitation: the turns-effect can't be triggered in a browser without a backend → geometry/decisions were measured, effect-gating was code-reviewed+unit-tested (an honest split).
- Risk: low. apps/web is UI-only (a Vite island, outside the TS reference graph). User-visible = a CHANGELOG entry. Finding: a single stick=false blip during the smooth-scroll animation self-heals (streaming deltas are under the threshold and unaffected, confirmed by Opus). Next = D-KO-S3 (i18n catalog, low priority · refactor risk may exceed payoff) — the remaining actionable queue, excluding D-E1b/c (deferred) and attended items, is only D-KO-S3.
- lesson: a web UI scroll slice's correct method is extracting the decision logic into a pure function → a unit (mutation-RED) + a real-browser measurement of the actual geometry (scrollHeight/scrollTop/clientHeight). A React state (turns) effect can't be triggered in a browser without a backend, so measure the geometry/decisions the effect consumes and split effect-gating into code review + a unit (honest — don't fake an e2e). testing.md's UI rule = measure numbers.

## fire 58 · 2026-07-12 · skill v2.x · 9303c0397
meta: slice=encryption-key-migration-backup · wave=W5 · pkg=@muse/calendar · kind=data-safety-backup · verdict=PASS · firesSinceDrill=7
ratchet: roadmap remaining [ ] = 5/65 (the encryption backup checked) · self-eval pass · fabrication 0 · calendar +4 backup tests (184/184) · ⚠consecutive-all-PASS=7 (the next fire=8 → triggers a JUDGE-DRILL)
- What: ① baseline green. ② remaining actionable items: passed over D-E1b/c (a shared push/commit hook = ~15 concurrent loops' blast radius, deferred) · D6-S2/D7-S4 (attended) · D-KO-S3 (an M refactor, roadmap-deprioritized, risk>payoff) and picked **encryption backup** (small · high value for data safety · autonomously doable) as the best marginal-value option. ③ verify-first sibling audit: shared's `encryptFileAtRest` and memory's `encryptAtRest` already back up; only calendar's writeAll self-initiates a plaintext→encrypted transition via the MUSE_CALENDAR_ENCRYPT flag without a backup = the sole gap. Fix: on writeAll's first transition, snapshot the existing on-disk plaintext to `.plaintext-backup-<ts>` (0o600), before the encrypted write.
- Why: §8.6.1's "for key-loss recovery." Turning on calendar encryption means the first write overwrites plaintext with ciphertext → losing/rotating the key permanently loses the schedule. The backup is the only recovery path. memory already has one — a parity gap for calendar.
- Review point: Opus's independent review PASSed — the backup happens before the encrypted write (no crash window) · only on the transition (shouldEncrypt&&!alreadyEncrypted, not every write/already-encrypted) · a brand-new file gets no backup (nothing to lose) · 0o600 · recovery demonstrated (the backup is readable plaintext without the key, events included) · format-preserving/wrong-key fail-closed unchanged · an honest sibling audit (MUSE_*ENCRYPT self-init flags exist only on calendar). 3/3 mutation-RED (a no-op helper → the backup test goes RED).
- Risk: low. The backup writes plaintext to disk but at 0o600 + adjacent + the roadmap-specified recovery mechanism (the same pattern as memory/shared), for the user to confirm and delete. Opus's minor note: calendar's writeAll has no cross-process lock (an existing property; a concurrent double-first-write could create 2 backups, but that's not data loss and this slice doesn't make it worse) → a backlog candidate. **The remaining actionable queue is now exhausted**: only D-E1b/c (shared hook) · D6-S2/D7-S4 (attended) · D-KO-S3 (low-value M) remain — the batch's natural closing point.
- lesson: an encrypted-store sibling audit should classify by "does the write path self-initiate encryption (a flag) vs. format-preserving (only encrypts if already encrypted)" — self-initiating stores need write-path backups, format-preserving ones already go through a migration that backs up and are already safe. Grepping for a `MUSE_*ENCRYPT` flag deterministically enumerates the self-initiating stores.

## fire 59 · 2026-07-12 · skill v2.x · AUDIT-PASS (audit requested by Jinan)
meta: kind=audit-and-fix · verdict=PASS · slices-audited=D2-S7,D3-S6,D7-S3,calendar-backup · bugs-fixed=4 · followups-logged=3
ratchet: roadmap remaining [ ] = 5/65 (unchanged, not a new slice) · self-eval pass · fabrication 0 · 4 independent Opus second-pass audits (hunting for missed defects)
- What: Jinan's request: "review what you built and fix what's wrong." Ran a system-health gate + **one independent Opus second-pass audit** (hunting for what the first gate missed) on each of this session's 4 slices. Found 4 real defects + logged 3 follow-ups. Fixes: ①🚨dangerous-command didn't block `rm -rf --no-preserve-root /` — a root-wipe (GNU rm rejects only the naive form → this is the real destructive form) — unified FLAGS (short|long) tokens+doas+chmod/chown; ②🚨the web smart-tail latched OFF during a fast stream (mid-smooth-scroll events)→now auto; ③MUSE_EVAL_REPEAT=0 disabled the gate entirely (0 runs)→a floor added; ④replaced a weak TOPOLOGY separator case in eval + added an npr regression case.
- Why: a grounding/security floor is only meaningful if it's verified. All 4 slices passed their first Opus gate, but a second adversarial audit found real defects — dangerous-command especially was "blocks only the harmless form, lets the real destructive form through," a false-confidence class of bug (the worst kind of security defect). The smart-tail broke on exactly the fast-stream case that was its core value proposition.
- Review point: verified each fix — the guard: 40 tests · mutation-RED (reverting FLAGS → 3 cases go RED) · zero false positives (22 adversarial) · ReDoS polynomial sub-second · an independent Opus PASS. Smart-tail: a real-browser measurement, auto=1 event at distance 0 (vs. a smooth mid-event) · Chat 15/15. REPEAT: confirmed MUSE_EVAL_REPEAT=0→pass^3 still runs.
- Risk: low. The guard loosens slightly but zero false positives (22 adversarial) + ReDoS-bounded. Calendar concurrency (C/D backup race · IO-fail-open · lost-update) is correctly deferred to "fix it properly with a lock" (avoiding an untestable bolt-on). dangerous-command's ReDoS tightening · quote-splitting · multi-agent dedup are also deferred (backlog).
- lesson: a first-gate PASS ≠ no defects. **An independent second-pass adversarial audit (hunting for what was missed) finds real defects the first gate didn't see** — a security guard especially needs verifying not just "what does it block" but "does it block the real threat form" (naive-form-only blocking, like missing --no-preserve-root, is exactly the false-confidence failure). Narrow race/IO defenses are more honestly deferred to a testable root fix (a lock) than an untestable bolt-on. With one Opus audit per slice, 4 slices = 4 parallel audits — a good defect-discovery rate for the token cost.
