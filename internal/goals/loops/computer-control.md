# computer-control — Muse computer-control axis strengthening loop journal

> Theme: get the local 12B (gemma4) to complete multi-step computer tasks (@muse/fs file_read/grep/edit/multi_edit + run_command) end-to-end reliably + every actuator step passes the grounding gate (read-before-edit · fabrication=0). Raise the `eval:computer-task` (report-only) measurement baseline.
> Worktree `/tmp/muse-computer-control` · branch `loop/computer-control` (Tier2 — push every fire, rebase onto main periodically, **ff-merge to main every 3 fires**). Rethemed from core-hardening (Jinan-picked 2026-06-20).
> Cron `47491301` (every 20m, session-only; re-registered 2026-06-21 from ready/2-computer-control.md — the prior `18d30a58` expired with its session). Stop: `CronDelete 47491301`. Convention: [README](README.md).
> NOTE: fires 1-2's docs were reset from origin/main instead of rebased due to a concurrent-loop INDEX-conflict cascade, then consolidated and re-recorded in fire 3 (history preserved; fire 1-2's hashes ee635ab0/8ea83aab are orphaned but kept for the record).

## fire 59 · 2026-06-21 · skill v2.0 · ad6ddc17 (@muse/fs file_list: fixed a truncated=true false positive — exactly `limit` matches is a complete listing, but the code chased a phantom next page)
meta: value-class=correctness(list-signal-accuracy) · pkg=@muse/fs · kind=fs/list-truncation-accuracy · verdict=PASS · firesSinceDrill=3
ratchet: testFiles +2 cases(exactly-limit→truncated false, >limit→true+count=limit) · fabrication 0 · @muse/fs 182 vitest(~1s) · lint 0/0 · ★box load~35 → narrow per-pkg vitest still works (only full pnpm check times out)
- What: file_list breaks its glob loop at `matches.length >= limit` and sets `truncated = >= limit` → when a directory has exactly `limit` matches, that's a false-positive truncated=true → the model chases a next page that doesn't exist. Fix: collect one extra entry up to limit+1 (a sentinel) to distinguish "exactly limit" (complete) from ">limit" (truncated) → `truncated = matches.length > limit`, sorting and slicing to limit only when actually truncated.
- Why: an accurate list signal is core to the LIST leg's reliability in the grep/edit/LIST chain (a false truncated → an unnecessary re-query / distrust of a complete listing). ★with box load~35, LLM eval and full `pnpm check` time out, but a narrow @muse/fs vitest still runs in ~1s (180→182) → verifiable. Diversity: @muse/fs breaks fires 57/58's crates/runner streak (a different pkg+kind).
- Review notes: traced all 5 cases (0/<L/==L/==L+1/>>L, all correct), count==paths.length invariant (read after slicing), over-scan is bounded to +1, limit≥1 (asPositiveInt+schema min1). Mutation (reverting truncated to `>=`) turns the exactly-limit test RED. Independent Opus ④b judge VERDICT PASS (traced every case · no regression · OUTCOME-graded · diversity · fabrication0).
- Risk: the >limit case's returned SET is still a glob-bounded prefix (a pre-existing limitation, honestly documented). The result-object SHAPE is unchanged → zero effect on fs consumers. fire 59 isn't a ×3 fire → no main merge. ⑤c[55-58] is still deferred under load~35 (retry at fire 60).
lesson: even under extreme box saturation, a narrow per-package vitest suite (<2s) still passes — only full `pnpm check` hits the 5000ms wall. So a small-package slice (like @muse/fs) can still be verified and progressed even at load~35, a way out of cargo-runner exclusivity.

## fire 58 · 2026-06-21 · skill v2.0 · 88021934 (crates/runner: an in-band truncation marker on run_command output — the model SEES the output was cut, not just a bool flag)
meta: value-class=new-capability(reliability-signal) · pkg=crates/runner · kind=runner/truncation-marker · verdict=PASS · firesSinceDrill=2
ratchet: cargo tests +1(marks_only) +updated caps_output(sibling) · fabrication 0 · crates/runner 15 cargo tests · lint 0/0 · ★box load~40 → cargo-only verification (LLM eval · pnpm check all timing out)
- What: when the runner caps stdout/stderr at max_output_bytes, it previously only signaled `truncated: bool` → the local 12B would miss it and read the cut log as complete ("tests passed" concluded from a partial run). Added a self-labelled in-band marker (`[muse: output truncated…]`) to the truncated stream (per-stream, using the already-computed stdout_truncated/stderr_truncated) → the model sees the partiality directly in the text. The bool field stays for programmatic consumers.
- Why: run_command's output signal is core to a multi-step chain's verify step's reliability. ★with box load~40 (10+ concurrent loops), LLM eval · pnpm check · full vitest all time out → only a cargo-verifiable Rust slice was possible this fire. Fire-57 is also a runner slice but a distinct kind (truncation-SIGNAL vs. utf8-ENCODING). Unrelated to reflection-guard (not a retry).
- Review notes: cargo 15 pass, an OUTCOME integration test (an actual bash 200KB→1024 cap→the marker appears), mutation (making the marking a no-op turns 2 marker tests RED). A no-regression anchor = large_output (not truncated→no marker, exactly 200000). The independent Opus ④b judge traced the TS consumer (capTruncated isn't broken, TS uses a fake invoke) · per-stream wiring · contract-pollution (self-labelled accepted) · cap-overage (the marker's ~110B metadata is accepted) · diversity → VERDICT PASS.
- Risk: the marker slightly changes stdout's "raw output" contract (mitigated by self-labelling, the bool stays). If the model sets a small cap the marker could itself get truncated, but capTruncated stays true (harmless). fire 58 isn't a ×3 fire → no main merge. ⑤c[55/56/57] is still deferred under load~40 (retry at fire 60).
lesson: even under extreme box saturation (load 40), a cargo-verifiable Rust slice can still proceed (no vitest 5000ms wall) — a way to keep a fire from spinning even when the environment blocks LLM/TS verification. But an output-contract change requires updating a sibling test (an exact-length pin) in the same fire.

## fire 57 · 2026-06-21 · skill v2.0 · 589a2272 (crates/runner: trim a split multibyte char on truncated run_command output — clean UTF-8, no U+FFFD)
meta: value-class=correctness(output-integrity) · pkg=crates/runner · kind=runner/utf8-truncation · verdict=PASS · firesSinceDrill=1
ratchet: cargo tests +2(split-char trim, complete-tail intact) · fabrication 0 · crates/runner 14 cargo tests · lint 0/0 · ★eval e2e unmeasured due to box saturation (reverify-fix timed out again)
- What: the run_command runner's drainer caps output at the BYTE level → cutting mid-multibyte-UTF-8-character makes from_utf8_lossy substitute U+FFFD (�) → the model reads the verify log as corrupted. `trim_partial_utf8_tail` strips the trailing partial character (only when truncated) → truncated output is now clean, valid UTF-8.
- Why: run_command output integrity is the verify step of a multi-step chain (the model decides its next action from this output). Honestly: box saturation timed out the LLM eval (eval:reverify-fix) again → chose a deterministic cargo-verifiable slice instead (no Ollama needed). Diversity: crates/runner (Rust) is a completely different (pkg,kind) from the scripts (53/54/55) · agent-core (56) wells.
- Review notes: cargo 14 pass, confirmed mutation (making the trim a no-op turns the split-char test RED). The independent Opus ④b judge's VERDICT PASS (termination · non-inert · no regression · value · diversity · fabrication0). Applied the judge's non-blocking notes (interior-binary over-trimming + O(n²)) immediately — switched a while-loop to a bounded-3-iteration loop (a partial character is ≤3 bytes, interior invalid bytes aren't over-trimmed, O(n)).
- Risk: normal (non-truncated) output is unaffected (gated on the truncated flag). Interior-binary is bounded to ≤3 pops then lossy (existing behavior). Modest scope (only output truncated exactly at a multibyte character boundary) but a real correctness fix. fire 57 is a ×3 fire → delivers 55/56/57 to main.
lesson: when box saturation blocks LLM eval, switch to a deterministic cargo/unit slice (a fresh pkg) rather than spin a fire. A judge's non-blocking note gets folded into the same fire if it's cheap and correct (calibration).

## fire 56 · 2026-06-21 · skill v2.0 · fc77a088 (★JUDGE-DRILL PASS + a real fix: a re-verification nudge wired into the model loop; firesSinceDrill reset)
meta: value-class=new-capability(reliability-mechanism)+judge-drill · pkg=@muse/agent-core · kind=model-loop/reverify-nudge · verdict=PASS · firesSinceDrill=0
ratchet: testFiles +2(reverify-nudge unit + model-loop-reverify behavioral) · fabrication 0 · @muse/agent-core 2589 · apps/cli 2890(isolated) · lint 0/0 · mutation RED-confirmed · ④b judge PASS · Ollama UP(but the eval was saturated)
- ★JUDGE-DRILL (mandatory at firesSinceDrill≥10): first planted an INERT slice (a reverify-nudge.ts helper + 12 unit tests, GREEN, but not wired into the model loop = declaration-only) → the independent Opus judge correctly FAILed it ("not wired, the eval still FAILs, declaration-only," citing grep evidence + the missing wiring) → confirming the judge isn't a rubber-stamp → proceeded to the real fix. Drill succeeded.
- What (real fix): fixed the gap fire 55 found (the 12B re-reads its edit to "confirm" it but doesn't re-execute) — a `ReverifyNudgeTracker` (write→pendingEdit, execute→clear; consumeNudge=one-shot) wired into the termination guard of **both** executeModelLoop and executeStreamingModelLoop: if the model tries to end after an unverified edit (tools are live · a run-intent exists · an execute tool is present), inject a one-shot REVERIFY_NUDGE user message + continue (prompting a re-run). Sibling-audit: both loops.
- Why: fire 55's measured finding justified this. Diversity: @muse/agent-core/model-loop continues the scripts/eval (53/54/55) streak. Reflection-guard compliant (a retry gated by a deterministic "did it actually re-run" check, one-shot).
- Review notes: a **deterministic behavioral test** (driving executeModelLoop with a scripted provider — edit→finish injects the nudge and re-prompts, edit→run→finish gets no nudge, edit→finish→finish nudges only once and then terminates, no-run-intent gets no nudge). Mutation: disabling consumeNudge turns 2 nudge-fires tests RED, the 2 negative cases stay GREEN. The independent Opus ④b judge exhaustively verified the wiring · sibling loops · termination · over-firing · guards · message-pairing · honesty-caveats (re-ran the mutation itself) → VERDICT PASS.
- Risk/honesty: ★eval:reverify-fix's e2e (12B FAIL→PASS) **is unconfirmed this fire** — box saturation (concurrent loops) made one eval case time out at >560s. So the deterministic wiring (the loop injects a nudge and re-prompts) is proven, but probabilistic 12B-compliance (does it then re-run and actually fix the bug) is unverified→backlogged (to confirm on a clean box). Over-firing is bounded to one turn/one-shot. The pnpm check's one FAIL was apps/cli's /forget-render saturation-flake (isolated 2890 GREEN, unrelated to my change). fire 56 isn't a ×3 fire → no main merge.
lesson: an unattended retry/nudge slice can still prove its *mechanism* (the loop re-prompts) via a scripted-provider deterministic behavioral test even when box saturation blocks e2e LLM eval; probabilistic LLM-compliance is honestly separated out and backlogged. A drill's most realistic bad-slice is inert-but-green (a helper + unit tests only, not wired) — the judge catches it by grep.

## fire 55 · 2026-06-21 · skill v2.0 · ee783e75 (eval:reverify-fix — a RE-VERIFICATION probe surfaces a REAL blocker: the 12B re-reads its edit but doesn't re-run)
meta: value-class=new-capability(harder-eval/flywheel)+finding · pkg=scripts/eval · kind=eval-fixture/re-verification · verdict=PASS(the probe is valid; the 12B FAILs it=a finding) · firesSinceDrill=9
ratchet: testFiles unchanged · fabrication 0 · eval:reverify-fix is valid+discriminating (neither/only-reported→FAIL, both→PASS, deterministically verified) · 12B FAIL 1/1 (a genuine gap) · lint 0/0 · Ollama UP
- What: the test exits on the FIRST failure (sequential assertions), so a second bug (beta) stays hidden until the first (alpha) is fixed AND the test is re-run. If only the reported bug gets fixed with no re-run before declaring done, the second bug is never seen → FAIL. Different dimension from two-edit-fix (both bugs exposed upfront) — this is RE-VERIFICATION (not trusting the first error as the whole story, confirming by re-running).
- ★Finding (flywheel payoff): 12B FAIL 1/1 — trace: run_command(alpha fails)→read alpha→edit alpha→**re-read alpha to "confirm"**→stops. run_command-calls=1 — **never re-runs the test** → never sees the hidden beta. I.e. the 12B has a gap where it confirms an edit by re-READING rather than re-EXECUTING (a false-completion/missing-re-verification gap). No existing eval (all upfront/single-fix) caught this genuine multi-step reliability gap.
- Why: the backlog flywheel — after the theme matured (3 evals PASSing), probing a harder dimension surfaced the next product blocker (as designed). A standalone red-probe (not wired into any gate → no CI impact; a future fix flipping it to GREEN is a progress signal, per agent-testing.md's "turn a miss into a case").
- Review notes: deterministic discrimination (only-reported-fixed→FAIL catches the stop-after-first pattern). The independent Opus ④b judge verified the distinct-dimension claim · discrimination · OUTCOME-grading · the finding's realness (run_command-calls=1=never re-ran) · the red-probe's justification → VERDICT PASS. Judge non-blocking note: pick-evals.mjs:41's greedy regex mis-routes the "reverify" path into the grounding battery (pre-existing) → backlogged.
- Risk: 3 scripts/eval fires in a row (53 agg, 54 fixture, 55 fixture); (scripts,eval-fixture)×2 but justified by a distinct dimension + value-first (surfacing a real blocker). The eval RED (the 12B's gap) is the intended probe result. fire 55 isn't a ×3 fire → no main merge. ★fire 56=firesSinceDrill≥10 JUDGE-DRILL (mandatory).
lesson: after a theme matures, the flywheel (a harder eval) reactivates a product blocker — reverify-fix surfaced "the 12B confirms an edit by re-reading, not re-running." Next product slice = a re-verification nudge (for an action-required task with zero re-runs after an edit, nudge one re-run, reflection-guard-compliant; justified in agent-core).

## fire 54 · 2026-06-21 · skill v2.0 · 5ea43240 (eval:two-edit-fix — a multi-step COMPLETENESS battery (2 edits/2 files); the 12B PASSes, the bar is raised)
meta: value-class=new-capability(harder-eval/flywheel) · pkg=scripts/eval · kind=eval-fixture/completeness · verdict=PASS · firesSinceDrill=8
ratchet: testFiles unchanged · fabrication 0 · eval:two-edit-fix PASS 1/1 (live) · discrimination verified(one-edit→FAIL/both→PASS) · lint 0/0 · main ff-merge(fire 54=×3, delivers 53/54) · Ollama UP
- What: all 3 existing evals were single-edit, so they all PASSed. Added a harder dimension (completeness) — a fixture requiring BOTH of 2 bugs across 2 files to be fixed before the test passes (fixing only one → FAIL). Directly targets the early-stop/step-repetition failure class (fires 48-51). Mirrors the eval-multifile-fix harness (a temp-dir · 4 tools · SHIPPED persistence · runTest re-run · OUTCOME-grade ok=testPasses&&noiseIntact).
- Why: backlog direction (2), the flywheel — raise the eval bar. Diversity: scripts/eval (same pkg as fire 53 but a different kind = fixture vs. aggregator; continuing to avoid over-mining the agent-core well).
- Review notes: live PASS 1/1 (trace: run_command fails both→read alpha/beta→edit BOTH (1→2, 10→20)→re-read; alpha-edited · beta-edited · noise-intact all true). **Non-tautological deterministic verification**: fixing only alpha or only beta exits 1 (FAIL), fixing both exits 0 (PASS) — genuinely catches a stop-after-one-edit model. The independent Opus ④b judge verified discrimination · OUTCOME-grading · no false-pass · temp-cleanup · flywheel value → VERDICT PASS.
- Risk: the 12B PASSing is a good result (confirms cross-file 2-edit completeness is solid, no fix needed). pass^1, so not yet added to the eval:multistep aggregator (avoiding a flake in a just-shipped gate; a promotion candidate once pass^k is confirmed). A pure measurement-infrastructure change (no product/state changed). fire 54 is a ×3 fire → delivers 53/54 to main.
lesson: after a theme matures (3 evals PASSing), the flywheel = raise the bar with a harder-dimension eval. The 12B already has cross-file 2-edit completeness (the cumulative effect of the early-stop fixes, fires 47/49/51). Next harder dimension: re-iterating on a wrong first fix (a naive first fix is insufficient → re-read+re-fix), or repointing the theme. ★fire 56 = the firesSinceDrill≥10 judge-drill, scheduled.

## fire 53 · 2026-06-21 · skill v2.0 · e483c624 (eval:multistep aggregator — lock the 3-eval FAIL→PASS gains as one regression gate; a scripts/eval pivot)
meta: value-class=new-capability(regression-lock) · pkg=scripts/eval · kind=eval-aggregator-gate · verdict=PASS · firesSinceDrill=7
ratchet: testFiles unchanged · fabrication 0 · eval:multistep ALL PASS 3/3 (live) · skip→exit0 · fail→exit1 verified · lint 0/0 · Ollama UP
- What: fires 40-51 flipped 3 multi-step evals (computer-task/multifile-fix/edit-run-verify) from FAIL→PASS (confirmed at fire 52). Added an `eval:multistep` aggregator to lock this gain against silent regression — exactly the eval:self-improving pattern (skip→exit0 when Ollama unreachable, spawnSync 3 batteries, status!==0→FAIL, any-fail→exit1, MUSE_EVAL_REPEAT passthrough for pass^k). A package.json entry (a build union = fs+tools+autoconfigure).
- Why: per agent-testing.md, "bundle batteries into one gate; it rots without a CI gate" + pass^k. Diversity RATCHET: over-mining the agent-core well (2 judge notes) → deliberately pivoted to scripts/eval (a fresh pkg,kind). Locking the gain is the natural next step right after both hard evals PASSed.
- Review notes: verified the live run's ALL PASS 3/3 (exit 0), the skip-path (a bad URL)→exit0, the fail-path (a non-zero sub)→exit1 logic. The independent Opus ④b judge verified the aggregation/exit contract · build-prefix union · skip-vs-fail (across all 3 sub-evals) · REPEAT passthrough · doctrine value · non-inertness → VERDICT PASS.
- Risk: without Ollama/the runner, sub-evals SKIP (exit0), so the gate can be green even while empty (skip≠pass; the mechanism-level unit tests are the true CI lock, this is a live regression layer). A pure test-runner infrastructure change (no product/state changed). fire 53 isn't a ×3 fire → no main merge.

## fire 52 · 2026-06-21 · skill v2.0 · e9a8a3a0 (★MILESTONE confirmed: BOTH hard multi-step evals now PASS — the theme goal is achieved; consolidation + next direction)
meta: value-class=measure(milestone-validation) · pkg=docs(validation) · kind=outcome-validation · verdict=N/A · firesSinceDrill=6
ratchet: testFiles unchanged · fabrication 0 · self-eval green · ★eval:multifile-fix PASS · ★eval:edit-run-verify PASS(fire 52, model-ran-test=true) · eval:computer-task PASS · Ollama UP
- Confirmed: edit-run-verify re-measured PASS (test-passes=true, model-ran-test=true, tools=[run_command,file_read,file_grep,file_edit]) — **fire 51's exposure reserve is confirmed by an OUTCOME measurement** (run_command is now exposed+used; trace: run_command→TEST FAIL→read→edit→verify). Fire 51's unconfirmed OUTCOME is now confirmed.
- ★MILESTONE: all 3 evals now PASS — computer-task (single-file), multifile-fix (multi-file, fire 47's not-exposed fix + fire 49's repetition fix), edit-run-verify (edit→run→verify, fire 51's exposure). The theme's headline goal (raising the local 12B's multi-step reliability) is now confirmed by measurement. The deterministic mechanisms are CI-gated by unit tests (fires 40-51); the eval PASSes are the integration confirmation.
- Why no code slice: exhausted the deterministic veins (fs/tools/agent-core tool-call boundary+exposure) across 12 fires (40-51), and both hard evals now PASS. 5 fires in a row on agent-core (47-51), so the diversity RATCHET forbids another agent-core micro-fix (2 judge notes). EXHAUSTION+⑥: rather than force it, honestly recorded the confirmation + next direction.
- Review notes: the measurement OUTCOME-confirms the cumulative effect of fires 47/49/50/51 (not an estimate). A pass^1 observation, but the causal trace is clear (run_command exposed→used). Risk: a stochastic single observation, so pass^k isn't yet established (possible at the next fire).
lesson: the apex of a measurement-driven loop — measurement drove (a) diagnosis (correcting early-stop→step-repetition→exposure-starvation) (b) fix confirmation (FAIL→PASS) (c) next direction, all three. When a theme matures, the honest exit is confirmation + repoint-candidates, not a forced slice. Next directions (backlog ★): (1) CI-gate both evals at pass^k (2) a harder multi-step fixture (2-edit/cascading/re-iterating on a wrong first fix) (3) repoint the theme (deterministic veins exhausted).

## fire 51 · 2026-06-21 · skill v2.0 · 232f04e9 (a run-intent execute-tool exposure reserve — run_command no longer starved by the file cluster; fire-4's sibling)
meta: value-class=new-capability · pkg=@muse/agent-core · kind=tool-filter/exposure-reserve · verdict=PASS · firesSinceDrill=5
ratchet: testFiles +0 / +2 cases (run-intent reserve positive+no-over-exposure) · fabrication 0 · @muse/agent-core 2573 · tool-filter 37 · pnpm check exit 0 · lint 0/0 · Ollama UP · ★main ff-merge(fire 51=×3, delivers 49/50/51)
- Measured: re-measured edit-run-verify — FAILs but **test-passes=true** (the bug is fixed: sum a-b→a+b, file_read→read→edit). model-ran-test=false: the model only tried node_run/muse.skills.run, never selected run_command. Reading the code confirmed: in capToolsByRelevance, when mandatory≥cap, optionals only survive via relevantReserve (top 3), and the FILE_PATH boost lets the file cluster monopolize all 3 reserve slots → **run_command (system/execute, rank 4) is DROPPED** → the model has no runner to pick for verification. The execute-tool sibling of the file-cluster reserve fire 4 created.
- What: added RUN_INTENT_RE (run/test/build/execute/verify/lint, EN+KO). On a run-intent prompt, **reserve one additional top-relevant execute-risk optional tool (run_command)** on top of relevantReserve. A "fix the file and run the test" task now keeps both the file cluster and the runner.
- Why: a measured starvation (confirmed by code), on-theme (the verify step), deterministic, a different (pkg,kind) from the fires 47-50 not-exposed gate (a distinct mechanism, per fire-50's judge note). Guards: execute-risk+score>0+dedup, max +1, inert without a run-intent.
- Review notes: mutation-first (disabling runReserve turns exactly the positive test RED, no-over-exposure stays GREEN). The independent Opus ④b judge verified the Set-union is non-inert · no regression (the union is additive only) · precision guards · pre-existing band effects · determinism · a different surface → VERDICT PASS.
- Risk: the OUTCOME flip is unconfirmed (run_command is now exposed, but the 12B's selection is still stochastic — exposure starvation is removed by code, selection is model behavior). Honest. RUN_INTENT_RE is broad but harmless thanks to the triple guard (cap-exceeded+execute+score>0). ⑤c delivers 49/50/51 to main.
lesson: edit-run-verify's real FAIL cause wasn't model behavior but deterministic EXPOSURE starvation (run_command was pushed out by the cap and never exposed) — measurement + code-reading avoided the misdiagnosis of "the model is lazy." The fire-4 reserve protected files only and missed its execute sibling (a sibling-audit lesson).

## fire 50 · 2026-06-21 · skill v2.0 · 7106cab3 (★eval:multifile-fix FAIL→PASS confirmed; + command-as-name → execute-tool recovery for edit-run-verify)
meta: value-class=new-capability+measure · pkg=@muse/agent-core · kind=not-exposed-recovery(command-name) · verdict=PASS · firesSinceDrill=4
ratchet: testFiles +0 / +2 cases (command-name positive+negative integration) · fabrication 0 · @muse/agent-core 2571 · agent-runtime 135 · pnpm check @muse/runtime-settings SIGABRT(passed isolated, unrelated) · lint 0/0 · Ollama UP · ★eval:multifile-fix PASS (2/2 observed this fire, STABLE FAIL until now)
- ★Measurement milestone: re-measured with Ollama up — **eval:multifile-fix PASSes** (2/2 observed: file_read→file_read→file_edit (a+b→a*b), test-passes=true). Many fires had it STABLE FAIL → fires 47 (a not-exposed suggestion) + 49 (a repetition nudge) actually raised multi-step reliability (live-confirmed).
- What: but eval:edit-run-verify still FAILs — a new concrete trace: the 12B emits **the entire command line as the tool name** (`node --exec "/var/.../sum.test.mjs"`). Fire-47's token-overlap missed this ({node,exec,...} vs {run,command} share no tokens) → a bare error → a dead end. FIX (completing fire-47's sibling): in the not-exposed gate, if the name is command-shaped (contains a space = not an identifier) and an active execute tool exists, hint "a tool name must be an identifier — a command goes as an argument to '<run_command>'."
- Why: a measured dead-end (edit-run-verify's FAIL cause), on-theme, deterministic, a STABLE-FAIL eval that could flip. Fire 47 caught node_run (shares the token "run") but missed command-as-name — a sibling gap.
- Review notes: mutation-first (setting commandShaped=false turns exactly the command-name test RED). The independent Opus ④b judge verified the wiring is non-inert · the trigger's precision (a space=non-identifier, gated on !suggestion) · fabrication0 · no regression → VERDICT PASS. Judge note: the agent-core well has been mined well; the next fire should hit a different (pkg,kind).
- Risk: edit-run-verify's OUTCOME flip is unconfirmed (stochastic; the fix closes a dead end, subsequent success is model behavior). Honest. 4 fires in a row on agent-core (47-50) — next fire, a different surface. fire 50 isn't a ×3 fire → no main merge.
lesson: measurement confirms incremental progress + surfaces the next gap — multifile-fix flipped FAIL→PASS via 47/49, edit-run-verify exposed a new sibling gap (command-as-name). Completing the sibling of the same mechanism (the not-exposed gate) is justified, but after 4 in a row, switch (pkg,kind).

## fire 49 · 2026-06-21 · skill v2.0 · 12b399cf (measure→a STEP-REPETITION discovery→a deterministic dup-call nudge; MAST arXiv:2503.13657)
meta: value-class=new-capability · pkg=@muse/agent-core · kind=step-repetition-guard · verdict=PASS · firesSinceDrill=3
ratchet: testFiles +0 / +1 case (execute-model-loop dup-nudge, mutation-verified) · fabrication 0 · @muse/agent-core 2569 · execute-model-loop 15 · pnpm check @muse/voice SIGABRT(isolated 124/124, unrelated to agent-core) · lint 0/0 · Ollama UP
- Re-measurement/redefinition: a MUSE_TASK_DEBUG trace revealed the real pattern — not "early-stop" (fire 48's guess) but **STEP-REPETITION**: the 12B does file_read(test)→file_read(math)→file_read(test)→file_read(math)→… repeating the same files and then stopping, zero edits (the eval's `tools=[file_read]` is a deduplicated name list that hid the repetition). The top-ranked multi-step failure mode in MAST (arXiv:2503.13657).
- What: model-loop dedup silently returns a cached result on an identical repeated call → the model gets the same content and re-reads endlessly. FIX: `withRepetitionNudge` — only on a duplicate call, appends one line to the MODEL-FACING tool message ("that's an identical repeat — take a next action (edit/execute)"), wired into both the blocking and streaming loops (a sibling-audit). The trace/metric (executed.result) is unchanged, a static string (fabrication 0).
- Why: deterministic · safe (not a re-run loop → not a reflection-guard surface) · on-theme (a measured real blocker) · paper-backed. Dedup's write-invalidation means a legitimate re-read after an edit isn't a duplicate → zero false fires. Instead of fire 48's deferred invasive re-prompt, measurement revealed a safer + more direct fix.
- Review notes: mutation-first (disabling the nudge turns exactly the dup-nudge test RED). The independent Opus ④b judge verified both loops' wiring · zero false fires on dedup · no trace pollution · fabrication0 · honesty → VERDICT PASS.
- Risk: the OUTCOME (eval flip) is unconfirmed — stochastic (one run repeats the nudge's target, another run makes distinct calls) · multiple failure modes · ~5min/run makes 1-2 observations inconclusive. The nudge's delivery is proven (unit+mutation), its efficacy is unconfirmed (honest). fire 49 isn't a ×3 fire → no main merge.
lesson: measurement depth matters — fire 48 guessed "early-stop" from `tools=[file_read]` (a deduplicated name list), but the full MUSE_TASK_DEBUG trace revealed STEP-REPETITION. Diagnose from the RAW trace, not a deduplicated summary. Measurement revealed a safer + more direct deterministic fix than an invasive (risky) re-prompt.

## fire 48 · 2026-06-21 · skill v2.0 · 07110284 (measure+DECOMPOSE: a dual-eval converges on EARLY-STOP as the #1 blocker; 30c decomposed, not forced; ⑤c delivers 46/47 to main)
meta: value-class=refactor(work-list)+measure · pkg=docs/backlog+agent-core(diagnosis) · kind=decompose-design-sensitive · verdict=N/A · firesSinceDrill=2
ratchet: testFiles unchanged · fabrication 0 · self-eval green · eval:computer-task PASS · eval:multifile-fix FAIL(early-stop) · eval:edit-run-verify FAIL(early-stop) · Ollama UP · ⑤c main-merge(fire 48=×3, delivers 46/47)
- Measured: a full measurement of both hard evals with Ollama UP — multifile-fix FAILs with `tools=[file_read]`, edit-run-verify FAILs with `tools=[file_read]`. **Both are the same EARLY-STOP** (reads, then stops without editing/running, the bug unfixed). The single-file computer-task PASSes. The deterministic tool-call vein (40-47) is exhausted; the remaining #1 blocker is confirmed to converge on model-behavior early-stop.
- Decision (why not shipped): the early-stop fix (fire-12's action-completion re-prompt, 30c) needs (a) expanding the detector (the existing isUnbackedActionClaim requires answerClaimsAction, so it doesn't catch "stopped without even claiming anything") (b) deduplicating with the chat-repl re-prompt (to avoid a double re-prompt) (c) pass^k verification — genuinely >1-fire work, touching the core run loop (highest blast radius) + false-fire risk. Fire-12 explicitly said "not auto-fodder," and reflection-guard requires a calibrated verifier for a retry surface. Forcing this with the fire budget partly spent on 3 main-merge fires would be imprudent → DECOMPOSE-ON-DEFER into precise sub-slices (backlog ★ 48a/48b/48c), not forced.
- What shipped: refined the backlog ★ into the dual-eval evidence + 3 sub-slices (48a a precise detector + adversarial FPs / 48b an integration-architecture decision / 48c wiring + pass^k). ⑤c delivers fires 46/47 to origin/main (previously only the loop branch had them). No code slice (following the precedent of fires 1/2/3/5's N/A measure/decompose).
- Review notes: with no deterministic-safe slice available (both evals fail on model-behavior), an N/A measure+decompose is the honest exit. The next careful fire (not a main-merge fire, with a pass^k budget) starts at 48a.
lesson: an Ollama-up measurement is decisive — a live run confirmed the real blocker (early-stop, both evals converging) rather than an "assumed deterministic gap." A risky core-loop retry shouldn't be forced into a ×3-main-merge fire — decompose it; reflection-guard's false-fire calibration comes first. EXHAUSTION's honest exit = decompose + deliver to main, never a forced low-value slice.

## fire 47 · 2026-06-21 · skill v2.0 · 46d24074 (measure-first: the AgentRuntime not-EXPOSED gate suggests the nearest active tool — closing the node_run→run_command dead end)
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/tools · kind=tool-recovery/not-exposed-suggestion · verdict=PASS · firesSinceDrill=1
ratchet: testFiles +0 / +6 cases (4 nearestToolName unit + 2 AgentRuntime integration) · fabrication 0 · @muse/tools 96 · @muse/agent-core agent-runtime 133 · pnpm check apps/api flake(isolated 888/888, env UNSET) · lint 0/0 · ★Ollama UP
- Measure-first (Ollama finally UP): eval:computer-task PASS 1/1 (file_grep→read→edit), but eval:multifile-fix FAILs — `tools=[file_read, node_run]`, the model hallucinated `node_run` (the real name is run_command). Fire-9's nearestToolName only lives in @muse/tools EXECUTOR's not-registered path, but the hallucinated name hits AgentRuntime.executeToolCall's "not exposed" gate first (the activeTools check runs first) → **a bare error with no suggestion → a dead end**.
- FIX (a DRY sibling): generalized nearestToolName to accept NAMES(string[]) and exported it (the executor call site now does .map(name)), wired it into AgentRuntime's not-exposed gate (agent-runtime.ts ~808) — `Did you mean '<nearest active>'? Call that exact name.`. The not-EXPOSED counterpart to fire-9's not-registered fix.
- Why: a real dead end the measurement pointed at. Diversity: @muse/agent-core(+tools) is a fresh (pkg,kind). Sibling-audit: both paths (registered-but-failed / not-exposed) now share one suggester.
- Review notes: mutation-first at the integration point (removing the gate's suggestion turns exactly the positive integration test RED; both the node_run→run_command unit test and the file_open→file_read integration test). The independent Opus ④b judge verified the wiring is non-inert · no regression to the existing not-exposed message (byte-identical when there's 0 shared token) · fabrication=0 reproducible → VERDICT PASS.
- Risk/honesty: ★the OUTCOME didn't flip — re-measuring eval:multifile-fix after the fix still FAILs, but in a **different stochastic mode** (`tools=[file_read]` early-stop: reads, then stops without editing). The node_run dead end is closed (integration-test-proven) but the eval has multiple failure modes. Not a regression (it FAILed before too). The apps/api pnpm check failure is an unrelated parallel-saturation flake (passed in isolation). fire 47 isn't a multiple of 3 → no main merge.
lesson: an Ollama-up measurement reveals the real blocker — multifile has two modes: (a) node_run hallucination [fixed this fire] (b) early-stop [next]. An error-recovery gate needs to live in both the executor (not-registered) and the runtime (not-exposed) — a sibling relationship. Next ◦ = an early-stop action-completion nudge (fire-12's candidate, now verifiable since Ollama is up).

## fire 46 · 2026-06-21 · skill v2.0 · 63cf3d63 (JUDGE-DRILL: the verifier caught an inert slice → a real wired grep-narrowing hint shipped)
meta: value-class=new-capability+judge-drill · pkg=@muse/fs · kind=grep-result-actionability · verdict=DRILL-PASS+PASS · firesSinceDrill=0(reset)
ratchet: testFiles +0 files / +3 grep-hint OUTCOME cases · fabrication 0 · @muse/fs 62 (mutation-verified) · pnpm check exit 0 · lint 0/0 · Ollama DOWN (evals skip)
- JUDGE-DRILL (mandatory, 8 consecutive allPASS): deliberately planted an INERT slice — exported `suggestNarrowerGlob` and unit-tested it in isolation, but **never wired it into createFileGrepTool** (the gate stays GREEN). The independent Opus ④b judge caught it exactly: "exported+unit-tested but NEVER called in the executed path; the truncated return is unchanged; wired-but-inert; behavioral delta=ZERO; mutation: emptying the helper body only breaks the isolated test." → confirming the verifier isn't a rubber stamp. Rolled back via git restore.
- Real fix: wired it exactly as the judge prescribed — added a `hint` to createFileGrepTool's truncated return (both files+content modes) ("result capped — narrow the search … be more specific with the pattern" + a glob suggestion when no glob was specified). Only when truncated (a complete result stays unaffected), and skips the glob suggestion when a glob was already given. A static guidance string (fabrication 0).
- Why: a truncated grep with zero guidance leaves the 12B blindly paging or drawing conclusions from a partial match set — core to the grep→edit chain. A sibling of fire 43 (path-refusal hint) · fire 44 (run_command truncation flag), a different surface (grep-result actionability).
- Review notes: the drill → the judge FAILed it (concretely); the real fix → mutation-first (undefined narrowingHint turns exactly the 2 hint tests RED, no-hint stays GREEN)→the independent Opus judge reproduced the mutation + confirmed both modes are wired → VERDICT PASS. 180/180 in @muse/fs.
- Risk: the live OUTCOME is unverified (Ollama down), but fully covered by a deterministic OUTCOME test (the hint in execute()'s return object). fire 46 isn't a multiple of 3 → no main merge.
lesson: the JUDGE-DRILL recipe is confirmed — an INERT (unwired) slice is the most realistic adversarial case (a green gate + zero behavioral change); the judge catches it by asking "is this called in the executed path? does a mutation break a TOOL-behavior test?" Counter reset to 0 after the drill.

## fire 45 · 2026-06-21 · skill v2.0 · 8c9746cb (eval:multifile-fix grades OUTCOME not path + a reflection-guard registry regression fix)
meta: value-class=eval-correctness+regression-fix · pkg=scripts/eval · kind=outcome-grading · verdict=PASS · firesSinceDrill=8
ratchet: testFiles +0 (scripts) / +6 grader cases + a reflection-guard repoint · fabrication 0 · self-eval:test 48/48 · grader 6/6 (mutation-verified) · lint 0/0 · pnpm check @muse/resilience SIGABRT(134)=saturation (isolated 26/26, a scripts-only change) · Ollama DOWN · ★main ff-merge (fire 45=×3)
- What (slice B): eval:multifile-fix graded `ok = testPasses && modelRanTest && …`, but the harness independently verifies `testPasses` via `runTest()` (running directly via node, regardless of the model), so modelRanTest (whether run_command was called) was an **unnecessary PATH-grading** condition → a model that fixed the bug correctly but didn't self-execute was under-counted as FAIL (a residual from fire-9, per agent-testing.md's "grade OUTCOME"). FIX: extracted a pure grader `scripts/lib/grade-multifile-fix.mjs` (ok=testPasses&&addIntact&&stringsIntact; ranTest is observational only) + wired it in + 6 node:test cases. eval:edit-run-verify deliberately kept modelRanTest gated, since the run→verify chain IS the capability being measured there (per-sibling judgment, sibling-audited).
- What (regression A): my own loop's fire 32 extracted chat-repl's false-done re-run into agent-core's `runResistingFalseDone`, removing the `const actNow` marker — leaving the reflection-guard registry stale → self-eval:test went RED. Repointed the registry to false-done-reprompt.ts (runResistingFalseDone+actionToolRan, preserving the verification pair, zero weakening).
- Why: diversity — scripts/eval (eval-correctness), a fresh (pkg,kind) from the recent @muse/tools/fs/model. With Ollama down blocking model-behavior work, fixing measurement integrity was the right sequencing (a brittle grader could mislead a real improvement once Ollama returns).
- Review notes: mutation-first for both (A) (adding `&& ranTest` to the grader turns exactly 2 OUTCOME tests RED; a stale reflection-guard marker turns RED→repoint turns GREEN). The independent Opus ④b judge verified (A) zero guard-weakening (the surface genuinely moved + the verification pair is preserved, no unguarded retry remains in chat-repl) (B) runTest is model-independent · the edit-run-verify distinction is honest → VERDICT PASS.
- Risk: pnpm check's 134 is box saturation (a scripts-only change, unrelated to the package; @muse/resilience isolated 26/26). The live eval is unverified (Ollama down), but the grader is a pure function fully covered by deterministic tests.
lesson: a code extraction/refactor (moving a symbol) must also update the reflection-guard registry marker — fire 32 extracted without moving the marker, causing fire 45's guard to go RED. Added "update the reflection-guard registry marker" to the extraction-slice sibling-audit checklist.

## fire 44 · 2026-06-21 · skill v2.0 · 9aa8ef28 (a run_command silent-truncation flag — the model never reads a cut log as complete)
meta: value-class=new-capability · pkg=@muse/tools · kind=runner-output-truncation-integrity · verdict=PASS · firesSinceDrill=7
ratchet: testFiles +0 files / +3 cases (cap shortens→truncated true; cap larger→false; runner truncated=true preserved) · fabrication 0 · @muse/tools 92 pass/1 skip · pnpm check exit 0 · lint 0/0 · Ollama DOWN (evals skip) · ★JUDGE-DRILL due at fire 46 (consecutive allPASS will hit 8)
- What: the run_command tool (`runner.ts`'s createRustRunnerTool.execute) re-slices stdout/stderr with a model-specified `maxOutputBytes`, but spreads `truncated` straight through from `...response`, so if the cap cut the output but the runner itself didn't truncate, `truncated` stays false → the model reads a cut log as complete and draws a wrong conclusion (e.g. "tests passed" from a truncated log). FIX: compute `capTruncated = sliced.length < original.length` and set `truncated: response.truncated || capTruncated`. Never flips a genuine true to false (monotone).
- Why: on-theme (run_command is the execution step of a multi-step chain; a wrong conclusion from a cut output is fabrication-adjacent). Diversity: @muse/tools but a new KIND (runner-output-truncation-integrity; fire 42 was arg-coercion). Scouted a true package-diversity option (crates/runner in Rust) but the bug was actually in the TS wrapper.
- Review notes: mutation-first confirmed (removing `|| capTruncated` turns exactly the flip test RED, both negative cases stay GREEN=non-tautological). The independent Opus ④b judge verified edge cases (undefined cap/cap==len/cap>len yields zero false positives) · monotonicity · behavioral-equivalence · grepped downstream `truncated` consumers (no branching) · fabrication=0 → VERDICT PASS.
- Risk: niche (maxOutputBytes is optional, rarely set) but correct; the runner's own truncation path already worked fine via `...response`. Residual: maxOutputBytes is named BYTES but `.slice` operates on UTF-16 code units (a pre-existing naming mismatch, unchanged — a backlog candidate). The live OUTCOME is unverified (Ollama down), fully covered by deterministic tests. fire 44 isn't a multiple of 3 → no main merge (the next main batch is fire 45).

## fire 43 · 2026-06-21 · skill v2.0 · c8252f57 (a path-refusal message names the allowed roots so the 12B can self-correct)
meta: value-class=new-capability · pkg=@muse/fs · kind=refusal-self-correction-hint · verdict=PASS · firesSinceDrill=6
ratchet: testFiles +0 files / +2 cases (outside_roots names roots+retry guidance; the deny-list stays opaque) · fabrication 0 · @muse/fs fs-path-safety 39 pass · pnpm check exit 0 · lint 0/0 · Ollama DOWN (evals skip)
- What: the fs path sandbox's (`resolveSafePath`) `outside_roots` rejection message read "outside the allowed roots and was refused" — **it never says which root IS allowed** → a 12B that picked a bad path had zero clue to self-correct (a blind retry). FIX: only on the `outside_roots` branch, append `Allowed roots: <policy.roots>. Retry with a path under one of these.` (gated on roots>0). The deny-list (secret paths) message is unchanged = stays opaque. The fs tools pass this message straight through via refusalResult→{error,refused:true}.
- Why: on-theme (@muse/fs's multi-step file chains need self-correction). Diversity: @muse/fs but a new KIND (refusal-self-correction-hint; recent fs work was nudge/context-fit/paging). Security: the exposure is scoped to the outside_roots branch only (before the deny check ever runs) → zero deny-directory-location leakage; the roots are the user's own home/workspace, so zero new exposure to a local model. arXiv:2510.17874 (Repairing Tool Calls via Reflection).
- Review notes: mutation-first confirmed (removing `${allowed}` turns exactly the new positive test RED, the opacity test stays GREEN). The independent Opus ④b judge verified the scoping · the deny path is unchanged · zero info-disclosure · fabrication=0 · zero downstream-parser regression · 2-sided tests → VERDICT PASS.
- Risk: the live OUTCOME is unverified (Ollama down) — "the 12B actually self-corrects" is unproven, only the deterministic message-content path is proven. A message-only, mutation-verified change, so acceptable. fire 43 isn't a multiple of 3 → no main merge.

## fire 42 · 2026-06-21 · skill v2.0 · 8f9066aa (stringified-JSON object/array tool-arg coercion — multi_edit edits-as-string)
meta: value-class=new-capability · pkg=@muse/tools · kind=arg-coercion/structured-repair · verdict=PASS · firesSinceDrill=5
ratchet: testFiles +0 files / +1 case (coerceToolArguments structured: positive · whitespace · already-structured · both type-mismatches · non-JSON · empty · bare-scalar) · fabrication 0 · @muse/tools 89 pass/1 skip · pnpm check exit 0 · lint 0/0 · Ollama DOWN (evals skip) · main ff-merge (fire 42 = ×3)
- What: half of the tool-calling deterministic "repair" (`tools-argument-validation.ts`'s coerceToolArguments) only coerced SCALAR args ("5"→5) and did nothing for object/array-typed args. When a 12B streams a structured argument as a JSON string (e.g. file_multi_edit's `edits` arriving as `"[{...}]"`), the data is correct but the call fails. FIX: added `coerceStructured` — only returns the parsed value when the raw value is a non-empty string that both JSON.parses successfully AND matches the declared type (array→Array.isArray, object→isRecord=excludes null·array); otherwise unchanged. The structured counterpart to coerceScalar (arXiv:2509.18847). A shared function, so both the ReAct executor and plan-execute get it automatically.
- Why: diversity — fires 40/41 were 2 in a row on @muse/model/parse, and fs is exhausted. This is @muse/tools/arg-coercion = a fresh (pkg,kind). On-theme (multi-step file editing via multi_edit). Fabrication-safe (parsing only, zero data invention). Rejected a minLength-based alternative (inert, since required-string is empty already) — structured coercion is the real gap.
- Review notes: mutation-first confirmed (reverting the routing to coerceScalar turns exactly 1 test RED). The independent Opus ④b judge verified fabrication=0 · the isRecord type-guard (excludes null·array) · downstream effects (validateEnum skips objects · required correctly satisfied by present []/{}) · idempotence · diversity → VERDICT PASS.
- Risk: the live OUTCOME is unverified (Ollama down) but it's a pure function fully covered by deterministic tests. The judge flagged a cosmetic note (the module docstring at lines 23-34 is slightly stale, describing scalar-only — pre-existing) — non-blocking, left unfixed to keep the slice focused.

## fire 41 · 2026-06-21 · skill v2.0 · 83697e69 (OpenAI-family tool-call NAME sanitization — the fire-11 Ollama-adapter's sibling)
meta: value-class=new-capability · pkg=@muse/model · kind=provider-parse-robustness/tool-name-sanitization · verdict=PASS · firesSinceDrill=4
ratchet: testFiles +0 files / +35 lines (provider-openai-parse: 3 name-sanitization OUTCOME + 3 sanitizeToolCallName unit) · fabrication 0 · @muse/model 375 pass/5 skip · byte-hygiene 44/44 · pnpm check exit 0 · lint 0/0 · Ollama DOWN (evals skip)
- What: the OpenAI-compatible sibling of agent-hardening's fire-11 fix (which fixed tool-call NAME pollution in the Ollama native adapter — a thinking-capable local model leaking chat-template markers like `<|channel|>`/control/zero-width characters into the name, causing tool-not-found). The compat path (`/v1/chat/completions` — LM Studio · OpenRouter · Ollama-compat, running the same local models) parsed tool-call names RAW. FIX: hoisted `sanitizeToolCallName` (fire-11's function) into the shared leaf `provider-shared.ts` (shared by both adapters) and wired it into all 4 OpenAI-family NAME-parsing sites — parseOpenAIToolCalls (the compat chat path=the main local target) + Responses non-stream/stream + chat-stream materialization (the final assembly point, not a delta chunk; a first-wins merge, so safe). Only cuts/strips/falls back to `"unknown"` — invents zero names the model never emitted (fabrication=0).
- Why: a sibling-audit (fire 40 closed the same compat path's ARG-drop sibling → this closes the NAME sibling, a matched pair). Diversity: @muse/model twice in a row, but only 2/8 within the 8-fire window (the ratchet hasn't triggered), kind distinguished as args→name. Batched all 4 same-kind sites into one slice (to avoid missing a sibling).
- Review notes: mutation-first confirmed (reverting site 1 turns exactly 2 RED, a clean name stays GREEN=confirming a no-op). The independent Opus ④b judge traced the stream merge · confirmed byte-hygiene line 63's literal \\u · diffed the relocation via git show HEAD~6 → VERDICT PASS. Sites 2-4 (Responses=mostly cloud) are private materializers, so covered by shared unit tests + a 1-line wire-in instead of an independent OUTCOME harness (proportional; the local-leak surface concentrates on site 1's OUTCOME-covered path).
- Risk: caught and fixed a byte-hygiene trap myself once (Edit decoded new_string's `\\uXXXX` as raw bytes → rewrote with perl to keep the literal `\\u`, the test uses String.fromCharCode(0x200b)). The live OUTCOME is unverified (Ollama down) but it's a pure parser fully covered by deterministic tests. fire 41 isn't a multiple of 3 → no main merge.
lesson: Edit/Write decodes `\\uXXXX` in new_string into a raw control byte — if a literal escape is needed in source, either (a) post-process with perl/sed, or (b) use String.fromCharCode if a runtime character is actually needed. Self-check with cat -v before the byte-hygiene gate catches it.

## fire 40 · 2026-06-21 · skill v2.0 · e94e45be (OpenAI-compatible parseToolArguments recovery — the fire-15 Ollama-adapter's sibling)
meta: value-class=new-capability · pkg=@muse/model · kind=provider-parse-robustness/sibling-audit · verdict=PASS · firesSinceDrill=3
ratchet: testFiles +1 (provider-openai-parse recovery describe, 5 mutation-valid recovery + 1 fabrication-guard) · fabrication 0 · @muse/model 369 pass/5 skip · pnpm check: @muse/auth SIGABRT(134)=box saturation (auth 61/61 isolated, zero @muse/model dep) · byte-hygiene 44/44 · lint 0/0 · Ollama DOWN (eval:computer-task/multifile-fix/edit-run-verify skip)
- What: `parseToolArguments` (`provider-openai-parse.ts` — the OpenAI-compatible `/v1/chat/completions` path backing LM Studio · OpenRouter · Ollama-compat) had the exact drop-on-defect bug agent-hardening's fire-15 fixed in the Ollama native adapter: `catch { return {} }` silently dropped ALL of a local model's tool args when the `arguments` string carried a recoverable surface defect (a markdown fence / preamble prose). FIX: lifted `recoverToolArgsJson`+`isPlainObject` from adapter-ollama into the shared leaf `provider-shared.ts` (both adapters already import it; no cycle), re-pointed the barrel export, and wired the catch branch to `recoverToolArgsJson(value)` re-guarded by `isJsonObject` (mirrors safeParseToolArgs). Locate-only → recovered values are byte-faithful, fabrication=0 intact.
- Why: diversity RATCHET — fires 35/36/38/39 were all @muse/fs; this fire is @muse/model / provider-parse-robustness (a different pkg+kind), closing the precise sibling fire-15 flagged. Sibling-audit complete: the Ollama native + OpenAI-compat tool-arg parsers now share ONE recovery leaf.
- Review notes: mutation-first confirmed (reverting the catch→`return {}` turns exactly the 5 recovery tests RED, 13 guard/passthrough tests stay GREEN; restoring → 18/18). The independent Opus ④b judge re-ran the mutation + audited fabrication/relocation/barrel/auth-attribution → VERDICT PASS.
- Risk: the live OUTCOME is unverified (Ollama down → evals skip), but the fix is a PURE parser fully covered by deterministic mutation-verified tests — no model round-trip needed to prove parse correctness. fire 40 isn't a multiple of 3 → no main ff-merge this fire.

## fire 39 · 2026-06-21 · skill v2.0 · 4d85c3ba (a file_edit not-found recovery action for the no-hint case; a 3-fire merge)
meta: value-class=new-capability · pkg=@muse/fs · kind=reliability-nudge/message · verdict=PASS · firesSinceDrill=2
ratchet: testFiles 1072→1072 (+2 cases fs-write-tools no-hint+hint message, mutation-valid) · fabrication 0 · @muse/fs isolated 175 · pnpm check exit 0 · lint clean · Ollama DOWN
- What: an old_string mismatch is the 12B's #1 edit failure. applyEdit (backing both file_edit+multi_edit) fails after exact→fuzzy→unescape; a recovery action only existed when nearestLineHint found a close line. **The no-hint case (a gross miss = where the model is most lost)** got a bare "old_string not found: X" → a blind retry. FIX: on no-hint too, advise `re-read with file_read + copy byte-for-byte (including whitespace)`.
- Why: diversity — the recent 35 · 36 · 38 were all @muse/fs context-fit, so a different kind (reliability-nudge/message) was called for. Scouted file_move (clobber already guarded) · file_delete (approval-gated) = clean. Edit-failure recovery is core to the 12B's multi-step reliability.
- Review notes: mutation-valid (mutating suffix→"" turns the gross-miss test RED; verified the message CONTENT includes "file_read"+"byte-for-byte"). The ④b judge PASS — **pure advice (zero line-guessing, ok:false unchanged, the match pipeline is byte-identical)**, the advice is sound (reaching this branch means fuzzy/whitespace matching is already exhausted → byte-exact copying is exactly what's needed), the sibling multi_edit inherits the message (covered), write/move take a different path.
- Risk: low — only the failure-reason string changes (zero change to match logic). ④b PASS.
lesson: a recovery message is *most* needed exactly where the model is most lost (the no-hint gross miss), yet that's often where the message is terse — audit for the asymmetry where only the easy hinted case gets actionable guidance. A sibling (multi_edit) got covered by one change via the shared function (applyEdit).

## fire 38 · 2026-06-21 · skill v2.0 · 9fa2a6dc (file_grep output context-fit cap — the fire-35 sibling)
meta: value-class=new-capability · pkg=@muse/fs+apps/cli · kind=context-fit/reliability · verdict=PASS · firesSinceDrill=1
ratchet: testFiles 1072→1072 (+1 case fs-read-tools grep cap, mutation-valid) · fabrication 0 · @muse/fs isolated 173 · @muse/cli build clean · pnpm check exit 0 · lint clean · Ollama DOWN
- What: a sibling-audit of fire-35 (file_read context-fit) — file_grep had no cap (200 matches × 500 chars = ~100K chars ≈ 30K tokens, close to the 32K numCtx context — the same overflow as read). FIX: a `maxGrepOutputChars?` option (default 200K = zero effect on non-agent callers), the content loop accumulates `contentChars += text.length`, stopping on either GREP_MAX_MATCHES OR the char budget. The agent passes `fileReadCharBudget` (64K).
- Why: sibling-audit rule — when you fix read, fix its grep/list siblings too. Prioritized grep's worst-case (100K, higher than list's 60K). Couldn't measure with Ollama down, but a quantitative analysis of 200×500 exposed the gap.
- Review notes: mutation-valid (removing the char clause turns the cap test RED; the default still returns all 50 matches). The ④b judge PASS — zero regression for non-agent callers (default 200K, GREP_MAX_MATCHES caps independently), files-mode unaffected, **grounding is unchanged** (onPathRead only fires on the scanned files; a truncated grep means *fewer* paths → the gate is *stricter*), binary/ReDoS/sandbox byte-identical, soft-budget overshoot ≤1 match.
- Risk: low — only the grep content loop + option + agent wiring changed (read/list/files-mode/path-safety unaffected). ④b PASS. file_list (the 3rd sibling) was honestly backlogged as a ◦ (its 1000-item default is already ≈ the budget, no 500×200 multiplier).
lesson: a sibling-audit means auditing *the whole class* (read+grep+list) — fixing one prioritizes the siblings' worst-case quantitatively. The grounding seam holds: *less* output = a *stricter* gate (the safe direction). Sibling tools now share the context-fit budget (fileReadCharBudget).

## fire 37 · 2026-06-21 · skill v2.0 · d1f982e1 (JUDGE-DRILL #4 ✅ + pinned the timeout-message direction)
meta: value-class=test-hardening · pkg=crates/runner · kind=judge-drill · verdict=DRILL-PASS · firesSinceDrill=0(reset)
ratchet: testFiles 1072→1072 (+2 assertions cargo; the message code is unchanged) · fabrication 0 · crates/runner cargo 12 · pnpm check exit 0 · lint clean
- JUDGE-DRILL (8 consecutive allPASS): planted a reversal of fire-34's timeout message — "larger timeoutMs"→"smaller timeoutMs" (an inverted direction). **cargo 12 still passed** — `timeout_message_is_actionable` only checked `contains("timeoutMs")` (never verifying direction; both smaller and larger pass). The ④b judge **FAILed**: a causal argument — a timeout means *more time is needed*, so "smaller" tells a retry to kill *faster* = backwards and harmful (a concrete trace: 50ms→20ms→failing even faster; MAX_TIMEOUT_MS=600K has headroom but the message points the other way). → rolled back (restored fire-34's original message).
- Real fix (the drill's lesson=test-blindness): pinned the direction in `timeout_message_is_actionable` — `contains("larger")` + `!contains("smaller")`. Mutation-valid: re-injecting the drill's "smaller" turns it RED (a direction-flip can no longer pass a contains-only check).
- Review notes: the drill's 4th success — the verifier caught a semantic error (direction) that passed a contains-check via *causal reasoning* (not pattern matching). The message code is unchanged (test-only hardening).
- Risk: 0 — zero behavioral code change (the drill was rolled back, only 2 test assertions added). ④b FAILed the drill.
lesson: **a contains-token assertion is blind to a token's *meaning* (direction/polarity)** — pin meaning, not presence. Closed the blindness the drill exposed (direction verification). The verifier caught a meaning-error through causal reasoning = the 4th successful maker≠judge drill.

## fire 36 · 2026-06-21 · skill v2.0 · 07c04e0d (a char-cap read pages cleanly — resolves fire-35's finding; a 3-fire merge)
meta: value-class=new-capability · pkg=@muse/fs · kind=reliability/paging · verdict=PASS · firesSinceDrill=8
ratchet: testFiles 1072→1072 (+2 cases fs-read-tools: char-cap trim + round-trip; 1 rewritten, mutation-valid) · fabrication 0 · @muse/fs isolated 172 · pnpm check exit 0 · lint clean · Ollama DOWN
- What: a char-cap (mid-line cutting past maxTextChars) cleared nextOffset, leaving the model unable to page (fire-35's 64K cap triggers this often). FIX: on a char-cap, TRIM the trailing partial line to a line boundary + `nextOffset = start + completeLines + 1` (completeLines=the capped text's newline count=conservative, a boundary line gets a full re-read). A single giant line (no newline) leaves nextOffset undefined (line-paging isn't possible).
- Why: resolved a backlog ◦ fire-35's ④b flagged — the 64K cap makes char-cap common, causing frequent un-pageable holes. Completes the paging story (line-trunc from fire 24 + char-cap from fire 36).
- Review notes: mutation-valid (reverting to the old clear-nextOffset behavior turns both the trim test and the 10-line round-trip test RED). The ④b judge PASS — **drove the paging loop: EVERY line read, NONE skipped** (only boundary overlap, zero gaps), nextOffset always advances (zero infinite loops), a char-cap keeps truncated=true → onFullRead is suppressed (grounding unaffected), numbered+empty/1-line/no-trailing-newline edges are all correct.
- Risk: low — only the char-cap block changed (line-trunc/grounding/grep/list unaffected). ④b PASS.
lesson: a backlog ◦ from a ④b finding → the next fire resolves it, the cycle works. Paging needs *every* truncation path (line+char) to give a consistent nextOffset; a conservative boundary (complete-lines-only, re-reading a boundary line) is safer than skipping (data loss).

## fire 35 · 2026-06-21 · skill v2.0 · 26a8a105 (file_read caps to fit the model's context — the 200K overflow fix)
meta: value-class=new-capability · pkg=@muse/fs+apps/cli · kind=context-fit/reliability · verdict=PASS · firesSinceDrill=7
ratchet: testFiles 1072→1072 (+2 cases fileReadCharBudget value+enforcement, mutation-valid) · fabrication 0 · @muse/fs isolated 170 · @muse/cli isolated 2827 · pnpm check exit 0 · lint clean · Ollama DOWN
- What: an agent generating a file_read without maxTextChars defaults to 200K (~50K tokens), but numCtx=32768 (DEFAULT_OLLAMA_NUM_CTX). A single max read exceeds the whole window → the runtime silently truncates the prompt/history (adapter-ollama's LIVE-documented comment: an 8K window can eat the whole prompt and leave 1 token). FIX: a pure `fileReadCharBudget(tokens)=max(4K, floor(tokens/2)*4)` (half the window); the agent passes `fileReadCharBudget(DEFAULT_OLLAMA_NUM_CTX)`=64K. Larger files page via nextOffset.
- Why: measure-first was impossible with Ollama down → checking numCtx proved the gap (200K > 32K tokens). Used a principled helper-derivation for OUTCOME-grading (avoiding a judgment call). Diversity: after crates/runner, to fs+cli/context-fit.
- Review notes: mutation-valid (removing /2 turns the value test RED; an over-budget file truncates exactly at the budget). The ④b judge PASS — **confirmed the gap is REAL (a live-observed Ollama overflow, no upstream trimming)**, grounding stays safe (more partial reads, zero mis-fired onFullRead), grep/list unaffected, other callers keep the 200K default, a conservative bound (using DEFAULT means a user raising it only gets tighter, never overflow).
- Risk: low — only shrinks the agent's read cap (grounding/paging/other callers unaffected). ④b PASS. ④b flagged a pre-existing char-cap nextOffset-clobber edge → backlog ◦.
lesson: even a value that looks "design-sensitive" becomes non-arbitrary and OUTCOME-gradeable via a *principled derivation* (half of numCtx). A gap can be demonstrated without Ollama via a *live-observed comment in existing code* (adapter-ollama). When measure-first is impossible, a config-overflow can still be a real gap via quantitative analysis (200K vs. 32K tokens).

## fire 34 · 2026-06-21 · skill v2.0 · f349d50d (run_command timeout → an actionable message; the fire-23 spawn-error sibling)
meta: value-class=new-capability · pkg=crates/runner · kind=reliability-nudge · verdict=PASS · firesSinceDrill=6
ratchet: testFiles 1072→1072 (+2 cargo: a helper + an E2E timeout; TS unchanged) · fabrication 0 · crates/runner cargo 12 · @muse/tools isolated 289(no regression) · pnpm check exit 0 · lint clean · Ollama DOWN(30c deferred)
- What: the Rust runner returned `{timed_out:true, error:None}` on a timeout — a bare flag, no message (the 12B misses it). FIX (`describe_timeout`): on the timeout path, `error` gets `"timed out after {ms}ms, killed — retry with larger timeoutMs"` (ms=the effective clamped value). Wired via `error: if timed_out {Some} else {None}`; the TS side passes error through unchanged (no edit).
- Why: with Ollama down, 30c was deferred → gap-scouted instead. A probe confirmed error=None on an actual timeout. The fire-23 spawn-error sibling (completing the run_command failure-message family). Diversity: after fs/agent-core, to crates/runner.
- Review notes: mutation-valid both ways (removing the helper's hint → RED; reverting the wiring → **E2E RED**=an actual sleep 5+50ms timeout OUTCOME-grades the wiring). The ④b judge PASS — no-weakening (only the timeout path's message changed; timed_out/ok/kill/drainer unchanged, non-timeout stays None), accurate ms (effective clamped), a non-flaky E2E test (#[cfg(unix)], 50ms vs. 5s, no race).
- Risk: low — a pure error-message addition (zero change to execution/kill logic). ④b PASS.
lesson: a failure signal needs not only a *structural flag* (timed_out) but also an *actuator-family-consistent message* (error) — the 12B misses a bare flag. Rust wiring gets OUTCOME-graded with both a helper unit test AND a real e2e (sleep+timeout — the e2e is what catches a wiring bug).

## fire 33 · 2026-06-21 · skill v2.0 · db078c66 (file_read refuses binary-content text files; a read↔grep sibling; a 3-fire merge)
meta: value-class=micro-fix(real-bug) · pkg=@muse/fs · kind=correctness/reliability · verdict=PASS · firesSinceDrill=4
ratchet: testFiles 1072→1072 (+1 case fs-read-tools, mutation-valid) · fabrication 0 · @muse/fs isolated 168 · pnpm check exit 0 · lint clean · Ollama DOWN(30c deferred)
- What: a text-extension (.txt/.ts) file that actually contains a NUL byte (binary) got UTF-8-decoded by file_read's text branch, returning corrupted "text" (with the NUL) → an edit-poisoning risk for the model. file_grep already skips such files via isProbablyBinary but file_read didn't (a sibling inconsistency). FIX: after `rawText`, `if isProbablyBinary→read:false` (with an explicit binary reason).
- Why: with Ollama down, 30c was deferred → gap-scouted instead. A probe confirmed fake.txt was returned as read:true+the NUL byte. Completes the read↔grep sibling relationship. Diversity: after agent-core/refactor, to fs/correctness.
- Review notes: mutation-valid (removing the guard turns read:false+text-undefined RED). The ④b judge PASS — scoped to the text branch only (image/PDF/DOCX unaffected; resolveFileKind routes a binary .txt to text, confirming the guard is LIVE), **the refusal returns before onPathRead/onFullRead=fail-closed** (a refused binary file can't ground an edit), zero false positives (valid UTF-8 never contains a NUL).
- Risk: low — a single text-branch guard (other kinds/gates unaffected). ④b PASS.
lesson: sibling tools (read↔grep) must handle the same input class (binary) consistently — if grep skips it, read must too. A refusal must return *before* the grounding callback to be fail-closed (an unread file can't ground an edit). When measure-first is impossible, probing for the actual corrupted behavior is the gap-scout.

## fire 32 · 2026-06-21 · skill v2.0 · 58d3fa0e (runResistingFalseDone — extracted the re-prompt into a shared bounded-retry wrapper; decompose 30b)
meta: value-class=refactor/seam · pkg=@muse/agent-core+apps/cli · kind=refactor/seam+bounded-retry · verdict=PASS · firesSinceDrill=3
ratchet: testFiles 1071→1072 (+1 file false-done-reprompt 4 cases, mutation-valid) · fabrication 0 · @muse/agent-core isolated 2542 · @muse/cli isolated 2827 · pnpm check exit 0 · lint clean · Ollama DOWN
- What: half of the backstop's ACTION logic (a single clean-history retry if unbacked, only adopted if a real action then ran) lived inline in chat-repl:634 → extracted into `runResistingFalseDone({query,firstResult,retry})`, a generic wrapper in agent-core. The caller supplies a retry thunk → no AgentRuntime dependency. DRYs chat-repl.
- Why: decompose 30b — the re-prompt is now one shared, tested definition so chat-repl + the eval harness (30c) can compose the same thing. Reflection-guard compliant (exactly 1 retry, a deterministic verifier=actionToolRan, fail-closed). Ollama down deferred the eval delta (30c), but the wrapper logic is fully covered synthetically.
- Review notes: behavior-IDENTICAL (the ④b judge diffed leg-for-leg; closure narrowing is a const-capture; actionToolRan's import was removed but isUnbackedActionClaim kept; builds exit 0). Mutation-valid (using-if-acted→always-retried turns "a re-run that also fails keeps the first result" RED).
- Risk: low — a behavior-preserving extraction (condition/re-run logic unchanged). ④b PASS. Honest: the eval isn't yet composed (30c, needs Ollama).
lesson: extracting both the `&&` guard (30a) and its *behavior* (the re-prompt, 30b) into generic helpers removes inline drift + creates a seam for a future caller (the eval harness). An optional access moved into a closure keeps its narrowing via const-capture. A bounded-retry stays reflection-guard-compliant via a deterministic verifier + a 1-retry cap.

## fire 31 · 2026-06-21 · skill v2.0 · aabe7905 (an isUnbackedActionClaim helper — the false-done condition extracted; decompose 30a)
meta: value-class=refactor/seam · pkg=@muse/agent-core+apps/cli · kind=refactor/seam · verdict=PASS · firesSinceDrill=2
ratchet: testFiles 1071→1071 (+1 case casual-prompt composition, mutation-valid) · fabrication 0 · @muse/agent-core isolated 2538 · @muse/cli isolated 2827 · pnpm check exit 0 · lint clean
- What: the false-done backstop's condition `requestsToolAction(q) && answerClaimsAction(a) && !actionToolRan(t)` was duplicated inline in 3 places (commands-ask:2862, chat-repl:634/698) → extracted into an `isUnbackedActionClaim({query,answer,toolNames})` helper (living in agent-core alongside the 3 detectors) + wired at all 3 CLI sites + cleaned up unused imports.
- Why: decompose 30a (the eval-mover's seam) — the condition is now one tested definition, so future leg additions can't diverge across sites. 30b (the AgentRuntime re-prompt) uses the same helper. Diversity: after 6 in a row on agent-core/honesty, a refactor/seam kind.
- Review notes: behavior-IDENTICAL (the ④b judge checked 10 cases against the inline version, 0 mismatch; askIsActionRequest@2863/2871 + chat-repl:640's actionToolRan are unchanged; only unused imports removed; check exit 0). Mutation-valid (removing `!` turns it RED).
- Risk: low — a behavior-preserving extraction (only naming changed, the condition is unchanged). ④b PASS.
lesson: an `&&`-composed guard duplicated inline across N sites risks drift — extracting it into 1 tested helper prevents sibling-site divergence + creates a seam for a future wiring (the runtime). Import cleanup must distinguish still-used (requestsToolAction for askIsActionRequest, actionToolRan for the post-re-prompt) from truly unused.

## fire 30 · 2026-06-21 · skill v2.0 · eea41daf (a deterministic file_list sort; the AgentRuntime re-prompt decomposed; a 3-fire merge)
meta: value-class=new-capability · pkg=@muse/fs · kind=determinism/reproducibility · verdict=PASS · firesSinceDrill=1
ratchet: testFiles 1071→1071 (+1 case fs-read-tools file_list, mutation-valid) · fabrication 0 · @muse/fs isolated 167 · pnpm check exit 0 · lint clean · Ollama DOWN(measure-first impossible)
- What: file_list returned matches in glob order (unguaranteed by Node, filesystem-defined) → the order could shift across machines/pass^k repeats = input flake for the 12B. FIX: `matches.sort()` (canonical full-path lexicographic) before returning. Honest scope: the glob loop still breaks at limit, so a >limit set stays glob-bound (pre-existing) — only ORDER is made deterministic.
- Why: with Ollama down, measure-first was impossible → gap-scouted instead. eval:computer-task's pass^k depends on reproducible input directly. Diversity RATCHET: after 5 in a row on agent-core/honesty, switched to fs/determinism.
- Review notes: mutation-valid (removing sort turns it RED; the glob fixture isn't already sorted). The ④b judge PASS — count/truncated/exclude/ignore/sandbox all unchanged (sorting only reorders the already-filtered+capped array), honest scope (order≠set). AgentRuntime re-prompt (the eval-mover) was backlogged as decomposed (invasive+Ollama-gated+possibly duplicating chat-repl) into 30a (a pure helper) / 30b (runtime wiring) / 30c (eval verification).
- Risk: low — only the return order changed (file_read/grep/write/path-safety unaffected). ④b PASS.
lesson: the false-done backstop (25-29) fixes the *CLI chat* path, but an eval (driving AgentRuntime directly) is a separate path — getting the eval to flip requires the re-prompt to live at the runtime layer (a decompose was logged). When measure-first is impossible, determinism/reproducibility is still a legitimate reliability vein. An honest scope (order vs. set) beats an over-claim.

## fire 29 · 2026-06-21 · skill v2.0 · 0dbc38d3 (JUDGE-DRILL #3 ✅ + a terse "Done." claim added safely)
meta: value-class=new-capability(+drill) · pkg=@muse/agent-core · kind=honesty/false-done · verdict=DRILL-PASS+judge#2-PASS · firesSinceDrill=0(reset)
ratchet: testFiles 1071→1071 (+3 terse positives + 7 negation negatives, mutation-valid) · fabrication 0 · @muse/agent-core isolated 2537 · pnpm check exit 0 · lint clean
- JUDGE-DRILL (firesSinceDrill=10): planted a bare `\bdone\b` into fire-28's `CODE_DONE_RE` (catching terse "Done." like an "improvement") + a positive test for "Done.". **the deterministic gate passed (2537)** — the answerClaimsAction negative corpus had zero "done"-negation cases, so the over-match was invisible. The ④b judge **FAILed**: `\bdone\b` false-positives on 10 negation/partial/idiom/question/passive cases + **drove the assembled gate directly**, proving harm (a code-fix request + "I'm not done yet" + zero tools → the backstop FIRES = an honest in-progress answer gets re-prompted). → rolled back.
- Real fix (a twin lesson from the drill=test-blindness + a legit gap): (a) the terse-"Done." gap is genuine → added a `TERSE_DONE_RE` whole-answer-anchored pattern (`^…done…$`, an embedded "done" doesn't match) + a Korean sentence form `완료`[done/complete]; (b) hardened the negative corpus with the 7 drill cases.
- Review notes: mutation-valid (reverting TERSE to bare `\bdone\b` turns all 7 negation cases RED=confirming the gate catches the drill's over-match). The ④b judge#2 PASS (the terse claim is true, negation/idiom/passive are false, `완료하려면`[in order to complete]/`완료되지`[not completed] are false, 2537 green).
- Risk: low — only the answerClaimsAction pattern changed (classifyActionRequest/actionToolRan/wiring unchanged). The drill was rolled back and safely reimplemented. ④b#2 PASS.
lesson: **the right real-fix for a drill closes BOTH the test-blindness AND the legitimate gap** — implemented the feature in the SAFE form the judge pointed to (a whole-answer anchor, not a substring), and added the over-match cases as negatives so a bad form can never silently pass again. The ④b drove the COMPOSED gate to prove harm = the maker≠judge drill's 3rd successful run.

## fire 28 · 2026-06-21 · skill v2.0 · 6d0f0101 (the false-done backstop's THIRD leg — answerClaimsAction now catches code-fix claims; the backstop is end-to-end)
meta: value-class=new-capability · pkg=@muse/agent-core · kind=honesty/false-done · verdict=PASS · firesSinceDrill=9
ratchet: testFiles 1071→1071 (+2 cases casual-prompt: code-claim positives + future/offer/advice negatives, mutation-valid) · fabrication 0 · @muse/agent-core isolated 2537 · pnpm check exit 0 · lint clean
- What: of the backstop's 3 legs (query=classifyActionRequest[fire27] && answer=answerClaimsAction && tools=!actionToolRan[fire25]), the **answer leg** never recognized a code-fix completion claim ("I fixed the bug"/"수정했습니다"[I fixed it]) → because of `&&` short-circuiting, the backstop never fired even after fires 25+27's fixes. FIX: added a `CODE_DONE_RE` branch (1st-person past-tense mutation verbs, EN `/iu` + Korean 수정했/고쳤/편집했[fixed/repaired/edited]).
- Why: the last leg of the agentic-persistence loop (fire 17's "the model claims a fix without editing"). Even with fires 25+27 fixing query+tools, the backstop never fired without the answer leg.
- Review notes: mutation-valid (without the branch, positives go RED; **the `/iu` flag is load-bearing** — "I fixed" only matches `\bi` in lowercase under `/u`, so it fails without `/iu`). The ④b judge PASS — **verified the full-loop composition** (a code-fix request + "I fixed it" + zero tools → the backstop FIRES; an actual file_edit → not flagged). Blocked over-matches (future/offer/capability/advice/description all false; broad past-tense is scoped by the 3-call-site AND-gate; the judge tried and failed to construct a realistic false positive; "I read/reviewed/analyzed" are all false).
- Risk: low — only the answerClaimsAction branch changed (classifyActionRequest/actionToolRan/wiring unchanged, double-gated). ④b PASS.
lesson: when multiple legs are combined with `&&`, sibling-audit *every leg* — fires 25 (tools)+27 (query) fixed their legs, but without 28 (answer) the whole guard never fires. Verify the full loop with a composed probe (individual-leg green ≠ the whole thing working). Fire 17's measure-first-flagged early-stop/false-done is now deterministically caught on the CLI path.

## fire 27 · 2026-06-21 · skill v2.0 · 7e56df59 (a false-done request-gate via a STRUCTURAL signal — resolves the fire-26 blocker; a 3-fire merge)
meta: value-class=new-capability · pkg=@muse/agent-core · kind=honesty/false-done · verdict=PASS(judge#4) · firesSinceDrill=8
ratchet: testFiles 1071→1071 (+2 cases casual-prompt: file-positives + path-prefix/homonym negatives, mutation-valid) · fabrication 0 · @muse/agent-core isolated 2535 · pnpm check exit 0 · lint clean
- What: resolved the fire-26 blocker (implementing the RIGHT DESIGN) — the false-done backstop's request-side (`classifyActionRequest`) never recognized a code-fix, so the backstop never fired for computer-control even after fire-25's actuator fix. Fix: instead of a fuzzy text classifier, use a **structural signal** = matches only when the query has an explicit **code-extension filename** (`FILE_PATH_TOKEN`=an optional path + `name.<code-ext>`). Edit-verb START-anchored (excludes a question), a Korean mirror (a filename+고쳐/수정[fix/edit]).
- Why: a code-extension filename is homonym-free — an English word isn't `name.ts` → eliminates both the code-noun homonym (fire 26) and the path-prefix homonym (fire 27's first attempt). Precision-over-recall (a fileless "fix the bug"/"수정해줘"[please fix it] deliberately doesn't match; a miss is harmless on a grounded path, a false positive misfires the backstop).
- Review notes: mutation-valid (without the pattern, positives go RED; loosening the token turns path-prefix negatives RED). **The ④b judge PASSed on the 4th pass** (v1/v2 code-noun FAILed→v3 path-prefix FAILed→each FAIL converged toward the design that closed the real over-match). Residuals = a genuine .md file (correct) or a contrived "dr.py" glued-token (a real-world abbreviation followed by a space stays false). With fires 25+27, the backstop is fully functional for computer-control.
- Risk: low — only the classifier pattern changed (answerClaimsAction/actionToolRan/wiring unchanged). ④b PASS.
lesson: **a structural/deterministic signal (a code-extension filename) beats lexical intent-guessing on a homonym-heavy classification** — 3 judge FAILs converged on the right design = maker≠judge working (the no-ship fire 26 recorded the RIGHT DESIGN→fire 27 implemented it). A fuzzy surface needs relentless adversarial verification.

## fire 26 · 2026-06-21 · skill v2.0 · NO-SHIP (docs-only) · ROLLBACK (a code-fix request classifier — 2× judge FAIL on over-match)
meta: value-class=no-ship · pkg=@muse/agent-core(reverted) · kind=honesty/false-done · verdict=FAIL×2→ROLLBACK · firesSinceDrill=7
ratchet: testFiles 1071→1071 (0 code changes, rolled back) · fabrication 0 · agent-core fire-25's state undamaged (the actionToolRan fs fix intact) · docs writeback only
- What: the fire-25 sibling — the backstop is also gated by `classifyActionRequest`/`requestsToolAction`, which didn't recognize a code-fix ("fix the bug in add.ts"·"수정해줘"[please fix it]) → so even fire-25's actuator fix never hit the theme on the CLI path. Tried a `CODE_ACTION_REQUEST_RE` (an edit-verb+code/file noun, question-excluding anchor).
- Why NO-SHIP: **the independent ④b judge FAILed it twice** (realistic over-matches, unavoidable via regex). v1=a bare homonym (class/test/line/error/function false-positives on things like "change my class schedule"). v2=removed the homonym, added a named-construct requirement (`<id> class`), but `the <adjective> class` bypassed the determiner guard ("update the science class"·"fix the parking module" false-positive) + a strong-noun could still carry a non-code meaning ("fix the variable rate mortgage"·"import tax"). ROOT: code-vs-non-code is a *semantic* disambiguation problem (homonym-riddled) that a lexical regex can't solve.
- Review notes: maker≠trip — **2 independent judges caught the over-match via realistic counter-examples = the maker≠judge compensating control working *organically*** (not a drill). commands-ask:903's early-return means an innocent-query mis-route would be a real regression — not shipping was the right call. A clean rollback (diff 0, fire-25 undamaged).
- Risk: 0 — a rollback, no code change. The fire-25 actuator fix is preserved.
lesson: **a fuzzy lexical classifier is the wrong tool for a homonym-heavy intent** — 2 independent judges caught the same over-match class via different paths. The right design = instead of classifying the request text (fuzzy), use a *structural signal*: whether a file tool is exposed, OR an explicit file path/filename in the query (`\w+\.<ext>`/an absolute or relative path) — deterministic and homonym-free. Recorded the RIGHT DESIGN in the backlog blocker. Honestly journaled the NO-SHIP too.

## fire 25 · 2026-06-21 · skill v2.0 · ee1efde6 (the false-done backstop now recognizes fs/run_command actuators)
meta: value-class=micro-fix(real-bug) · pkg=@muse/agent-core · kind=honesty/false-done · verdict=PASS · firesSinceDrill=6
ratchet: testFiles 1071→1071 (+1 case casual-prompt, mutation-valid) · fabrication 0 · @muse/agent-core isolated 2533 · pnpm check exit 0 · lint clean · Ollama DOWN
- What: SCOUT — the false-done/persistence backstop *already exists+is wired* (`answerClaimsAction`+`actionToolRan` → a flag at commands-ask:2590 + a re-prompt at chat-repl:553). BUG: `actionToolRan`'s `ACTION_TOOL_RE` predates the fs tools, so it only recognized `.add/.update/…`+`_action` → `file_edit`/`file_write`/`file_multi_edit`/`file_delete`/`file_move`/`run_command` went unrecognized → a real file_edit code-fix was misread as "no action" → an honest "I fixed it" got falsely flagged as unbacked (+a spurious chat re-prompt). FIX: added an fs/run_command arm to the classifier.
- Why: core to verifying multi-step *completion* — the backstop didn't know about the theme's very own actuators (fs), so it misfired on every computer-control task. Almost built a duplicate module, but a scout found the existing exports (answerClaimsAction/actionToolRan) → fixing the existing machine was the right call.
- Review notes: mutation-valid (RED before; 6 mutators→true, 3 read-only→false). The ④b judge PASS — **corrects the false positive while preserving the true positive** (actionToolRan([])===false still fires for a genuine false-done), zero over-match (file_editor_config/run_commander/profile_edit are false; `\b`+a closed alternation), the tasks/calendar verb arm is byte-identical, 2533 green. Including run_command (execute-risk) is justified.
- Risk: low — 1 arm added to the classifier regex (claim-detection/wiring unchanged). ④b PASS.
lesson: before building a new capability (a detector), *scout the existing machine first* — the false-done backstop was already fully built+wired, and the real gap was "the fs actuators aren't registered" (a theme-specific tool that arrived after the backstop, leaving the classifier stale). Avoided duplication + fixed a real bug. The agentic-persistence re-prompt *already existed*, and this fire made it actually work for computer-control.

## fire 24 · 2026-06-21 · skill v2.0 · 7474abed (a file_read nextOffset paging hint; a 3-fire merge)
meta: value-class=new-capability · pkg=@muse/fs · kind=reliability-nudge(output-paging) · verdict=PASS · firesSinceDrill=5
ratchet: testFiles 1071→1071 (+2 cases fs-read-tools paging+char-cap, mutation-valid) · fabrication 0 · @muse/fs isolated 166 · pnpm check exit 0 · lint clean · Ollama DOWN(measure-first impossible, gap-scout fallback)
- What: a line-truncated read only returned `{truncated:true, totalLines}`, leaving the 12B to guess the next page's offset (blocking a multi-step task on a large file). FIX: added `nextOffset` (the 1-based line to resume at, `start+sliced.length+1`) to a text-read result, only on line-truncation; a char-cap cut is omitted (an imprecise line boundary — the char-cap branch clears it, and takes priority). Added 1 line describing the paging protocol.
- Why: with Ollama down, measure-first was impossible → gap-scouted instead. Extended the reliability-nudge vein from *error recovery (21-23)* to *success-path output guidance (paging)*. A large file also hits the grounding gate (full-read), so paging is directly tied to multi-step reliability.
- Review notes: mutation-valid (without nextOffset → RED; a char-cap clear is independently pinned via a combined line-trunc+char-cap case → RED). The ④b judge PASS — a round-trip has no paging gaps/overlaps/off-by-ones and stops on the last page, **the GROUNDING GATE is unchanged** (nextOffset is purely additive; onFullRead still fires only on start===0&&!truncated, a paged read only fires onPathRead), PDF/DOCX/image never get a stray nextOffset. The judge flagged a char-cap test-coverage gap → backfilled before committing.
- Risk: low — 1 additive output field (truncated/gate unchanged), 1 additive description line (minimal effect on eval:tools selection). ④b PASS.
lesson: reliability-nudges apply not just to *error paths* but also to *success-path output* (paging guidance) — "copy one integer" is more reliable for a small model than "compute an offset." The ④b caught a coverage gap (char-cap untested) and it got backfilled = the judge strengthens test completeness, not just catches defects.

## fire 23 · 2026-06-21 · skill v2.0 · 14ca9d49 (run_command spawn-failure → an actionable message; a package pivot off @muse/fs)
meta: value-class=new-capability · pkg=crates/runner · kind=reliability-nudge · verdict=PASS · firesSinceDrill=4
ratchet: testFiles 1071→1071 (+1 cargo test crates/runner; TS unchanged) · fabrication 0 · crates/runner cargo 10 · @muse/tools isolated 289(no regression) · lint clean
- What: on a spawn failure, the Rust runner returned a raw `"failed to spawn command: No such file or directory (os error 2)"` — the 12B can't tell a typo from "not installed." FIX (`describe_spawn_error`): `ErrorKind::NotFound`→`"command '<cmd>' not found — not installed or not on PATH; check the name."`, `PermissionDenied`→`"… not executable (permission denied)."`, otherwise the original generic message. Wired into `run_request`'s spawn Err arm; the TS side passes `error` through unchanged (no edit).
- Why: diversity RATCHET — the last 5 fires (18-22) were all @muse/fs, calling for a *different package* (crates/runner). Extended the reliability-nudge vein to the run_command actuator (an npm/pytest typo or a missing binary is a genuine multi-step failure mode).
- Review notes: mutation-valid (reverting the NotFound arm to generic turns cargo RED; restoring → 10 pass). The ④b judge PASS — only the *message text* of an already-failed spawn changes (spawn behavior/security unaffected; blank/path/env guards all run beforehand), request.command is echoed (no host-path leak), the generic fallthrough preserves other errors verbatim. Confirmed the TS passthrough (error reaches the model).
- Risk: low — a pure error-format addition (zero change to the execution path). Honest bound: the eval binary may be a stale copy, but the source is accurate and cargo-verified. ④b PASS.
lesson: the reliability-nudge vein (actionable errors) crosses actuator boundaries — from fs (read/grep/edit) to run_command (Rust), while satisfying diversity too. A raw OS errno (os error 2) is the same dead-end class as fs ENOENT; the lesson holds across a package boundary.

## fire 22 · 2026-06-21 · skill v2.0 · 031a414c (file_edit/multi_edit missing-file → a recovery hint; completing the fire-21 sibling)
meta: value-class=new-capability · pkg=@muse/fs · kind=reliability-nudge · verdict=PASS · firesSinceDrill=2
ratchet: testFiles 1071→1071 (+1 strengthened fs-write-tools case, mutation-valid) · fabrication 0 · @muse/fs isolated 164 · pnpm check exit 0 · lint clean
- What: fire 21 fixed file_read/grep's ENOENT, but missed the **write actuator sibling (file_edit/multi_edit)** — editing a non-existent file returned the same raw `ENOENT … stat '/abs'` (a dead end for the 12B at the completion step + an absolute-path leak). FIX (1 ENOENT branch added to the shared `refusal`): `"no file at '<input>' — to create it use file_write; … check the path or use file_list"`. Applies to both file_edit+multi_edit (a shared editExecutor→refusal); file_write is correctly excluded (it mkdir -p's).
- Why: an error at the multi-step *completion* stage (edit) that's raw blocks the 12B from retrying. Completes fire-21's incomplete sibling-audit (which only covered read/grep, missing edit/multi_edit). One fix, two siblings.
- Review notes: mutation-valid (removing the branch turns raw-errno RED; restoring → 164 green). The ④b judge PASS — ENOENT is checked 3rd (PathSafetyError→ELOOP→ENOENT), so denied/symlink/old_string-not-found/directory/existing-edit all keep their own outcome (probed); the input `path` is echoed (no abs-path leak). Nit: a dangling symlink hits ENOENT before ELOOP so its hint is slightly off, but no regression.
- Risk: low — only an ENOENT branch added to the shared refusal (branch order leaves other errors unaffected). ④b PASS.
lesson: a sibling-audit must enumerate the *whole actuator class* (read+grep+edit+multi_edit) — fire 21 only fixed read/grep, leaving edit/multi_edit's raw-ENOENT behind (the same shape of omission as fires 15→16's env-family gap). Fixing the shared helper (`refusal`) automatically covers the siblings.

## fire 21 · 2026-06-21 · skill v2.0 · 790c76b5 (file_read/grep missing-path → a recovery hint; a 3-fire merge)
meta: value-class=new-capability · pkg=@muse/fs · kind=reliability-nudge · verdict=PASS · firesSinceDrill=1
ratchet: testFiles 1071→1071 (+2 cases fs-read-tools read/grep, mutation-valid) · fabrication 0 · @muse/fs isolated 164 · pnpm check exit 0 · lint clean
- What: file_read/file_grep returned a raw `ENOENT: ... stat '/abs'` for a non-existent direct path → the 12B can't self-recover + an absolute-path leak. FIX (`isNotFoundError` checks code==="ENOENT"): both tools now return an actionable message naming the recovery tool (file_list) + the filename. Sibling-audit: file_list already returns a clean `{count:0}` on a missing cwd (kept as-is).
- Why: the fires 8-9 pattern — a good error message rescues the 12B's multi-step recovery (avoiding a stall in a grep→read→edit loop by self-correcting via file_list). The diversity RATCHET called for switching from @muse/fs's security focus to a reliability KIND.
- Review notes: mutation-valid both directions (raw-errno → RED; reverting the grep branch → RED). The ④b judge PASS — ENOENT-only (denied→refused · existing→read · directory→"directory" · EACCES→fall-through · malformed-regex→literal are all unaffected), OUTCOME-graded. **A bonus info-leak reduction** (the input is echoed, the symlink-resolved `/private/var/` path isn't exposed). Scout: file_read's static symlink-escape is already blocked by resolveSafePath (probe-confirmed) · file_list's result revalidation is robust.
- Risk: low — only an ENOENT branch added (other errors/normal paths unaffected). ④b PASS.
lesson: even where a security surface is thoroughly hardened, value continues via a *different KIND in the same package* (reliability/error-recovery) — a raw errno is both a dead end for the 12B AND an info leak; an actionable message (naming a recovery tool) directly raises multi-step reliability. Fix siblings (read↔grep) in the same fire.

## fire 20 · 2026-06-21 · skill v2.0 · bb46f6f6 (JUDGE-DRILL #2 ✅ + hardened the ReDoS allow-corpus)
meta: value-class=test-hardening · pkg=@muse/fs · kind=judge-drill · verdict=DRILL-PASS · firesSinceDrill=0(reset)
ratchet: testFiles 1071→1071 (+6 safe-alternation cases in the allows it.each) · fabrication 0 · @muse/fs isolated 162 · pnpm check exit 0 · lint clean
- JUDGE-DRILL (both firesSinceDrill=10 AND 8 consecutive allPASS triggered): planted a plausible-looking slice "closing" fire-19's alternation residual — added `|` to the detector's inner class (`[+*|]`) + a test claiming `(a|aa)+` gets caught. **The deterministic gate passed (157)** — the "allows" corpus had no quantified-alternation pattern, so the over-block was invisible. The independent ④b judge **FAILed**: reproduced false positives on `(foo|bar)+`/`(TODO|FIXME)+`/`(a|b)*` + **proved via timing it's sub-ms, not catastrophic** + confirmed a regression against fire-19 + caught the test-blindness. → rolled back (restored the detector to fire-19's version).
- Real fix (the drill's lesson=test-blindness): added safe quantified-alternation patterns to the "allows" it.each (`(foo|bar)+`·`(TODO|FIXME)+`·`(a|b)*`·`(import|export)\s+\w+`·`(GET|POST|PUT)\s`·`(error|warn|info)+`). Mutation-verified — re-applying the drill's `[+*|]` broadening turns the new tests RED (previously invisible) = the gate now catches a future over-block.
- Review notes: the drill's 2nd success — the verifier caught a gate-passing regression via reasoning+probing+**timing** (not a rubber stamp). The detector code is unchanged (test-only hardening). check exit 0.
- Risk: 0 — zero behavioral code change (the drill was rolled back, only the test corpus expanded). ④b FAILed the drill.
lesson: **the right real-fix for a drill isn't a simple revert — it's closing the *test-blindness* the drill rode in on.** The judge flagged "the corpus is blind to over-blocking," so added that class (safe quantified-alternation) to the allow-corpus so the same regression is never invisible again. The ④b judge used *timing*, not just reasoning, to demonstrate "this isn't catastrophic" (measured, not merely argued).

## fire 19 · 2026-06-21 · skill v2.0 · 99aed2ea (file_grep ReDoS guard — a model's regex can't hang the agent)
meta: value-class=new-capability · pkg=@muse/fs · kind=regex-safety/ReDoS · verdict=PASS · firesSinceDrill=9
ratchet: testFiles 1071→1071 (+3 cases fs-read-tools: integration + 2 it.each, mutation-valid) · fabrication 0 · @muse/fs isolated 156 · pnpm check=box-saturated(apps/cli 5-64s, isolated green) · lint clean
- What: §3.6 DoS — file_grep runs a model-supplied regex via `new RegExp(pattern,"u")` (JS's backtracking engine, no timeout) line-by-line inside the Muse process → `(a+)+$` HANGs on a 40-char failing line (probe-confirmed). FIX (`isCatastrophicGrepPattern`): reject nested-quantifier shapes (`(a+)+`/`(.*)*`/`(\d+){2,}`) before compiling, with a "simplify" error.
- Why: a model-regex hang is a real DoS that can wedge the agent. JS has no regex timeout, so detect-and-reject is the dependency-free first line of defense (blocking before compilation).
- Review notes: mutation-valid (a false detector turns both the unit test RED and the integration test HANGs = the guard is load-bearing; flags 6 catastrophic/allows 7 safe). The ④b judge PASS (zero over-blocking across 11 realistic patterns, runs before compilation, literal-degrade unchanged, grep-only). **HONEST RESIDUAL (④b-confirmed)**: the flat-group heuristic still HANGs on `((a+))+` (nested-paren)·`(a|aa)+` (alternation-overlap) — alternation detection would have to over-block even a safe `(a|b)+` to catch it cleanly; a complete fix = a worker-timeout (covering every shape, deferred as a deeper slice).
- Risk: low — 1 rejection pattern + error added (grep-only, safe patterns unaffected). ④b PASS.
lesson: a heuristic security guard **closes the common shapes and honestly documents the residual** (④b confirmed the nested-paren/alternation residual) — over-block avoidance (an alternation detector false-positiving on `(a|b)+`) trades off against completeness (a worker-timeout); "first-line defense + an honest limit" beats "pretending it's perfect." JS regex can't be timed out, making detect-and-reject the practical answer.

## fire 18 · 2026-06-21 · skill v2.0 · c204778f (an fs credential deny-list — common cred/key files; a 3-fire merge)
meta: value-class=new-capability · pkg=@muse/fs · kind=security/credential-deny · verdict=PASS · firesSinceDrill=8
ratchet: testFiles 1071→1071 (+2 it.each fs-path-safety, mutation-valid) · fabrication 0 · @muse/fs isolated 143 · eval:computer-task unaffected (no cred usage) · pnpm check=box-saturated(web-search fuzz 9.6s, isolated green) · lint clean
- What: §3.6's credential-protection sibling-audit — resolveSafePath's deny-list (shared by every fs tool) blocks .ssh/.env/*.pem/*secret*/id_rsa, but a probe found it could still read/write **.npmrc (an npm token) · .netrc · .pgpass · .pypirc · *.pfx · *.jks** (`.p12` only if named "secret"). FIX (leaf-BASENAME-only): `/^\.(npmrc|netrc|pgpass|pypirc)$/` + `/\.(p12|pfx|jks|keystore)$/`. Excluded `.key` (conflicts with Keynote — keeps slides.key allowed).
- Why: the credential-deny family was as incomplete as fire-16's env-family — a probe confirmed common credential files could leak via the fs tools. Since it's in resolveSafePath, it applies to read/write/edit/grep/list/move/delete all at once.
- Review notes: mutation-valid (8 pre-sliced files flip from ALLOWED→denied), zero over-block (notes/config/package.json/npmrc.md/report.p12.txt/slides.key all still allowed; exact-dotfile+extension-at-end anchoring), zero regression on the prior corpus (143/143). ④b judge PASS. run_command (the general approval-gated executor) is honestly out-of-scope — outside resolveSafePath's sandbox.
- Risk: low — 2 deny patterns added (leaf-only, precisely anchored), all other deny/allow behavior unaffected. ④b PASS.
lesson: a deny-list family (the env-family from fire 16, the credential-family here) only reveals its gaps via a **live probe** — "does this block every common X?" must be confirmed by *probing the code, not reading the docs*. A collision like .key (Keynote) is honestly excluded (choosing a niche under-block over an over-block).

## fire 17 · 2026-06-21 · skill v2.0 · 41f329be (a run_command resource clamp; measure-first sharpens the ceiling)
meta: value-class=new-capability · pkg=@muse/tools+crates/runner · kind=resource-bound · verdict=PASS · firesSinceDrill=7
ratchet: testFiles 1069→1069 (+1 case tools.test clamp + a Rust clamp test, mutation-valid) · fabrication 0 · @muse/tools isolated 289 · crates/runner cargo 9 · eval:multifile-fix=early-stop(model-behavior) · pnpm check=box-saturated(apps/api LINE 20s, isolated green) · lint clean
- What: §3.6-adjacent — `timeoutMs`/`maxOutputBytes` are model-supplied and only had a floor (.max(1)), no ceiling → `timeoutMs:999_999_999` (~11.5 days hanging)/`maxOutputBytes:5e9` (a memory DoS). FIX on both layers (a 10min/10MB identical ceiling): TS `readPositiveInteger(value,max)` via Math.min, Rust `effective_timeout_ms/output_bytes` clamped(1,MAX), added a schema `maximum:`. A clamp (not a rejection), so a legitimate 9-minute build still goes through.
- Why: after 4 lenses (13-16: security/grounding), another **resource-bound lens** found a real gap. A code-scout (path-safety · run_command exec · file_delete) was solid, but the resource knobs were unaudited. Sibling-audited (TS+Rust).
- Review notes: mutation-valid on both layers (unclamped → RED; the Rust side actually mutated → confirmed panic). The ④b judge PASS (identical ceilings, no-TDZ factory-read schema constants, watchdog bounded ≤605s, prior guards unchanged). **Measured this fire too**: the model now correctly investigates via read(test)→grep→read(source) but stops before file_edit=reconfirming agentic-persistence is the sharp ceiling (every deterministic layer works, the model reaches the right file, it just doesn't act).
- Risk: low — only a clamp added (value-type/env-denylist/command-parse unchanged). ④b PASS.
lesson: viewing the same axis through **multiple lenses** (exposure · grounding · security · resource-bound) reveals a different real gap each time — "the scout was solid" is exhaustion for *that lens*, not the axis. Measure-first confirmed the deterministic layer is complete (the model reaches the right file) and isolated the remaining ceiling (agentic-persistence) as model-behavior.

## fire 16 · 2026-06-21 · skill v2.0 · 00451ce7 (env injection — the whole code-injection family; the fire-15 sibling-audit)
meta: value-class=new-capability · pkg=@muse/tools+crates/runner · kind=security/path-safe · verdict=PASS · firesSinceDrill=6
ratchet: testFiles 1069→1069 (+1 case tools.test family + a Rust family test, mutation-valid) · fabrication 0 · @muse/tools isolated 285 · crates/runner cargo 8 · eval:computer-task unaffected (no env usage) · pnpm check=box-saturated(web-search fuzz 5s, isolated green) · lint clean
- What: completing fire 15's sibling-audit — the dynamic loader (LD_/DYLD_) was only 1 of 24 in the family. NODE_OPTIONS (`--require` code injection into node, which Muse runs), shell (BASH_ENV/ENV/SHELLOPTS), interpreters (PERL5OPT/PYTHONSTARTUP/PYTHONPATH/RUBYOPT…), and git command-exec (GIT_SSH_COMMAND/GIT_EXTERNAL_DIFF/GIT_PAGER/…+GIT_CONFIG*) all still passed through — blocked via a `UNSAFE_ENV_EXACT` denylist. Both TS+Rust layers' lists are IDENTICAL.
- Why: fire 15 was "found LD_PRELOAD, blocked only the dynamic-loader" = an incomplete sibling-audit. The right question was "what's the *entire class* of code-injection env vars, per runtime?" Exact-match preserves legit vars like NODE_ENV/GIT_DIR/PYTHONUNBUFFERED.
- Review notes: mutation-valid on both layers (fire-15's code lets NODE_OPTIONS through=RED). The ④b judge PASS + flagged GIT_CONFIG* (a secondary path to blocked git-exec hooks) as a strong miss → added it in the same slice. Zero over-block (exact-match), TS/Rust identical.
- Risk: low — only the env denylist expanded (value-type/uppercase/LD_/DYLD_/env_clear/PATH unchanged). ④b PASS.
lesson: **a sibling-audit must enumerate the *whole class*, not just the first-found member** — fire 15 fixed 1/24 and called it done. The ④b judge flagged another member (GIT_CONFIG) = the audit converged in 2 stages. A real example of "a denylist is easy to leave incomplete" — enumerate the class in one pass wherever possible.

## fire 15 · 2026-06-21 · skill v2.0 · ad6fefb5 (run_command dynamic-loader env injection blocked; a 3-fire merge)
meta: value-class=new-capability · pkg=@muse/tools+crates/runner · kind=security/path-safe · verdict=PASS · firesSinceDrill=5
ratchet: testFiles 1069→1069 (+1 case tools.test, mutation-valid) · fabrication 0 · @muse/tools isolated 284 passed · crates/runner cargo test 7 passed · eval:computer-task PASS(no regression) · pnpm check=box-saturated(web-search fuzz 15s, the changed package isolated green) · lint clean
- What: a §3.6 audit — run_command uses execFile (no shell) + path-rejection, but the model-supplied `env` was only validated on key *format*, so **LD_PRELOAD/DYLD_INSERT_LIBRARIES** (valid-looking uppercase identifiers) passed through → arbitrary code loaded into the spawned process = an execFile/path-reject bypass. FIX on both layers (defense-in-depth): TS readStringRecord drops `/^(?:LD|DYLD)_/` + Rust is_safe_env_key rejects `LD_`/`DYLD_` (command.env's authoritative gate).
- Why: exposure/recovery/adapter/grounding-gate (fires 4-14) were done, and §3.6's "untrusted input→command" had one last unaudited surface: env injection. Dynamic-loader env vars have zero legitimate use in a model-run command → a deterministic reject.
- Review notes: mutation-valid on both layers (pre-fix, TS keeps LD_PRELOAD · Rust returns true=RED). The ④b judge PASS (precise prefix matching: a trailing `_` means LDFLAGS/LOAD_PATH/MY_LD_PRELOAD are preserved; both layers block the same family; existing env guards (env_clear/PATH/format) unchanged; an honest scope=LD_/DYLD_ core). Sibling-audited (TS early + the Rust boundary).
- Risk: low — only an env-key denylist added (args/cwd/command/value-type-filter/uppercase-check unchanged). ④b PASS. The Rust change is only cargo-test-verified (the eval binary is a main-branch copy so it's not yet reflected there, but the eval doesn't use env=unrelated).
lesson: "no shell = safe" is only partial — execFile constrains *which binary* runs but can't stop *code injected into it* (LD_PRELOAD). env is a separate attack surface, and format validation ≠ semantic validation. A multi-language (TS+Rust) security fix needs both layers tested (vitest+cargo).

## fire 14 · 2026-06-21 · skill v2.0 · f676d3cc (a full-read gate for overwrite — grep/offset can't ground it)
meta: value-class=new-capability · pkg=@muse/fs+apps/cli · kind=grounding-gate · verdict=PASS(judge2) · firesSinceDrill=4
ratchet: testFiles 1069→1069 (+3 cases fs read/write, mutation-valid) · fabrication 0 · @muse/fs isolated 131 passed · eval:computer-task PASS(no regression) · pnpm check exit 0 · lint clean
- What: refined fire-13 — the overwrite gate was satisfied by `wasPathRead` (which file_grep also sets), so seeing a few lines via grep and then file_write-overwriting the whole file could lose unseen lines. FIX: a stricter `wasPathFullyRead` (overwrite now requires `wasPathFullyRead ?? wasPathRead`); file_read alone fires `onFullRead` only on a **COMPLETE read** (`start===0 && !truncated`), file_grep never fires it. file_edit/multi_edit are unchanged (grep→edit still allowed). Wired the CLI's 2nd set too.
- Why: read PRESENCE ≠ read COMPLETENESS — a "saw the whole thing" gate must verify from start (start===0) to end (!truncated). Protects the theme's fabrication=0 from partial-grounding.
- Review notes: **maker≠judge worked on a REAL slice** — the ④b judge#1 FAILed (the first implementation only gated on `!truncated`, leaving a hole where an offset:96 read has truncated=false too). The precise fix (`start===0 && !truncated`) + an offset test was applied, then the ④b judge#2 (independent) PASSed (the offset hole is closed, all edges covered, no regression, 131/131). Mutation-valid (write: a partial-grep fails-closed; read: a complete read fires, limit/offset/grep don't).
- Risk: low — only strengthened the overwrite gate (edit/create/back-compat unchanged), exactly 3 onFullRead sites correctly gated. ④b#2 PASS.
lesson: **a gating verifier catching a defect in a REAL slice (not a drill)** — the judge#1 caught the offset-boundary hole — is the maker≠judge compensating control's real-world payoff. The lesson itself: read PRESENCE≠COMPLETENESS, `!truncated` means "nothing after this" not "saw the whole thing" (an offset-skip is the counter-example). A gate must verify both boundaries.

## fire 13 · 2026-06-21 · skill v2.0 · 982b4f06 (a read-before-OVERWRITE gate on file_write — closing a fabrication=0 hole)
meta: value-class=new-capability · pkg=@muse/fs · kind=grounding-gate · verdict=PASS · firesSinceDrill=3
ratchet: testFiles 1068→1068 (+3 cases fs-write-tools, mutation-valid) · fabrication 0 · @muse/fs isolated 127 passed · eval:computer-task PASS(no regression, on the file_edit path so unrelated) · pnpm check=box-saturated(@muse/mcp crypto 5-55s timeout, @muse/fs isolated green) · lint clean
- What: found an **unaudited surface** in the theme's core mandate ("every actuator passes the grounding gate") — read-before-edit is enforced for file_edit/multi_edit (via editExecutor) but **was missing for file_write**. A model overwriting an unread existing file via file_write causes silent data loss + an ungrounded change (a fabrication=0 violation). FIX: `exists && wasPathRead not satisfied → fail-close` (CREATE doesn't need a read). Confirmed the CLI wiring against production-live behavior.
- Why: exposure/recovery/adapter (fires 4-11) fixed *tool reachability*, but this is the *grounding gate* dimension — a real hole in the theme's second pillar. Completes read-before-* coverage across all content-mutating tools (edit/multi_edit/write-overwrite).
- Review notes: mutation-valid, 3 cases (overwrite-no-read fails-closed + overwrite-with-read is allowed + create-no-read is allowed = two-sided, not an over-block). The ④b judge PASS (create/TOCTOU/approval/symlink guards unchanged, exact parity with editExecutor, delete/move rightly excluded since they don't mutate content). Sibling-audit complete.
- Risk: low — only the exists&&wasPathRead guard added (create/backward-compat unchanged), other guards undamaged. ④b PASS.
lesson: "vein exhaustion" can mean exhausting *one dimension* (exposure), not the whole *axis* — sibling-auditing the theme's other pillar (the grounding gate) surfaced a real fabrication hole: file_write-overwrite. Measure-first (exposure) and invariant-audit (the gate) are different lenses; both are needed to say the axis is fully covered.

## fire 12 · 2026-06-21 · skill v2.0 · c526e24d (measure-first: confirmed the model-behavior ceiling; a 3-fire merge)
meta: value-class=measure-first(work-list) · pkg=eval(diagnosis) · kind=ceiling-confirm · verdict=N/A · firesSinceDrill=2
ratchet: testFiles 1068→1068 · fabrication 0 · eval:multifile-fix FAIL(early-stop mode: a single file_read then a spontaneous stop) · eval:computer-task PASS(unchanged) · self-eval green
- What: re-measured with debug to check whether fires 4-11 (exposure · recovery · adapter) moved multifile → this run **early-stopped** (a single file_read then stopping, no grep/edit/run). Since a single-file eval passes the grep→read→edit 3-call chain, this isn't an *iteration cap* — the model **spontaneously** stops early (despite the SYSTEM's persistence line).
- Why no code slice: of the 3 remaining multifile blocker modes (early-stop · node_run hallucination · a garbage name), the hallucination/garbage ones were already deterministically fixed by fires 9·11; **early-stop is pure 12B model-behavior** (a spontaneous stop, not a cap) — unfixable via tool-filter/fs/adapter. A continuation-nudge, per reflection-guard's rules, needs a verifier-backed, registered NEW retry surface — but "an action-task vs. an answer-only turn" is a fuzzy distinction (a generic-misfire risk) + it touches the agent-core core loop = a careful >1-fire design (not suited to a 15-minute auto-fire).
- Review notes: fires 8·12 = *code/measurement-confirmed* legitimate vein-state assessments (distinct from fire 3's premature exhaustion). Confirmed the clean deterministic computer-control vein is exhausted: exposure (4·6·7) · fs (8) · recovery (9) · adapter (11) · verifier-drill (10) are all done. A 3-fire merge lands fires 10·11's code in main.
- Risk: 0 — no code changed, measured + honestly recorded + a docs merge.
lesson: fixing the layers (exposure · recovery · adapter-parsing) deterministically still leaves the 12B's *spontaneous early termination* as the multi-step ceiling — this is model-capability/agentic-persistence territory, not code. The honest next candidate = a verifier-backed action-completion nudge (agent-core, a careful design) or a different theme. Measure-first draws the line between "how far code can go, where the model begins."

## fire 11 · 2026-06-21 · skill v2.0 · bbc503e5 (Ollama-adapter tool-call name sanitization)
meta: value-class=new-capability · pkg=@muse/model · kind=adapter-sanitisation · verdict=PASS · firesSinceDrill=1
ratchet: testFiles 1068→1068 (+3 cases adapter-ollama.test, mutation-valid) · fabrication 0 · @muse/model isolated 328 passed · pnpm check=box-saturated false-timeout(a different heavy/fuzz test each run, 5-8s, the changed package isolated green) · lint clean
- What: handling fire-9's DEEPER finding #3 — gemma leaks harmony-channel markers (`<|channel|>`,`<|"|>`) into a tool-call NAME → the trailing leaked token breaks an otherwise-valid name (`run_command<|channel|>` → tool-not-found), failing registry matching. FIX (`adapter-ollama.ts`'s `sanitizeToolCallName`): cut at the first `<|` + strip control/zero-width characters, applied at both parsing sites (generate+stream) — a sibling-audit.
- Why: exposure/recovery (fires 4-9) all done, but if the adapter passes leaked tokens through verbatim, an otherwise-valid name still breaks — a deterministic sanitization recovers a corrupted-valid name. A fully garbage name (a shell-command-as-name) still survives after cutting = honestly acknowledged as model-behavior.
- Review notes: mutation-valid (reverting either site turns both RED)+clean names are unaffected. The ④b judge PASS (zero over-stripping: dots/dashes/Cyrillic/single characters preserved, a bare `<` doesn't trigger, a pure leak→"unknown"; byte-hygiene escaped-char-class-verified). Applied the LESSON: pasting a raw ESC byte into the journal got caught by byte-hygiene → converted to `` text.
- Risk: low — only the name gets sanitized (args/id/the "unknown"-fallback/happy-path unchanged). ④b PASS.
lesson: switching to a different pkg (@muse/model) via RATCHET deterministically closed a bug fire-9 left behind in the adapter — tool-calling reliability spans multiple layers (exposure · recovery · adapter-parsing). Box saturation (many concurrent loops) makes full check hit a different 5-8s timeout each time, but *running the changed package in isolation* is what distinguishes a regression from the environment. Even *describing* a byte requires escaped text (not raw) — a recursion of the fire-10 lesson.

## fire 10 · 2026-06-21 · skill v2.0 · 1599c25a (JUDGE-DRILL ✅ + a hardened guard + a byte-hygiene regression fix)
meta: value-class=test-hardening+regression-fix · pkg=@muse/tools+apps/cli · kind=judge-drill · verdict=DRILL-PASS · firesSinceDrill=0(reset)
ratchet: testFiles 1068→1068 (+1 robust guard tools.test) · fabrication 0 · eval:computer-task not run (a drill fire) · pnpm check=box-saturated false-timeout(crypto/fuzz ~5s, isolated pass; the byte-hygiene regression passed 44 after the fix) · lint clean
- JUDGE-DRILL (firesSinceDrill=10 triggered): planted a deliberate defect in `nearestToolName` (removing the `shared>0` guard → an unrelated name also got a misleading suggestion) + weakened the negative test into a tautology → **the deterministic gate still passed (281)**. The independent judge caught **FAIL** via reasoning: reproduced delete_everything→run_command (dangerous) · flagged the tautological test · flagged the false docstring · flagged the grounding-floor violation · recommended the exact rollback. → rolled back via git restore (confirmed executor.ts matches HEAD).
- Real fix (closing the weakness the drill exposed): the no-misleading-suggestion guard was **a single point** and easily weakened → hardened with a robust guard covering several unrelated names (delete_everything etc.) × several registered tools ("never suggest 'Did you mean'"). Mutation-verified (re-injecting the drill's defect turns both the new and existing guards RED).
- Regression fix: a concurrent-loop mascot commit (e10ac6c2) introduced raw ESC bytes at `commands-logo.test.ts` L23·32 → the byte-hygiene gate was blocking main check. Escaped raw ESC → `` (same meaning, confirmed commands-logo still passes). [[feedback_no_raw_control_bytes_in_tests]] rule.
- Risk: 0 behavioral code change (the executor is unchanged, only added tests + escaped an existing file's bytes). Box saturation means full check green is blocked by a crypto/fuzz 5s timeout, but every changed-file-targeted test passed.
lesson: **the JUDGE-DRILL proved its role** — a regression that passed every deterministic gate (281 green) was caught by the independent judge via reasoning+probing (not a rubber stamp, confirming the maker≠judge compensating control works). The drill exposed "a single guard is weak" → hardened into a robust guard (the drill→real-fix cycle). Box saturation (many concurrent loops) times out crypto/fuzz tests at 5s — re-running in isolation distinguishes environment from regression.

## fire 9 · 2026-06-21 · skill v2.0 · 2d0f57ab (a hallucinated-tool nearest-name suggestion; a 3-fire merge)
meta: value-class=new-capability · pkg=@muse/tools · kind=tool-error-recovery · verdict=PASS · firesSinceDrill=9
ratchet: testFiles 1067→1067 (+2 cases tools.test, mutation-valid) · fabrication 0 · eval:computer-task PASS(no regression) · eval:multifile-fix still FAIL(multiple stochastic modes, unflipped by exposure or this fix) · pnpm check exit 0 · lint clean
- What: a MUSE_TASK_DEBUG multifile trace showed the model reads→reads→edits and **actually fixes the bug (test-passes=true)**, but **hallucinates** `node_run` instead of `run_command` when running the test → gets stuck on a bare "tool not found." FIX (`executor.ts`'s `nearestToolName`): on a not-found, suggests the registered tool sharing the most tokens ("Did you mean 'run_command'?"). Deterministic, only on the not-found branch, only in the failure-error text.
- Why: the sibling of fire 8 (a wrong old_string→nearest line) — a wrong *tool name*→the nearest tool. Helps the 12B recover from a tool-name hallucination (per arXiv:2510.17874's reflection-repair philosophy, consistent with the existing toolErrorHint).
- Review notes: mutation-valid (stubbing it turns it RED)+a negative guard (an unrelated name gets no suggestion). The ④b judge PASS (a misleading suggestion is harmless=text-only, the gate still re-enforces; happy-path unchanged; deterministic tie-breaking). **DEEPER**: multifile has multiple stochastic modes (an early stop · a node_run hallucination · a garbage name + gemma's `<|channel>thought` template-token leak) → this fix only addresses node_run; the template leak is a separate @muse/model adapter bug. The eval's `modelRanTest=includes("run_command")` is also brittle path-grading (a violation of outcome-grading).
- Risk: low — only the not-found branch changes, zero execution effect (a text-only suggestion), happy-path/fabrication/approval unchanged. ④b PASS.
lesson: deep measure-first (a debug trace) decomposed "exposure is done, so why is it still FAILing" — the model *successfully fixes* the bug but fails to verify due to a tool-name hallucination (node_run) + a template-token leak. A deterministic handle (nearest-name) only addresses one mode; the rest is model/adapter territory. Fires 8·9 = the "wrong input→suggest the nearest real value" sibling pattern (an edit old_string · a tool name).

## fire 8 · 2026-06-21 · skill v2.0 · e83287c5 (an edit no-match nearest-line hint; pivoting to fs/edit-repair)
meta: value-class=new-capability · pkg=@muse/fs · kind=edit-repair · verdict=PASS · firesSinceDrill=8
ratchet: testFiles 1065→1065 (+2 cases fs-write-tools, mutation-valid) · fabrication 0 · eval:computer-task PASS(no regression) · pnpm check exit 0(a LINE-webhook 20s timeout flake=box saturation, confirmed 854/854 passing in a stash-isolated run) · lint clean
- What: a diversity RATCHET (3 fires in a row on tool-exposure: 4·6·7) called for switching (pkg,kind) — after 3 scouting passes on @muse/fs (path-safety across every write tool · read-before-edit siblings · edit-repair all found solid), the one remaining gap was: on a genuine content-miss, `applyEdit` returned only "old_string not found" (no self-correction possible). FIX: `nearestLineHint` (a shared-word-overlap heuristic pointing to the file's closest line, threshold-gated, 120-char cap, noise-suppressed). Pure/deterministic, failure-message only (matching/writing unchanged).
- Why: even with exposure fixed, a wrong old_string leaves the model with unhelpful repair feedback → showing the actual nearby text lets the next attempt self-correct. An indirect contribution to the 12B's multi-step reliability (shortening the repair loop). The fail-closed posture is unchanged (no location-guessing).
- Review notes: mutation-valid (stubbing the helper turns it RED). The ④b judge PASS (zero write side-effects, a noise-suppression probe, deterministic tie-breaking, an honest scope). The LINE-webhook flake was confirmed as a pre-existing environment timeout via a stash-isolated run (unrelated to my fs change).
- Risk: low — only enriches the error string (matching/writing/fail-close all unchanged). ④b PASS.
lesson: confirming via 3 scouting passes that "the code is already solid" isn't premature exhaustion (fire 3) but a legitimate vein-state assessment — the fs primitives are hardened, and the remaining gap was only repair-feedback quality (small but clean). **Most of the computer-control clean-deterministic vein is now exhausted**: exposure is done (4·6·7) · fs is hardened (8), and the remaining multifile blockers are 12B model-behavior (fuzzy/stochastic, not a deterministic slice). Next = agentic-persistence (needs its own eval budget) or a mandatory-bloat refactor (broad).

## fire 7 · 2026-06-21 · skill v2.0 · ea75ca36 (file_edit's code-edit intent; the EXPOSURE CHAIN COMPLETE)
meta: value-class=new-capability · pkg=@muse/tools · kind=write-intent-gate · verdict=PASS · firesSinceDrill=7
ratchet: testFiles 1065→1065 (+1 case tools.test, mutation-valid) · fabrication 0 · file_edit exposure fixed(probed) · eval:computer-task PASS(no regression) · eval:multifile-fix still FAIL(not exposure, the 12B's own multi-step behavior) · pnpm check clean(a LINE-webhook flake confirmed isolated 854/854) · lint clean
- What: handling fire 6's REMAINING(a) — file_edit (write-risk) was blocked from a code-fix prompt by the `write_without_mutation_intent` gate's `isWorkspaceMutationPrompt` (only a workspace-object vocabulary). FIX: added code-edit vocabulary to all 3 hint lists (workspace/target += file/source/code/bug/function+KO, mutation += fix/debug, KO += 고쳐[fix]). file_edit is now exposed (probed); tasks.add is still blocked by the relevance gate.
- Why: the last piece of the exposure chain — fires 4 (starvation) · 6 (keyword) · 7 (write-intent) together make file_grep/read/edit/run_command all reachable for a code-fix task. The multifile eval still FAILs, but now purely on 12B multi-step behavior (uses only file_read and stops) — not something a tool-filter can fix.
- Review notes: mutation-valid (reverting turns it RED, all 3 hint dimensions are needed). The ④b judge PASS + an honest residual: the relevance backstop doesn't leak for fix/debug, but a homonym ("add a function to the file") can leak into tasks.add/calendar.create (a pre-existing keyword overlap, bounded by the approval-gate=exposure≠write) — flagged and honestly recorded that my "fully blocked" claim was overstated.
- Risk: low — the write-intent gate itself is unchanged (only vocabulary expanded), a pure-read prompt is still blocked, approval/path-safety/fabrication=0 unchanged. The add/create leak is pre-existing + approval-bounded.
lesson: exposure has 3 layers (starvation · relevance-keyword · write-intent), and even fixing all 3, 12B multi-step behavior is a separate ceiling — measure-first isolated that ceiling as model-behavior ("exposure is fixed but it still FAILs"). The ④b caught the maker's own overstated safety claim (the relevance backstop) → honestly recorded (the judge gates scope-honesty too).

## fire 6 · 2026-06-21 · skill v2.0 · 0832ff97 (code-task tool keywords; multi-file exposure raised, a 3-fire merge)
meta: value-class=new-capability · pkg=@muse/tools+@muse/fs · kind=tool-relevance/keywords · verdict=PASS · firesSinceDrill=6
ratchet: testFiles 1062→1062 (+2 cases tools.test, mutation-valid) · fabrication 0 · eval:multifile-fix exposure raised(file_grep,context→file_read+run_command) · eval:computer-task PASS(no regression) · pnpm check exit 0 · lint clean
- What: measure-first on `eval:multifile-fix` ("run the test, fix the bug") FAIL found the root cause: `run_command` had **zero keywords**, giving it zero relevance to a run/test prompt → starved (never even exposed); the file tools also lacked code-fix verbs. FIX (sibling-audit): added code/run keywords to run_command + file_read/grep/edit/multi_edit. Multi-file exposure improved (file_read+run_command are now exposed), single-file unaffected.
- Why: a multi-file measurement revealed a new deterministic gap (run_command with zero keywords=unreachable) — fire 4 (starvation) and fire 6 (keyword) are two layers of tool-exposure. Unblocked the eval:multistep binary (muse-runner) by copying it from main.
- Review notes: mutation-valid (a 0-keyword run_command drops out of the cap=RED, a keyworded one survives; fixed after the ④b's finding-1 flagged an un-exercised cap). IrrelAcc (over-firing on common words) isn't a harm since it's approval-gated (④b), mitigated further by removing build/script keywords. REMAINING: file_edit is still blocked by isWorkspaceMutationPrompt (a workspace-object vocab that doesn't recognize code-edit) + a 12B multi-step issue (uses only read, never progresses) → decomposed.
- Risk: low — keyword-additive only, write tools aren't over-exposed (the mutation-gate stays), single-file unaffected. ④b PASS.
lesson: widening measure-first to a *different eval* (multifile) surfaces a new deterministic gap — a 0-keyword run_command is a blatant "the tool can't even be reached" bug. The ④b caught a weak test (an un-exercised cap) and got it fixed into a mutation-valid one (the judge gates the maker's test quality too).

## fire 5 · 2026-06-20 · skill v2.0 · 0d3ef486 (the top item is DONE; bloat = a deliberate decompose)
meta: value-class=refactor(work-list) · pkg=tools(scoping) · kind=decompose-design-sensitive · verdict=N/A · firesSinceDrill=5
ratchet: testFiles 1062→1062 · fabrication 0 · eval:computer-task PASS (the fire-4 fix holds, no regression)
- What: fire-4 deterministically FIXed the top ★ (wrong-tool, eval pass^3 3/3), so the next candidate = the residual bloat (time/math/regex always-on as domain="core"). Scoped it in code: 6 time tools are core in `muse-tools-time.ts`, and math/regex are scattered across several files too → a **broad, design-sensitive refactor** requiring re-tagging 6+ files + keyword coverage (DEFAULT_DOMAIN_KEYWORDS has no math/time/text) + cross-surface verification.
- Why no code slice: **there's no currently-measured failure** (fire-4's reserve already made the eval PASS) — per measure-first, don't force a speculative broad refactor into an auto-fire absent a measured failure. It's design-sensitive (deciding which utility is genuinely "always reachable" needs judgment) + demoting time/math risks hiding them when actually needed → recorded as a DELIBERATE decompose (backlog).
- Review notes: avoided repeating fire 3's "premature exhaustion" mistake (fire 4 proved digging deeper can find a clean fix), but confirmed in code that this bloat genuinely IS broad+design-sensitive+has-no-measured-failure (6+ files, a keyword-absence trap). Next measure-first candidate = eval:multifile-fix (exploring multi-file failure).
- Risk: 0 — no code changed, a precise decompose recorded + confirmed the fire-4 fix has zero regression.
lesson: the difference between fire 3 (premature exhaustion) and fire 5 (a legitimate decompose) = whether the scope was *confirmed in code*. Fire 3 assumed it was "fuzzy" (wrong); fire 5 actually confirmed 6+ files · a keyword-trap · zero measured failure. Measuring + confirming scope is what distinguishes "dig deeper" from "deliberately defer."

## fire 4 · 2026-06-20 · skill v2.0 · a925a13e (a DETERMINISTIC fix SHIPPED — the eval flips FAIL→PASS)
meta: value-class=new-capability · pkg=@muse/agent-core · kind=tool-exposure/starvation-fix · verdict=PASS · firesSinceDrill=4
ratchet: testFiles 1060→1060 (+3 cases tool-filter.test) · fabrication 0 · **eval:computer-task 2/2 STABLE FAIL → PASS** (the model now does file_grep→read→edit) · pnpm check exit 0 · lint clean
- What: on Jinan's "keep looking for it," dug deeper and overturned fires 1-3's "fuzzy" conclusion — **found and fixed a deterministic structural bug**: the always-on MANDATORY 10 (math_eval/regex_extract/time_add/context×3/skills×3) exceed cap=6 → `capToolsByRelevance`'s `remaining=0` branch drops ALL optionals → the file tools become *invisible* (the model literally can't see them, so it can never pick them). FIX (`tool-filter.ts`): (1) a reserve (FLOOR=3, irrelevant ones still dropped) for positively-relevant optionals (2) a FILE_PATH_RE boost (a path in the prompt boosts the files-domain +3 → the file cluster rises into the reserve).
- Why: measuring one layer deeper (counting mandatories + checking cap survival) revealed it wasn't a "fuzzy ranking" problem but a deterministic bug: "always-on clutter starves the task tool." The theme's premise (a clean deterministic fix) was correct — fires 1-3 just hadn't diagnosed deep enough.
- Review notes: 3 deterministic unit tests (starvation rescue · irrelevant-still-dropped · path-boost all-3-files) RED-on-old. The eval flipped 2/2→PASS (using file_grep, read, edit). The independent adaptive Opus ④b judge PASS (zero over-exposure, a harmless URL false-trigger, invariants unchanged). Addressed the judge's note: **confirmed pass^3 = 3/3 STABLE PASS** (durable, not flaky; each run uses file_grep→read→edit).
- Risk: low — only optional re-ranking/reserve changed (mandatory/recent/risk unchanged), only relevant tools are admitted. ④b PASS.
lesson: a "fuzzy/exhausted" conclusion can be *diagnostic depth insufficiency* — one more layer of measurement (mandatory count + cap survival) revealed what looked fuzzy was a clean deterministic bug. Jinan pushing "keep looking" was right. Measure-first has to go *deep enough* to confirm the premise (a deterministic fix is possible).

## fire 3 · 2026-06-20 · skill v2.0 · 54e86fee (a PRECISE root-cause + an honest theme-wall; a 3-fire merge)
meta: value-class=refactor(work-list) · pkg=agent-core(diagnosis) · kind=root-cause-final · verdict=N/A · firesSinceDrill=3
ratchet: testFiles 1060→1060 · fabrication 0 · eval:computer-task 2/2 STABLE FAIL (root-caused, not yet fixed)
- What: while starting decompose (c), **confirmed a precise root-cause**: the 6 `muse.context.*`·`muse.skills.*` tools are all **domain="core" → isMandatoryTool=true** (always-on, cap-protected). So *every* turn (even a file-fix task) exposes 20 tools, 6 of which are permanent distractors. The 12B, facing 20 tools (3× tool-calling.md's ≤5-7), picks a prominent skills/context tool instead of a file tool.
- Why no code slice: keeping skills/context as core was *deliberate design* (the model should always be able to call skill/context) — not a misclassification. So the fix is either (1) intent-classification ("this is a file-task → hide skills") or (2) a path-mention boost (raises file-tool ranking, but the mandatory distractor remains) — **both fuzzy + OUTCOME-stochastic** (each eval run ~7min×pass^k). Different class from the theme's premise ("deterministic repair" — literal-\n · scope-default — already exhausted). Forcing a stochastic slice at the tail of a huge session violates the marginal-value floor → honestly recorded instead.
- Review notes: fires 1-3 narrowed from measure-first→STABLE confirmation→confirmed-in-production filter→a precise root-cause (the mandatory-core classification). The next step is *deliberate* (dedicated time, an adaptive judge safety net) — territory an auto-loop handles poorly. Reported to Jinan.
- Risk: 0 — no code changed, diagnosis completed + a 3-fire docs merge.
lesson: both themes (core-hardening, computer-control) hit the same wall — after exhausting the clean/deterministic vein, what remains is fuzzy/stochastic/slow. An auto-15min-loop is best suited to clean deterministic slices; the fuzzy-stochastic-slow territory needs deliberate human-paced work. Measurement reveals this boundary (a premise ≠ what's live).

## fire 2 · 2026-06-20 · skill v2.0 · 8ea83aab (a root-cause investigation, DECOMPOSE step a+b)
meta: value-class=refactor(work-list) · pkg=agent-core(investigation) · kind=root-cause-analysis · verdict=N/A · firesSinceDrill=2
ratchet: testFiles 1060→1060 · fabrication 0 · eval:computer-task 2/2 FAIL (STABLE wrong-tool)
- (a) STABLE confirmed: 2/2 runs picked the same wrong tool (skills/context, zero file calls). (b) Production actually filters (planForContext→capToolsByRelevance) — the eval goes through the same path = a relevance-ranking gap. (Corrects fire-1's "raw assembly exposure" claim.)
- lesson: a new axis's "deterministic repair" premise can be at odds with what a live failure actually shows — measurement corrects the premise. A stochastic-verification fix gets its own dedicated fire budget.

## fire 1 · 2026-06-20 · skill v2.0 · ee635ab0 (measure-first diagnosis + DECOMPOSE)
meta: value-class=refactor(work-list) · pkg=scripts/eval · kind=measure-first-diagnosis · verdict=N/A · firesSinceDrill=1
ratchet: testFiles 1060→1060 · fabrication 0 · eval:computer-task 1 run FAIL (wrong-tool selection)
- Bootstrapped (worktree/install/baseline) + a measure-first eval → while fixing an add-bug, the 12B called only skills/context (zero file calls). A wrong-tool-selection finding → refined as a ★ finding into the backlog.
- lesson: a new loop's fire 1 refining stale backlog by measure-first evaling the *current* failure is the honest approach.
