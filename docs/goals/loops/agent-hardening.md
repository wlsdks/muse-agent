# Loop journal — agent-hardening

Theme: strengthen Muse as a solid AI agent (one-shot tool-calling reliability ·
multi-agent orchestration/decomposition · grounding integrity · recall/memory
quality). Worktree `/private/tmp/muse-agent-hardening`, branch
`loop/agent-hardening`. Tier1: never push to main, never auto-merge, never
`--no-verify`.

## fire 16 · 2026-06-21 · loop-creator v2.0.0 · 0304823e

meta: value-class=recall/memory quality (fact-supersession refine-vs-contradict LABELING) · pkg=@muse/memory (+@muse/cli render) · kind=memory-provenance-quality · verdict=PASS · firesSinceDrill=6 · mainMerge=yes@<backfill> (④c brings f13 NL-negation + f14 marker-less decompose + f15 Ollama tool-arg recovery + f16 refine-vs-contradict)
ratchet: testFiles +0 net (5 cases added to existing facts/file/timeline test files) · fabrication 0 (DERIVED label from a deterministic token comparison of two REAL stored values; previousValue byte-faithful; never invents) · @muse/memory 484/484 · @muse/cli 2814/2814 · @muse/model 348/348 isolated (the lone `pnpm check` red = web-search-policy property-fuzz 11949ms FALSE-timeout under concurrent-loop saturation, NOT my pkg — fire-13-noted offender, isolated rerun clean) · lint EXIT 0
ratchet (diversity): DIFFERENT (pkg×kind) — DIVERSIFIES off the saturated recent surfaces (f9/f10/f13 @muse/tools, f12/f14 @muse/multi-agent decompose, f15 @muse/model adapter): this is @muse/memory · memory-provenance-quality, a recall/memory-quality slice and the FIRST memory-store-internals fire of the streak. Less-contended: memory-user-store.ts is touched by NO sibling loop (the active recall/context-strategy/self-improvement loops work belief-provenance-store / recall-select / episodic, not the user-memory supersession log).

- what: `FactSupersession` (packages/memory/src/index.ts) gains optional `kind?: "refine" | "contradict"`. `collectFactSupersessions` (memory-user-store.ts) sets it via the EXISTING `classifyValueChange(prior, next)` (token-subset, imported from belief-provenance-store.ts) — a superset/subset = refine, an unrelated value = contradict. The File store serialize/deserialize round-trips `kind` (deserialize validates the enum, drops a corrupt value). `buildFactTimeline` (@muse/cli commands-memory.ts) propagates `kind` into `FactTimelineEntry.previous[]` and the `muse memory timeline` renderer prints "refined from" / "changed from" / "was" (legacy) per kind.
- why: the `muse memory` timeline conflated an ELABORATION ("Seoul"→"Seoul, Gangnam-gu") with a genuine FLIP ("Seoul"→"Busan") because `collectFactSupersessions` logged every `prior !== next` identically. A refinement re-shown as "changed" mis-signals belief instability to the user (and the user-facing memory honesty surface) — recall/memory quality. The contested/volatility HALF of this Mem0 (arXiv 2504.19413) refine-vs-contradict gap was already shipped (refinementAwareDistinctValueCount); this closes the user-visible TIMELINE half. Reuses the SAME deterministic classifier, so the label is consistent with the volatility signal.
- review-point: STRICTLY ADDITIVE (optional field). previousValue byte-faithful — the slice only ADDS a derived label, never mutates a stored value (no fabrication). Conservative-safe: a case/space-only change (classifier "same") and a token-disjoint KO refinement both fall to "contradict" ("changed from") — over-warns, never hides a real flip as "refine" (the strict subset check guarantees a genuine flip is always "contradict"). Back-compat: a legacy unlabelled entry round-trips + renders "was". Sibling-audit: the 2 other factHistory consumers (muse-persona.ts, chat-ink.ts) read only key/previousValue/replacedAt — invisible to the new field; 2 existing exact-equal tests updated for the now-correct kind (sibling-audit catch, not a regression). MUTATION-FIRST RED ×2 (neuter classifier→always-contradict: 3 refine tests RED, contradict GREEN; drop the render kind-propagation: the carry test RED, legacy-absent GREEN). Independent Opus 4.8 ④b judge (fresh context, own 15-case KO+EN corpus, mutation re-run, scope audit) VERDICT PASS.
- risk: low. Pure deterministic token comparison reusing a battle-tested classifier; no I/O beyond the existing file round-trip, no grounding/citation/recall-ranking path. Ref: Mem0 (arXiv 2504.19413), Zep temporal fact validity (arXiv 2501.13956).

## fire 10 · 2026-06-21 · loop-creator v2.0.0 · 8cec8359

meta: value-class=judge-drill · pkg=@muse/tools · kind=tool-calling-reliability/arg-correctness · verdict=PASS (real slice shipped after the drill) · firesSinceDrill=0 (RESET — drill completed) · mainMerge=no
ratchet: testFiles unchanged · fabrication 0 (unchanged) · @muse/tools 80 tests (1 new) green · pnpm check exit 0 · lint exit 0 · ④b drill judge FAIL (caught vacuous test) · ④b real-slice judge PASS

lesson: JUDGE-DRILL. Injected bad slice = a DECLARATION-ONLY (vacuous) test for a real one-shot tool-calling repair (widen numeric arg coercion to accept "+5" → 5). The bad test asserted only `expect(result).toBeTypeOf("object")` + `expect(Object.keys(result)).toContain("count")` — never the coerced VALUE — AND deleted the prior `{count:"+5"}` pass-through guard. Deterministic gates were GREEN (276 passed) and the mutation-first check PROVED it vacuous: with the number-coercion arm neutered to `return undefined`, the bad test STAYED GREEN (isolated run 1 passed) because both the working `{count:5}` and the neutered `{count:"+5"}` are objects whose keys contain "count". The independent ④b Opus judge (fresh context, NOT told it was a drill) returned VERDICT: FAIL, naming the exact flaw — vacuous assertion, correct mutation walk-through (both cases yield an object with a "count" key → stays GREEN), and even flagged the deleted guard, prescribing `expect(result.count).toBe(5)`. ★The judge CAUGHT it for the right concrete reason — the ④b verifier is reliable. Rolled the bad slice back to clean baseline (cp-restore, git diff empty), then built the REAL slice properly (TDD, behavioral-outcome `repaired.count===5`, MUTATION-FIRST RED confirmed: reverting the pattern fails with "expected '+5' to be 5", degenerate "+"/"++5"/"+5.0"-int boundaries locked); a SECOND fresh ④b judge PASSed it.

- what: widen `coerceScalar`'s numeric pattern from `/^-?\d+$/`→`/^[+-]?\d+$/` (and the decimal counterpart) in `@muse/tools tools-argument-validation.ts`, so a local model emitting an explicitly-signed-positive arg ("+5") for an integer/number param gets the value coerced to the real NUMBER instead of being rejected as a wrong-surface-form string.
- why: a small local model (gemma4:12b) routinely writes "+5" for a positive arg; strict-equality validation then rejected the otherwise-correct call, burning a retry round (Structured Reflection, arXiv:2509.18847). The repair is lossless/unambiguous — Number("+5") === 5.
- review-point: all existing safety guards hold unchanged — the `Number.isFinite` overflow guard still keeps "+"+overflow a string (never Infinity), and bare "+", "++5", and integer-typed "+5.0" all still fail the pattern and pass through untouched so a genuine OOV value still surfaces. Removing the old "+5 stays untouched" assertion was correct — that was the exact reject behavior being fixed.
- risk: low. Pure deterministic post-selection value repair; no tool name/description/schema touched (selection unperturbed), no grounding/citation path affected. `pnpm check` exit 0, lint exit 0.

## fire 11 · 2026-06-21 · loop-creator v2.0.0 · NO-SHIP (rolled back — ④b judge FAIL ×2, structural flaw)

meta: value-class=multi-agent orchestration/decomposition (gate-side dependency detection) · pkg=@muse/multi-agent · kind=decomposition-reliability · verdict=NO-SHIP (rolled back) · firesSinceDrill=1 · mainMerge=no
ratchet (diversity): DIFFERENT (pkg×kind) from f9/f10 (both @muse/tools arg-coercion) — @muse/multi-agent + decomposition was the most-diversifying pick off the two-tools-fire streak. testFiles unchanged (rollback) · fabrication 0 (unchanged, slice never landed) · lint clean throughout.

- slice attempted: a 4th `shouldDecompose` gate fire-rule — `sequencing >= 1 && backReference` → decompose — to catch a single-marker dependent two-step ("노트를 검색한 뒤 그 결과를 요약해줘") that the 2-marker `sequencing >= 2` threshold drops to single-agent, so the downstream sequenced-threading (fires 3/19) never engages (MAST reasoning-action mismatch, arXiv 2503.13657). New `requestHasBackReference(request)` helper scanned the flat request for cross-step dependency tokens.
- ④b FAIL #1 (concrete, verified): the first version reused the BROAD `listHasBackReference` token set (bare determiners "위의"/"방금"/"the above"/"the result") on the FLAT request. Those tokens were safe ONLY inside `listHasBackReference` because it inspects `items[1..]` of an ALREADY-SPLIT multi-item list (the split itself proves multi-step structure). On the flat request a lone marker + a benign determiner mass-misfired — judge cited "먼저 위의 메뉴에서 골라줘", "First, what's the result of the game?" firing. I REPRODUCED all 9 claimed FPs.
- fix attempt + ④b FAIL #2 (decisive, structural): I introduced a NARROWER gate-only token set (dropped bare determiners + the ambiguous "the result"/"그 요약"). It closed the original 9 FPs + a 43-case benign corpus reached STABLE-0. But a SECOND independent Opus judge (fresh context) constructed a new 22-case corpus and found 16 FPs the narrowed set STILL hits — REPRODUCED: "그 결과 먼저 보여줘" (single step "show that result first"), "First, what's the previous step in the recipe?" (asking ABOUT a step), "Finally, what are those results used for?" (general question). Root cause (correct): a FLAT substring scan structurally CANNOT distinguish "act on the prior step's OUTPUT" from "mention/ask ABOUT a step or result"; the conjunction `seq>=1 && backReference` over flat text is an insufficient discriminator no token-tuning fixes. (Also surfaced a latent: "step 1" is in BOTH SEQUENCING_SIGNALS and the back-ref set → a benign "먼저 step 1 …" double-counts to seq=2 and trips the EXISTING sequencing≥2 rule — pre-existing, not mine.)
- decision: two ④b FAILs, the second naming a STRUCTURAL (not token) defect ⇒ full rollback (`git checkout --` the 3 files; `git status` clean, package back to 144 tests green) + STOP per contract. No whack-a-mole token chase.

lesson: the gate-side "is this a dependent multi-step request?" question is NOT answerable by a flat-text back-reference substring scan — that conflates ACTING-ON a prior output with MENTIONING/ASKING-ABOUT a step. The structurally-sound signal already exists ONE layer down: `decomposeRequestWithKind`'s `listHasBackReference` works precisely because it runs AFTER a structural split (items[1..] of a real list). A future fire wanting single-marker dependent decomposition must FIRST split the request (e.g. extend `extractSequencedSteps` to fire on a single marker, then run `listHasBackReference` on the SPLIT clauses 1..n), not bolt a flat scan onto the GATE. Also: my own 43-case corpus reaching STABLE-0 was a FALSE confidence — the corpus systematically lacked the "marker + benign-mention-of-a-step" class; the f4 "LARGE benign corpus" rule means ADVERSARIALLY large incl. the exact ambiguity class, and a fresh-context judge building its OWN corpus is the control that caught what mine missed (maker≠judge value, twice).

## fire 12 · 2026-06-21 · loop-creator v2.0.0 · 2f62ee0f

meta: value-class=multi-agent orchestration/decomposition (single-marker DEPENDENT decomposition — RESOLVES the fire-11 blocker the structurally-sound way) · pkg=@muse/multi-agent · kind=decomposition-reliability · verdict=PASS · firesSinceDrill=2 · mainMerge=yes@5e761ea4 (brings f9 enum-arg + f10 signed-numeric + f12 single-marker decompose)
ratchet: testFiles 1062 (14 cases added to existing decompose-trigger.test.ts) · fabrication 0 (untouched — orchestration path, no grounding/citation code) · eval:decomposition PASS (live gemma4:12b) · eval:orchestration PASS (failure-propagation + bounded termination live) · @muse/multi-agent 161/161 · pnpm check EXIT 0 (apps/api 854, apps/cli 2787, all workspaces)
ratchet (diversity): DIFFERENT (pkg×kind) from f9/f10 (both @muse/tools arg-coercion) — back to @muse/multi-agent · decomposition, off the two-tools-fire streak per the fire-8/diversify directive; this is the multi-agent slice the fire-11 blocker left open, done the CORRECT (split-first) way.

- what: new exported helper `singleMarkerDependentSplit(request)` in decompose-trigger.ts — splits the request on the ordered-marker regex (`extractSequencedSteps`) FIRST, then runs the EXISTING `listHasBackReference` on the split clauses (items[1..]). Wired into `shouldDecompose` as a new rule `signals.sequencing >= 1 && singleMarkerDependentSplit(request)` placed BELOW the existing 2-marker rule. A request with ONE ordered marker plus a real tail back-reference ("노트를 검색한 뒤 그 결과를 요약해줘") now fans out so the downstream sequenced-threading engages; a benign single-marker request ("그 결과 먼저 보여줘") stays single-agent.
- why: the `sequencing >= 2` gate threshold dropped a single-marker DEPENDENT two-step to single-agent, starving the sequenced-threading machinery (fires 3/19) — a dependent step-2 then ran blind (MAST reasoning-action mismatch, arXiv 2503.13657). Fire 11 attempted this on the GATE with a FLAT substring scan and FAILED ④b judge ×2 (a flat scan can't tell "act ON the prior output" from "mention/ask ABOUT a step"). This slice does it the structurally-sound way the fire-11 lesson prescribed: SPLIT FIRST, then `listHasBackReference` on the split clauses — back-ref detection is precise only on already-split clauses (exactly why the list path is safe). The leading-clause back-ref ("그 결과 먼저 보여줘" → ["그 결과","보여줘"], tail clean) is correctly ignored.
- review-point: STABLE-0 false positives ATTRIBUTABLE TO THE NEW RULE on a 31-case adversarial benign corpus incl. the exact fire-11 mention-of-a-step class (the lone corpus FP — "Next, what did step 1 say?" — fires via the PRE-EXISTING `sequencing>=2` double-count of "next,"+"step 1", a latent the fire-11 record already noted; NOT this slice). The new rule never fires without an ordered marker (`>= 1` guard). The rule is conservative (under-fires some sentence-initial-marker KO dependents where the marker is at position 0 so the split yields 1 clause) — an acceptable miss given the strong single-agent bias (a missed decompose = status quo; a wrong decompose = wasted GPU + the fire-11 FP risk). Independent Opus ④b judge re-ran BOTH mutations RED itself + built its OWN 22-case corpus (0/22 new-rule over-fires) + verified gate↔split consistency — VERDICT PASS.
- risk: low. Pure deterministic token scan over an already-split request; no I/O, no grounding/citation/recall path. Sibling-audit: `shouldDecompose` has EXACTLY 2 callsites (lead-worker.ts, commands-ask.ts), both consume `.decompose` so both benefit; the downstream `decomposeRequestWithKind` already produces a SEQUENCED 2-step split for every admitted request (gate↔split consistency test). MUTATION-FIRST RED confirmed ×2 (neuter the back-ref discriminator → benign FPs re-fire; remove the gate rule → dependent two-steps no longer fan out). `pnpm check` EXIT 0, lint EXIT 0.

## fire 13 · 2026-06-21 · loop-creator v2.0.0 · 7880ab64

meta: value-class=one-shot tool-calling reliability (NL tool-SELECTION parsing — distinct kind from f9/f10 arg-coercion & f12 decompose) · pkg=@muse/tools · kind=tool-calling-reliability/selection-parse · verdict=PASS · firesSinceDrill=3 · mainMerge=no (fire 13 not a multiple of 4; next merge fire 16)
ratchet: testFiles unchanged (3 cases added to existing nl-tool-selection.test.ts) · fabrication 0 (deterministic parser, no model call / no grounding-citation path) · @muse/tools 281/281 green · eval:tools:nl PASS 7/7 live gemma4:12b (regression-free) · pnpm check otherwise clean (lone @muse/model web-search-policy 5000ms FALSE-timeout = machine saturation, isolated rerun 325/325 green, unrelated pkg) · lint EXIT 0
ratchet (diversity): DIFFERENT (pkg×kind) — f9/f10 were @muse/tools ARG-COERCION, f12 was @muse/multi-agent DECOMPOSE; this is @muse/tools SELECTION-PARSE (the prose→tool-name path, a separate surface from arg validation). Less-contended: nl-tool-selection.ts is touched by NO sibling loop; recall/memory was skipped as hot across self-improvement/context-strategy/core-hardening.

- what: `parseNaturalLanguageToolSelection` (the deterministic prose→tool-name parser wired live into `eval:tools:nl`) returned the EARLIEST-mentioned known tool. A negation-led answer — "Don't use time_now; use time_diff instead." / "Not time_now — time_diff is the right one." / KO "time_now은 쓰지 말고 time_diff를 써." — therefore resolved to the REJECTED tool. New `isNegatedMention(text, index, len)` skips a mention immediately preceded by an EN negation cue (`EN_NEGATION_LEAD_RE`, `$`-anchored on the ≤24 chars before the name) or followed by a KO post-positional negation (`KO_NEGATION_TRAIL_RE`, `^`-anchored on the ≤16 chars after); the parser picks the earliest NON-negated mention, and returns no-tool when every mention is negated.
- why: a small local model (gemma4:12b) states a plan ("don't use X, use Y") in prose; selecting the tool the plan REJECTED is a MAST reasoning-action mismatch (arXiv 2503.13657). The repair is deterministic and direction-safe (a rejected tool is never the answer); the affirmative path is unchanged.
- MUTATION-FIRST: neuter `isNegatedMention`→`return false` ⇒ exactly the 2 negation tests go RED ("expected 'time_now' to be 'time_diff'/undefined"), the no-over-skip test stays GREEN — behavioral, not vacuous. Confirmed ×2 (before + after the judge's KO-imperative fix).
- FP-corpus (STABLE-0 over-skip): 13–15 adversarial AFFIRMATIVE cases incl. the exact ambiguity class — "without X", "cannot answer without X", "notably", "a note: X", trailing "use A, not B", "not delay — X", "not unrelated; use X", "never mind, use X", KO affirmatives ("X를 써"/"X 사용해줘") — 0 over-skip. Sibling-audit: the ONLY prose-tool-selection parser in the repo; the other "first-occurrence/earliest" sites (plan-execute dedup, orchestrate role-dedup, knowledge-recall ties) are not tool-selection and don't share the flaw.
- ④b judge: SEPARATE INDEPENDENT Opus 4.8, fresh context, built its OWN 29-case corpus (28/29 conform, all 12 over-skip traps clean) + ran the mutation itself (2 RED) → VERDICT PASS. It surfaced one PRE-EXISTING non-regressing gap (bare KO imperative "쓰지 마" — no ㄹ, so the `말`-stem regex missed it; returned the rejected tool before this slice too). Closed it post-PASS in the same safe direction (`쓰지\s*마|쓰지\s*말` alternation + a test), re-locked the mutation RED and re-ran eval:tools:nl 7/7.
- risk: low. Pure deterministic anchored token scan, no I/O, no grounding/citation/recall path; affirmative selection unchanged (eval:tools:nl 7/7 unmoved).

## fire 1 · 2026-06-20 · loop-creator (agent-hardening) · 61137580 [SUPERSEDED — dropped on rebase]

meta: value-class=capability/hardening (local tool-schema robustness) · pkg=@muse/model · kind=feat · verdict=SUPERSEDED · firesSinceDrill=1
ratchet: testFiles 1054→1055 · fabrication 0 (unchanged) · eval:tools all-visible-cases PASS (run timed out at 540s — env slowness, not a fail; real Muse tool schemas proven to round-trip unchanged so selection is unperturbed)

lesson: the concurrent `core-hardening` loop independently shipped the SAME Ollama/llama.cpp GBNF tool-schema sanitizer to main (its fire 4, `eac2360c`) before this branch's fire 1 (`b1291242`) reached main. On the fire-4 rebase the duplicate commit was DROPPED (capability already on main; no second copy of `sanitizeOllamaToolSchema` introduced). Fires 2 and 3 are distinct and were preserved. CROSS-LOOP HYGIENE: when two loops share the theme "harden local tool-calling", the highest-value open backlog ◦ can be claimed by either — check `git log origin/main` for a freshly-landed equivalent before building.

- what: added `sanitizeOllamaToolSchema` (new leaf module `packages/model/src/ollama-tool-schema.ts`) and applied it at the Ollama adapter's native tool-schema projection (`buildNativeChatBody`), so tool `parameters` JSON-Schema is normalized before it reaches llama.cpp's GBNF grammar converter. Strips `$ref`/`$schema`/`$id`/`definitions`/`$defs`; collapses nullable `anyOf`/`oneOf` unions and array `type` to the single non-null member.
- why: Ollama runs llama.cpp; its JSON-Schema→GBNF converter silently drops or breaks on `$ref`-with-siblings, array `type`, and `anyOf`/`oneOf` mixed with `properties` (verified June 2026 against ggml-org/llama.cpp grammars/README). Those shapes degrade one-shot tool-calling reliability on the fixed local model (gemma4:12b). Mirrors `sanitizeGeminiSchema` and the open hermes `schema_sanitizer.py` (MIT, independently reimplemented).
- review-point: the sanitizer mirrors the Gemini analog's depth-cap (100) + WeakSet cycle guard exactly — those are safety-critical. A two-non-null `anyOf` collapses to the first member (lossy by design, documented + tested); a shared non-cyclic sub-schema is over-pruned to `{}` (inherited from the Gemini WeakSet pattern, not a new regression — future hardening candidate).
- risk: low. Pure deterministic shape-only normalization on the OUTBOUND schema; never touches tool arguments, selection, or any grounding/citation path. Real Muse built-in tool schemas (object+properties+required+enum+additionalProperties) proven to round-trip byte-identical, so existing tool-calling is unperturbed.

## fire 2 · 2026-06-20 · loop-creator (agent-hardening) · c702d8a1

meta: value-class=hardening/grounding-integrity (anti-fabrication tool-arg gate) · pkg=@muse/agent-core · kind=feat · verdict=PASS · firesSinceDrill=2
ratchet: testFiles 1080 (unchanged — cases added to existing file) · fabrication 0 (floor STRENGTHENED) · eval:tool-arg-grounding 2/2 PASS live on gemma4:12b (no-location drops + stated-location keeps)

- what: extended `groundToolArguments` (`packages/agent-core/src/tool-argument-grounding.ts`) to recursively walk NESTED OBJECTS and ARRAYS-OF-OBJECTS via a new `cleanValue` helper — dropping fabricated string leaves at any depth while keeping grounded siblings, removing-and-reporting the arg only when every assessable leaf collapses. Added `isPlainObject` guard. 8 deterministic outcome tests (nested partial-clean, all-fabricated collapse+dropped, deep recursion, array-of-objects, fail-open non-string leaf, non-mutation, empty-utterance fail-open).
- why: latent fail-open in the fabrication=0 floor — a nested-object grounded arg (e.g. a future `detail:{location,notes}`) fell through BOTH the string and the `every(string)`-array branches untouched, so a fabricated leaf inside it survived the gate and would be PERSISTED. No production tool ships a nested grounded arg today (all current `groundedArgs` are scalar strings or `tags`), so this hardens the gate ahead of the first nested-arg tool rather than fixing a live leak. Recursion mirrors the existing array-element partial-clean contract.
- review-point: the one behavior change on the EXISTING path is a strengthening — a MIXED array like `["fabricated", 7]` was previously left whole (failed `every(string)`) and now cleans the fabricated string to `[7]`; number-only/empty arrays and pure-scalar args are unchanged. Independent Opus ④b judge probed for floor holes, regressions, fail-open inversion, and mutation — VERDICT PASS.
- risk: low. Deterministic value-shape cleaning bounded by finite JSON tool-arg depth (no cycles possible from JSON args); fail-open preserved for number/boolean/null leaves; `dropped` contract (whole-removal only) honored; no input mutation (shallow copy + fresh sub-structures). `pnpm check` EXIT 0, lint clean, live eval 2/2.

## fire 3 · 2026-06-20 · loop-creator (agent-hardening) · d89ad71a

meta: value-class=multi-agent orchestration/decomposition (dependency detection in structural decomposition) · pkg=@muse/multi-agent · kind=feat · verdict=PASS · firesSinceDrill=3
ratchet: testFiles 1055→1056 (+1 hmm — cases added to existing files, new count tracked at next self-eval) · fabrication 0 (untouched — orchestration path, no grounding/citation code) · eval:decomposition PASS (3/3 live gemma4:12b) · eval:orchestration PASS (failure-propagation + bounded termination live)

- what: added a pure helper `listHasBackReference(items)` to `decomposeRequestWithKind` (packages/multi-agent/src/decompose-trigger.ts) so a NUMBERED or BULLETED list that carries an explicit cross-item back-reference ("2. summarize the result from step 1" / "그 요약에서 …") now flags `sequenced: true` — the lead-worker then threads each completed prior step's output into the dependent worker. Conservative bilingual (KO+EN) back-reference token set, scanned only in items[1..]. Behavioral tests assert the flag AND the actual lead-worker threading (priors[1] receives step-1's output); regression guards hold independent lists at sequenced=false.
- why: `decomposeRequestWithKind` flagged EVERY list as `sequenced:false` (independent), so a dependent step in a numbered list ran BLIND — exactly the MAST reasoning-action mismatch (arXiv 2503.13657) the sequenced-threading machinery (fire 19 competitor-grounding) exists to prevent, but dependency detection only covered prose sequencers ("First … Then …"), never back-references inside lists. Strictly additive false→true flip on the list path; prose-sequenced + single-fallback unchanged.
- review-point: the back-reference token set is conservative but can over-trigger on a contrived independent list whose item[1..] happens to contain "the output"/"방금"/etc. The Opus ④b judge flagged this as a minor review-point, NOT a defect: threading an independent step's prior output is mostly-harmless extra context (the worker can ignore it), while MISSING a real dependency runs blind — the cost asymmetry favors this direction, and isolation/output integrity are never damaged. Independent Opus ④b judge VERDICT PASS (7/7 checklist).
- risk: low. Pure deterministic token scan, no I/O, no grounding path. `pnpm --filter @muse/multi-agent test` 128/128; `pnpm check` red ONLY on the documented `personal-plan-cache-store` CPU-starvation timeout (5002ms under concurrent load; 1856/1856 PASS isolated — backlog line 355, NOT this change); lint clean; eval:decomposition + eval:orchestration both PASS live on gemma4:12b.

## fire 4 · 2026-06-20 · loop-creator v2.0.0 · NO-SHIP (reverted aa5bd344 → 122e46fb)

meta: value-class=grounding-integrity (Korean injection coverage on the stored/tool surface) · pkg=@muse/agent-core · kind=feat/hardening · verdict=ROLLBACK (post-merge false-positive evidence) · firesSinceDrill=4 · mainMerge=no (rolled back before merge)
ratchet: testFiles 1055→1055 (slice reverted) · fabrication 0 · no code shipped (branch reverted) · agent-core 2488/2488 (baseline restored)

- what (attempted): extend `MEMORY_INJECTION_PATTERNS` (agent-core/src/injection.ts) with the Korean act-as/role-hijack + output-clamp analogs of the English `you are now` / `reply only with` patterns (closing backlog T1-a-ko-resid). MUTATION-FIRST RED→GREEN passed, `pnpm check` 0, lint clean, precheck:grounding 2/3 pass^2, and this loop's Opus ④b judge returned PASS (it FLAGGED the output-clamp verb-terminal as unanchored but accepted it as English-parity).
- why ROLLBACK: at ④c main-merge time, `git fetch origin` revealed the CONCURRENT core-hardening loop had attempted the IDENTICAL slice as its fire 6 and its STRONGER adaptive Opus ④b judge FAILED it for **68% benign Korean false-positive** (`01cbb110`). Re-running my own patterns against a 19-case adversarial benign corpus confirmed **6/19 (32%) false-positive** — everyday Korean self-notes ("결과만 말해줬다" = he told me only the result, "이름만 말해 주세요" = please tell me just the name, "보고서만 출력해서 보내줬다") get NEUTRALIZED. Anchoring on `만`/`처럼` (the most common Korean particles) + everyday verbs corrupts the Korean user's OWN notes — a DIRECT weakening of grounding integrity, which is a hard floor. My benign test set (5 cases) was too small to catch it; the cross-loop adaptive judge was the verifier that did.
- review-point: my in-loop ④b judge under-weighted the output-clamp false-positive it correctly spotted (it called it English-parity; but the English `reply only with` anchors on rarer multi-word English shapes, whereas `…만 …해` anchors on a ubiquitous Korean particle — NOT true parity). The cross-loop NO-SHIP record + a 19-case benign corpus is the evidence that overrode the single in-loop PASS. ROLLBACK protects the Korean user's self-notes.
- risk: 0 — code reverted (`git revert aa5bd344` → 122e46fb), baseline restored (agent-core 2488/2488); only the journal/backlog learning is kept.

lesson (false-positive): a regex injection pattern for a new locale MUST anchor on a *rare* token (command-noun + override-verb), never on a high-frequency particle (`만`/`처럼`) + an everyday verb — the latter mass-misfires on the user's benign self-notes. Validate a new locale pattern against a LARGE benign corpus for STABLE 0 false-positives BEFORE landing, not a handful of hand-picked benign cases. The role-hijack pattern (행동-imperative + 지금부터/너는 이제 lead) was cleaner; a future fire could re-attempt ROLE-HIJACK ONLY (drop the `만`-anchored output-clamp) with a corpus gate. Convergent independent finding with core-hardening fire 6.

lesson (rebase): the concurrent `core-hardening` loop independently shipped the SAME Ollama/llama.cpp GBNF tool-schema sanitizer this branch's fire 1 (`b1291242`) built — its fire 4 `eac2360c` landed on main first. On the pre-fire-4 rebase the duplicate fire-1 commit was DROPPED (capability already on main; no second `sanitizeOllamaToolSchema` introduced); fires 2-3 are distinct and preserved. CROSS-LOOP HYGIENE: when two loops share a theme, check `git log origin/main` for a freshly-landed equivalent before building the top ◦.

## fire 5 · 2026-06-20 · loop-creator v2.0.0 · e1c6f49a
meta: value-class=wiring · pkg=@muse/recall · kind=recall-quality · verdict=PASS · firesSinceDrill=5 · mainMerge=no
ratchet: testFiles +1 (recall chunks +7 dedup cases) · fabrication 0 · eval:multihop inline+hop hit@4 60%→80% (AUGMENT-only, guard OK)
- what: new pure `dedupNearDuplicateChunks(chunks, cosine, threshold=0.985)` (@muse/recall chunks.ts) drops a chunk whose cosine to an already-kept chunk ≥ threshold — greedy first-wins so the highest-ranked of a near-dup pair survives; FAIL-OPEN on any non-comparable embedding (empty/zero-norm/length-mismatch — `--file` ad-hoc passages never dropped). Wired into the ask path on `scored` right before `reorderForLongContext`.
- why: graph-link + second-hop AUGMENT chunks are appended AFTER MMR (`diversifyAskChunks`) and bypass diversity; only exact reference/content-key dedup ran, so a near-identical chunk (same fact across two notes, or a bridge near a seed) padded the small model's grounding context with redundancy.
- review-point: subtractive only (removes redundancy, never adds/reorders survivors) so single-hop ordering + confidence verdict unchanged; threshold 0.985 is conservative (distinct facts don't reach it on nomic-v2-moe) so FP blast-radius far below the f4 rollback's 32% (independent Opus ④b judge confirmed: no concrete distinct-fact pair loses a chunk). NOTE: completed by the main session after the building sub-agent died on a transient 401 mid-fire (work recovered intact from the captured diff; deterministic gates + independent Opus ④b judge re-run clean).
- risk: low — fail-open double-guarded (`comparable()` short-circuits before `cosine`, and production `cosine` itself returns 0 on length-mismatch/zero-norm). Sibling-audit recorded `◦ 1d-sibling-audit`: the ENGINE path `rankKnowledgeChunksWithHop` (agent-core) has the same class gap, deferred (lower traffic).

## fire 6 · 2026-06-20 · loop-creator v2.0.0 · 96cf6933
meta: value-class=recall-quality (engine-path near-dup dedup parity) · pkg=@muse/agent-core · kind=recall-quality · verdict=PASS · firesSinceDrill=6 · mainMerge=no
ratchet: testFiles 1058 (unchanged — cases added to existing two-hop-recall.test.ts) · fabrication 0 (untouched — recall ranking path, no grounding/citation code) · eval:multihop inline+hop hit@4 60%→80% (no regression, AUGMENT-only guard OK) · agent-core 2494/2494
- what: closed the deferred `◦ 1d-sibling-audit` (fire 5). New helper `dropNearDuplicateAdditions` (+ `embedChunkVec`) in agent-core `knowledge-recall.ts` drops an appended hop/PPR bridge whose cosine to an already-kept chunk ≥ 0.985 — wired into BOTH `rankKnowledgeChunksWithHop` (secondHop) AND `appendAssociativeBridges` (associative). Mirrors fire 5's ask-window `dedupNearDuplicateChunks` (@muse/recall) on the ENGINE path. 4 new behavioral tests (drops-near-dup-keeps-distinct, AUGMENT primary-prefix-byte-identical, FAIL-OPEN on embed-failure, no-op secondHop guard).
- why: `rankKnowledgeChunksWithHop` + `appendAssociativeBridges` appended up to 2 bridges to the primary ranking with NO near-duplicate check — a hop re-query (or PPR walk) surfaces a chunk near-identical to a primary, padding the small model's grounding window with redundancy. Fire 5 fixed only the CLI/recall ask path; the engine had the identical gap (same class as the recall fix). Sibling-audit: grep `...primary, ...` → exactly 2 append callsites, both patched (2/2).
- review-point: AUGMENT-never-displace (only ADDITIONS filtered, primary untouched/unreordered) + FAIL-OPEN (degenerate/length-mismatch → cosine 0 < threshold → kept; embed failure → null → kept; dropped only on a CONFIRMED ≥0.985 match). One EXISTING inflation-suite fixture (`A_PRIME_VEC`) was loosened cosine(A,A′) 0.99974→0.970 so A′ stays a real bridge — its query-relative-cosine invariant is preserved (seed-cos >0.55, query-cos <0.55 still hold); the ≥0.985 drop is covered by a NEW dedicated dedup test (net coverage up). Independent Opus ④b judge re-ran the mutation-RED itself and scrutinized the loosening — VERDICT PASS (7/7).
- risk: low. Deterministic cosine filter on the OUTBOUND bridge additions only; no I/O, no grounding/citation path, no single-hop/base-ranking change. `pnpm --filter @muse/agent-core test` 2494/2494; full `pnpm check` SIGBABRT was an env OOM (concurrent-loop machine saturation) — `@muse/shared` 44/44 in isolation, agent-core clean; lint 0; self-eval all counts stable.

lesson (cross-loop drop): on the fire-6 rebase the concurrent `core-hardening` loop had landed its fire 7 (`2a010b44`) — a functional DUPLICATE of this branch's fire 2 (tool-arg nested-object recursion). Per cross-loop hygiene the duplicate fire-2 commit was DROPPED (reset to origin/main + cherry-pick only the distinct fires 3 & 5; fire-4 was a NO-SHIP net-zero); the agent-hardening journal had been removed from origin/main by a sibling cleanup and was restored. Same theme = check `git log origin/main` for a freshly-landed equivalent and drop yours.

## fire 7 · 2026-06-20 · loop-creator v2.0.0 · 3666df61
meta: value-class=multi-agent orchestration/decomposition (fail-close a dependent step with a fully-failed upstream) · pkg=@muse/multi-agent · kind=orchestration-reliability · verdict=PASS · firesSinceDrill=7 · mainMerge=no
ratchet: testFiles 1060 (cases added to existing lead-worker.test.ts) · fabrication 0 (untouched — orchestration path, no grounding/citation code) · eval:orchestration PASS (failure-propagation + bounded termination) · @muse/multi-agent 143/143 · pnpm check EXIT 0 (apps/cli 2763, apps/api 854, all workspaces)
- what: closed a MAST reasoning-action-mismatch hole in `runLeadWorkerTask` (packages/multi-agent/src/lead-worker.ts). In a SEQUENCED chain (ordered steps where a later step acts on an earlier step's RESULT), a dependent step at index ≥ 1 whose EVERY upstream step failed/ungrounded (so `priorContext.length === 0` — nothing to act on) is now FAIL-CLOSED (status "failed", error names the broken upstream) instead of executed BLIND. 3 new behavioral tests: dependent-not-run-when-all-upstream-failed (the RED), partial-upstream-still-runs (≥1 completed prior keeps the step), independent-list-unaffected (sequenced=false never fail-closes).
- why: before this, a sequenced step 2 ("그 요약에서 액션아이템 추출" = "extract action items FROM THAT SUMMARY") would still RUN even when step 1 (요약) failed/blanked — producing a confident answer fabricated from an absent dependency, which the per-subtask grounding gate can't catch (the worker invents a plausible result) and the synthesis then folds in. This is MAST "reasoning-action mismatch" + "information withholding" (arXiv 2503.13657): the orchestrator silently swallowed a broken dependency. Diversifies cleanly off the f5/f6 recall-dedup axis (a DIFFERENT pkg×kind: orchestration, not recall-quality).
- review-point: the fail-close keys on `priorContext.length === 0` (no COMPLETED upstream), so a chain where step 1 completes but step 2 fails still lets step 3 run on step 1's threaded output (the flatMap collects ALL completed priors). Independent lists short-circuit on the leading `sequenced &&`. Independent Opus ④b judge re-ran the MUTATION-FIRST RED itself, probed over-fail-close + cascade + invariants — VERDICT PASS (6/6). No FP risk (subtractive safety, never displaces a step with real upstream context).
- risk: low. Deterministic guard in the execution loop ONLY; no I/O, no grounding/citation/recall path, no change to independent-list / synthesis / conflict / coverage behavior. fail-CLOSE (AUGMENT-never-displace honored: suppresses only a blind dependent step, never a grounded result). MUTATION-FIRST RED confirmed (dependent leaked into `ran` without the guard); sibling-audit: the sequenced-execution loop is the SOLE callsite (ask-decompose.ts delegates via deps.execute, doesn't re-implement the loop) — 1/1 covered.

## fire 8 · 2026-06-20 · loop-creator v2.0.0 · NO-SHIP (cross-loop duplicate dropped on the ④c-merge fetch)
meta: value-class=recall/memory quality (MemoryBank Ebbinghaus strength-aware fade resistance) · pkg=@muse/memory · kind=recall-quality · verdict=NO-SHIP (cross-loop dup) · firesSinceDrill=8 · mainMerge=N/A (no-ship — branch stays 6-ahead/0-behind, FF available next fire)
ratchet: testFiles unchanged (slice dropped) · fabrication 0 (untouched) · @muse/memory 472/472 on the BUILT slice before drop · the capability now lives on main via the sibling commit (parity, not a regression)

- what (built, then dropped): added a strength floor to `selectForgettable` (`packages/memory/src/recall-promotion.ts`) — a memory recalled ≥ `strengthFloorHits` (default 5) resists fading even when idle+decayed, faithful to MemoryBank's Ebbinghaus memory strength (arXiv:2305.10250 — the more a memory is reviewed, the slower it is forgotten). Strictly subtractive on the (non-destructive) fade report, opt-out via `strengthFloorHits:0`, default wired through `consolidationPlan` → the daemon tick + `muse memory consolidate`. Diversified cleanly off f5/f6 (recall-dedup) and f7 (orchestration): a memory-quality kind on a not-recently-hit pkg.
- BUILD WAS COMPLETE + GREEN: MUTATION-FIRST RED confirmed (neutering the new filter clause to `true` re-failed the new test; restored GREEN), 3 outcome tests added, `pnpm --filter @muse/memory test` 472/472, `pnpm check` EXIT 0 (apps/cli 2763, apps/api 854), lint 0, self-eval all-green. An INDEPENDENT Opus 4.8 ④b judge re-ran the mutation itself and returned **VERDICT PASS** (behavior-verified, non-vacuous, subtractive, no grounding path touched, default reaches production).
- why DROPPED (NO-SHIP): at ④c main-merge time, `git fetch origin` revealed the concurrent `self-improvement` loop had landed the SAME backlog ◦ as its fire 3 — `3a2c29e7` "a high-frequency memory resists fading even when idle (MemoryBank importance term)" — minutes earlier. It is a functional duplicate: same file/function (`selectForgettable`), same mechanism (a lifetime-hits floor `importanceHitsFloor` default 8 excludes from fade candidacy), same paper (arXiv:2305.10250), same ◦. Per CROSS-LOOP HYGIENE the duplicate is DROPPED rather than shipping a second copy of the same capability. My feat commit was `git reset --soft`'d, the src/test restored from the cp-backup (NOT `git checkout`), and the branch rebased onto the advanced origin/main (which now carries the sibling's version). Theirs (floor 8) vs mine (floor 5) differ only in a reasoning-set default — no behavioral gap worth a follow-up.

lesson (cross-loop dup, recurring): the STALE codegraph index showed me an `importanceHitsFloor` filter inside `selectForgettable` at the START of the fire — that was the sibling's already-in-flight version the index had picked up, NOT my branch's file (which lacked it). I cross-checked against the live file, saw the floor absent, and built it. The miss: a `git fetch origin && git log origin/main` for a freshly-landed equivalent should run AGAIN right before BUILD on a high-traffic shared-theme module (memory/recall/orchestration are hot across 3+ loops), not only at fire setup — a sibling can land the same ◦ between setup and build. The in-loop work (build+judge) was sound; the waste was building a dup the cross-loop check would have caught earlier. Convergent-finding pattern with f1/f4/f6 drops. NEXT fire: pick a DIFFERENT (pkg,kind) — a one-shot tool-calling-reliability slice (@muse/model or agent-core tool path) is the most-diversifying unhit pair, and is less contended than memory/recall right now.

## fire 9 · 2026-06-20 · loop-creator v2.0.0 · 686d76b4
meta: value-class=one-shot tool-calling reliability (enum arg-value repair) · pkg=@muse/tools · kind=tool-calling-reliability/arg-correctness · verdict=PASS · firesSinceDrill=9 · mainMerge=no
ratchet: testFiles 1062 (cases added to existing files) · fabrication 0 (untouched — value-shape repair, no grounding/citation path) · eval:tools all visible cases PASS live on gemma4:12b (selection unperturbed — no tool name/desc/schema changed) · @muse/tools 275/276 · @muse/agent-core 2512/2512
ratchet (diversity): DIFFERENT (pkg×kind) from every prior ship — f5/f6 recall-dedup, f7 orchestration, f8 memory; @muse/tools + tool-calling-reliability both UNHIT this loop. Least-contended surface per the fire-8 recommendation (memory/recall/orchestration hot across 3+ siblings).
- what: new pure `coerceEnumArguments(inputSchema, args)` (@muse/tools `tools-argument-validation.ts`) — the enum counterpart of `coerceToolArguments` (which repairs right-value/wrong-TYPE). Rewrites a STRING enum/const arg to the schema's canonical spelling ONLY when, after `trim()`, it matches an allowed STRING choice case-insensitively AND matches EXACTLY ONE such choice. Wired into BOTH gates that run `validateEnumArguments`: the ReAct execution path (`agent-runtime.ts` ~850, before validateEnumArguments) and the plan-validation path (`plan-execute.ts` 409). 2 helper tests (repair+OOV/ambiguous/non-string/no-schema; AMBIGUOUS untouched) + a 21-value STABLE-0 FP corpus + 1 agent-core wired behavioral test (model emits "HEX" → executes once with `to:"hex"`, not blocked).
- why: gemma4:12b routinely emits an enum value with the right MEANING but the wrong surface form ("HEX"/"  octal  "/"Turn_Off") — strict-equality `validateEnumArguments` then REJECTED it, burning a retry round or failing the call (Structured Reflection arXiv:2509.18847: a right value in the wrong surface form invalidates an otherwise-correct call). The type-coercion repair existed; the enum-casing repair did not — only rejection. Closes the gap by repairing the provably-safe case and leaving genuine OOV values to surface for rejection.
- review-point: subtractive/conservative — ambiguous case-fold match (two choices differ only by case), already-canonical value, non-string value/choice, and unconstrained free-text props are NEVER rewritten (the f4 FP-floor lesson: a new matching rule must be STABLE-0 FP on a large benign corpus before landing — 21 benign values pass byte-identical). A rewrite target is always drawn from the schema's allowed set (never invents a value). Independent Opus ④b judge re-ran all 3 mutations RED→GREEN itself + 10 of its own adversarial benign cases — VERDICT PASS.
- risk: low. Deterministic value-shape normalization on the OUTBOUND arg only; no I/O, no tool selection/description/schema change (selection mathematically unperturbed), no grounding/citation/fabrication-floor path. Sibling-audit: `validateEnumArguments` has EXACTLY 2 callsites, both now run the repair consistently (2/2). MUTATION-FIRST RED confirmed on ambiguity guard (`===1`→`>=1`), match logic (neuter), AND the agent-runtime wiring (unwrap → "HEX" blocked instead of repaired). `pnpm check`: api 854/854 + cli 2768/2768 first inline run; the re-run's single api flake was `messaging-webhooks.test.ts` (a LINE-webhook env-gating test in apps/api — a package my diff does NOT touch; passes 3/3 isolated) — a pre-existing test-isolation flake, not this change. lint 0.

## fire 14 · 2026-06-21 · loop-creator v2.0.0 · 4db07c35
meta: value-class=multi-agent orchestration/decomposition (marker-less dependent two-step) · pkg=@muse/multi-agent · kind=orchestration-reliability · verdict=PASS · firesSinceDrill=4 · mainMerge=no
ratchet: testFiles unchanged (22+1 cases added to existing decompose-trigger.test.ts) · fabrication 0 (untouched — orchestration trigger path, no grounding/citation code) · eval:orchestration PASS (failure-propagation + bounded termination) · eval:decomposition PASS (3/3 grounded live gemma4:12b) · @muse/multi-agent 185/185
ratchet (diversity): pkg=@muse/multi-agent is UNCONTENDED right now — computer-control owns @muse/tools (its fires 7-9) and self-improvement owns @muse/memory; agent-hardening's last 4 ships were f9/f10/f13 (@muse/tools) + f12 (multi-agent). Ratchet PUSHES AWAY from @muse/tools (hit 3 of last 4); multi-agent decompose is the least-contended in-theme surface, so this widens off the tool-arg axis the siblings are saturating.
- what: new `sentenceSplitDependentTwoStep(request)` + `splitSentences` helper (packages/multi-agent/src/decompose-trigger.ts). A MARKER-LESS dependent two-step ("오늘 매출을 집계해. 그 결과를 정리해." / "Pull the list. Use those results to draft a note.") now SPLITS on sentence boundaries and, iff a NON-interrogative TAIL clause carries an explicit back-reference token (reuses listHasBackReference), flags a SEQUENCED 2-step. Wired into BOTH shouldDecompose (new branch gated `signals.sequencing === 0`) AND decomposeRequestWithKind (returns the sequenced 2-step) so the lead-worker threads step 1's output into step 2. 23 new tests (3 positive gate, 16 STABLE-0 FP benign KO+EN corpus, 6 helper unit incl. interrogative-guard + marker-path-ownership pins).
- why: before this, a marker-less dependent two-step ran BLIND on a single agent — step 2 ("그 결과를 정리해" = act ON step 1's output) had nothing to act on, so the model invents a plausible result the per-subtask grounding gate can't catch (MAST reasoning-action mismatch, arXiv 2503.13657). singleMarkerDependentSplit (fire 12) closed the SINGLE-MARKER half; this closes the NO-marker half. Probe-confirmed the gap (decompose:false, sequenced:false, n:1) and the fix end-to-end (lead-worker fans out + step 2 receives `["OUT(오늘 매출을 집계해.)"]`).
- review-point: a NEW matching rule → STABLE-0 FP discipline (f4/f11 lesson). The maker built a 16-note benign corpus; the INDEPENDENT Opus ④b judge built its OWN 22-note KO+EN corpus and re-ran all mutations itself (force-return-false → 4 RED; drop-`?`-guard → 1 RED) — VERDICT PASS, zero over-fires. Judge flagged the `extractSequencedSteps>=2` early-return as uncovered (redundant defense-in-depth) → added a marker-path-ownership pinning test in-fire (drop early-return → RED). Sibling-audit: shouldDecompose + decomposeRequestWithKind are the SOLE decompose consumers (lead-worker.ts:231/246 + commands-ask.ts:2149 delegate to them), both patched consistently — gate↔split consistent.
- risk: low. Pure deterministic sentence-split + token scan on the trigger path; no I/O, no grounding/citation/fabrication floor. Precision: split-first + back-ref-in-tail-required + interrogative-tail-excluded + sequencing===0-gated. `pnpm check` red ONLY on the pre-existing `commands-logo.test.ts` raw-ESC-byte offender (origin/main mascot feat e10ac6c2, NOT my diff — surfaces/CLI loop territory; my 3 files have zero raw bytes); lint 0; eval:orchestration + eval:decomposition PASS live.

## fire 15 · 2026-06-21 · loop-creator v2.0.0 · 96da29f2
meta: value-class=one-shot tool-calling reliability (Ollama tool_calls arg-JSON recovery) · pkg=@muse/model · kind=tool-calling-reliability · verdict=PASS · firesSinceDrill=5 · mainMerge=no
ratchet (diversity): pkg=@muse/model is UNHIT this loop — every prior ship was @muse/tools (f9/f10/f13), @muse/multi-agent (f3/f7/f12/f14), @muse/agent-core (f6), @muse/recall (f5), @muse/memory (f8 no-ship). This is the fire-8 lesson's "most-diversifying unhit pair" (the Ollama tool_calls projection seam, NOT arg-coercion) and is uncontended: computer-control owns @muse/tools, self-improvement owns @muse/memory; the adapter-ollama tool-args path has no sibling traffic (last touch core-hardening fire 4, a different — schema-sanitize — seam).
ratchet: testFiles unchanged (15 helper + 1 adapter case added to existing adapter-ollama.test.ts) · fabrication 0 (LOCATE-only recovery, never rewrites a value — STABLE-0 FP) · @muse/model 345/345 · eval:tools all visible cases PASS live (selection-neutral; suite hit the 480s wall-clock under saturation, EXIT=124 timeout, ZERO FAIL lines)

- what: new pure exported `recoverToolArgsJson(raw)` in `packages/model/src/adapter-ollama.ts`. When `safeParseToolArgs` hits a `JSON.parse` failure on a STRING-typed `tool_calls[].function.arguments`, it now (a) strips a markdown ``` fence and reparses, then (b) runs a string-aware balanced-brace scan to extract the outermost `{...}` (a `}` inside a JSON string value never terminates early) and reparses — returning the object only when it is a plain object, else `undefined` so the caller keeps the existing `{}`. Wired via `safeParseToolArgs` → both the non-stream and stream tool-call mappers already route through it. 15 helper unit tests (5 positive recovery + a self-built FP corpus) + 1 adapter-level behavioral test (a fenced arg string flows through `generate()` to `{city:"Seoul"}`).
- why: gemma4:12b sometimes emits the args string wrapped in a ``` fence or behind preamble prose ("Here are the args: {...}"); strict `JSON.parse` then threw and the prior `catch { return {} }` dropped EVERY argument, firing the tool argument-less even though the model SELECTED correctly and FILLED the args — a one-shot tool-calling reliability loss the prior arg-coercion fires (f9/f10/f13, @muse/tools) never touched because that layer never sees the raw provider string. Diversifies cleanly off arg-coercion (different pkg, different seam: provider response parse, not post-validation coercion).
- review-point: NEW matching/repair rule → STABLE-0 FP discipline (f4/f11 lesson). LOCATE-only is the floor guarantee: recovery extracts a verbatim balanced object span and reparses it — it NEVER rewrites token contents (no trailing-comma fix, no quote translation, no Python-literal coercion), so a recovered object's values are byte-faithful to the model's literal emission and a genuinely-unrecoverable string (`"not json"`, arrays, scalars, `{'a':True}`, single-quoted JSON, unterminated `{`) stays `{}`. The independent Opus ④b judge built its OWN 50+-case adversarial corpus probing cross-object splice / nested-object hijack / brace-in-string early-termination — found zero fabricated values, re-ran the mutation itself (6 RED, FP-corpus GREEN) — VERDICT PASS.
- risk: low. Deterministic parse-recovery on the OUTBOUND provider response only; no I/O, no tool selection/description/schema change (selection mathematically unperturbed), no grounding/citation/fabrication-floor path. Happy path unchanged (recovery runs only after a `JSON.parse` throw). MUTATION-FIRST RED confirmed by both maker and judge (neuter → exactly the 6 recovery tests RED, 14-case FP corpus stays GREEN). `pnpm check`: the only red was `web-search-policy.test.ts` (a triple-nested property-fuzz, NOT my diff) hitting the 5000ms vitest timeout under machine saturation — PASSES 16/16 with `--testTimeout=30000`, the documented OOM/saturation pattern; my touched pkg @muse/model is 345/345 isolated. lint 0.
