# Loop journal — agent-core-cognition

Per-loop journal (loop-creator v1.14.0). Cron 39b9bdec (15m). Successor to the
fires 1–41 chain recorded in the shared `loop-digest.md` (retired for this loop to
avoid cross-loop conflict). Theme: agent-core Muse cognition core strengthening,
paper-grounded (public arXiv only), 5-theme round-robin → gap-scout. Tier1.

## fire 1 · 2026-06-13 · skill v1.14.0 · 9b6b5a3e
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=recall-ranking-selection · verdict=PASS · firesSinceDrill=1
ratchet: testFiles 905→906 (+commands-ask-adaptive-k) · fabrication 0 (STRENGTHENED — fewer decoys) · eval n/a (deterministic selection)
- 무엇: Adaptive-k (arXiv:2506.08479, Taguchi/Maekawa/Bhutani EMNLP 2025) — `selectByScoreGap` (largest consecutive-gap knee) trims the `muse ask` grounding window to `effectiveK = min(topK, gap-cut)` in `diversifyAskChunks`. Trim-only (cap topK, floor 1, top match always kept). Distinct from shipped MVT (mean give-up vs largest cliff).
- 왜+arXiv: fixed topK=3 padded the prompt with near-miss decoys when scores fall off a cliff (fabrication surface for the local 12B); the paper's single-pass distribution-based k trims to the natural knee. Diversified off the last 2 council/semantic fires (40/41). arXiv:2506.08479.
- 리뷰지점: maker=Sonnet / judge=Opus 4.8 (independent, Fable-5 세션 미가용). v1 FAIL (FLOOR: gap-cut fed the trimmed set to classifyRetrievalConfidence which keys on top AND runner-up → borderline-flat verdict flipped "ambiguous"→"confident", false confidence). v2 PASS: split prompt-window trim from verdict input (notesGroundingFraming takes UNTRIMMED preGapScored). Judge swept ALL trimmed-set consumers floor-safe/fail-closed; counterfactual non-vacuous (verdict-from-trimmed → confident); non-inert real-revert (cliff→1 fails when disabled). Independent re-verify: agent-core 1943 + cli 2563 + builds + lint green.
- 리스크: 낮음 — trim-only count selection over the user's own chunks; verdict provably unchanged (from untrimmed distribution); fewer chunks → lower coverage → MORE ungrounded (fail-closed); fail-open on flat distribution. Caveat (process): the Opus judge accidentally `git checkout`ed the uncommitted commands-ask.ts mid-judging and reconstructed it byte-faithfully — orchestrator independently re-ran the full suite (floor + counterfactual + value tests pass) before commit, confirming soundness. Backlog: extend to recall/render surfaces; A/B gap vs MVT vs fixed on embedder-ab; per-query-type k.

## fire 2 · 2026-06-13 · skill v1.14.0 · bcab580e
meta: value-class=wiring (merge-integration/regression-fix) · pkg=@muse/recall+@muse/cli+@muse/agent-core · kind=merge-resolution · verdict=PASS · firesSinceDrill=2
ratchet: testFiles ~915 (origin refactor) · fabrication 0 · lint pass→fail→pass (regression fixed)
- 무엇: ORIENT의 `git merge origin/main`이 origin의 **@muse/recall 패키지 추출**(diversifyAskChunks/notesGroundingFraming/ScoredChunk가 commands-ask.ts→packages/recall/src/chunks.ts로 이동)을 끌어왔고, fire-1이 같은 함수들을 commands-ask.ts에서 수정했던지라 충돌. 내가 markers 미해결 상태로 merge를 잘못 커밋(lint pass→fail 회귀) → reset 후 재해결.
- 왜+근거: ①의 "self-eval non-zero면 그 회귀가 이번 이터레이션" — lint 회귀(conflict marker)가 fire 2의 일. commands-ask.ts는 origin 구조 채택(함수 @muse/recall import) + fire-1 call-site(preGapScored→verdictInput) 보존; chunks.ts에 fire-1 adaptive-k(selectByScoreGap, arXiv:2506.08479)+floor-fix 재이식(origin 추출본은 fire-1 이전). followup-capture-hook.test.ts의 in-test 배럴 dynamic import(~5s cold transform > 5000ms)가 결정적 타임아웃 → 정적 import로 근본 수정(17/17, 1.21s).
- 리뷰지점: maker=Opus worker(복잡 멀티패키지 merge). 별도 ④b 적대 judge 없음 — merge-resolution은 fire-1 슬라이스(이미 fire 1서 Opus judge v2 PASS)를 재배치한 것이라, 결정적 게이트로 검증: lint exit 0(markers 제거=회귀픽스), @muse/recall 70, agent-core followup 17/17, cli ask+adaptive-k floor 테스트 통과, fire-1 floor-fix(verdict=untrimmed) chunks.ts에 보존+통과. 오케스트레이터 독립 재검증(전 게이트 직접 실행). 51 origin 파일 중 commands-ask.ts만 충돌(나머지 auto-merge).
- 리스크: 낮음 — origin 구조+fire-1 가치 둘 다 보존, selectByScoreGap 단일 정의(agent-core, origin이 MMR 셀렉터 안 옮김). pnpm check 잔여 실패=문서화된 mcp playbook-store weighted-eviction 타임아웃 flake(미터치, scope상 mcp 회피, 경부하서 통과). pnpm install로 recall→mcp workspace symlink 배선(lockfile 무변). 교훈: merge 시 markers를 docs뿐 아니라 `git diff --name-only --diff-filter=U` 전체로 확인할 것(이번에 commands-ask.ts marker를 놓쳐 회귀 커밋).

## fire 3 · 2026-06-13 · skill v1.14.0 · abf1ecbf
meta: value-class=new-capability · pkg=@muse/agent-core · kind=playbook-selection-diversity · verdict=PASS · firesSinceDrill=3
ratchet: testFiles 920 (+playbook MMR cases) · fabrication 0 · eval n/a (deterministic selection)
- 무엇: MMR diversity-aware playbook selection (arXiv:2502.09017, Wang et al. 2025) — `mmrSelectStrategies`(greedy λ·normScore−(1−λ)·maxSim, λ=0.7, strategyTextSimilarity Jaccard 재사용)를 rankEligible의 phase-B 최종 top-K 컷에 배선. 근접-중복 전략이 매 `muse ask`의 [Learned Strategies] 슬롯(≤6)을 잠식하던 것 차단.
- 왜+arXiv: playbook 선택층이 relevance+reward+lifecycle은 있으나 selected set 내 redundancy 제어가 0이었음(Memp/ACE/MemRL 어느 것도 안 함). diversity가 RAG/long-context 성능↑(2502.09017). KIND=자기강화/playbook-diversity(fire1 recall·이전 council과 다름), 저-floor-risk(사용자 자기 전략 중 재선택, hint surface).
- 리뷰지점: maker=Sonnet / judge=Opus 4.8 PASS. relevance-dominance 수학적 보장(greedy 첫 픽=normScore 1=최관련, 절대 안 드롭) 적대 probe로 검증; non-inert 실제-revert(no-op stub→non-vacuity+assembled-path 2 테스트 fail); min-max divide-by-zero 가드(range0→1); 교차언어 safe-direction(KO+EN 패러프레이즈 Jaccard~0→둘 다 유지, 정직전략 무손실). agent-core playbook 52/52·build·lint·check green. scope: playbook.ts만(@muse/recall 무관).
- 리스크: 낮음 — additive selection, 게이트/citation 무관, avoidance/probation/recency-floor/small-bank 불변, fabrication=0 유지. **backlog caveat(judge):** counterfactual 테스트가 equal-score(pure-diversity 분기)라 λ-blend의 relevance side가 counterfactual로 안 pinned(probe로만 검증) — unequal-score counterfactual 테스트 추가 + λ 실-transcript 튜닝.

## fire 4 · 2026-06-13 · skill v1.14.0 · b93fbda7
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=orchestration-semantic-consensus · verdict=PASS · firesSinceDrill=4
ratchet: testFiles 921 (+council/swarm cases) · fabrication 0 · eval n/a (deterministic gate)
- 무엇: semantic council consensus gate (Cleanse arXiv:2507.14649 + ReConcile arXiv:2309.13007) — `hasCouncilConsensusSemantic`(기존 proven `councilMemberSupportsSemantic` cosine 재사용, DEFAULT_COUNCIL_AGREE_AT_COSINE=0.5)가 `muse swarm council` debate 루프의 consensus 게이트를 lexical Jaccard→임베딩 cosine으로. for→while 재구조(round-cap≤3·length>1 가드 보존). Jaccard는 no-embed fallback 유지.
- 왜+arXiv: fire-40이 outlier 스크린만 semantic화, consensus 게이트는 Jaccard 잔존 → KO+EN 합의 패널이 cross-script 0겹침으로 오"분쟁"→debate 라운드 낭비(backlog line 1015, judge-flagged fire 31). 명시적 backlog·언블록된 follow-on, 기존 primitive 재사용. KIND=멀티에이전트(fire1 recall·fire3 playbook과 다름).
- 리뷰지점: maker=Sonnet / judge=Opus 4.8 PASS. for→while 재구조 behavior-preserving 추적(never-converge=3 gathers·immediate=1·1-peer=0, off-by-one 없음); non-inert 실제-revert(Jaccard로 되돌리면 KO+EN→ONE-gather 테스트 fail, expected 1 got 2); 기존 ReConcile 5 테스트 assertion 불변(임베더만 주입, Ollama-free화); non-vacuous 양방향(agreeing→true·orthogonal→false); fail-open(embed throw→support 0→debate 진행). council 89/89·swarm 20/20·check 0·lint 0. scope: 6파일(council/index/swarm/program+테스트), @muse/recall·outlier-screen 무관.
- 리스크: 낮음 — 게이트는 라운드 수만 결정, synthesis/citation/RGV/fabrication=0 못 건드림(synthesis는 루프 後 1회). semantic cosine(lexical 아님)이라 cross-lingual 네이티브. **backlog caveat(judge):** AGREE_AT=0.5는 실 nomic서 같은-언어 무관답변 2개를 ~0.4-0.6로 오-합의 판정해 debate 1라운드 일찍 멈출 수 있음(품질 nit, floor 무관) — live KO/EN battery 튜닝. fire-36 injection 재스코프는 여전히 open.

## fire 5 · 2026-06-13 · skill v1.14.0 · 5db44f3d
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=answer-verification · verdict=PASS · firesSinceDrill=5
ratchet: testFiles 925 (+claim-support-screen, +per-claim cases) · fabrication 0 · eval n/a (judge stalls без Ollama)
- 무엇: semantic-screened MiniCheck per-claim 검증 (arXiv:2404.10774) — `screenClaimsBySemanticSupport`(claim↔evidence cosine<0.45면 suspect) pre-filter로 verifyGroundingPerClaim의 judge를 suspect claim에만 발화 → default-on 감당가능. `--verify-claims` 전용이던 refiner를 grounded-PASS 답변에 기본 실행.
- 왜+arXiv: per-claim refiner가 이미 있으나 flag 뒤 dead(judge가 모든 claim에 발화=고비용). semantic 사전선별로 비용↓→기본화. KIND=답변검증(fire1 recall·3 playbook·4 council과 다름; RGV/conformal/k-consensus와도 구분). **정직한 스코프(judge 검증): chat 경로는 답변을 stdout에 LIVE 스트리밍 後 refiner가 stderr에 "🔬 Per-claim check — 일부만 ground됨" 사후 경고만 append — 약-grounded 문장이 터미널에 닿는 걸 막지 않음(prevent-before-display 아님, 사후 WARNING). "구멍 닫음" 주장 금지.**
- 리뷰지점: maker=Sonnet / judge=Opus 4.8 PASS(binding 조건: 정직 framing). non-inert 실제-revert(--verify-claims-only로 되돌리면 assembled-path 테스트 fail — fabricated 문장이 경고 없이 스트림); floor-monotonic(whole-answer-PASS에서만·drop/uncertainty만·upgrade/inflate 없음·supported는 내부 비노출); screen 단독 드롭 안 함(judge만 드롭, missed-suspect=today no-regression); fail-open 양층; all-supported→ZERO judge call(저렴). claim-support 8/8·knowledge-recall 1964·cli program 263·lint 0.
- 리스크: 낮음 — additive, floor STRENGTHEN(default-on 경고 > 무검사), fabrication=0 못 약화. **backlog:** (a) prevent-before-display(ask 경로 buffer-then-render — 진짜 차단); (b) chat 경로(finalizeGatedChatAnswer) 배선; (c) DEFAULT_CLAIM_SUPPORT_FLOOR live 튜닝. 교훈: 스트리밍 경로에선 "검증=사후경고"지 "차단" 아님 — framing 정직히.

## fire 6 · 2026-06-13 · skill v1.14.0 · 616a2971
meta: value-class=new-capability · pkg=@muse/agent-core · kind=memory-redundancy-suppression · verdict=PASS · firesSinceDrill=6
ratchet: testFiles 928 (+episodic-lateral-inhibition) · fabrication 0 · eval n/a (deterministic selection)
- 무엇: lateral inhibition for episodic recall (arXiv:2601.02744 SYNAPSE) — `applyLateralInhibition`(candidate score를 inhibitionStrength·maxCos(이미 선택된 더-강한 match)만큼 demote, minScore 재게이트)를 StoreBackedEpisodicRecallProvider.resolve의 slice(0,topK) 대체. 재발 토픽이 [Episodic Recall] 블록을 중복으로 triple-fill하던 것 차단. 이미 계산된 임베딩 재사용(re-embed 0).
- 왜+arXiv: episodic recall이 redundancy 제어 0인 유일 retrieval 표면 — 중복 내러티브가 topK 슬롯 잠식·distinct 에피소드 crowding out. SYNAPSE lateral inhibition: 유사 co-activated 메모리 상호 억제. KIND=메모리 redundancy-suppression(fire3 playbook·4 council·5 verification과 다름; MMR과도 구분).
- 리뷰지점: maker=Sonnet / judge=Opus 4.8 PASS. non-inert 실제-revert(plain slice로 되돌리면 assembled-path fail — 2 중복 surface·distinct crowded out); floor-inert(inhibited score는 soft [Episodic Memory] 블록+telemetry만, classifyRetrievalConfidence/grounding/citation 게이트 무관 — below-gate surface/inflate/inject 불가); MMR과 구분(demote+re-gate, relevant-item displacement 없음 — judge 검증); off-switch(strength 0/no-vec=byte-identical) + fail-soft + deterministic. agent-core 1972·lint 0. scope: episodic-recall.ts+index.ts+test, Embedding provider 무관.
- 리스크: 낮음(floor 무관, context-quality만). **backlog caveat(judge):** strength=0.5가 related-but-distinct 에피소드(narrative cosine 0.55-0.75)를 과억제(stored sim [minScore, minScore+0.5·cos) drop) — ~0.35로 낮추거나 minScore 스케일, live 세션 corpus 튜닝. EmbeddingEpisodicRecallProvider 미러링·env knob도 backlog.

## fire 7 · 2026-06-13 · skill v1.14.0 · d1698b08
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=memory-contradiction-reconciliation · verdict=PASS(v2) · firesSinceDrill=7
ratchet: testFiles 931 (+contradiction-detection) · fabrication 0 (STRENGTHENED: 프롬프트→코드) · eval n/a
- 무엇: read-time 증거-충돌 결정적 플래그 (Mem0 arXiv:2504.19413) — `detectEvidenceContradictions`(same-script + cosine≥0.86 + 토큰겹침≥0.5 + neither-subset)가 충돌 노트쌍 탐지, commands-ask <<note>> 블록에 NEUTRAL 추가 마커. 프롬프트-지시(commands-ask.ts:162 "CONFLICTS & UPDATES")를 결정적 코드로 하드닝(계약의 "reconciliation≠prompt" 안티패턴 해소).
- 왜+arXiv: 12B가 프롬프트 reconciliation을 불신뢰. Mem0 contradiction-resolution을 read-time additive로. KIND=메모리 contradiction(fire4 council·5 verification·6 episodic과 다름).
- 리뷰지점: maker=Sonnet / judge=Opus 4.8. **v1 FAIL→fix→v2 PASS**(judge가 2 실결함 적발): (1) 마커가 "more recent value" 주장했으나 aIndex는 cosine score(relevance≠recency)라 stale 노트를 newer로 오표기 — false provenance → NEUTRAL 마커로(recency 주장 제거). (2) "differs in 1 token" 신호가 elaboration("2pm"/"2pm in room 4") 5/8 오플래그 → neither-subset 게이트(A⊄B AND B⊄A)로 elaboration 제거, 진짜 value-conflict만. non-inert 실제-revert; floor-additive(마커만, drop/rewrite 안 함); fail-open; same-script 가드. agent-core 1981·cli 2585·lint 0.
- 리스크: 낮음 — additive 마커(거짓주장 0, 노트 둘 다 표시), floor STRENGTHEN. **backlog(judge):** 잔여 borderline over-flag(mutual-different-DETAIL 비충돌 "lunch Bob"/"lunch Alice at noon"·gate12/gate14)는 attribute-vs-value 구분 없어 충돌처럼 플래그 — neutral 마커라 low-harm, slot/NER 타입 비교로 tighten 가능. 짧은 충돌 노트(<0.5 overlap) 미탐(safe-direction). threshold live 튜닝·chat 배선. 교훈: 마커가 단언하는 속성(recency)에 실제 신호 없으면 단언 금지(NEUTRAL).

## fire 8 · 2026-06-13 · skill v1.14.0 · 03c64182
meta: value-class=new-capability · pkg=@muse/agent-core · kind=planning-dependency-validation · verdict=PASS · firesSinceDrill=8
ratchet: testFiles 938→939 (+plan-execute-dep-validation) · fabrication 0 (STRENGTHENED: no partial-write on broken-dep plan) · eval n/a
- 무엇: LLMCompiler-근거(arXiv:2312.04511) 플랜 의존-참조 검증 — `validateStepDependencies(steps)` + private `extractDependencyRefs`를 `plan-execute.ts`에 추가, `validatePlan` errors에 concat. 각 스텝의 string arg에서 참조 토큰({{stepN}}·<step N>·<result of step N>·<단계 N…>·"step N output"·"단계 N 결과") 스캔 → 해소된 1-based 타깃 N이 FORWARD/SELF(N≥i+1) 또는 DANGLING(N<1‖N>len)이면 PlanValidationError. backward(N<i+1)는 NON-flag(스코프: un-satisfiable class만). plan-execute-loop.ts:134 validatePlan → :153 PlanValidationFailedError로 모든 tool 실행 前 fail-close.
- 왜+arXiv: ISR-LLM 검증(fire37)이 max-steps/blank-tool/unregistered/missing-arg/dup만 보고 의존-참조 해소성은 무검증 — 실행 루프가 inter-step 치환을 안 하니 forward/dangling 참조 스텝이 literal garbage arg로 실행돼 *앞 스텝이 이미 side-effect를 쓴 뒤* 실패(부분-side-effect harm). LLMCompiler의 의존-DAG(참조는 EARLIER 태스크를 가리켜야)를 검증으로 재구현. backlog "plan-validation remainder (b)" 종결. KIND=planning-validation(최근 3 fire 메모리/검증과 다름).
- 리뷰지점: maker=Sonnet / 독립 judge=Opus 4.8 → PASS. non-vacuity 실제-revert(concat 비활성→rebuild→정확히 2 wiring/e2e 테스트만 FAIL, reverted run이 write 실행 보임→복원 21/21). assembled-path가 write 스텝1+forward-ref 스텝2 플랜을 구동→PlanValidationFailedError throw AND write 효과 ABSENT(executeSpy 0회) 검증(handler-only 아님). judge 적발한 false-positive(bare `{{N}}`가 `{{2025}}`/`{{0}}` 오플래그)를 커밋 前 직접 tighten: mustache 패턴 `step` 키워드 필수화(bare `{{N}}` 드롭, bare `$N`처럼) + 회귀 테스트({{2025}}/{{0}}/{{2}}→0). dist-probe 확인({{step2}}→1·backward→0·{{2025}}→0). floor-additive(reject만, 새 플랜 admit 0), fail-closed-only, IMMUTABLE-CORE 무접촉. agent-core 2018·lint 0.
- 리스크: 낮음 — fail-closed 방향(broken-dep 플랜 reject, over-rejection이 유일 harm이고 tight 문법으로 최소화). **backlog(judge+self):** (e) 잔여 false-negative class(bare `$N`·bare `{{N}}` undetected — currency/template-ambiguous, safe 방향) tighten; (f) backward-ref SUBSTITUTION 배선(LLMCompiler Task Fetching Unit — `{{step1.output}}`를 앞 스텝 출력으로 해소, 검증만이 아니라); self-ref 에러 라벨이 "forward"로 표기(cosmetic, 탐지 방향은 정확). 교훈: 참조 문법은 데이터값(통화 $N·연도/인덱스 {{N}})과 충돌하니 키워드 필수(verbatim 토큰≠의도) — false-negative(safe)를 택하고 false-positive(valid 플랜 reject) 회피.
