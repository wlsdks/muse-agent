# Loop journal — agent-core-cognition

Per-loop journal (loop-creator v1.14.0). Cron 39b9bdec (15m). Successor to the
fires 1–41 chain recorded in the shared `loop-digest.md` (retired for this loop to
avoid cross-loop conflict). Theme: agent-core Muse cognition core strengthening,
paper-grounded (public arXiv only), 5-theme round-robin → gap-scout. Tier1.

## fire 1 · 2026-06-13 · skill v1.14.0 · 9b6b5a3e
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=recall-ranking-selection · verdict=PASS · firesSinceDrill=1
ratchet: testFiles 905→906 (+commands-ask-adaptive-k) · fabrication 0 (STRENGTHENED — fewer decoys) · eval n/a (deterministic selection)
- 무엇: Adaptive-k (arXiv:2506.08479, Taguchi/Maekawa/Bhutani EMNLP 2025) — `selectByScoreGap` (largest consecutive-gap knee) trims the `muse ask` grounding window to `effectiveK = min(topK, gap-cut)` in `diversifyAskChunks`. Trim-only (cap topK, floor 1, top match always kept). Distinct from shipped MVT (mean give-up vs largest cliff).
- 왜+arXiv: fixed topK=3 padded the prompt with near-miss decoys when scores fall off a cliff (fabrication surface for the local 12B); the paper's single-pass distribution-based k trims to the natural knee. Diversified off the last 2 council/semantic fires (40/41). arXiv:2506.08479.
- 리뷰지점: maker=Sonnet / judge=Opus 4.8 (independent, Fable-5 세션 미가용). v1 FAIL (FLOOR: gap-cut fed the trimmed set to classifyRetrievalConfidence which keys on top AND runner-up → borderline-flat verdict flipped "ambiguous"→"confident", false confidence). v2 PASS: split prompt-window trim from verdict input (notesGroundingFraming takes UNTRIMMED preGapScored). Judge swept ALL trimmed-set consumers floor-safe/fail-closed; counterfactual non-vacuous (verdict-from-trimmed → confident); non-inert real-revert (cliff→1 fails when disabled). Independent re-verify: agent-core 1943 + cli 2563 + builds + lint green.
- 리스크: 낮음 — trim-only count selection over the user's own chunks; verdict provably unchanged (from untrimmed distribution); fewer chunks → lower coverage → MORE ungrounded (fail-closed); fail-open on flat distribution. Caveat (process): the Opus judge accidentally `git checkout`ed the uncommitted commands-ask.ts mid-judging and reconstructed it byte-faithfully — orchestrator independently re-ran the full suite (floor + counterfactual + value tests pass) before commit, confirming soundness. Backlog: extend to recall/render surfaces; A/B gap vs MVT vs fixed on embedder-ab; per-query-type k.

## fire 2 · 2026-06-13 · skill v1.14.0 · bcab580e
meta: value-class=wiring (merge-integration/regression-fix) · pkg=@muse/recall+@muse/cli+@muse/agent-core · kind=merge-resolution · verdict=PASS · firesSinceDrill=2
ratchet: testFiles ~915 (origin refactor) · fabrication 0 · lint pass→fail→pass (regression fixed)
- 무엇: ORIENT의 `git merge origin/main`이 origin의 **@muse/recall 패키지 추출**(diversifyAskChunks/notesGroundingFraming/ScoredChunk가 commands-ask.ts→packages/recall/src/chunks.ts로 이동)을 끌어왔고, fire-1이 같은 함수들을 commands-ask.ts에서 수정했던지라 충돌. 내가 markers 미해결 상태로 merge를 잘못 커밋(lint pass→fail 회귀) → reset 후 재해결.
- 왜+근거: ①의 "self-eval non-zero면 그 회귀가 이번 이터레이션" — lint 회귀(conflict marker)가 fire 2의 일. commands-ask.ts는 origin 구조 채택(함수 @muse/recall import) + fire-1 call-site(preGapScored→verdictInput) 보존; chunks.ts에 fire-1 adaptive-k(selectByScoreGap, arXiv:2506.08479)+floor-fix 재이식(origin 추출본은 fire-1 이전). followup-capture-hook.test.ts의 in-test 배럴 dynamic import(~5s cold transform > 5000ms)가 결정적 타임아웃 → 정적 import로 근본 수정(17/17, 1.21s).
- 리뷰지점: maker=Opus worker(복잡 멀티패키지 merge). 별도 ④b 적대 judge 없음 — merge-resolution은 fire-1 슬라이스(이미 fire 1서 Opus judge v2 PASS)를 재배치한 것이라, 결정적 게이트로 검증: lint exit 0(markers 제거=회귀픽스), @muse/recall 70, agent-core followup 17/17, cli ask+adaptive-k floor 테스트 통과, fire-1 floor-fix(verdict=untrimmed) chunks.ts에 보존+통과. 오케스트레이터 독립 재검증(전 게이트 직접 실행). 51 origin 파일 중 commands-ask.ts만 충돌(나머지 auto-merge).
- 리스크: 낮음 — origin 구조+fire-1 가치 둘 다 보존, selectByScoreGap 단일 정의(agent-core, origin이 MMR 셀렉터 안 옮김). pnpm check 잔여 실패=문서화된 mcp playbook-store weighted-eviction 타임아웃 flake(미터치, scope상 mcp 회피, 경부하서 통과). pnpm install로 recall→mcp workspace symlink 배선(lockfile 무변). 교훈: merge 시 markers를 docs뿐 아니라 `git diff --name-only --diff-filter=U` 전체로 확인할 것(이번에 commands-ask.ts marker를 놓쳐 회귀 커밋).

## fire 3 · 2026-06-13 · skill v1.14.0 · abf1ecbf
meta: value-class=new-capability · pkg=@muse/agent-core · kind=playbook-selection-diversity · verdict=PASS · firesSinceDrill=3
ratchet: testFiles 920 (+playbook MMR cases) · fabrication 0 · eval n/a (deterministic selection)
- 무엇: MMR diversity-aware playbook selection (arXiv:2502.09017, Wang et al. 2025) — `mmrSelectStrategies`(greedy λ·normScore−(1−λ)·maxSim, λ=0.7, strategyTextSimilarity Jaccard 재사용)를 rankEligible의 phase-B 최종 top-K 컷에 배선. 근접-중복 전략이 매 `muse ask`의 [Learned Strategies] 슬롯(≤6)을 잠식하던 것 차단.
- 왜+arXiv: playbook 선택층이 relevance+reward+lifecycle은 있으나 selected set 내 redundancy 제어가 0이었음(Memp/ACE/MemRL 어느 것도 안 함). diversity가 RAG/long-context 성능↑(2502.09017). KIND=자기강화/playbook-diversity(fire1 recall·이전 council과 다름), 저-floor-risk(사용자 자기 전략 중 재선택, hint surface).
- 리뷰지점: maker=Sonnet / judge=Opus 4.8 PASS. relevance-dominance 수학적 보장(greedy 첫 픽=normScore 1=최관련, 절대 안 드롭) 적대 probe로 검증; non-inert 실제-revert(no-op stub→non-vacuity+assembled-path 2 테스트 fail); min-max divide-by-zero 가드(range0→1); 교차언어 safe-direction(KO+EN 패러프레이즈 Jaccard~0→둘 다 유지, 정직전략 무손실). agent-core playbook 52/52·build·lint·check green. scope: playbook.ts만(@muse/recall 무관).
- 리스크: 낮음 — additive selection, 게이트/citation 무관, avoidance/probation/recency-floor/small-bank 불변, fabrication=0 유지. **backlog caveat(judge):** counterfactual 테스트가 equal-score(pure-diversity 분기)라 λ-blend의 relevance side가 counterfactual로 안 pinned(probe로만 검증) — unequal-score counterfactual 테스트 추가 + λ 실-transcript 튜닝.

## fire 4 · 2026-06-13 · skill v1.14.0 · b93fbda7
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=orchestration-semantic-consensus · verdict=PASS · firesSinceDrill=4
ratchet: testFiles 921 (+council/swarm cases) · fabrication 0 · eval n/a (deterministic gate)
- 무엇: semantic council consensus gate (Cleanse arXiv:2507.14649 + ReConcile arXiv:2309.13007) — `hasCouncilConsensusSemantic`(기존 proven `councilMemberSupportsSemantic` cosine 재사용, DEFAULT_COUNCIL_AGREE_AT_COSINE=0.5)가 `muse swarm council` debate 루프의 consensus 게이트를 lexical Jaccard→임베딩 cosine으로. for→while 재구조(round-cap≤3·length>1 가드 보존). Jaccard는 no-embed fallback 유지.
- 왜+arXiv: fire-40이 outlier 스크린만 semantic화, consensus 게이트는 Jaccard 잔존 → KO+EN 합의 패널이 cross-script 0겹침으로 오"분쟁"→debate 라운드 낭비(backlog line 1015, judge-flagged fire 31). 명시적 backlog·언블록된 follow-on, 기존 primitive 재사용. KIND=멀티에이전트(fire1 recall·fire3 playbook과 다름).
- 리뷰지점: maker=Sonnet / judge=Opus 4.8 PASS. for→while 재구조 behavior-preserving 추적(never-converge=3 gathers·immediate=1·1-peer=0, off-by-one 없음); non-inert 실제-revert(Jaccard로 되돌리면 KO+EN→ONE-gather 테스트 fail, expected 1 got 2); 기존 ReConcile 5 테스트 assertion 불변(임베더만 주입, Ollama-free화); non-vacuous 양방향(agreeing→true·orthogonal→false); fail-open(embed throw→support 0→debate 진행). council 89/89·swarm 20/20·check 0·lint 0. scope: 6파일(council/index/swarm/program+테스트), @muse/recall·outlier-screen 무관.
- 리스크: 낮음 — 게이트는 라운드 수만 결정, synthesis/citation/RGV/fabrication=0 못 건드림(synthesis는 루프 後 1회). semantic cosine(lexical 아님)이라 cross-lingual 네이티브. **backlog caveat(judge):** AGREE_AT=0.5는 실 nomic서 같은-언어 무관답변 2개를 ~0.4-0.6로 오-합의 판정해 debate 1라운드 일찍 멈출 수 있음(품질 nit, floor 무관) — live KO/EN battery 튜닝. fire-36 injection 재스코프는 여전히 open.

## fire 5 · 2026-06-13 · skill v1.14.0 · 5db44f3d
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=answer-verification · verdict=PASS · firesSinceDrill=5
ratchet: testFiles 925 (+claim-support-screen, +per-claim cases) · fabrication 0 · eval n/a (judge stalls без Ollama)
- 무엇: semantic-screened MiniCheck per-claim 검증 (arXiv:2404.10774) — `screenClaimsBySemanticSupport`(claim↔evidence cosine<0.45면 suspect) pre-filter로 verifyGroundingPerClaim의 judge를 suspect claim에만 발화 → default-on 감당가능. `--verify-claims` 전용이던 refiner를 grounded-PASS 답변에 기본 실행.
- 왜+arXiv: per-claim refiner가 이미 있으나 flag 뒤 dead(judge가 모든 claim에 발화=고비용). semantic 사전선별로 비용↓→기본화. KIND=답변검증(fire1 recall·3 playbook·4 council과 다름; RGV/conformal/k-consensus와도 구분). **정직한 스코프(judge 검증): chat 경로는 답변을 stdout에 LIVE 스트리밍 後 refiner가 stderr에 "🔬 Per-claim check — 일부만 ground됨" 사후 경고만 append — 약-grounded 문장이 터미널에 닿는 걸 막지 않음(prevent-before-display 아님, 사후 WARNING). "구멍 닫음" 주장 금지.**
- 리뷰지점: maker=Sonnet / judge=Opus 4.8 PASS(binding 조건: 정직 framing). non-inert 실제-revert(--verify-claims-only로 되돌리면 assembled-path 테스트 fail — fabricated 문장이 경고 없이 스트림); floor-monotonic(whole-answer-PASS에서만·drop/uncertainty만·upgrade/inflate 없음·supported는 내부 비노출); screen 단독 드롭 안 함(judge만 드롭, missed-suspect=today no-regression); fail-open 양층; all-supported→ZERO judge call(저렴). claim-support 8/8·knowledge-recall 1964·cli program 263·lint 0.
- 리스크: 낮음 — additive, floor STRENGTHEN(default-on 경고 > 무검사), fabrication=0 못 약화. **backlog:** (a) prevent-before-display(ask 경로 buffer-then-render — 진짜 차단); (b) chat 경로(finalizeGatedChatAnswer) 배선; (c) DEFAULT_CLAIM_SUPPORT_FLOOR live 튜닝. 교훈: 스트리밍 경로에선 "검증=사후경고"지 "차단" 아님 — framing 정직히.

## fire 6 · 2026-06-13 · skill v1.14.0 · 616a2971
meta: value-class=new-capability · pkg=@muse/agent-core · kind=memory-redundancy-suppression · verdict=PASS · firesSinceDrill=6
ratchet: testFiles 928 (+episodic-lateral-inhibition) · fabrication 0 · eval n/a (deterministic selection)
- 무엇: lateral inhibition for episodic recall (arXiv:2601.02744 SYNAPSE) — `applyLateralInhibition`(candidate score를 inhibitionStrength·maxCos(이미 선택된 더-강한 match)만큼 demote, minScore 재게이트)를 StoreBackedEpisodicRecallProvider.resolve의 slice(0,topK) 대체. 재발 토픽이 [Episodic Recall] 블록을 중복으로 triple-fill하던 것 차단. 이미 계산된 임베딩 재사용(re-embed 0).
- 왜+arXiv: episodic recall이 redundancy 제어 0인 유일 retrieval 표면 — 중복 내러티브가 topK 슬롯 잠식·distinct 에피소드 crowding out. SYNAPSE lateral inhibition: 유사 co-activated 메모리 상호 억제. KIND=메모리 redundancy-suppression(fire3 playbook·4 council·5 verification과 다름; MMR과도 구분).
- 리뷰지점: maker=Sonnet / judge=Opus 4.8 PASS. non-inert 실제-revert(plain slice로 되돌리면 assembled-path fail — 2 중복 surface·distinct crowded out); floor-inert(inhibited score는 soft [Episodic Memory] 블록+telemetry만, classifyRetrievalConfidence/grounding/citation 게이트 무관 — below-gate surface/inflate/inject 불가); MMR과 구분(demote+re-gate, relevant-item displacement 없음 — judge 검증); off-switch(strength 0/no-vec=byte-identical) + fail-soft + deterministic. agent-core 1972·lint 0. scope: episodic-recall.ts+index.ts+test, Embedding provider 무관.
- 리스크: 낮음(floor 무관, context-quality만). **backlog caveat(judge):** strength=0.5가 related-but-distinct 에피소드(narrative cosine 0.55-0.75)를 과억제(stored sim [minScore, minScore+0.5·cos) drop) — ~0.35로 낮추거나 minScore 스케일, live 세션 corpus 튜닝. EmbeddingEpisodicRecallProvider 미러링·env knob도 backlog.

## fire 7 · 2026-06-13 · skill v1.14.0 · d1698b08
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=memory-contradiction-reconciliation · verdict=PASS(v2) · firesSinceDrill=7
ratchet: testFiles 931 (+contradiction-detection) · fabrication 0 (STRENGTHENED: 프롬프트→코드) · eval n/a
- 무엇: read-time 증거-충돌 결정적 플래그 (Mem0 arXiv:2504.19413) — `detectEvidenceContradictions`(same-script + cosine≥0.86 + 토큰겹침≥0.5 + neither-subset)가 충돌 노트쌍 탐지, commands-ask <<note>> 블록에 NEUTRAL 추가 마커. 프롬프트-지시(commands-ask.ts:162 "CONFLICTS & UPDATES")를 결정적 코드로 하드닝(계약의 "reconciliation≠prompt" 안티패턴 해소).
- 왜+arXiv: 12B가 프롬프트 reconciliation을 불신뢰. Mem0 contradiction-resolution을 read-time additive로. KIND=메모리 contradiction(fire4 council·5 verification·6 episodic과 다름).
- 리뷰지점: maker=Sonnet / judge=Opus 4.8. **v1 FAIL→fix→v2 PASS**(judge가 2 실결함 적발): (1) 마커가 "more recent value" 주장했으나 aIndex는 cosine score(relevance≠recency)라 stale 노트를 newer로 오표기 — false provenance → NEUTRAL 마커로(recency 주장 제거). (2) "differs in 1 token" 신호가 elaboration("2pm"/"2pm in room 4") 5/8 오플래그 → neither-subset 게이트(A⊄B AND B⊄A)로 elaboration 제거, 진짜 value-conflict만. non-inert 실제-revert; floor-additive(마커만, drop/rewrite 안 함); fail-open; same-script 가드. agent-core 1981·cli 2585·lint 0.
- 리스크: 낮음 — additive 마커(거짓주장 0, 노트 둘 다 표시), floor STRENGTHEN. **backlog(judge):** 잔여 borderline over-flag(mutual-different-DETAIL 비충돌 "lunch Bob"/"lunch Alice at noon"·gate12/gate14)는 attribute-vs-value 구분 없어 충돌처럼 플래그 — neutral 마커라 low-harm, slot/NER 타입 비교로 tighten 가능. 짧은 충돌 노트(<0.5 overlap) 미탐(safe-direction). threshold live 튜닝·chat 배선. 교훈: 마커가 단언하는 속성(recency)에 실제 신호 없으면 단언 금지(NEUTRAL).

## fire 8 · 2026-06-13 · skill v1.14.0 · 03c64182
meta: value-class=new-capability · pkg=@muse/agent-core · kind=planning-dependency-validation · verdict=PASS · firesSinceDrill=8
ratchet: testFiles 938→939 (+plan-execute-dep-validation) · fabrication 0 (STRENGTHENED: no partial-write on broken-dep plan) · eval n/a
- 무엇: LLMCompiler-근거(arXiv:2312.04511) 플랜 의존-참조 검증 — `validateStepDependencies(steps)` + private `extractDependencyRefs`를 `plan-execute.ts`에 추가, `validatePlan` errors에 concat. 각 스텝의 string arg에서 참조 토큰({{stepN}}·<step N>·<result of step N>·<단계 N…>·"step N output"·"단계 N 결과") 스캔 → 해소된 1-based 타깃 N이 FORWARD/SELF(N≥i+1) 또는 DANGLING(N<1‖N>len)이면 PlanValidationError. backward(N<i+1)는 NON-flag(스코프: un-satisfiable class만). plan-execute-loop.ts:134 validatePlan → :153 PlanValidationFailedError로 모든 tool 실행 前 fail-close.
- 왜+arXiv: ISR-LLM 검증(fire37)이 max-steps/blank-tool/unregistered/missing-arg/dup만 보고 의존-참조 해소성은 무검증 — 실행 루프가 inter-step 치환을 안 하니 forward/dangling 참조 스텝이 literal garbage arg로 실행돼 *앞 스텝이 이미 side-effect를 쓴 뒤* 실패(부분-side-effect harm). LLMCompiler의 의존-DAG(참조는 EARLIER 태스크를 가리켜야)를 검증으로 재구현. backlog "plan-validation remainder (b)" 종결. KIND=planning-validation(최근 3 fire 메모리/검증과 다름).
- 리뷰지점: maker=Sonnet / 독립 judge=Opus 4.8 → PASS. non-vacuity 실제-revert(concat 비활성→rebuild→정확히 2 wiring/e2e 테스트만 FAIL, reverted run이 write 실행 보임→복원 21/21). assembled-path가 write 스텝1+forward-ref 스텝2 플랜을 구동→PlanValidationFailedError throw AND write 효과 ABSENT(executeSpy 0회) 검증(handler-only 아님). judge 적발한 false-positive(bare `{{N}}`가 `{{2025}}`/`{{0}}` 오플래그)를 커밋 前 직접 tighten: mustache 패턴 `step` 키워드 필수화(bare `{{N}}` 드롭, bare `$N`처럼) + 회귀 테스트({{2025}}/{{0}}/{{2}}→0). dist-probe 확인({{step2}}→1·backward→0·{{2025}}→0). floor-additive(reject만, 새 플랜 admit 0), fail-closed-only, IMMUTABLE-CORE 무접촉. agent-core 2018·lint 0.
- 리스크: 낮음 — fail-closed 방향(broken-dep 플랜 reject, over-rejection이 유일 harm이고 tight 문법으로 최소화). **backlog(judge+self):** (e) 잔여 false-negative class(bare `$N`·bare `{{N}}` undetected — currency/template-ambiguous, safe 방향) tighten; (f) backward-ref SUBSTITUTION 배선(LLMCompiler Task Fetching Unit — `{{step1.output}}`를 앞 스텝 출력으로 해소, 검증만이 아니라); self-ref 에러 라벨이 "forward"로 표기(cosmetic, 탐지 방향은 정확). 교훈: 참조 문법은 데이터값(통화 $N·연도/인덱스 {{N}})과 충돌하니 키워드 필수(verbatim 토큰≠의도) — false-negative(safe)를 택하고 false-positive(valid 플랜 reject) 회피.

## fire 9 · 2026-06-13 · skill v1.14.0 · d6588a3c
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/autoconfigure+@muse/cli · kind=playbook-temporal-discount · verdict=PASS · firesSinceDrill=9
ratchet: testFiles 941→942(no new file; +16 cases in playbook.test) · fabrication 0 · eval:self-improving 6/6 PASS
- 무엇: Discounted-UCB(arXiv:0805.3415, Garivier & Moulines) 시간-감쇠 보상을 playbook 랭커에 — `recencyDiscount(strategy, nowMs, halfLifeDays=30)` = `0.5^(ageDays/30)`(anchor=lastReinforcedAt??createdAt; 없거나 garbage/future면 1, boost 없음)를 `effectiveStrategyReward`의 **양의 보상 성분에만** 곱함(nowMs 있을 때만). `rankPlaybookStrategies`/`…ByRelevance`에 optional `nowMs` 스레드, `applyPlaybook`이 `Date.now()` 전달. provider projection(context-engineering-builders buildPlaybookProvider) + cli embed-rank 매퍼가 store의 lastReinforcedAt/createdAt를 PlaybookStrategy로 투영(이 와이어가 non-inert 핵심 — 이전엔 랭커가 타임스탬프를 못 봄).
- 왜+arXiv: playbook은 non-stationary bandit(취향 drift)인데 보상이 selection-time에 recency-blind였음 — 작년 thumbs-up이 어제 것과 동일 가중. D-UCB γ-감쇠를 half-life 형태로 재구현. KIND=self-reinforcement/playbook 새 메커니즘(Memp lifecycle·ACE delta-merge·MemRL retrieval·MMR diversity와 다름; fires 5-8 verification/memory/memory/planning과도 다름).
- 리뷰지점: maker=Sonnet / 독립 judge=Opus 4.8 → PASS + **필수 정직 교정**. judge가 maker의 "order-only/membership 불변" 주장을 반증: eligible≤topK(inject-all)일 땐 order-only지만 **eligible>topK일 땐 감쇠가 topK truncation에 들어가 stale 전략이 fresher로 교체돼 membership이 바뀔 수 있음**. floor-safe 확인(실 eligible끼리 swap만, fabricate 0, 음수 un-sink 0(INV-2), eligible set 확장 0; eligibility 자체는 시간-독립 — avoidance/probation은 clampReward로 비-감쇠). non-vacuity 실제-revert(recencyDiscount→1 → counterfactual+assembled-path 등 4 RED). INV-1 byte-identical(@muse/recall 미전달→discount 호출 안 됨, exact-number pin). agent-core 2036·lint 0.
- 리스크: 낮음 — 의도된 D-UCB 동작(capacity-limited cut에서 stale 강등). **backlog(judge):** (a) @muse/recall non-embed selectPlaybookSection 경로도 recency 투영(스코프 제외); (b) half-life A/B vs 데몬 30일 step 튜닝; (c) cli embed-rank 매퍼는 타임스탬프 투영하나 nowMs 미전달이라 아직 inert — 5번째 positional 인자 배선하면 활성. 교훈: "order-only" 주장 전에 downstream 임계/truncation(topK) 확인 — utility 감쇠는 membership을 바꿀 수 있고, 그게 floor-safe여도 주장은 사실과 일치해야.

## fire 10 · 2026-06-13 · skill v1.14.0 · 8b9f490f
meta: value-class=wiring(+JUDGE-DRILL) · pkg=@muse/cli · kind=playbook-embed-rank-recency-wiring · verdict=PASS · firesSinceDrill=0
ratchet: testFiles 943→944(+playbook-embed-rank.test) · fabrication 0 · cli 2604 pass · lint 0
- 무엇: **JUDGE-DRILL(필수, firesSinceDrill≥10 트리거)** — 고의 inert 슬라이스(`reinforcementVelocity`: export+단위테스트 통과하나 rankEligible/applyPlaybook 어디서도 호출 안 됨, 커밋이 "comparator에 배선" 거짓 주장)를 독립 Opus judge에 제출 → judge가 정확히 FAIL(호출처 0개 추적, declaration-only, mutate→-999로 3 산술테스트만 RED 입증) → git checkout 롤백(잔여 0 확인). maker=judge 보상통제가 inert 함정을 실제로 잡음을 실증. **그다음 진짜 fix**: fire-9에서 logged한 inert 경로 해소 — cli embed-rank playbook 경로(`MUSE_PLAYBOOK_EMBED_RANK=true`)가 lastReinforcedAt/createdAt는 실었지만 nowMs 미전달이라 D-UCB recency 할인이 죽어있던 것을, inline 매핑+랭킹을 순수 모듈 `apps/cli/src/playbook-embed-rank.ts`(`toPlaybookStrategy`·`rankPlaybookEntriesByRelevance`)로 추출하고 호출처가 `Date.now()` 전달하도록 배선.
- 왜+arXiv: D-UCB(arXiv:0805.3415) recency 할인이 fire 9에서 출하됐으나 embed-rank 표면에선 nowMs 미전달로 inert였음. 추출은 그 경로를 테스트 가능하게 만들어 NON-INERT를 assembled-path로 증명. value-class=wiring/refactor(fire 5-9 new-capability에서 다양성). 메커니즘은 fire 9이고 이건 배선 — 새 메커니즘으로 과대주장 금지(judge 확인).
- 리뷰지점: maker=inline(orchestrator) / **드릴 judge + 실fix judge 둘 다 독립 Opus 4.8** → 드릴 FAIL(설계대로) + 실fix PASS. 실fix non-vacuity 실제-revert(nowMs→undefined → "fresh outranks stale" RED, 복원 4/4). counterfactual 유효: stale를 먼저 삽입해도 fresh가 1위(삽입순 아님 recency가 reorder); 동일 타임스탬프면 삽입순 복귀. tieEmbed로 cosine 동률→relevance 숨은 변수 배제. flag-off(else selectPlaybookSection) byte-unchanged. cli build·2604 pass·lint 0·pnpm check exit 0.
- 리스크: 낮음 — opt-in 경로(MUSE_PLAYBOOK_EMBED_RANK)만 변경, 기본 경로 무변경, 추출은 behavior-preserving(now-live nowMs 제외). **backlog:** fire-9 remainder (a) @muse/recall non-embed 경로 recency(스코프 제외 유지), (b) half-life A/B 튜닝 잔존. 교훈: 드릴이 maker=judge 보상통제의 신뢰성을 실측(green 게이트가 inert를 놓쳐도 judge가 잡음); 배선 슬라이스는 "인자 전달됨"이 아니라 revert-proof assembled-path로 증명해야(드릴 직후 같은 함정 금지).

## fire 11 · 2026-06-13 · skill v1.14.0 · 3451ceb1
meta: value-class=new-capability · pkg=@muse/a2a+@muse/cli · kind=orchestration-straggler-timeout · verdict=PASS(re-judge) · firesSinceDrill=1
ratchet: testFiles 944→946(+council-wire timeout cases, +council-gather-timeout) · fabrication 0 · a2a 120 pass · cli assembled 1 pass · lint 0
- 무엇: a2a council per-peer 종료 가드 — `requestCouncilReasoning`(council-wire.ts)에 AbortController+setTimeout(DEFAULT_COUNCIL_TIMEOUT_MS=30s) 추가, fetch init에 signal 전달, finally에서 clearTimeout. abort→fetch reject→기존 `catch{return null}`(graceful). 버그: `gatherCouncil`(commands-swarm.ts:162)이 `Promise.all(peers.map(requestReasoning))`인데 requestCouncilReasoning에 타임아웃이 없어 **단일 hung peer가 muse swarm council 전체를 무한 블록**. timeoutMs optional(backward-compat), <=0이면 no-timeout(test-only).
- 왜+arXiv: MAST(arXiv:2503.13657 "Why Do Multi-Agent LLM Systems Fail?") coordination/termination 실패 — 오케스트레이터가 진행 못 함. bounded per-peer wait로 항상 종료. KIND=orchestration-termination(untouched a2a 테마; fire 8-10 planning/playbook과 다름; shipped council 메커니즘은 전부 keep/drop screen이라 이 timeout과 무관).
- 리뷰지점: maker=Sonnet / 독립 Opus judge **v1 FAIL→테스트교정→재judge PASS**. v1: 프로덕션 fix는 correct/load-bearing(judge가 signal 제거→honest hung fetch 10s 행 입증)이나 **shipped 테스트가 vacuous** — fake fetch가 `init.signal!`라 signal 미배선 시 동기 TypeError→reject→catch→null로 *틀린 이유로* green. 교정: 두 fake 모두 `if(init.signal)` 가드(미배선 시 정직하게 행) + per-test `{timeout:4000}`. **revert-proof 자체 실증+재judge 재실증: signal 제거→테스트 RED(4000ms 타임아웃), 복원→GREEN**. fail-open(timed-out peer→null→council 잔여 peer로 진행), grounding/synthesis 무변경, IMMUTABLE-CORE 무접촉.
- 리스크: 낮음 — fail-open graceful degradation(오늘의 erroring peer와 동일). **backlog:** (a) env override MUSE_A2A_COUNCIL_TIMEOUT_MS deferred(A2AEnv 미확장); (b) Roundtable Policy(arXiv:2509.16839) consensus-weighted contributor RANKING — synthesizeCouncilAnswer가 이미 계산한 support로 kept utterances를 정렬해 synthesis 프롬프트 salience(스카웃 발굴, 이번엔 a2a 우선). 교훈: ④b 게이트의 "테스트 교정 재검증" 경로 — 프로덕션이 옳고 테스트만 vacuous면 롤백 말고 테스트 고쳐 revert-proof 재실증; fake가 `init.signal!`로 throw하면 vacuous(미배선 시 행이 아니라 throw→false green), `if(init.signal)` 가드 필수.

## fire 12 · 2026-06-13 · skill v1.14.0 · 77173907
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/autoconfigure · kind=proactivity-semantic-dedup · verdict=PASS · firesSinceDrill=2
ratchet: testFiles 945→946(+background-review-wiring assembled) · fabrication 0 (SUBTRACTIVE: output ⊆ input) · agent-core commitment 22 pass · autoconfigure wiring 8 pass · lint 0
- 무엇: SemDeDup(arXiv:2303.09540) 의미 근접중복 collapse를 user commitments에 — `collapseNearDuplicateCommitments(commitments, embed, {threshold})` (commitment-detector.ts, COMMITMENT_DEDUP_COSINE=0.86): greedy single-pass로 각 commitment.text 임베딩→cosine≥threshold면 기존 클러스터 합류, cluster당 high-confidence rep 1개 유지(tie→earlier/shorter), 입력순 보존. `scanCommitmentsFromTurns`(autoconfigure, 데몬 check-in 스케줄러)에 `.map(c=>c.text)`→scheduleCheckins 전 배선(createGateEmbedder). detectUserCommitments는 lexical-only(`seen` keyed kind:text.toLowerCase)라 "email Bob the report"/"email Bob about the report"/"I'll email Bob"가 3건으로 살아 **근접중복마다 별도 proactive nag** 스케줄됨.
- 왜+arXiv: SemDeDup(embed→cosine 클러스터→rep 1개)을 proactivity 신뢰성에 적용 — 데몬이 한 loop에 중복 check-in을 스케줄하는 user-felt 버그. KIND=background-thread/proactivity(untouched 테마; fire 9-11 playbook/playbook/a2a과 다름; shipped fade/lateral-inhibition/contradiction/salient-fact와 다른 메커니즘·입력).
- 리뷰지점: maker=Sonnet / 독립 Opus judge → PASS. non-vacuity 실제-revert(collapse→input 그대로 → 단위 positive-collapse RED AND assembled-path "fewer check-ins" RED `expected 2 to be less than 2`, 복원 22/8 green). NON-INERT: collapse 출력이 scheduleCheckins의 유일 feed(fewer commitments→fewer check-ins), createGateEmbedder는 실 프로덕션 nomic 임베더. SUBTRACTIVE(출력은 `commitments[idx]` 참조만, rewrite 0, fabrication=0), fail-soft(embed throw/zero-norm→입력 그대로), non-over-collapse(orthogonal→둘 다 생존; threshold↑→둘 다). detectUserCommitments 순수 유지.
- 리스크: 낮음 — subtractive(중복 nag만 제거), fail-soft. **honest caveat(backlog):** (a) 0.86 threshold가 **실 임베딩 미튜닝**(테스트는 합성 stub cos≈0.999/0이라 0.86 자체의 변별력 미검증) — 실 nomic 코퍼스 A/B 필요; (b) greedy single-pass라 rep 교체 후 타 클러스터 재검사 안 함(SemDeDup 설계대로, 의도적); (c) chat-ink recap-count 경로 미배선(별도 fire); (d) 이 표면은 MUSE_BACKGROUND_REVIEW_ENABLED opt-in(assembled-path가 stub 임베더 주입으로 직접 구동). 교훈: 합성 stub로 메커니즘은 증명되나 threshold 변별력은 실 분포 A/B 전까진 미검증 — "calibrated" 주장 금지.

## fire 13 · 2026-06-13 · skill v1.14.0 · 80e9165b
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=grounding-set-level-sufficiency · verdict=PASS(re-judge) · firesSinceDrill=3
ratchet: testFiles 946→948(+knowledge-recall-sufficiency, +commands-ask-sufficiency) · fabrication 0 (STRENGTHENED: advisory) · agent-core sufficiency 12 · cli sufficiency 9 · lint 0
- 무엇: Sufficient Context(arXiv:2411.06037, Joren et al. ICLR2025) set-level 의미 충분성 — `assessContextSufficiency(subQueries, evidenceVecs, {coverAt,sufficientAt})` (knowledge-recall.ts): 각 sub-query가 evidence와 max cosine≥coverAt(=DEFAULT_CONFIDENT_AT 0.55)면 covered, coveredFraction≥1.0이면 sufficient; <2 clause면 no-op(multi-part gate). commands-ask.ts에 `sufficiencyAdvisory({json,answer,subQueries,subQueryVecs,evidenceVecs})`(모든 gate 포함) 배선 → multi-part·non-refusal·non-json·insufficient면 uncovered part 명시 stderr 권고. 기존 `classifyRetrievalConfidence`는 TOP 단일 passage cosine만 봐서 2-part 질문("when AND where")의 한쪽만 커버돼도 confident → 12B가 미커버 절반 fabricate.
- 왜+arXiv: sufficiency는 passage relevance와 직교한 SET-level 속성(논문: insufficient context면 모델이 abstain 대신 fabricate). top-cosine confidence·lexical answerability(post-answer) 둘 다 못 잡는 multi-part 미커버를 pre-answer 의미 cosine로. KIND=grounding/answer-verification(shipped MiniCheck-per-claim/RGV/k-consensus/CRAG와 다름; fire 10-12 playbook-wiring/a2a/proactivity와 다름). identity wedge(grounding floor) 직격.
- 리뷰지점: maker=Sonnet / 독립 Opus judge **v1 PASS-with-caveat→테스트교정→재judge PASS**. v1 caveat: 프로덕션 충분성은 옳으나 **emission 게이팅(!json·!refusal·multipart·length-match)이 inline·미테스트**("if-block 삭제해도 19 테스트 green"). 교정: 전 게이팅을 테스트가능 helper `sufficiencyAdvisory`로 fold(call site는 trivial `if(line) io.stderr`), 9 테스트가 각 gate 분기 독립 검증. **revert-proof 2개 live: refusal gate 제거→refusal 테스트 RED; assessContextSufficiency 중립화→fires 테스트 RED.** ADVISORY-ONLY(답변·citation gate 무변경), multi-part-gated, fail-open(degenerate→sufficient:true), semantic cosine(lexical 아님). subqueryEmbeddings/splitClauses hoist는 기존 동작 무변경(judge 확인).
- 리스크: 낮음 — additive stderr 권고(최악=추가 정직 caveat), fabrication=0 STRENGTHEN. **honest caveat(backlog):** (a) coverAt=0.55 **실 nomic 임베딩 미튜닝**(테스트는 합성 orthogonal basis cos 0/1) — 실 multi-part 코퍼스 A/B 필요; (b) call site의 `io.stderr` write + `scored.map(embedding)` projection은 read-verified glue(trivial); (c) chat-grounding 경로 미배선(별도 fire); (d) remainder: coveredFraction을 classifyRetrievalConfidence에 set-level demotion으로 먹이는 gating 변경(별도 floor proof 필요). 교훈: 추출 helper가 cosine만 덮고 게이팅은 inline이면 그 게이팅은 미테스트 — 전 emission 결정을 helper로 fold해야 assembled-path가 진짜 NON-INERT 증명(call site는 trivial glue만 남김); fire-11처럼 "테스트 교정 재검증" 경로.

## fire 14 · 2026-06-13 · skill v1.14.0 · 33dbcbfc
meta: value-class=new-capability · pkg=@muse/agent-core · kind=memory-outcome-conditioned-plan-cache · verdict=PASS · firesSinceDrill=4
ratchet: testFiles 952 (no new file; +4 unit +3 assembled in existing plan-cache tests) · fabrication 0 (SUBTRACTIVE) · agent-core 2068 pass · lint 0
- 무엇: Agent Workflow Memory(arXiv:2409.07429, Wang/Mao/Fried/Neubig ICML2025) outcome-conditioned plan-cache 저장 — `selectSuccessfulPlanSteps(executed)` (plan-cache.ts): executed 레코드 중 `stepResult.success===true`인 step만 순서보존 필터. plan-execute-loop.ts:272 `recordPlan(userId,prompt,steps)`를 `recordPlan(...,selectSuccessfulPlanSteps(executed))`로 + `successfulSteps.length>0` 가드. 기존엔 ≥1 step 성공하면 **실패한 step 포함 전체 플랜**을 future few-shot exemplar로 캐시 → 다음 패러프레이즈 요청이 그걸 retrieve해 로컬 모델에게 *작동 안 한* tool 시퀀스를 가르침.
- 왜+arXiv: AWM은 workflow induction이 OUTCOME-conditioned(성공 trajectory의 step만 재사용 루틴). ISR-LLM(shape pre-exec)·LLMCompiler(dep ref)와 다르게 *recorded* 플랜을 실행결과로 필터하는 건 미shipped. backlog (d)"cache repaired not invalid"와 다름(그건 이미 :149 reassign으로 충족) — 별개 outcome-conditioning gap. KIND=memory(cache importance gating; fade/lateral-inhibition/salient-fact/SemDeDup과 다름; fire 11-13 a2a/proactivity/grounding과 다름).
- 리뷰지점: maker=Sonnet / 독립 Opus judge → PASS. non-vacuity 실제-revert(selectSuccessfulPlanSteps→identity `executed.map(r=>r.step)` → 3 RED: mixed unit·all-fail unit·assembled "failed step excluded", 복원 14 green). assembled-path가 실 plan_execute 루프 구동(step_fail tool이 "Error: ..." 반환→classifyStepEffect가 success:false 마킹)→recorded[0].steps가 step-1만; counterfactual(step-2 성공→둘 다). 가드 변경(steps.length>0→successfulSteps.length>0)은 동작 동일(all-failed는 :251 PLAN_ALL_STEPS_FAILED로 recordPlan 전에 throw하므로 recordPlan 도달 시 항상 ≥1 성공). SUBTRACTIVE(survivor 순서보존, add/reorder 0), 모델콜 0, 캐시 스키마 불변. 인용 arXiv:2409.07429만(2605.06716 미인용).
- 리스크: 낮음 — 캐시 write-path subtractive(실패 step만 제거), zero-successful→캐시 안 함. **NON-INERT 확인:** buildPlanCacheProvider default-on(MUSE_PLAN_CACHE, autoconfigure/index.ts:888,942 런타임 주입) → `muse chat --mode plan_execute` on-invocation 표면에서 라이브, 다음 유사요청의 exemplar로 소비(judge의 "surface 미배선" 지적은 autoconfigure 미추적이라 부정확 — 배선 확인함). **backlog:** (a) exemplar 품질 A/B(success-filter가 one-shot plan validity 올리나)는 라이브 eval 필요(별도); (b) per-step success를 renderPlanExemplar 주석으로(richer signal). 교훈: judge의 NON-INERT 지적이 패키지 경계(autoconfigure 배선)를 못 넘으면 오케스트레이터가 실제 caller 체인 직접 확인.

## fire 15 · 2026-06-13 · skill v1.14.0 · 5b7de5b7
meta: value-class=new-capability · pkg=@muse/agent-core · kind=council-consensus-weighted-ordering · verdict=PASS · firesSinceDrill=5
ratchet: testFiles 952 (no new file; +10 council cases) · fabrication 0 (ORDER-ONLY permutation) · agent-core 2078 pass · lint 0
- 무엇: Roundtable Policy(arXiv:2509.16839, Yao et al. 2025) consensus-weighted contributor ORDERING — `rankUtterancesBySupport(utterances, supports)` (council.ts): support 내림차순 stable sort(tie→입력순), length-mismatch면 입력 그대로(fail-open), permutation-preserving. `synthesizeCouncilAnswer`가 outlier screen 후 kept(`forSynthesis`)의 support를 직접 계산(embed면 semantic cosine, 아니면 Jaccard; try/catch fail-open) → `rankUtterancesBySupport(forSynthesis, keptSupports)` → `ordered`를 buildCouncilPrompt에. 기존엔 support가 keep/drop screen에만 쓰이고 kept는 원래 순서로 synthesis 프롬프트에 감(고합의 추론이 리드 안 함).
- 왜+arXiv: Roundtable은 contribution을 consensus support로 가중 집계. 고정 8B엔 logit 가중 불가 → 충실한 analog은 프롬프트 SALIENCE 순서(고합의 추론을 먼저). shipped council 메커니즘은 전부 binary keep/drop(outlier/relevance/consensus/termination/a2a-timeout) — ORDERING은 미shipped, 다른 KIND. fire 12-14 proactivity/grounding/memory와 다름. 5테마 라운드 완료 후 gap-scout가 council/orchestration을 최저-served fresh KIND로 지목 + 이미 계산된 신호 재활용(value-per-line 높음).
- 리뷰지점: maker=Sonnet / 독립 Opus judge → PASS. non-vacuity 실제-revert(rankUtterancesBySupport→identity `[...utterances]` → 4 RED: ranking·counterfactual unit + assembled "high-support leads"·counterfactual; 복원 66 green). **support 정렬 정확**: keptSupports를 `forSynthesis`에 직접 계산(forOutlier에서 투영 안 함 → drop 후 인덱스 어긋남 없음), keptSupports[i]=forSynthesis[i]의 support, 구성상 정렬. ORDER-ONLY: contributors grounding은 set 기반(순서 무관), reverify는 ordered 아닌 forSynthesis 받음, excludedPeers 불변. assembled-path가 실 synthesizeCouncilAnswer 구동(stub provider가 프롬프트 캡처)→고합의 peer 리드 + counterfactual flip. semantic/Jaccard 선택은 screen과 동일.
- 리스크: 낮음 — 순수 순서(permutation), fail-open(mismatch/embed-throw→오늘 동작). **honest caveat(backlog):** (a) **순서는 프롬프트에 도달하나 8B 품질 델타는 미증명**(salience/positional, 논문 가설이지 이 모델서 측정 안 함 — 라이브 Ollama eval 필요); (b) support를 kept subset에 재계산(screen의 forOutlier 벡터 재사용 안 함) = 정확한 정렬 선택이나 extra cheap pass + embed 시 1 추가 라운드트립(작음). 교훈: kept subset 정렬 시 support는 반드시 그 subset에 정렬(전체 벡터 투영은 drop 후 어긋남) — 구성상 정렬이 운-정렬보다 안전.

## fire 16 · 2026-06-13 · skill v1.14.0 · ab612c85
meta: value-class=new-capability · pkg=@muse/agent-core · kind=planning-near-duplicate-step-collapse · verdict=PASS · firesSinceDrill=6
ratchet: testFiles 955 (no new file; +12 unit +2 assembled in plan-execute tests) · fabrication 0 (subtractive) · agent-core 2097 pass · lint 0
- 무엇: Mem0(arXiv:2504.19413, Chhikara et al. 2025) "의미적으로 동등하면 재추가 안 함" consolidation을 plan-step에 — `dedupeNearDuplicateSteps(steps)` (plan-execute.ts): `normalizedStepKey`(tool trim+lower + 정규화 args의 stableStringify; string→trim+내부공백축약+lowercase, numeric-string→`String(Number(x))`, bool/null 통과, 재귀)가 같으면 later step 드롭(first 유지, **원본 args 무변경**, 순서보존). plan-execute-loop.ts:116/149의 `dedupeExactSteps`(byte-identical만)를 swap. 기존엔 case/whitespace/numeric-format만 다른 near-dup이 exact key를 빠져나가 실행 → budget slot 낭비 + WRITE 도구면 DOUBLE-ACT(outbound-safety) 위험.
- 왜+arXiv: Mem0 ADD-only-when-no-equivalent를 plan-step에 적용(byte-identity 넘어). shipped ISR-LLM-validation/LLMCompiler-dependency/AWM-cache와 다른 메커니즘. fire 13-15(grounding/memory/council)과 다른 KIND=planning. embedding/LLM 없는 결정적 정규화 — 최저 floor-risk.
- 리뷰지점: maker=Sonnet / 독립 Opus judge → PASS. non-vacuity 실제-revert(dedupeNearDuplicateSteps→identity → 8 RED: positive-collapse 단위 + assembled no-double-act; Alice/Bob counterfactual은 green 유지; 복원 38 green). NON-INERT: streamPlanExecute가 매 plan_execute run에서 무조건 dedupe→validate→execute, `muse chat --mode plan_execute`(env flag 없음). assembled-path가 실 executePlanExecuteLoop 구동(executeToolCall spy)→case-variant 중복 WRITE는 spy 1회(double-act 방지), 진짜 다른 args면 2회. SUPERSET(exact가 collapse한 건 전부 collapse), conservative(다른 값/도구 절대 미병합), key-only(실행 args 무변경, 원본 보존).
- 리스크: 낮음(bounded) — subtractive(중복 tool call 제거→evidence shrink/keep, invent 0). **honest caveat(backlog):** case-folding이 case-SENSITIVE write 식별자를 잘못 병합할 수 있으나 **Muse plan-execute write 도구(notes/tasks/calendar/email/messaging)는 전부 NL 콘텐츠라 case=표현**(write_file{path} 같은 case-sensitive write 표면 없음) → 오늘은 안전(중복 write 드롭=safe direction + approval gate 통과). 향후 case-sensitive-id write 도구 추가 시 그 필드는 case-folding 빼고 trim+numeric만. 교훈: 정규화 dedup의 over-merge는 *현재 도구 표면의 arg 의미*로 평가 — case가 식별자인 표면이 없으면 case-folding 안전, 있으면 필드별 보수화.

## fire 17 · 2026-06-13 · skill v1.14.0 · 309cb2b0
meta: value-class=new-capability · pkg=@muse/agent-core · kind=playbook-injection-redundancy-suppression · verdict=PASS · firesSinceDrill=7
ratchet: testFiles 957 (no new file; +6 cases in playbook tests, 2 existing tests adapted) · fabrication 0 (SELECTION-only) · agent-core 2108 pass · lint 0
- 무엇: 주입시점 near-dup 억제를 playbook small-bank 경로에 — `suppressNearDuplicateStrategies(scored, threshold=PLAYBOOK_INJECT_DEDUP_THRESHOLD=0.8)` (playbook.ts): composite 내림차순 greedy admit, 이미 admit된 것과 `strategyTextSimilarity`(Jaccard)<threshold일 때만 admit(아니면 drop, higher-composite 유지). `rankEligible`의 small-bank inject-all 분기(eligible≤topK)에 배선. 비대칭 해소: large-bank Phase-B는 MMR 중복제어가 있는데 small-bank(흔한 실사용자 케이스, 매 턴)는 무제어라 같은 교훈의 near-paraphrase 2개가 둘 다 `[Learned Strategies]`에 주입돼 프롬프트 예산 낭비+이중 가중.
- 왜+arXiv: arXiv:2510.17940(Lin 2025, 고정 예산서 redundancy-suppressed diverse 셋이 relevance-only 이김) + 이미 shipped된 MMR(arXiv:2502.09017) 원리를 small-bank로 확장. KIND=playbook self-reinforcement(Memp/ACE/MemRL/MMR-PhaseB/D-UCB와 다름; fire 14-16 memory/council/planning과 다름). grounding/citation 동시 루프 회피해 playbook 선택.
- 리뷰지점: maker=Sonnet / 독립 Opus judge → PASS. non-vacuity 실제-revert(suppress→identity → positive-dedup 단위 + assembled "한 paraphrase만" RED `not to contain juliet`; 복원 2108 green). **수정한 기존 테스트 2개 정당성 확인**: provenance tie-break(동일텍스트 2개가 이제 dedup→grounded 유지·reflected drop, 원의도 "evidence beats synthesis at tie" 보존/강화)·lifecycle confident>neutral(동일텍스트→비동일 Jaccard0.6<0.8로 교체, 둘 다 생존, 원의도 보존) — masked regression 아님, 올바른 새 동작에 적응. SELECTION-only(membership shrink-or-equal, rewrite 0), keep-higher-composite, cross-lingual SAFE(CJK bigram→KO/EN Jaccard 0→절대 미병합), large-bank MMR 무변경.
- 리스크: 낮음 — selection-only(near-dup만 제거), 절대 distinct 미드롭, fail-safe(missed dup=status quo). **honest caveat(backlog):** Jaccard는 **동일언어 near-verbatim만** 잡음(설계상; 의미적 중복/교차언어/0.8미만 재서술은 미병합) — 0.8 threshold **실 playbook 코퍼스 미튜닝**(테스트 bigram math로 선택); 의미적 dedup은 embedding 업그레이드(별도 fire, async/latency 고려). 교훈: 기존 테스트가 동일-텍스트 fixture로 새 dedup과 충돌하면 — 테스트를 약화 말고 *올바른 새 동작*(dedup desirable)에 맞게 적응하고 원의도 보존 확인. **JUDGE-DRILL 카운터: firesSinceDrill=7; fire 11 v1-FAIL(vacuous test)·fire 13 v1-caveat-escalation이 judge 변별력 라이브 증거 — 다음 강제 드릴은 firesSinceDrill≥10(fire 20) 또는 8연속 clean-allPASS.**

## fire 18 · 2026-06-14 · skill v1.14.0 · 7d988c3c
meta: value-class=new-capability · pkg=@muse/agent-core · kind=episodic-adaptive-cluster-cutoff · verdict=PASS(re-judge) · firesSinceDrill=8
ratchet: testFiles 959→960(+episodic-cluster-transition) · fabrication 0 (STRENGTHENED: cuts low-relevance tail) · agent-core 2120 pass · lint 0
- 무엇: CAR(arXiv:2511.14769, Xu et al. 2025 — clustered relevant→less-pertinent 전이점에서 적응적 cutoff) 적응 컷오프를 episodic-recall에 — `selectByClusterTransition(scoresDesc, {topK, dropRatio=EPISODIC_CLUSTER_DROP_RATIO=0.5})` (episodic-recall.ts): 내림차순 인접쌍에서 `next < cur*(1-dropRatio)`(0.5=다음이 절반 미만=급격한 cliff) 첫 지점 i에서 k=i+1, [1,topK] clamp; 전이 없으면 topK(오늘과 byte-identical), 빈/비유한/음수→topK(fail-soft). 3 provider(InMemory/Embedding/StoreBacked) 모두 fixed `slice(0,topK)`→adaptive k; StoreBacked는 k를 applyLateralInhibition topK로. knowledge-recall은 적응-k(selectByScoreGap)가 있는데 episodic은 무(無)였음 — 매 ask/chat턴 약한 episode로 `[Episodic Memory]` padding→12B fabrication 표면 확대.
- 왜+arXiv: CAR 상대-drop 전이 컷오프(shipped selectByScoreGap의 절대-max-gap과 다른 함수·다른 모듈·다른 논문; shipped lateral-inhibition의 redundancy 억제와 직교 — 이건 low-relevance tail 제거). KIND=episodic-memory selection(fire 15-17 council/planning/playbook과 다름). episodic-recall.ts는 citation 동시 루프 비접촉.
- 리뷰지점: maker=Sonnet / 독립 Opus judge **v1 FAIL→테스트교정→재judge PASS**. v1: 프로덕션 컷오프는 correct/non-inert이나 **assembled "marginal cut" 테스트가 vacuous** — s-C가 컷오프 아닌 LATERAL INHIBITION으로 드롭됨(inhibited −0.115<minScore, 2D 임베더가 C를 A와 0.81 유사하게 만듦)→always-topK 중립화해도 green. 교정: 3D 임베더로 C를 A/B와 직교(axis-2)시켜 cos(A,C)=0.24→inhibition penalty 0.12→C inhibited 0.18>minScore 0.05→C가 inhibition+minScore 생존, 컷오프만이 유일 제외 경로. **revert-proof live: 컷오프→min(n,topK) 중립화→cut 테스트 RED(return 3 expected 2), 복원 18/18 green.** 프로덕션 함수+3-provider 배선 무변경(테스트만). counterfactual(tight→3)·fade-path(uniform multiplier→no spurious cut)·regression(no-transition→topK) 다 통과, 미약화. CAR 논문 judge가 web-verify(실재).
- 리스크: 낮음 — SELECTION-only(k≤topK, never-add, minScore-first), fail-soft, scale-robust(상대-drop은 uniform multiplier 불변). **honest caveat(backlog):** dropRatio=0.5 **실 episode 코퍼스 미튜닝**(보수적 ≥50%-cliff만; 33% drop은 통과); assembled 테스트는 hand-constructed 직교 임베딩으로 컷오프를 inhibition에서 분리(메커니즘 binding 증명, 실 nomic 임베딩 동작은 미측정 — live recall 이득은 미측정). 교훈: assembled 테스트에서 다중 필터(inhibition+minScore+cutoff)가 한 결과를 만들면, 증명하려는 필터만이 *유일 원인*이 되게 입력 설계(직교 임베딩으로 inhibition 무력화) — 안 그러면 다른 필터가 결과를 만들어 vacuous. **JUDGE-DRILL: firesSinceDrill=8; fire 18 v1-FAIL(vacuous assembled)이 judge 변별력 라이브 증거 + 연속-clean 스트릭 리셋 → 다음 강제 드릴 firesSinceDrill≥10(fire 20).**

## fire 19 · 2026-06-14 · skill v1.14.0 · 645dc606
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=council-aggregate-confidence-advisory · verdict=PASS · firesSinceDrill=9
ratchet: testFiles 960 (no new file; +council/swarm cases) · fabrication 0 (ADVISORY-only) · agent-core 2129 pass · cli 2626 pass · lint 0
- 무엇: 협의체 집계-신뢰도 advisory(ConfMAD arXiv:2509.14034 — 집계에 confidence 신호를 실어 나름; 가드레일 arXiv:2511.07784 — consensus는 confidence지 truth 아님 → advisory-only) — `classifyCouncilConsensus(supports, {floor})` (council.ts): `len<=1`→strong, else MEDIAN(supports)<floor→weak. `CouncilAnswer.consensus?` optional 필드, `synthesizeCouncilAnswer`가 이미 계산한 `keptSupports`로 설정(embed면 cosine floor 0.5, 아니면 Jaccard floor 0.16; normal+reverify 경로 둘 다), `renderCouncilResult`가 weak일 때만 "⚠ weak consensus" 라인. 기존엔 강한 합의든 간신히 cohere든 동일하게 synthesis 방출 — 신호가 ordering(fire15)에만 쓰이고 버려짐.
- 왜+arXiv: MAD는 confidence를 집계에 carry해야(2509.14034); 단 majority pressure가 flawed reasoning을 mask하므로(2511.07784) 신호는 advisory만(gate 변경 금지). KIND=multi-agent 첫 CONFIDENCE 메커니즘(shipped는 전부 screen/ordering/timeout — outlier/relevance/consensus-gate/Roundtable-ordering/straggler-timeout과 다름). fire 16-18 planning/playbook/episodic과 다름. backlog fire-15 remainder(c) 종결. council.ts 비접촉(grounding-integrity 루프 council 커밋은 머지완료).
- 리뷰지점: maker=Sonnet / 독립 Opus judge → PASS. non-vacuity 실제-revert(classifyCouncilConsensus→항상 "strong" → weak 단위·floor-source·orthogonal-embed assembled 3 RED; 복원 108 green). toEqual→toMatchObject 변경 정당(새 optional 필드 수용만, answer+contributors 여전히 정확 pin, regression 미은폐). ADVISORY-only(answer/excludedPeers/contributors/reverify 무변경, weak는 caution 라인만 추가). MEDIAN-not-min([0.0,0.6,0.6]→strong이 min impl이면 weak로 pin), floor-source-match(embed→cosine 0.5, else Jaccard 0.16). papers judge가 둘 다 검증.
- 리스크: 낮음 — additive optional 필드 + render 라인, 절대 gate/drop/alter 안 함, 틀린 weak은 caution 라인 비용만. **honest caveat(backlog):** (a) 두 floor(0.5/0.16)는 ReConcile gate에서 재사용·실 council 분포 미튜닝(advisory라 low-risk); (b) **embed-fallback edge**: councilMemberSupportsSemantic가 throw해 Jaccard로 fallback해도 options.embed truthy면 cosine floor(0.5)가 Jaccard 분포에 적용돼 over-report "weak"(드문 catch-path, advisory-only라 over-caution=safe 방향) — floor를 실제 성공한 support 계산에서 파생하도록 후속. 교훈: floor-source는 옵션 플래그가 아니라 *실제 쓰인 신호*에서 파생해야(fallback 시 mismatch). **JUDGE-DRILL: firesSinceDrill=9 → 다음 fire 20이 firesSinceDrill≥10 강제 드릴(고의 나쁜-슬라이스→verifier FAIL→롤백→진짜 fix, 미루기 불가).**

## fire 20 · 2026-06-14 · skill v1.14.0 · f6dcb253
meta: value-class=refactor(+JUDGE-DRILL) · pkg=@muse/agent-core · kind=council-consensus-floor-correct-by-construction · verdict=PASS · firesSinceDrill=0
ratchet: testFiles 963 (no new file; refactor behavior-preserving, 51 council+grounding-verify green) · fabrication 0 · lint 0
- 무엇: **JUDGE-DRILL(필수, firesSinceDrill≥10)** — 이번엔 inertness(fire10) 아닌 **FLOOR/불변식 위반** 축 테스트: fire-19 advisory-only 협의체 consensus를 "weak면 답변 suppress(abstain)"로 바꾸는 플랜트 주입(안전 기능처럼 보이나 advisory-only 계약 + arXiv:2511.07784 가드레일 위반 — consensus는 confidence지 truth 아님, 합의 약하다고 이미-grounded 정답을 버리는 건 "majority pressure가 correction 억제"). 독립 Opus judge가 정확히 FAIL(파일 자체 주석의 advisory-only 불변식 인용 + stash-isolation으로 plant가 council-grounding-verify 3 테스트도 깨뜨림 적발) → git checkout 롤백(잔여 0). maker=judge 보상통제가 floor 위반(가장 중요한 축)을 실제로 잡음 실증. **그다음 진짜 fix(refactor)**: fire-19 caveat(a) 조사 → MOOT 판명(councilMemberSupportsSemantic는 per-member catch라 절대 throw 안 함 → 외부 fallback catch는 unreachable → cosine floor는 embed 경로서 이미 항상 옳음). floor를 실제 실행된 support 계산과 묶는 `supportFloor` 변수(correct-by-construction; fallback도 Jaccard floor)로 refactor + 오해소지 주석 수정.
- 왜+arXiv: 드릴은 maker=judge 신뢰성의 floor-축 실측(green 게이트가 floor 위반을 놓쳐도 judge가 잡나 — 잡음). refactor는 fire-19 caveat을 정직 해소(버그 아님 판명 → backlog moot 처리 + 코드 correct-by-construction). value-class=refactor(fire 13-19 new-capability 런에서 다양성).
- 리뷰지점: maker=inline(orchestrator) / **드릴 judge + refactor judge 둘 다 독립 Opus 4.8** → 드릴 FAIL(설계대로, floor 위반 적발) + refactor PASS. refactor는 behavior-preserving(embed→cosine·no-embed→Jaccard 불변, fallback 분기만 cosine→Jaccard이나 unreachable), 51 테스트 unmodified green, plant 완전 제거, advisory-only/grounding/excludedPeers/reverify/IMMUTABLE-CORE 무변경. judge가 honest labeling 확인(behavioral fix 과대주장 아님; moot 분석 정확).
- 리스크: 낮음 — refactor는 reachable 동작 무변경(correctness-by-construction + 주석 정정). **honest note:** fire-19 caveat(a)는 버그 아니었음(fallback unreachable, cosine floor 이미 옳음) → backlog moot 표기. 교훈: backlog caveat도 검증 대상 — "embed-fallback floor mismatch"는 councilMemberSupportsSemantic가 throw 안 하는 걸 못 본 misread였음; 진짜 fix는 비-버그에 fix 날조 말고 *정직히 moot 처리 + correct-by-construction 하드닝*. **JUDGE-DRILL 완료 → firesSinceDrill=0 리셋; 다음 강제 드릴 firesSinceDrill≥10(fire 30) 또는 8연속 clean-allPASS.**

## fire 21 · 2026-06-14 · skill v1.14.0 · 5a32ca92
meta: value-class=new-capability · pkg=@muse/agent-core · kind=planning-write-step-precondition · verdict=PASS · firesSinceDrill=1
ratchet: testFiles 965→966(+plan-execute-write-precondition) · fabrication 0 (STRENGTHENED: no partial-side-effect on placeholder write) · agent-core 2158 pass · lint 0
- 무엇: ISR-LLM(arXiv:2308.13724, translator→planner→VALIDATOR; PDDL feasibility=precondition-satisfaction) 쓰기-스텝 전제조건 검증 — `validateWritePreconditions(steps, toolRisks)` (plan-execute.ts): `risk:"write"|"execute"` 스텝(reads 면제)의 string arg가 trimmed-empty 또는 UNFILLED_PLACEHOLDER_RE(`<…>`·`{{…}}`·TODO/TBD/XXX/null/undefined/N/A, whole-value-anchored) 면 PlanValidationError. `validatePlan`에 `toolRisks` optional 입력으로 append(toolSchemas 패턴; 없으면 byte-identical). plan-execute-loop.ts가 tools에서 toolRisks 빌드해 두 validatePlan 호출에 전달 → placeholder write면 PlanValidationFailedError로 모든 tool 실행 前 fail-close. 기존엔 ref 토큰 없는 미충전 placeholder(`""`·`"<id>"`)가 valid:true로 통과해 garbage write를 commit(부분-side-effect, τ-bench/outbound-safety 위반).
- 왜+arXiv: forward/dangling ref는 fire-8이 잡지만 *ref 없는 미충전 arg*는 무검증이었음 — ISR-LLM 검증을 "arg present"에서 "arg satisfiable"로 확장. KIND=planning(fire-8 ref-reject/ISR-LLM-validation/AWM-cache/Mem0-near-dup과 다름; fire 18-20 episodic/council과 다름). backlog 1321(c) 종결.
- 리뷰지점: **worker가 빌드 중 API ConnectionRefused로 사망**(2 실패 테스트+lint 에러 잔존) → 오케스트레이터가 완성: empty-string 체크 누락(regex alternation이 ""를 못 매치 → `trimmed.length===0 ||` 추가) 수정 + lint(`_tools` 미사용 param) 수정. 독립 Opus judge → PASS. non-vacuity 실제-revert(validateWritePreconditions→`return []` → 단위 placeholder 10개 + assembled "writes[] empty" RED; 복원 47 green). assembled-path가 실 executePlanExecuteLoop 구동(executeToolCall spy): baseline(risk:read)→placeholder write 실행(writes 1), risk:write→PlanValidationFailedError+writes EMPTY. over-reject 0(whole-value-anchored: "send the TODO list"/"N/A items"/"NASA" 미플래그), reads 면제, additive-optional(toolRisks 없으면 불변), fail-close-only.
- 리스크: 낮음 — fail-close-only(거부만 추가, 새 admit 0), no-partial-side-effect STRENGTHEN, fabrication=0/IMMUTABLE-CORE 무접촉, 모델콜 0. **honest caveat(backlog):** (a) string-arg만 검사(write arg가 빈 배열/`{}`이면 미검사 — placeholder-string 흔한 케이스 우선); (b) placeholder grammar는 고정 토큰셋(non-exhaustive, over-reject 회피 쪽 편향); (c) backward-ref `{{step1.output}}` write arg는 substitution 미배선이라 placeholder로 거부(현재 옳음, 치환 배선은 별도 fire). 교훈: 워커 사망 시 잔존 partial work를 평가(build+test)해 *완성 가능하면 완성*(regex가 empty 못 잡는 버그 + lint), 버리지 말 것; regex alternation은 빈 문자열을 표현 못 하니 명시적 length 체크 필요.

## fire 22 · 2026-06-14 · skill v1.14.0 · f10a2c6d
meta: value-class=new-capability · pkg=@muse/agent-core · kind=playbook-staleness-reprobation-gate · verdict=PASS · firesSinceDrill=2
ratchet: testFiles 967 (no new file; +staleness cases in playbook-lifecycle) · fabrication 0 (SUBTRACTIVE selection) · agent-core 2171 pass · lint 0
- 무엇: SSGM(arXiv:2603.11768, Lam et al. 2026 — temporal-decay 메모리 거버넌스: 마지막 성공 reinforcement 후 cold해진 항목을 use 前 withhold/discard) staleness 게이트 — `isStaleStrategy(strategy, nowMs?)` (playbook.ts): nowMs 있고 lastReinforcedAt 있고 파싱되고 age>PLAYBOOK_STALE_AFTER_DAYS(120) AND tally `(reinforcements+decays)<3`일 때만 true. `rankEligible` 필터를 `isInjectableStrategy(s) && !isStaleStrategy(s, nowMs)`로 → applyPlaybook(매 턴 Date.now())가 cold-sparse 전략을 `[Learned Strategies]` 주입 블록에서 제외. 기존엔 graduate된 전략이 영원히 injectable(reward-floor avoidance·Memp confidently-bad·probation만 제외) — 한 번 earned 후 cold된 전략이 ancient 증거로 계속 모델 행동 형성(SSGM temporal obsolescence/semantic drift).
- 왜+arXiv: SSGM discard-half를 가역 staleness 게이트로. KIND=playbook self-reinforcement(staleness 게이트; fire 19/20/21 council/council/planning과 다름). **D-UCB(fire 9)와 구별**: D-UCB는 rank SCORE를 곱셈 할인(stale도 inject-all 경로선 여전히 주입); 이건 ELIGIBILITY 필터(membership drop, rank 前 단계, lastReinforcedAt만 anchor·createdAt fallback 없음 — never-reinforced 면제의 load-bearing 차이). 논문 실재 WebFetch 검증(오케스트레이터).
- 리뷰지점: maker=Sonnet / 독립 Opus judge → PASS. non-vacuity 실제-revert(isStaleStrategy→`return false` → 5 RED incl. assembled applyPlaybook end-to-end; 복원 32 green). never-reinforced 면제 load-bearing(no lastReinforcedAt + old createdAt 300d → false; blanket age>120라면 깨짐 — D-UCB anchor 재사용 안 함이 핵심). assembled real-revert(게이트 없으면 stale 주입, 있으면 omit + fresh 유지). SUBTRACTIVE-only, fail-safe(5 면제: nowMs/anchor/parse/age/depth), reversible(재-reinforce→injectable). legacy-identical(@muse/recall nowMs 미전달→no-op).
- 리스크: 낮음(safe 방향) — playbook GUIDANCE(grounding 아님; withheld hint는 fabricate 불가), 가역, re-distillable. **honest caveat(backlog):** 120d+tally<3 threshold **실 playbook 코퍼스 미튜닝**(SSGM framing+합성 fixture로 선택) — 드물게 trigger되는 유용한 seasonal 전략(연례 이벤트 前 1회 reinforce)이 재-reinforce 전까지 withheld될 수 있음; selection-only+reversible+re-distillable이라 safe 방향이나 실 reinforcement-interval 데이터로 튜닝 필요. 교훈: staleness anchor는 lastReinforcedAt만(createdAt fallback 쓰면 never-reinforced를 age로 잘못 게이트) — "earned-then-cold"만 대상.

## fire 23 · 2026-06-14 · skill v1.14.0 · a3631cf1
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/cli · kind=correction-distillation-gist-gate · verdict=PASS · firesSinceDrill=3
ratchet: testFiles 968 (no new file; +gist-gate cases in correction-distiller + assembled verbatim-drop in chat-distill) · fabrication 0 (STRENGTHENED: narrows playbook entry) · agent-core 2178 pass · cli 7 chat-distill · lint 0
- 무엇: SIB(arXiv:2603.01455, Lian et al. ACL2026 — distilled memory는 verbatim 복사가 아니라 GIST/추상; co-grounded ReasoningBank 2509.25140) gist 천장 — `distillStrategyFromCorrection`(correction-distiller.ts)의 held-out support 게이트가 LOWER bound(cosine≥0.50 grounded)만 있고 UPPER bound이 없어 correction의 near-verbatim 재진술(cosine~0.95+)이 통과해 playbook에 promote됨(un-distilled overfit one-off, 일반화 안 됨). `DEFAULT_STRATEGY_VERBATIM_CEILING=0.92` + `verbatimCeiling?` 추가: support accept 後 `cosineSimilarity(embed(correction), embed(strategy))≥ceiling`이면 drop → 게이트가 BAND [0.50,0.92)(grounded AND abstracted). cross-script 면제, embed 없으면 skip(back-compat).
- 왜+arXiv: 게이트가 one-sided(hallucination 하한만, overfit 상한 없음)였음 — SIB verbatim→gist로 상한 완성. KIND=correction-distillation(fire 20/21/22 council/planning/playbook과 다름). **fire-17과 구별**: fire-17은 inject-time 전략-vs-다른-bank-전략; 이건 distill-time 전략-vs-SOURCE-correction(다른 stage/참조텍스트/분포).
- 리뷰지점: maker=Sonnet / 독립 Opus judge → PASS. non-vacuity 실제-revert(ceiling→999 → 단위 verbatim-drop 2 RED). **judge가 assembled 테스트 vacuity 적발**(KEEP 경로만 구동, ceiling 꺼도 green) → 오케스트레이터가 seam 가드 추가: chat-distill-corrections에 assembled verbatim-drop 케이스(verbatim embed→status "skipped"·playbook 0) + revert-proof 자체 실증(ceiling→999면 "recorded"로 RED). 수정한 기존 테스트 2개 정당(옛 fixture가 우연히 verbatim [1,0]→[1,0]이라 새 ceiling이 옳게 drop → grounded-abstracted 벡터로 갱신, 원의도 보존; 단언 강화). SUBTRACTIVE-only, over-drop 가드(band가 0.8 유지), cross-script 면제, fail-open, distinct-from-fire-17.
- 리스크: 낮음(safe 방향) — playbook GUIDANCE write narrowing(subtractive, re-distillable), grounding 무접촉, fabrication=0 STRENGTHEN. **honest caveat(backlog):** 0.92 ceiling **실 nomic 미튜닝**(합성 벡터로만) — 짧은 correction의 유효한 concise 일반화("respond in Korean" ← "한국어로")가 ≥0.92로 drop될 수 있음(subtractive+re-distillable이라 safe 방향이나 실데이터 calibration 필요). 교훈: judge가 PASS해도 assembled-vacuity(seam이 기능 안 가둠)는 fire 11/13/18 패턴대로 seam 케이스 추가+revert-proof로 닫는다(unit-pinned+composition만으론 통합 seam 미보장).
