# Loop journal — competitor-grounding

Theme: competitor-adopted grounding-edge hardening + self-development, one verified slice/fire. Source: code-level survey of cloned openclaw + hermes (both MIT) vs Muse — both LACK a deterministic grounding gate (Muse's moat). Adopt PATTERNS only, re-implemented fresh in Muse's own TS. Backlog: `## ★ Open — competitor-adopted grounding hardening`. Cron 6c90833a (every 20m, session-scoped). Tier1 (local commits, never push).

## fire 1 · 2026-06-20 · skill loop-creator · 4f0b… (memory poisoned-source defang)
meta: value-class=new-capability · pkg=@muse/recall + @muse/cli · kind=security-hardening · verdict=PASS(deterministic; inline first-fire, no Opus judge) · firesSinceDrill=1
ratchet: testFiles 1053 · fabrication 0 · recall 306 / cli 2731 / lint / self-eval green
- 무엇: T1-a (memory-fact path). Shared `defangMemoryInjection` (@muse/recall/injection.ts) neutralizes an injection-shaped fact value at `renderMemoryFact` render time → ask-path memory block + conflict cue now defanged (was cli-persona-only). `muse-persona.ts` consolidated onto the shared single pattern source.
- 왜: poisoned-source / GROUNDED≠TRUE — the #1 known gap, and the axis BOTH competitors lack (they treat grounding as a prompt instruction). A fact poisoned at write time (malicious tool result/paste) must not steer the model when rehydrated; raw stays in the store (user can remove), prompt sees the neutralized form.
- 리뷰지점: patterns are narrow (a legit "always reply in Korean" passes); defang applies to the VALUE so the conflict cue sees the neutralized form too (good — a poisoned fact shouldn't manufacture conflicts).
- 리스크: notes/episodes threat-scan NOT yet covered (only markers escaped) — next fire. Inline first-fire skipped the Opus ④b judge (autonomous fires run it); deterministic gates (TDD red→green + OUTCOME proof + lint + self-eval) stood in.

## fire 2 · 2026-06-20 · skill loop-creator · (this commit) — episode + feed defang
meta: value-class=new-capability · pkg=@muse/recall · kind=security-hardening · verdict=PASS(Opus ④b judge, independent) · firesSinceDrill=2
ratchet: testFiles 1053 · fabrication 0 · recall 306→309 / cli 2731 / lint / self-eval green
- 무엇: extend `defangMemoryInjection` (fire-1 shared source) to EPISODE summaries + FEED headlines/summaries in present.ts — composed under the existing `escapeSystemPromptMarkers`. Both are non-user-authored untrusted text (auto-summaries / external RSS).
- 왜: memory flags "framing alone does NOT stop the 8B obeying embedded instructions" — so a deterministic defang (not just `<<feed>>` framing) is needed for the most-untrusted sources. Notes (user-authored) deferred — blanket defang would false-hide the user's own content.
- 리뷰지점: Opus judge PASS but flagged whole-prose false-defang (a benign episode containing a trigger word loses its whole summary) → span-level neutralization is the next-fire follow-up (logged in backlog T1-a ii).
- 리스크: whole-text defang precision on prose (rare false-hide of an augmentative episode; fail-toward-safe). VALUE-CLASS: fires 1+2 both @muse/recall security-hardening — next fire should change value-class or do the span-level precision fix, not extend the same defang again.

## fire 3 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — span-level neutralization, ALL prose surfaces
meta: value-class=new-capability · pkg=@muse/recall · kind=security-hardening · verdict=PASS(Opus ④b judge, independent, mutation-checked) · firesSinceDrill=3 · surfaces=4
ratchet: testFiles 1053→1054 · fabrication 0 · recall 309→320 / cli build / lint / self-eval green
- 무엇: SUBSTANTIAL fire (진안 "fires too easy" 교정 후 첫 fire). New `neutralizeInjectionSpans` (span-level: replace only the matched injection SPAN, keep the rest of the prose) applied to ALL 3 prose surfaces — episodes, feeds, AND notes (the previously-deferred surface, now safe via span-level). Atomic facts keep whole-defang. Closes T1-a fully (4 surfaces).
- 왜: fire-2 Opus judge flagged whole-prose defang nuking a whole summary on one benign trigger ("forget about the previous vendor"). Span-level limits damage to the matched phrase; the rest of the recall content survives → unblocks the notes surface (user-authored prose now safe to scan).
- 리뷰지점: Opus judge PASS — substantial(4 surfaces, not sliver) + benign-prose-survives test + OUTCOME on assembled blocks + no ReDoS + atomic-fact path unchanged. Mutation-checked RED.
- 리스크: residual `^system:` start-anchored misses mid-prose `\nsystem:` (backlog T1-a-resid). MONOTONY: fires 1-3 all @muse/recall injection-hardening — fire 4 MUST move to a distinct lever (T1-b claim-health or T2 FTS5/SSRF), not a 4th injection repeat (Opus judge + VALUE-CLASS RATCHET both flag this).

## fire 4 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — SCOPE CORRECTION (no code)
meta: value-class=scout/scope-correction · pkg=docs · kind=backlog-accuracy · verdict=PASS(honest audit, no code slice) · firesSinceDrill=4 · surfaces=0
ratchet: testFiles 1054 (unchanged) · fabrication 0 · baseline green
- 무엇: audited the top-2 Tier1/2 items before building. Found both NOT a clean substantial slice: T1-b (claim-health) is DATA-MODEL-BLOCKED (facts have no per-fact timestamp/evidence; only store updatedAt + factHistory for replaced facts) → DECOMPOSED into a prereq (per-fact provenance) + a feasible-now status unit. T2-b (SSRF guard) is ALREADY comprehensively built (web-url-guard.ts covers all ranges + metadata + IPv6 + DNS-rebinding, applied to every web tool + --url ingest) — the competitor survey was WRONG; marked ✓ with a feeds-fetch verify residual.
- 왜: the SUBSTANTIAL-FIRE rule forbids slivers, but the honest move when the top items are blocked/already-done is to CORRECT the backlog (DECOMPOSE-ON-DEFER + survey-error fix) rather than manufacture busywork or grind a degraded large fire at the tail of a very long session. Backlog accuracy stops the loop wasting future fires on a blocked item or re-building a done one.
- 리뷰지점: no code change — a deliberate scope-correction fire. The next fire has a clean substantial different-lever target: T2-d (skill curator + provenance, feasible/substantial) or T1-b-ii (claim STATUS via existing detectSourceConflict, no data-model dep). Monotony guard satisfied (moves off @muse/recall injection).
- 리스크: a no-code fire could read as "loop stalled" — it isn't; it's the contract's honest output when blocked. If several no-code fires recur, that signals the theme's easy veins are drying → widen the theme or pause.

## fire 5 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — `contested` grounding outcome (sources-disagree → not clean-grounded)
meta: value-class=new-capability · pkg=@muse/recall+@muse/cli+@muse/mcp · kind=grounding-confidence · verdict=PASS(Opus ④b judge, independent) · firesSinceDrill=5 · surfaces=1(contested outcome, wired across 4 pkgs)
ratchet: testFiles 1054 · fabrication 0 · recall 309→324 / cli 2732 / check green / eval:self-improving grounding+misgrounding+conflict-surfacing batteries PASS (faithfulness-rate cut by 360s timeout, not a failure) / lint
- 무엇: a `grounded` answer whose OWN grounding sources DISAGREE on a field (`detectSourceConflict`) is downgraded to a `contested` trace outcome (new `contestedOutcome`, parallel to the existing `misgrounded`). Wired across 4 pkgs: recall (AskOutcome + contestedOutcome + `source-conflict` axis), cli run-log (`contested` FailureKind → scout clusters it), mcp ledger (`source-conflict` WeaknessAxis), cli ask (compute the conflict BEFORE the outcome, chain it, dedup the now-redundant stderr-cue recompute).
- 왜: GROUNDED != TRUE, the #1 gap — the gate matched the claim to a real source, but two of the user's OWN sources give different values, so the cited one may be the wrong half. Previously this only produced an answer-time stderr cue while the TRACE stayed clean-"grounded" (the flywheel never saw it). Now it's a distinct outcome = real error-analysis fuel. Different LEVER than fires 1-3 (injection-defense) → monotony guard satisfied.
- 리뷰지점: Opus judge PASS 6/6 — floor untouched (relabels TRACE only, not the answer or gate verdict), precedence sound (misgrounded dominates contested), byte-identical cue dedup, re-implementation. TDD red→green on contestedOutcome + the run-log contested cluster; OUTCOME-verified on the dist downgrade chain.
- 리스크: `source-conflict` ledger axis is written but not yet read by `selectRemediableWeaknesses`/`DEV_FIXABLE_AXES` (judge flag) — run-log contested cluster is the working scout path; ledger-surfacing + the `superseded`(factHistory) status are logged as T1-b-ii remainders. VALUE-CLASS: fires 1-3 injection, fire 4 scope-correction, fire 5 grounding-confidence — lever diversity healthy.

## fire 6 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — neutralize LIVE tool/MCP/sub-agent output injection (+ move neutralizer to correct layer)
meta: value-class=new-capability+refactor · pkg=@muse/agent-core (move from @muse/recall) · kind=security-hardening · verdict=PASS(Opus ④b judge — FAILED under-scoped first, fixed in-fire, RE-JUDGED PASS) · firesSinceDrill=6 · surfaces=3(model-loop + plan-execute synthesis + plan-execute history)
ratchet: testFiles 1054 · fabrication 0 · agent-core 2459→2464 / recall 319 (re-export) / check green / eval:self-improving 14 batteries PASS / lint
- 무엇: tool/MCP/sub-agent output reached the model VERBATIM (only length-capped) — the PRIMARY agentic injection vector. Applied `neutralizeInjectionSpans` at all 3 chokepoints (capToolOutput + the two plan-execute paths). MOVED the neutralizer from @muse/recall → @muse/agent-core (the lowest layer both consumers depend on; recall can't be imported into agent-core — cycle); recall re-exports for back-compat.
- 왜: memory finding "a prompt 'this is untrusted' TAG does NOT stop the 8B obeying an embedded instruction" — so the defense must be deterministic CODE at the chokepoint, not the originally-specced `<untrusted-text>` tag. The architectural move puts the single-source neutralizer in the correct layer so BOTH stored-grounding (recall) and live-tool-output (agent-core) share it.
- 리뷰지점: the maker≠judge loop WORKED — the first Opus judge FAILED this as UNDER-SCOPED (capToolOutput wasn't the only chokepoint; plan-execute mode bypassed it via renderPlanResultSummary + planExecuteIntermediateMessages). Per the FAIL protocol (under-scoped → fill in-fire + re-judge), I closed BOTH plan-execute paths with TDD + tests, then a FRESH Opus judge swept all tool-output→prompt paths and PASSED (ref-store expansion also closed; sub-agent feeds LLM text not raw data; no remaining bypass).
- 리스크: residual control-char-strip (Cc/Cf/Zl/Zp before neutralizing — zero-width-hidden injection like "ignore" evades the regex) logged as T1-c (i). VALUE-CLASS: fires 1-3 recall-injection, 4 scope-fix, 5 recall-grounding-confidence, 6 agent-core-injection (DIFFERENT package) — package diversity restored.

## fire 7 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — zero-width/control-char evasion-strip (injection-defense CAPSTONE)
meta: value-class=new-capability(hardening) · pkg=@muse/agent-core · kind=security-hardening · verdict=PASS(Opus ④b judge, independent, 7/7) · firesSinceDrill=7 · surfaces=7(all defended surfaces inherit via 2 chokepoints)
ratchet: testFiles 1054 · fabrication 0 · agent-core 2464→2467 / check green (byte-hygiene caught my own raw bytes → fixed) / eval:self-improving 14 PASS / lint
- 무엇: new `stripInjectionEvasionChars` removes Unicode Cc/Cf/Zl/Zp (zero-width space/ZWNJ/ZWJ/BOM/soft-hyphen/bidi/NUL, keeping tab/newline/CR) BEFORE the patterns match, applied once in `isMemoryInjection` (atomic facts) + `neutralizeInjectionSpans` (prose/tool) so ALL 7 defended surfaces (facts/notes/episodes/feeds/tool-output/plan-execute×2) inherit it through one change.
- 왜: a mid-word zero-width char `ig<U+200B>nore` defeats `\bignore\b` — without this, EVERY prior injection fire (1-3 stored, 6 live tool) was bypassable. The capstone that makes the whole edifice un-bypassable on the literal-zero-width class.
- 리뷰지점: Opus judge PASS 7/7 — no bypass (escapeSystemPromptMarkers wraps the already-stripped output; @muse/policy's matcher is a parallel user-input surface that already strips). Found a pre-existing more-complete normalizer in @muse/policy → unification is the next-value follow-up. byte-hygiene gate caught raw zero-width bytes I'd put in the prose docs — fixed (String.fromCodePoint in tests, codepoint notation in docs).
- 리스크: entity-encoded + NFKC-homoglyph evasion still OPEN on these surfaces (T1-c-resid ii — unify with @muse/policy normalizer). emoji-ZWJ/bidi strip collateral acknowledged (display-only, acceptable). VALUE-CLASS: 5th injection-flavored fire (1,2,3,6,7) — judge OK'd as security-critical capstone but flagged fire 8 should PIVOT off injection (T2-d skill curator / T1-b-ii) for lever diversity.

## fire 8 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — THEME-EXHAUSTION audit (no code)
meta: value-class=scout/scope-correction · pkg=docs · kind=backlog-accuracy+exhaustion · verdict=PASS(honest audit) · firesSinceDrill=8 · surfaces=0
ratchet: testFiles 1054 (unchanged) · fabrication 0 · scout-signals=0 failure clusters (1139 traces, clean board)
- 무엇: audited the remaining Tier2 before building — found TWO more items ALREADY BUILT (like T2-b SSRF in fire 4): T2-d skill curator (curate/consolidate/recordUsage in @muse/skills, fully wired + tested, Hermes pattern already adopted) and T2-a session search (user `muse recall` + model-callable `knowledge_search` over the unified notes+episodes+feeds+memory corpus — past sessions ARE searchable both ways). Marked both ✓. T2-c (promotion gate) is BLOCKED on the same per-fact-recall-count data model as T1-b. Promoted T1-b-i (per-fact provenance data model) to the TOP remaining ★ — it unblocks BOTH T1-b-ii (freshness/confidence) AND T2-c (promotion gate).
- 왜: the competitor-grounding theme is essentially COMPLETE for readily-buildable items — fires 1-7 hardened injection across all 7 surfaces (un-bypassable); SSRF / curator / cited-recall were already built (the competitor survey over-claimed gaps). The ONLY substantial remaining work needs a memory data-model foundation (T1-b-i). scout-signals = 0 failure clusters (no real-usage fuel — the flywheel is starved, [[project_error_analysis_flywheel]]).
- 리뷰지점: a no-code EXHAUSTION fire (the loop-creator skill's honest output when veins are dry — NOT manufacturing busywork). 2nd such fire in this theme (after fire 4) = a real signal the theme is maturing. The next fire has a clear, substantial, genuinely-open target: T1-b-i (the data-model foundation, a deliberate risky migration).
- 리스크: T1-b-i is a memory-store migration (corruption risk if back-compat read of old facts-only stores fails) — must never lose a fact. If 진안 prefers, WIDEN the theme (grounding + self-development is largely done → a broader axis) or pivot. VALUE-CLASS: fires 4 + 8 both scope-correction — consistent with an honestly-maturing theme, not micro-fix monotony.

### theme WIDENED · 2026-06-20 (after fire 8, 진안 chose "widen") · cron 931038d7 → 3844085b
The grounding/injection sub-theme is COMPLETE (fires 1-7 + already-built SSRF/curator/recall). 진안 widened the SAME loop to 4 surfaces — memory-integrity · self-development · orchestration · grounding-floor. Opus gap-scout (codegraph-verified) seeded 5 open candidates into backlog "## ★ Open — Muse edge hardening": G1 source-conflict-axis surfacing (self-dev, clean), G2 injection-normalizer unify homoglyph/entity/NFD (grounding-floor, clean), G3 per-fact provenance data-model (memory-integrity, L) → G4 promotion gate, G5 chat orchestration parity. Confirmed ALREADY-BUILT (not re-scouted): skill curator, playbook RL, ask→orchestration wiring. Journal continues in this file.

## fire 9 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — surface the `source-conflict` weakness axis (whetstone dead-end closed)
meta: value-class=new-capability · pkg=@muse/mcp+@muse/cli · kind=self-development · surface=self-development · verdict=PASS(Opus ④b judge 6/6) · firesSinceDrill=9 · surfaces=1(source-conflict axis end-to-end)
ratchet: testFiles 1054 · fabrication 0 · mcp 1856 / cli 2733 / check green / eval:self-improving 14 PASS / lint
- 무엇: the `source-conflict` weakness axis (the user's OWN notes disagree → contested ask outcome → recorded since fire 5) was written to the ledger but read by NO selector (dead-end fuel). Now user-remediable: `USER_REMEDIABLE_AXES={grounding-gap,source-conflict}` + `RemediableWeakness.axis` + new `remediationHint(axis,topic)` (per-axis copy) + the evening recap renders each its own fix under an axis-agnostic header.
- 왜: first slice of the WIDENED theme on the SELF-DEVELOPMENT surface (whetstone metacognition) — the monitor→detect→remediate loop was broken for the conflict axis (detected, never surfaced). Closing it makes a recurring source-conflict actionable to the user ("reconcile your notes"), distinct from a grounding-gap ("add a note").
- 리뷰지점: Opus judge PASS 6/6 — verified EXHAUST (only 4 of 7 axes are production-written; all 4 now read by a selector; source-conflict was the lone gap). End-to-end OUTCOME (ledger→recap), dev-fixable axes stay out of the user nudge, grounding-gap floor preserved.
- 리스크: doctor `WEAKNESS_AXIS_LABEL` lacks source-conflict/misgrounding friendly labels (safe fallback, cosmetic — backlog residual). VALUE-CLASS: fires 6,7 grounding-floor/injection → 8 scope-fix → 9 self-development (DIFFERENT surface) — surface diversity restored per the widened theme.

## fire 10 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — G2 injection-normalizer unify (homoglyph/entity/NFKC) + JUDGE-DRILL
meta: value-class=new-capability(hardening) · pkg=@muse/agent-core · kind=security-hardening+judge-drill · surface=grounding-floor · verdict=PASS(Opus ④b judge 6/6) · firesSinceDrill=0 (DRILL DONE) · surfaces=2(facts + all-prose/tool chokepoints)
ratchet: testFiles 1054 · fabrication 0 · agent-core 2468→2471 / check green / eval:self-improving 16 batteries PASS / lint
- 무엇: BOTH injection chokepoints (`isMemoryInjection` facts + `neutralizeInjectionSpans` tool/MCP/sub-agent/prose) now match against @muse/policy's `normalizeForInjectionDetection` (entity-decode+NFKC+zero-width+homoglyph+diacritical), so a homoglyph-`іgnore`/`&#105;gnore` injection no longer evades. `neutralizeInjectionSpans` has a clean-text FAST PATH (no injection in normalized form → return original byte-identical → no diacritical/NFKC collateral on clean recall content).
- 왜: closes a real cross-arm bypass — the user-INPUT path already homoglyph/entity-decoded (policy) but the live tool/recall surface did not (agent-core), so a poisoned note/feed/tool-result with a Cyrillic-і `ignore` slipped through. Unifying on the one shared normalizer closes it everywhere.
- 리뷰지점: **JUDGE-DRILL (firesSinceDrill≥10, mandatory)** — planted a deliberately UNDER-SCOPED bad slice (normalize only `isMemoryInjection`, leave `neutralizeInjectionSpans` on zero-width-only + a facts-only test); the independent Opus judge FAILED it precisely (caught the live-surface gap + the missing test + even that native tool output is incidentally ToolOutputSanitizer-covered but recall-prose is not) → git restore → built the REAL fix (both chokepoints + fast path) → judge PASS 6/6 with a rigorous fast-path-false-negative probe. maker≠judge reward-control VERIFIED working (not rubber-stamping). firesSinceDrill reset 0.
- 리스크: `stripInjectionEvasionChars` is now a strict subset of the normalizer (dead-ish but kept as a tested public export — harmless). VALUE-CLASS: grounding-floor (the maintained floor) — next fire should rotate to memory-integrity (G3) or another self-dev/orchestration surface for diversity.

## fire 11 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — per-fact provenance DERIVED from the belief-provenance log (no migration)
meta: value-class=new-capability · pkg=@muse/memory+@muse/cli · kind=memory-integrity · surface=memory-integrity · verdict=PASS(Opus ④b judge 6/6, mutation-tested) · firesSinceDrill=1 · surfaces=1(per-fact provenance+freshness)
ratchet: testFiles 1054 · fabrication 0 · memory 436→438 / cli 2735 / check green / eval:self-improving all batteries PASS / lint
- 무엇: STALE-finding — the gap-scout claimed a risky `facts: Record<string,string>` migration, but the per-fact provenance DATA already exists in the append-only `BeliefProvenance` log (`learnedAt` written on every learn). New `deriveFactProvenance` aggregates the log per key → firstSeen(min)/lastConfirmed(max)/confirmCount/source(user-outranks-auto)/value(at-latest); new `classifyFactFreshness` → fresh/aging/stale; wired into `muse memory why <key>` ("confirmed N× since <firstSeen> · <freshness>").
- 왜: memory-integrity foundation — the named prerequisite (was T1-b-i) that UNBLOCKS G4 (promotion gate uses confirmCount/lastConfirmed) + gives a fact freshness signal ("this fact is stale — re-confirm?"). Derived-from-log avoids a corruption-risk store migration entirely (never loses a fact).
- 리뷰지점: Opus judge PASS 6/6 + MUTATION-TESTED (3 mutations — firstSeen=last, source-auto-wins, stale `>`-vs-`>=` — each went RED → tests are load-bearing, not declaration-only). Stale-finding verified correct (data really exists in the log, both write paths confirmed). Existing formatBeliefWhy tests preserved.
- 리스크: an unparseable `learnedAt` could surface as lastConfirmed (fail-soft to "fresh"; the validator already requires non-empty ISO, so real entries are safe) — non-blocking. VALUE-CLASS: fires 9 self-dev → 10 grounding-floor → 11 memory-integrity — full surface rotation across the widened theme.

## fire 12 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — memory-promotion gate (fail-close, consumes G3)
meta: value-class=new-capability · pkg=@muse/memory+@muse/cli · kind=memory-integrity · surface=memory-integrity · verdict=PASS(Opus ④b judge 6/6) · firesSinceDrill=2 · surfaces=1(durable-promotion gate)
ratchet: testFiles 1054 · fabrication 0 · memory 438→441 / cli 2735→2737 / check green / eval:self-improving 14 batteries PASS (grounding/misgrounding all green; slow tail timeout-cut, G4 doesn't touch grounding) / lint
- 무엇: new `selectPromotableFacts` (mirrors `selectPromotableMemories`) — a fact earns DURABLE status iff NOT injection-flagged AND (source=user [immediate] OR confirmCount≥3 AND recent). FAIL-CLOSE: an injection value is never promoted however confirmed; `isInjection` is DI'd (CLI passes real `isMemoryInjection`, keeps @muse/memory agent-core-free). Pure (selects, never promotes-in-place). Wired into `muse memory why` ("· durable"/"· provisional").
- 왜: closes the G3→G4 memory-integrity arc — the fact store becomes self-curating: a once-seen auto-extract (possibly a mis-extraction) stays PROVISIONAL until reconfirmed, a user-stated truth is durable immediately, and a poisoned value can NEVER earn durable trust. Higher-confidence grounding foundation.
- 리뷰지점: Opus judge PASS 6/6 — fail-close verified real AND wired end-to-end (the memory-why test proves the isMemoryInjection DI is live, not the no-op default); pure/no-partial-side-effects; user-source bypass defensible (staleness decoupled — surfaced separately by classifyFactFreshness). Judge flagged + I removed a stray `.ts.x` editor backup before staging.
- 리스크: durable/provisional not yet tied to GROUNDING confidence (a provisional fact should ground more cautiously) — logged as the higher-value G4 follow-up. VALUE-CLASS: fires 11+12 both memory-integrity (G3→G4 arc, 2 in a row — NOT 3); fire 13 should rotate to orchestration (G5) or self-dev for surface diversity.

## fire 13 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — subtask dedup gate (MAST #3) + parsePlannerLines fix
meta: value-class=new-capability · pkg=@muse/multi-agent+@muse/cli · kind=orchestration-hardening · surface=orchestration · verdict=PASS(Opus ④b judge 6/6) · firesSinceDrill=3 · surfaces=1(subtask dedup, both sources+ask)
ratchet: testFiles 1054 · fabrication 0 · multi-agent 105 / cli 2740 / check green / eval:self-improving 16 batteries PASS / lint
- 무엇: Opus scout found the ONE open MAST gap (the other 4 enforced+tested): no dedup of subtask TEXT. New `dedupeSubtasks` (normalized keep-first, re-id, drop-empty) in `runLeadWorkerTask` after BOTH sources (structural+planner) → one chokepoint covers both AND the ask path. + fixed `parsePlannerLines` (the dedup surfaced it): old greedy `^[-*•\d.)\s]+` ate the `1` from `1분기` → identical "분기 정리" ×3 (manufactured dups); precise `^(?:[-*•]|\d+[.)])\s*` preserves `1분기`.
- 왜: MAST #3 — a planner returning ["요약","요약"] runs the SAME work twice on a single GPU (Anthropic's "vague sub-agent → identical searches"). The parsePlannerLines bug was a SECOND dup source (collapsing distinct quarters), surfaced when the dedup flipped a pre-existing test's decomposed expectation.
- 리뷰지점: G5 (chat orchestration parity) DEFERRED — Opus scout: poor fit (streaming chat vs batch decompose = UX stall; "don't fork" already satisfied at engine level). Opus judge PASS 6/6 — probed 18 regex cases + dedup false-merge (distinct quarters/spellings stay distinct); OUTCOME (execute once); ask-path inheritance real.
- 리스크: structural-source dup covered-by-construction but not directly tested; a bare-decimal planner line ("3.14 파이") still mangled (same as before — theoretical). VALUE-CLASS: fire 13 = orchestration (4th & final surface of the widened theme now ALL hit: self-dev·grounding·memory·orchestration).

## fire 14 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — tie provisional facts to grounding (the G3→G4 edge payoff)
meta: value-class=new-capability · pkg=@muse/memory+@muse/recall+@muse/cli · kind=memory-integrity+grounding · surface=memory-integrity · verdict=PASS(Opus ④b judge 7/7) · firesSinceDrill=4 · surfaces=1(provisional→grounding)
ratchet: testFiles 1054 · fabrication 0 · memory 441→443 / recall 319→321 / cli 2740 / check green / eval:self-improving 14 batteries PASS (completed slowly post-commit; 0 regression) / lint
- 무엇: the edge payoff of fires 11-12 (provenance → promotion gate). New pure `provisionalFactKeys` (known-but-not-durable; unknown/legacy NOT over-marked) + `buildMemoryContextBlock` provisional annotation + `muse ask` wiring (loads belief-provenance, derives, marks, fail-soft). A once-seen auto-extract renders "(unconfirmed)" in the grounding block.
- 왜: GROUNDED != TRUE on the SOURCE side — the gate matches a claim to a real fact, but if that fact is a once-seen mis-extraction it shouldn't be asserted as confirmed truth. The promotion gate (fire 12) decided durable-vs-provisional; this surfaces it where it matters (grounding). Injection value → marked provisional AND defanged (defense-in-depth).
- 리뷰지점: Opus judge PASS 7/7 — known-but-not-durable correctness (no over-marking of legacy facts), suffix-only (no fact dropped, no citation altered), fail-soft real, efficacy honestly scoped (deterministic marking + prompt signal, not a claim the 8B obeys). 3-package coordinated slice.
- 리스크: a prompt "(unconfirmed)" tag may be under-weighted by the 8B (the memory finding) — but the marking is deterministic + honest + the foundation for a deterministic treatment (e.g. exclude provisional from confident grounding). VALUE-CLASS: named candidates G1-G6 + follow-ups now done; next fire = gap-scout to refill the 4 surfaces.

## fire 15 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — fan-in objective-satisfaction verifier (maker≠judge on the ask decomposition)
meta: value-class=new-capability · pkg=@muse/multi-agent+@muse/cli · kind=orchestration-hardening · surface=orchestration · verdict=PASS(Opus ④b judge 6/6) · firesSinceDrill=5 · surfaces=1(fan-in verify)
ratchet: testFiles 1054 · fabrication 0 · multi-agent 105→111 / cli 2741 / check green / eval:self-improving 28 batteries PASS (completed slowly post-commit; 0 regression) / lint
- 무엇: gap-scout (refill batch 2) found the CLI ask synthesis was a shallow SELF-REPORT (MAST "done-by-self-report") while the server had `verifyFinalAnswer`. New deterministic `verifySynthesisCoverage` (a completed sub-task whose salient tokens are ENTIRELY absent from the synthesis = dropped; conservative). `LeadWorkerDeps.verifySynthesis?` + `synthesisIncomplete` (fail-soft) wired into `runDecomposedAgentAsk`; `muse ask` surfaces "⚠ some sub-results may be missing".
- 왜: maker≠judge on Muse's PRIMARY surface — a fan-in that silently drops a worker is a confident-but-incomplete answer (GROUNDED-adjacent: the user is told "done" when a sub-result vanished). The server had the judge; the CLI didn't (verified via gap-scout: grep verifyFinalAnswer lead-worker.ts = 0).
- 리뷰지점: Opus judge PASS 6/6 — heuristic sound (false-positive guards: failed/empty/stopword-only skipped; conservative under-flag is the right bias for a user note), answer/grounding-gate untouched (only a flag), fail-soft real, back-compat. Flag-only (not auto-retry) → correctly NOT a reflection-guard retry surface.
- 리스크: lexical-coverage is a heuristic (a dropped sub-task sharing ONE token with the synthesis under-flags — degrades to prior silent behavior, never a false alarm). Auto re-synthesis (the server's evaluator-optimizer loop) is logged as H1 follow-up. VALUE-CLASS: fire 15 = orchestration (rotated off the memory-heavy fires 11/12/14). Refill batch 2 (H1✓ H2-H4◦) keeps the loop fueled ~3 more fires.

## fire 16 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — value-volatility in the promotion gate (self-correction of the fire-12 flaw)
meta: value-class=new-capability(bugfix-hardening) · pkg=@muse/memory+@muse/cli · kind=memory-integrity · surface=memory-integrity · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=6 · surfaces=1(volatility gate)
ratchet: testFiles 1054 · fabrication 0 · memory 443→446 / cli 2742 / check green / eval:self-improving slow on loaded box (memory-integrity; volatility only adds provisional marks to flipped facts — grounding batteries use stable/user facts) / lint
- 무엇: the gap-scout (refill batch 2, H2) found a flaw in the builder's OWN fire-12 promotion gate — `confirmCount` was BLIND to whether the value flipped. New `distinctValueCount` on FactProvenance; `selectPromotableFacts`' auto branch requires `distinctValueCount === 1` (stable). A volatile auto belief (X→Y→Z) no longer auto-promotes; a user-stated flip still does (latest = truth). `muse memory why` shows "value changed N× (volatile)".
- 왜: the INVERSE signal — `confirmCount: 3` from a belief the user/extractor CHANGED 3× ("address X→Y→Z") was promoted as durable truth identically to a stably re-confirmed fact. A volatile belief asserted as durable is a confident wrong memory (GROUNDED != TRUE on the source side). The provisional chain (fire 14) automatically caveats it "(unconfirmed)".
- 리뷰지점: Opus judge PASS 5/5 — the corrected-then-stable edge (X→Y→Y→Y stays provisional forever) is a DEFENSIBLE fail-safe (provisional ≠ dropped; user-confirm promotes it), and the chosen distinctValueCount is MORE conservative than a current-value-run metric (which would auto-promote a previously-contradicted value — riskier for the floor). Only STRENGTHENS the gate (conjunctive clause), no floor weakening; user-source + injection fail-close preserved.
- 리스크: a legitimately-corrected auto belief stays provisional until the user confirms it (conservative by design). VALUE-CLASS: fire 16 = memory-integrity (rotated off orchestration 13/15); the loop is fixing its OWN earlier gaps now (a healthy maturity signal). H4 (volatile-belief weakness axis) now unblocked.

## fire 17 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — surface volatile beliefs in the recap (close the H2 loop)
meta: value-class=new-capability · pkg=@muse/memory+@muse/cli · kind=self-development · surface=self-development · verdict=PASS(Opus ④b judge 5/5, defect fixed in-fire) · firesSinceDrill=7 · surfaces=1(volatile-belief recap nudge)
ratchet: testFiles 1054 · fabrication 0 · memory 446→448 / cli 2742→2744 / check green / eval:self-improving slow on saturated box (self-dev recap — additive, no grounding path; deterministic gates all green) / lint
- 무엇: new pure `selectVolatileBeliefs` (@muse/memory) = recently-active AUTO beliefs the extractor keeps flipping (distinctValueCount≥2; user-stated excluded — their latest is deliberate truth). The evening recap renders "🔄 These keep changing — confirm" with a runnable `muse memory set <kind> <key> <value>` nudge.
- 왜: closes the H2 monitor→detect→remediate loop — H2 (fire 16) demotes a volatile belief to provisional; H4 surfaces it so the user confirms → `muse memory set` makes it user-source → `selectPromotableFacts` promotes it durable. Mirrors fire 9's source-conflict recap nudge (self-dev whetstone surface).
- 리뷰지점: Opus judge PASS 5/5 — caught a REAL defect (the nudge command was `muse memory set <key> <value>`, missing the `<kind>` first positional → would be rejected if pasted) → fixed in-fire (added kind to VolatileBelief + the render) so the command actually runs. Additive recap section (no existing output changed), fail-soft, single-user scoping consistent with the weakness ledger.
- 리스크: single-user scoping (deriveFactProvenance over all log entries, no userId filter) — acceptable for Muse's single-user default (same as the weakness ledger). VALUE-CLASS: fire 17 = self-development (last self-dev was fire 9). Refill batch 2 now H1✓ H2✓ H4✓; H3 (semantic conflict, grounding-floor, embedding-heavy) remains. firesSinceDrill=7 → fire 18 (consecutive allPASS≥8) is the JUDGE-DRILL.

## fire 18 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — JUDGE-DRILL + Hangul-labelled source-conflict detection
meta: value-class=new-capability(judge-drill) · pkg=@muse/recall · kind=grounding-floor · surface=grounding-floor · verdict=PASS(Opus ④b judge 5/5; DRILL: bad-slice→FAIL→rollback→real→PASS) · firesSinceDrill=0(reset) · surfaces=1(Hangul conflict)
ratchet: testFiles 1054 · fabrication 0 · recall 323→327 / check green / eval:self-improving 24 PASS / lint · JUDGE-DRILL passed (verifier caught the planted bad slice)
- 무엇: JUDGE-DRILL (firesSinceDrill≥10 OR 연속 allPASS≥8 트리거). (1) BAD slice: `LABELLED_VALUE` first char `[\p{L}]` but rest stays ASCII (2-char Hangul "주소" NOT detected) + DECLARATION-ONLY test (`length >= 0`). (2) Independent Opus judge → VERDICT FAIL (caught both: declaration-only test + behavior broken, verified by running dist). (3) Rollback. (4) REAL fix: label class `[\p{L}][\p{L}\p{N} ]` (BOTH chars) + Korean PROSE_LABELS + 3 OUTCOME tests. (5) Judge → PASS 5/5.
- 왜: the maker≠judge compensating control (Muse runs ONE Opus tier so judge=maker model) needs periodic proof it actually catches a bad slice, not rubber-stamps. The drill PROVED the verifier fails a declaration-only/broken slice AND passes the real fix — and shipped a real grounding-floor capability (Korean source-conflict detection, our GROUNDED≠TRUE edge now sees Hangul-labelled disagreements).
- 리뷰지점: judge verified by BUILDING + RUNNING the dist (not trusting the diff) on both slices. REAL judge confirmed: OUTCOME-tested (toHaveLength(1)+field), strengthens (superset regex), false-positive = parity with existing English prose (not a new class), all guards hold (clock/digit/URL/prose), 327/327, honestly scoped (Hangul-LABELLED, not EN↔KO semantic which is deferred H3b).
- 리스크: widened `[\p{L}\p{N}]` could parse non-Korean prose colons — mitigated by cross-hit-repeated-field requirement + Korean PROSE_LABELS (same posture as the pre-existing English behavior). EN↔KO semantic field matching (주소≡address) needs embeddings → deferred H3b. VALUE-CLASS: fire 18 = grounding-floor (rotated off self-dev 17). JUDGE-DRILL complete → counter reset 0.

## fire 19 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — dependency-aware sequenced decomposition (MAST reasoning-action mismatch)
meta: value-class=new-capability · pkg=@muse/multi-agent+@muse/cli · kind=orchestration · surface=orchestration · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=1 · surfaces=1(sequenced threading)
ratchet: testFiles 1054 · fabrication 0 · multi-agent 111→118 / cli 2745 / check green / eval:self-improving 21 PASS / lint
- 무엇: a gap-scout (refill batch 3, J1) found a MAST reasoning-action mismatch — `extractSequencedSteps` split "먼저 X … 그 다음 Y" into sub-tasks but the lead-worker ran each in ISOLATED context, so a sequenced step 2 (act on step 1's result) ran BLIND. FIX: `decomposeRequestWithKind` flags sequenced vs independent; `execute(subtask, priorContext?)` threads completed prior outputs forward for sequenced splits only (fail-close on failed/blank); `ask-decompose` prepends "이전 단계 결과:". Independent lists + model-planned splits stay isolated.
- 왜: a confident-but-wrong synthesis where step 2 extracted from nothing is fabrication-adjacent (the fan-in verifySynthesisCoverage can't catch it — step 2 "completed" with non-empty output). Closes the one real coordination gap on Muse's only live fan-out path; diversifies off grounding-floor (fire 18).
- 리뷰지점: Opus judge PASS 5/5 — drove runLeadWorkerTask against dist (sequenced→step2 sees step1, independent/planner→isolated, blank-prior→not threaded). Sound distinction (numbered/mixed/planner default to isolation = safe direction), fail-close real, all existing invariants (groundingGate/dedup/cap/verifySynthesis) byte-unchanged, back-compat signature, the changed isolation test is an HONEST clarification (old sequenced fixture's assertion is now genuinely false → numbered list is the correct independent fixture; coverage increased).
- 리스크: threads ALL prior completed outputs (bounded by MAX_SUBTASKS=8 — no unbounded bloat). LIVE proof (does the 8B actually USE the threaded context) deferred to a verify-decomposition.mjs case (box saturated) — the deterministic mechanism is fully proven; the live add is additive quality. VALUE-CLASS: fire 19 = orchestration (rotated off grounding-floor 18). firesSinceDrill=1 (drill was fire 18).

## fire 20 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — faithfulness verifier on the LIVE in-chat reflection (GROUNDED≠TRUE asymmetry)
meta: value-class=new-capability · pkg=@muse/cli(+agent-core reuse) · kind=self-development · surface=self-development · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=2 · surfaces=1(in-chat reflect verifier)
ratchet: testFiles 1054 · fabrication 0 · cli 2745→2753 / reflection-guard 6→7 surfaces / check green / eval:self-improving 16 PASS / lint
- 무엇: a deep gap-scout found the OFFLINE dreaming reflection is RGV-gated (`verifyReflectionsGrounding`) but the LIVE in-chat one (`synthesizeReflection`, used by `/reflect` + morning brief) was NOT — a confabulated cross-session "observation" reached the chat raw. FIX: `synthesizeReflection` gains `reverify?` (fail-close: NO/throw/empty-evidence→drop); `buildModelGroundingReverify` reuses the SAME exported reverify primitives; `chat-ink reflectInsight` builds+passes it (gates both surfaces); in-chat surface added to the reflection-guard registry.
- 왜: fabrication=0 must hold under EVERY surface CLAUDE.md names (recall/proactivity/reflection/vision). Reflection was gated offline but NOT on the user-facing live path = a real GROUNDED≠TRUE leak ("you keep coming back to X and never finished Y" — citing a real topic, inventing the unfinished-Y). The fix REUSES the proven offline judge, not a new one.
- 리뷰지점: Opus judge PASS 5/5 (built+ran dist) — live path REALLY gated (reflectInsight unconditionally passes the reverify; not a dead optional dep), fail-close real, faithful reuse (same REVERIFY primitives, `{"supported":...}` JSON shape confirmed), back-compat, floor strictly strengthened (only drops). Honest minor diff: in-chat does 1 judge call vs offline k-sample consensus (per-poll latency, still fail-close).
- 리스크: single judge call (no k-consensus) on the live path — acceptable for latency, fail-close holds. LIVE negative-entailment case deferred (box saturated; deterministic gate + model-built-judge e2e fully prove it). STALE-BACKLOG: confirmed H3b semantic-PROSE already built (detectEvidenceContradictions); cross-lingual is a deliberate fail-open. VALUE-CLASS: fire 20 = self-dev new-capability (rotated off orch 19); deep scout assessed surfaces saturated → escalated to this real verifier-gap (not a micro-fix).

## fire 21 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — faithfulness gate on the proactive Phase D push notice (GROUNDED≠TRUE on the PUSH surface)
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/mcp+@muse/cli+@muse/api · kind=proactivity/grounding-floor · surface=grounding-floor · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=3 · surfaces=1(proactive notice verifier)
ratchet: testFiles 1055 · fabrication 0 · agent-core 2474 / mcp 1861 / cli 2753 / reflection-guard 7→8 surfaces / check green / eval:self-improving 14 PASS / lint
- 무엇: a focused scout (the K1 gate-asymmetry pattern again) confirmed VISION is already gated but found the proactive Phase D notice (`synthesizeNoticeText`, T=0.4 prose over factSheet) reaches the user as an UNASKED PUSH through only length/JSON filters — no faithfulness check. FIX: shared `buildGroundingReverify` (@muse/agent-core, narrow-provider via free-text YES/NO fallback); `synthesizeNoticeText` gains `reverify?` → fail-close to the verbatim store-grounded `item.text` on NO/throw/empty; BOTH live callers (commands-proactive + proactive-tick) wired; reflection-guard registry +1.
- 왜: a proactive push is higher-trust than a Q&A answer (the user didn't ask) — a confabulated "standup moved to 3pm in Room B" is maximally damaging and bypassed every gate. fabrication=0 must hold on the PUSH surface too. The fail-close target (item.text) is grounded by construction (built from the real CalendarEvent/Task), so degrading to it is safe.
- 리뷰지점: Opus judge PASS 5/5 (ran dist + traced the chain) — both callers REALLY gated (tick-daemons→proactive-tick→…→reverify; commands-daemon doesn't run Phase D, absence correct), fail-close target safe (not another LLM output), faithful reuse (same REVERIFY primitives, free-text fallback), registry non-vacuous (mutation trips the guard). stale-dist @muse/auth flake on first check → clean-rebuild green.
- 리스크: single judge call (latency), live-Ollama proof deferred (deterministic + buildGroundingReverify unit fully prove it). Minor pre-existing duplication (fire-20 buildModelGroundingReverify vs the new shared one, responseFormat tradeoff) → consolidation follow-up. VALUE-CLASS: fire 21 = grounding-floor new-capability (rotated off self-dev 20); the gate-asymmetry pattern (K1→L1) has now closed reflection + proactivity; recall + vision confirmed already-gated → all 4 CLAUDE.md surfaces gated.

## fire 22 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — contested-fact (volatile-value) caution on the recall/ask hot path
meta: value-class=new-capability · pkg=@muse/memory+@muse/recall+@muse/cli · kind=memory-integrity · surface=memory-integrity · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=4 · surfaces=1(point-of-use contested caution)
ratchet: testFiles 1056 · fabrication 0 · memory 450→451 / recall 331 / cli 2753 / check green / eval:self-improving 14 PASS / lint
- 무엇: a capability scout found `selectVolatileBeliefs` (value flipped across confirmations) was consulted ONLY in the daily recap — never on the ask hot path. So a 5×-confirmed-but-oscillating fact got the WRONG "(unconfirmed — learned once)" mark. FIX: `buildMemoryContextBlock` gains `contestedKeys?` → "(value has changed before — confirm it's current)" with PRECEDENCE over the once-seen mark; new pure `contestedFactKeys` (mirrors provisionalFactKeys, lifts the top-3 cap); `commands-ask` wires it from the already-loaded provenance.
- 왜: GROUNDED≠TRUE on the memory SOURCE side — the citation gate checks claim↔evidence, detectSourceConflict checks evidence-vs-evidence, but nothing flagged a SINGLE memory key whose own value is internally contested. Muse already KNEW it was volatile; the answer just asserted it (or worse, called a contested value "learned once"). Now an answer cautions "confirm it's current".
- 리뷰지점: Opus judge PASS 5/5 (ran dist) — precedence fixes the wrong label, contested ALSO provisional so precedence matters, selectVolatileBeliefs exclusions (user-flipped/stale) carry through, wiring real (live provenance, not dead set). Judge caught the top-3-cap edge (4th+ volatile match missed) → fixed in-fire (maxResults lifted; fail-soft anyway since the missed key still got the provisional mark).
- 리스크: read-time caution only; write-time refinement-vs-contradiction NLI reconciliation (Mem0 UPDATE) is the L follow-up. STALE-BACKLOG: scout confirmed memory consolidation/decay (Mem0 ADD/UPDATE/NOOP/DELETE + ACT-R recallActivation) ALREADY BUILT. VALUE-CLASS: fire 22 = memory-integrity new-capability (rotated off grounding-floor 21); builds on fires 14(provisional)+16(distinctValueCount)+17(selectVolatileBeliefs) — coherent arc.

## fire 23 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — cross-subtask conflict reconciliation on the fan-in (grounding edge on the fan-OUT)
meta: value-class=new-capability · pkg=@muse/agent-core+@muse/multi-agent+@muse/cli · kind=orchestration · surface=orchestration · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=5 · surfaces=1(fan-in conflict)
ratchet: testFiles 1056 · fabrication 0 · agent-core 2474 / multi-agent 111→125 / cli 2754 / check green / eval:self-improving 14 PASS / lint
- 무엇: a capability scout found the fan-in checked COVERAGE (each subtask represented) but NOT CONSISTENCY — two completed workers asserting contradicting values ("deadline Tuesday" vs "Wednesday") got silently concatenated into an internally-inconsistent answer (both passed groundingGate). The contradiction primitive existed but applied only to source notes. FIX: EXTRACTED `detectPairwiseContradictions` shared core (agent-core); new `detectSubtaskConflicts` (multi-agent) over completed sub-answers; LeadWorkerDeps.detectConflicts + LeadWorkerResult.subtaskConflicts; ask threads embed + surfaces "⚠️ sub-results disagree".
- 왜: the grounding edge applied to the fan-OUT — a self-contradicting answer where each half is individually grounded is a GROUNDED≠TRUE fabrication coverage-checking can't catch (the same gate the evidence layer has, now on the orchestrator's sub-answers). One shared detector = the contradiction POLICY can't drift between the two layers.
- 리뷰지점: Opus judge PASS 5/5 (built+ran dist) — the extraction is byte-identical (all 17 pre-existing detectEvidenceContradictions tests pass — the highest-risk refactor validated), ONE shared detector with no opts (identical thresholds), only completed non-empty sub-answers compared, ask wiring real (live embed + stderr cue), fail-soft, back-compat (no embed → no check). False-positive bounded (0.5 overlap + neither-subset; worst case a "verify" cue, never a drop).
- 리스크: read-time stderr cue only (doesn't BLOCK the answer — correct: it's a heads-up, the user verifies). VALUE-CLASS: fire 23 = orchestration new-capability (rotated off memory 22). STALE-BACKLOG: chat single-turn BY DESIGN (no parity gap). Runner-up follow-up: remediationHint never reaches the runtime prompt (weakness learn→apply open at runtime — needs deterministic-not-prompt design, the "security≠prompt" rule).

## fire 24 · 2026-06-20 · skill loop-creator v(substantial-fire) · (this commit) — runtime weakness nudge on the ASK path (close learn→apply at point-of-use)
meta: value-class=new-capability · pkg=@muse/mcp+@muse/cli · kind=self-development · surface=self-development · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=6 · surfaces=1(ask-time weakness nudge)
ratchet: testFiles 1056 · fabrication 0 · mcp 1861→1864 / cli 2754 / check green / eval:self-improving 14 PASS / lint
- 무엇: a confirmed RECURRING weakness was surfaced only in the daily recap, not at the moment the user hits the same wall via `muse ask`. STALE-BACKLOG: the CHAT path ALREADY had an inline KO/EN runtime nudge (chat-repl.ts:693) — the scout over-stated the gap as "ask/chat"; real gap = ASK only. FIX: new pure `askTimeWeaknessNudge` (recurring user-remediable topic, count≥2, not mastered); `commands-ask` reads the ledger after recording + surfaces a KO/EN axis-aware "💡 …" stderr cue.
- 왜: the learn→apply loop was open at the ASK runtime layer — Muse RECORDED the repeated failure but never told the user "you've hit this N× — add a note" at the failure point (only at night). A deterministic USER-facing cue (NOT a model-steering prompt — respects security≠prompt; the floor/abstention is untouched) closes it, point-of-use like fires 20/21/22.
- 리뷰지점: Opus judge PASS 5/5 (ran dist) — the cue fires AFTER the answer is streamed (no prompt injection, floor untouched), wiring real (ledger read after upsert → 2nd recurrence fires), excludes dev-fixable axes (never tells the user to fix Muse's OWN bug), STALE-BACKLOG honest (chat already done → ask-only is correct parity, not under-scoped). New helper is more general than chat's inline (grounding-gap + source-conflict + mastery).
- 리스크: leaving the chat's inline nudge (vs DRY-unify onto the shared helper) = a noted follow-up. VALUE-CLASS: fire 24 = self-dev new-capability (rotated off orch 23). NOTE: 진안 directive this fire — PUSH to origin/main after each commit (Tier1 no-push default overridden by explicit user instruction).

## fire 26 · 2026-06-20 · skill v2.0.0 · (this commit) — wire the DEAD time-parse weakness axis to its deterministic producer
meta: value-class=new-capability(wiring) · pkg=@muse/mcp+@muse/cli · kind=self-development · surface=self-development · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=7 · surfaces=1(time-parse producer)
ratchet: testFiles 1057 · fabrication 0 · mcp 1864→1871 / cli 2763 / check green(stale-dist @muse/api flake → clean-rebuild green) / eval:self-improving 14 PASS / lint
- 무엇: gap-scout found `time-parse` was a DEAD weakness axis — declared/remediable/doctor-displayed but ZERO producers (only test fixtures). New pure `recordTimeParseWeakness(phrase, failed, deps)` (@muse/mcp); wired at BOTH calendar time-phrase parse sites (add + sibling edit, fail-soft on the `!parseEventStart` branch). A code-detected time-misread now reaches selectDevFixableWeaknesses/doctor.
- 왜: the whetstone loop (monitor→detect→classify→remediate) can't remediate a failure class that's never recorded. A time-misread is high-blast-radius on a JARVIS assistant (reminder/event on the wrong day — MEMORY tool-time-field-naming records this exact 0/8→8/8 class). The parser (not the model) deciding "can't resolve" is a clean deterministic oracle — exactly what whetstone wants as code, not self-report. Same dead-end-detector class fire 9 fixed for source-conflict.
- 리뷰지점: v2 ADAPTIVE Opus judge PASS 5/5 — ran dist (garbage --at → time-parse entry → dev-fixable; valid --at → none), MUTATION-PROVEN (judge flipped the add-branch call true→false → integration test went RED), 형제-감사 (both add+edit wired = grep count 2; --repeat=enum/--remind=numeric correctly excluded; loopback deferral honest — that handler lacks weaknessesFile). Purely additive, floor untouched.
- 리스크: loopback-reminder `parseReminderDueAt` (agent path, lacks env) + `wrong-tool` sibling dead axis (no clean oracle) deferred to backlog. v2 NOTE: ④b judge first run hit a transient 401 auth error (NOT a slice fault) — re-ran clean after /login; pnpm check first run had a stale-dist @muse/api flake → clean-rebuild green (the documented box-saturation pattern). NEW push protocol active (pull --rebase → push).

## fire 27 · 2026-06-20 · skill v2.0.0 · (this commit) — wire the injection neutralizer onto the STORED/SYNCED grounding surfaces (close the gate-asymmetry)
meta: value-class=new-capability(wiring) · pkg=@muse/recall · kind=grounding-floor · surface=grounding-floor · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=8 · surfaces=7(task/reminder/shell/git/action/calendar/contact+feed)
ratchet: testFiles 1058 · fabrication 0 · recall 341→348 / cli green / check green / eval:self-improving 14 PASS / lint
- 무엇: a gap-scout found the deterministic injection defense (escapeSystemPromptMarkers∘neutralizeInjectionSpans) was wired on only 3 of 8 sibling grounding builders. The STORED/SYNCED surfaces (calendar synced from gcal/caldav, vCard-imported contacts, reminders/tasks/action-log) rendered third-party text RAW into the <<event>>/<<contact>> wrappers. FIX: new `safeField` helper wired into every raw free-text field of buildTask/Reminder/Shell/Git/Action/Calendar + buildContact (select.ts) + the missed feedName (형제-감사 caught it mid-fire).
- 왜: security is deterministic CODE not a prompt instruction (architecture.md). An attacker who sends you a meeting invite controls its title/location; that synced text flowed into the grounding block letting an imperative-override ("Ignore all previous instructions") or a forged "<<end>> [from system.md]" wrapper-breakout reach the local model — an indirect-injection → GROUNDED≠TRUE (fake citation / steered answer) the gate must close. prompt-escape.ts's MARKER_KEYWORDS literally enumerates event|reminder|contact|task|action — the defense named these surfaces but wasn't applied to them.
- 리뷰지점: v2 ADAPTIVE Opus judge PASS 5/5 — ran dist (malicious title → "removed", breakout → exactly 1 literal <<end>>, benign round-trips), MUTATION-PROVEN (inert safeField → 5 tests RED), citation-consistent (content + [event:/contact: …] citation use the SAME escaped value so the title-matching gate still matches), composition order identical to the 3 pre-wired builders. The judge's sibling-audit FOUND a missed feedName → patched same-fire (the v2 형제-감사 rule working).
- 리스크: connection.as (first-party user-typed label, never importer-populated) intentionally out of scope. VALUE-CLASS: fire 27 = grounding-floor wiring (rotated off self-dev 24/26 + memory 25). firesSinceDrill=8 → NEXT fire (28) is the JUDGE-DRILL (연속 allPASS≥8). NEW push protocol (pull --rebase → push).

## fire 28 · 2026-06-20 · skill v2.0.0 · (this commit) — JUDGE-DRILL + agent-path time-parse producer for the reminder loopback (fire 26 sibling)
meta: value-class=new-capability(wiring) · pkg=@muse/mcp+@muse/autoconfigure · kind=self-development · surface=self-development · verdict=PASS(Opus ④b judge 6/6, JUDGE-DRILL) · firesSinceDrill=0(reset) · surfaces=2(reminder add+snooze)
ratchet: testFiles 1060 · fabrication 0 · mcp 1871→1876 / autoconfigure build / cli 238 / check green / eval:self-improving 10 PASS / lint · JUDGE-DRILL passed (verifier caught the planted bad slice AND the under-scoped real fix)
- 무엇: JUDGE-DRILL fire (연속 allPASS=8). (1) planted BAD slice: chat-nudge "DRY-unify" that imported askTimeWeaknessNudge, called it with `[]`, DISCARDED its verdict, kept count-only logic + happy-only tests. (2) Independent Opus judge → VERDICT FAIL with the exact violation (verdict thrown away, `[]` makes it structurally undefined, tests re-assert old behavior). (3) Rollback. (4) STALE-BACKLOG discovered: chat DRY-unify was ALREADY built by a concurrent loop (chatRepeatWeaknessNudge, mastery-aware) → pivoted the real fix to fire-26's deferred sibling: the loopback reminder agent-path time-parse producer (add + snooze branches). (5) Judge FAILed the first real-fix attempt (missed the in-file `snooze` sibling) → patched same-fire → re-judge PASS 6/6.
- 왜: the maker≠judge compensating control needs periodic proof it catches a BAD slice — and this drill proved it TWICE (the planted inert-DRY slice AND the under-scoped real fix's missed snooze sibling). The shipped capability completes the time-parse axis on the AGENT path so an agent `reminder add/snooze` with an unresolvable dueAt surfaces in the dev-fixable list, like the CLI calendar producer (fire 26).
- 리뷰지점: v2 ADAPTIVE judge cited SPECIFIC violations both times (no vague rollback) — drill: "verdict discarded + `[]` + happy-only"; real-fix v1: "in-file snooze sibling missed (precedent fire 26 wired both add+edit)". Re-judge PASS: mutation-proven (snooze call removed → length 2→1 RED), in-file complete (both parseReminderDueAt sites), prod-live (resolveWeaknessesFile(env)).
- 리스크: cross-file siblings (loopback-calendar startsAt, loopback-followups scheduledFor) un-instrumented → backlog follow-up (separate slices, not in-file). lesson: a time-parse/producer slice MUST enumerate EVERY parse-failure branch IN THE TOUCHED FILE (fire 26 set the both-sites precedent); the judge's sibling-audit is the backstop. JUDGE-DRILL complete → firesSinceDrill reset 0. NEW push protocol (pull --rebase → push).
lesson: when wiring a deterministic-failure PRODUCER (time-parse, injection-defang), grep EVERY call site of the failing parser IN THE FILE and instrument all branches the same fire — a single-branch wiring is an under-scoped FAIL the ④b sibling-audit catches.

## fire 29 · 2026-06-20 · skill v2.0.0 · (this commit) — fan-in source-leak: an ungrounded sub-task's sources must not grade the answer
meta: value-class=new-capability · pkg=@muse/multi-agent+@muse/cli · kind=orchestration · surface=orchestration · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=1 · surfaces=1(fan-in source filter)
ratchet: testFiles 1062 · fabrication 0 · multi-agent 125→131 / cli 2774 / check green / eval:self-improving 16 PASS / lint
- 무엇: a gap-scout found the decomposition path merged every sub-task's retrieved sources BEFORE judging its output — so an ungrounded (refused) sub-task's `secret.md` stayed in the evidence the synthesized answer was graded against + the Sources footer. FIX: `SubtaskExecution.sources?` status-linked (runOne threads them into all returns); `ask-decompose` derives mergedSources from only completed executions ∪ the synthesis run's sources. RATCHET: orchestration (the diversity-target — last 8 were 4× self-dev).
- 왜: the fan-out's flat source merge introduced a GROUNDED≠TRUE seam the single-run path doesn't have — the answer could be marked grounded on / cite a source NO surviving sub-task used (a fabricated citation). The engine already withheld a refused sub-task's OUTPUT; this extends "withheld" to its SOURCES, so it grades nothing.
- 리뷰지점: v2 ADAPTIVE Opus judge PASS 5/5 — ran dist (secret.md DROPPED, completed actions.md + synth.md KEPT), MUTATION-PROVEN (revert to merge-all → leak test RED), and crucially NO FALSE-NEGATIVE (the dangerous direction): single-completed + non-decomposed paths still keep their real sources (allowed = completed-executions sources ∪ synthesis sources, not synthesis-only). Floor strengthened (only drops leaked sources).
- 리스크: relies on SubtaskExecution.sources being populated by the ask `execute` impl (it maps result.groundingSources→names) — a future worker that retrieves but doesn't surface sources would under-merge (fail-safe direction: fewer sources = stricter gate, never a fabricated one). VALUE-CLASS: fire 29 = orchestration new-capability (diversity RATCHET satisfied). firesSinceDrill=1.

## fire 30 · 2026-06-20 · skill v2.0.0 · (this commit) — forget durability: a forgotten fact must not silently resurface (user > auto)
meta: value-class=new-capability · pkg=@muse/memory+@muse/cli · kind=memory-integrity · surface=memory-integrity · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=2 · surfaces=2(CLI+chat forget)
ratchet: testFiles 1063 · fabrication 0 · memory 475→480 / cli 2780 / check green / eval:self-improving 13 PASS / lint
- 무엇: a gap-scout found `forget` was a pure key-drop with NO tombstone — the auto-extractor only sees the CURRENT stored value, so after a forget a later mention re-classifies as "add" and SILENTLY re-persists the deleted fact. FIX: `retraction?` marker on the belief-provenance log (deriveFactProvenance skips it); `keysWithActiveRetraction` (newest-event logic; user re-set clears); `recordRetraction` helper; auto-extract suppresses an auto add/update on a retracted key; CLI + chat forget both wired (형제). RATCHET: memory-integrity (least-recent surface).
- 왜: an inference overriding an explicit user retraction is the auto>user authority inversion — a "forget that doesn't stick" is a trust break, not cosmetic, against Muse's "Tell it everything. It can't tell anyone." identity. The belief-provenance layer already encodes source: user > auto; this makes forget a DURABLE, auditable op (a retraction event) instead of a transient key-drop.
- 리뷰지점: v2 ADAPTIVE Opus judge PASS 5/5 (ran dist + mutation) — NO over-suppression (newest-event: a user re-statement reopens the key, the user can always re-add; only auto add/update suppressed, never the user's own `set` which is a different path), fire 16/22 invariant PRESERVED (deriveFactProvenance ignores the value-less retraction marker — confirmCount/value/distinctValueCount unpolluted), both forget sites DRY-wired, mutation-proven (remove suppression → forgotten fact resurfaces → RED). clear-all/dreaming-purge correctly out of scope (bulk/internal, not targeted user retractions).
- 리스크: fail-open (no provenance store → no suppression — legacy behavior, never blocks a write). VALUE-CLASS: fire 30 = memory-integrity new-capability (diversity RATCHET satisfied — memory was least-recent). firesSinceDrill=2.

## fire 31 · 2026-06-21 · skill v2.0.0 · (this commit) — chat date-drift gate covers PROSE/Korean dates (calendar-date fabrication hole)
meta: value-class=new-capability · pkg=@muse/cli · kind=grounding-floor · surface=grounding-floor · verdict=PASS(Opus ④b judge, re-judged after modal fix) · firesSinceDrill=3 · surfaces=1(date guard)
ratchet: testFiles 1065 · fabrication 0 · cli 2786 / check green / eval:self-improving 13 PASS / lint
- 무엇: a gap-scout found the hard chat gate `answerAssertsUnsupportedDate` matched ISO dates only, but the calendar grounding block renders month-name dates (toLocaleString) — so a drifted prose calendar date ("September 14" vs evidence 09-13) passed as grounded. FIX: `monthDayKeys` = script-neutral month-day key across ISO + EN prose + KO ("9월 14일"); year dropped (number guard owns it). RATCHET: grounding-floor (least-recent surface).
- 왜: a wrong calendar/renewal/deadline date is Muse's high-harm fabrication class, and the gate drives a real abstain — but the live evidence path (calendar present month:long) emits prose dates the ISO-only matcher was blind to. Deterministic, false-positive-safe, no model call (security-as-code).
- 리뷰지점: v2 ADAPTIVE Opus judge FIRST FAILED it — the prose regex matched the English modal verb "may" + a count ("you may 3 extensions") as a May date → a hard FALSE-REFUSAL of a correct answer (the ask path stoplists "may" for exactly this). Fixed same-fire: month tokens matched CASE-SENSITIVELY (only capitalized "May N" = a rendered date, not lowercase modal "may"). Re-judge PASS (modal-may → no false-refuse; "May 5"/prose/KO/ISO drift still caught; mutation: i-flag revert → RED).
- 리스크: case-sensitivity means a LOWERCASE prose date ("september 14") under-catches — but that's the SAFE direction for a hard-abstain guard (a missed abstain ≪ a false-refusal of a correct answer; the block renders capitalized so the model mirrors it). VALUE-CLASS: fire 31 = grounding-floor new-capability (diversity RATCHET satisfied). firesSinceDrill=3.
lesson: extending a HARD-ABSTAIN value gate to a new token form (prose dates), audit homonyms — a month abbreviation that is also a common word (English "may") over-fires as a false-refusal; case-sensitivity or a stoplist (as the ask path already does for "may") is the fix.

## fire 32 · 2026-06-21 · skill v2.0.0 · (this commit) — close the misgrounding-resolution half of the whetstone BKT loop
meta: value-class=new-capability · pkg=@muse/mcp · kind=self-development · surface=self-development · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=4 · surfaces=1(BKT resolution symmetry)
ratchet: testFiles 1065 · fabrication 0 · mcp 1879→1882 / check green / eval:self-improving 13 PASS / lint
- 무엇: a gap-scout found the whetstone BKT loop was ASYMMETRIC — recordWeakness lowers mastery for all 7 axes, but recordWeaknessResolved raised it for grounding-gap ONLY, so a misgrounding (GROUNDED≠TRUE core failure) accumulated forever and never resolved; selectDevFixableWeaknesses (→doctor) was also mastery-blind. FIX: GROUNDED_SUCCESS_RESOLVABLE_AXES={grounding-gap, misgrounding}; selectDevFixableWeaknesses gains !isMasteredWeakness. RATCHET: self-dev (hadn't run since fire 28).
- 왜: misgrounding is literally the GROUNDED≠TRUE failure the product edge kills — a permanently-unresolvable entry means doctor reports a "Muse bug" on a fixed topic forever (eroding the self-eval signal) and BKT mastery monotonically decreases (the calibration brake can never register Muse got better). The whetstone is supposed to be a CLOSED loop (detect→nudge→master→stop); it was a one-way ratchet for its most important axis.
- 리뷰지점: v2 ADAPTIVE Opus judge PASS 5/5 (ran dist) — correct axis set (grounded success learns away grounding-gap+misgrounding; actuator axes time-parse/wrong-tool/unbacked-action + user-action source-conflict NOT auto-resolved), NO false-resolution (a misgrounding turn isn't grounded so can't resolve itself — only a LATER grounded success does; both call-sites already gate on grounded-success), floor untouched (pKnown never read by the abstain/faithfulness gate — only the doctor nag + BKT), mutation-proven, 3 selectors now consistently mastery-aware (recap/doctor/nudge); formatWeaknesses is honest-historical, correctly not filtered.
- 리스크: a grounded success on a DIFFERENT subtopic that maps to the same topicKey could over-resolve — but topicKeyFromMessage clustering is the same signal recordWeakness uses, so the resolution is symmetric with the detection (no new asymmetry). VALUE-CLASS: fire 32 = self-dev new-capability (diversity RATCHET satisfied). firesSinceDrill=4.

## fire 33 · 2026-06-21 · skill v2.0.0 · (this commit) — surface fan-out trust signals on `muse ask --json` + run-log (GROUNDED≠TRUE leak on the machine surface)
meta: value-class=new-capability(wiring) · pkg=@muse/multi-agent+@muse/cli · kind=orchestration · surface=orchestration · verdict=PASS(Opus ④b judge) · firesSinceDrill=5 · surfaces=2(--json payload + run-log)
ratchet: testFiles 1066 · fabrication 0 · multi-agent 145 / cli 2792 / check green / eval:self-improving 13 PASS / lint
- 무엇: a gap-scout found the decomposition trust signals (subtaskConflicts/synthesisIncomplete/truncation) were surfaced ONLY on human STDERR (gated !options.json), NOT in the `--json` payload or run-log. FIX: `LeadWorkerResult.truncated` STRUCTURED flag; `decompositionJsonFields` helper → a `decomposition` block in the --json payload + buildAskRunLog (both machine surfaces). RATCHET: orchestration (only 1 of the last 8 — the under-represented axis).
- 왜: the --json consumer IS the agent/script case (the whole reason --json exists). It received `groundedVerdict:"grounded"` + a confident answer with ZERO indication two sub-answers contradicted, a sub-result was dropped, or the list was capped — a GROUNDED≠TRUE leak on the one surface that can't read a stderr banner. The run-log facet also starved the error-analysis flywheel (a fan-out failure logged as a clean success).
- 리뷰지점: v2 ADAPTIVE Opus judge PASS (ran dist) — BOTH machine surfaces actually wired into the real payload + run-log (not just the tested pure helper — the decisive check), NO third surface (api/SSE don't decompose), truncated no-drift (same const as the reason-string), floor untouched (answer/verdict computed independently of the signals, purely additive). Judge noted a non-blocking weakness: the wiring is inspection-verified but not integration-test-pinned (reverting the spread leaves tests green) → backlog follow-up.
- 리스크: helper-level mutation only (the two spreads aren't pinned by a command-level stdout-parse test) — judge-noted, backlog ◦. VALUE-CLASS: fire 33 = orchestration new-capability/wiring (diversity RATCHET satisfied). firesSinceDrill=5.
lesson: when surfacing a signal that already exists for humans (stderr), audit the MACHINE surfaces too (--json, run-log, API) — a trust signal gated on `!options.json` is invisible exactly where a downstream agent consumes it.

## fire 34 · 2026-06-21 · skill v2.0.0 · (this commit) — contested-fact caution on the CHAT persona path (close the gate-asymmetry)
meta: value-class=new-capability(wiring) · pkg=@muse/cli · kind=memory-integrity · surface=memory-integrity · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=6 · surfaces=2(chat-ink + chat-repl persona)
ratchet: testFiles 1067 · fabrication 0 · cli 2795 / check green(@muse/model stale-dist flake → clean-rebuild 325 pass) / eval:self-improving 13 PASS / lint
- 무엇: a gap-scout found the contested-fact caution (a flipped/volatile value → "confirm it's current", refinement-aware) was wired on `ask` (buildMemoryContextBlock) but NOT on the CHAT persona (buildMusePersona — the system prompt every chat turn). FIX: buildMusePersona `contestedKeys?` (precedence over the value-blind `(previously X)` note); derived from belief-provenance + wired on BOTH chat persona callers (Ink chat-ink + one-shot chat-repl), fail-soft. RATCHET: memory-integrity (least-recent surface).
- 왜: same gate-asymmetry class as fires 20/21 (a grounding caution on ask but not a second surface). In chat — Muse's PRIMARY continuous-companion surface — the model was fed a volatile fact (home_city flipped 3× this month) with no signal it's unstable, so it confidently asserted a value Muse itself knows is contested. Closing it = the contested caution holds at EVERY memory point-of-use ("one gate under EVERY surface", CLAUDE.md), raising the grounded-surface count.
- 리뷰지점: v2 ADAPTIVE Opus judge PASS 5/5 (ran dist) — NO over-firing (a stable fact + empty set renders BYTE-IDENTICAL to pre-slice — zero attention cost on the every-turn prompt, the dangerous direction), refinement-aware (passes the SAME contestedFactKeys engine ask uses, not a value-blind diff), fail-soft (provenance error → no caution, never a throw), mutation-proven (remove the branch → 2 RED). 형제-감사: ask's persona left un-wired (its grounding block already carries the caution — redundant, honest); brief/proactive non-grounded (out of scope).
- 리스크: chat-repl re-reads provenance per one-shot turn (small file, fail-soft) — acceptable. VALUE-CLASS: fire 34 = memory-integrity new-capability/wiring (diversity RATCHET satisfied — memory was least-recent). firesSinceDrill=6.

## fire 35 · 2026-06-21 · skill v2.0.0 · (this commit) — date-drift gate on the ASK path (the INVERSE of fire 31 + de-dup monthDayKeys)
meta: value-class=new-capability(+de-dup) · pkg=@muse/agent-core+@muse/cli · kind=grounding-floor · surface=grounding-floor · verdict=PASS(Opus ④b judge 5/5) · firesSinceDrill=7 · surfaces=1(ask value guard)
ratchet: testFiles 1068 · fabrication 0 · agent-core 2526 / cli date 11/96 / check (@muse/shared byte-hygiene RED = goddess-loop's commands-logo.test.ts, NOT mine) / eval:self-improving 13 PASS / lint 0
- 무엇: a gap-scout found the INVERSE of fire 31 — the CHAT date gate catches drifted month-day dates, but the ASK value guard (answerAssertsUnsupportedValue) doesn't (month names stoplisted, a drifted day digit waved through by an unrelated same-digit in evidence). FIX: moved `monthDayKeys` (ISO+prose+KO, case-sensitive months) INTO @muse/agent-core + exported; the ask guard checks month-day drift then strips dates before the bare-number check; chat-grounding deletes its private copy + imports the shared one (de-dup). RATCHET: grounding-floor (longest gap — last was fire 31), and @muse/agent-core (NOT a repeat of @muse/cli+grounding-floor — the date logic moved into agent-core).
- 왜: a wrong calendar/renewal/deadline date is the highest-harm GROUNDED≠TRUE class, and `muse ask` is the WEDGE surface (cited recall). The chat path closed it in fire 31; the ask path never did — a true gate-asymmetry. Sharing ONE copy of monthDayKeys in the lowest common package (agent-core) is what removes the divergence that let the hole open.
- 리뷰지점: v2 ADAPTIVE Opus judge PASS 5/5 (ran dist) — FAIL-OPEN preserved (the ask path NEVER hard-refuses: a flag costs one judge pass, a judge error keeps grounded — the existing 1614 escalation), NO false-fire (ISO↔prose-equivalent correct date → same key → no escalation; date's day stripped before the bare-number check so it's not re-flagged), NO chat regression (answerAssertsUnsupportedDate 11/96 pass after the de-dup — fire 31 modal-may/ISO-prose/month-only all hold), mutation-proven (neutralize the drift branch → prose-drift test RED).
- 리스크: KO drift case is double-covered (bare-number "14" + month-day) — EN-prose isolates the new branch cleanly for mutation. lesson: a guard DUPLICATED across two surfaces is where a gate-asymmetry hides — share one copy in the lowest common package. NOTE: pnpm check is RED on @muse/shared byte-hygiene, but that's the GODDESS-pixel concurrent loop's commands-logo.test.ts (raw control bytes, NOT my files — all 4 of mine are byte-clean, judge-confirmed); that loop must fix it. VALUE-CLASS: fire 35 = grounding-floor new-capability+de-dup. firesSinceDrill=7.
lesson: a deterministic guard duplicated across surfaces (chat date gate vs ask value guard) is a gate-asymmetry waiting to open — move the shared helper into the lowest common package and have both import one copy.
